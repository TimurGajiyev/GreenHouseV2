# Микроэнергосистема завода: КГУ + BESS + сеть

## Полный технический отчёт: модели, формулы, код, результаты и разбор ошибок

Документ собирает всю работу целиком: от разбора единиц измерения до
годовых моделей на 8760 часов, интерактивной визуализации и кросс-валидации.
Сохранены все итерации, включая тупиковые, потому что найденные в них дефекты
и есть основная ценность.

---

## Оглавление

| № | Раздел | Что внутри |
|---|---|---|
| 1 | Основы: мощность и энергия | Почему кВт и кВт·ч это разные величины, откуда берётся кВт·п |
| 2 | Слой единиц измерения | Как переключать кВт/МВт без ошибок в 1000 раз |
| 3 | Итерация 1: MILP на 3 КГУ + BESS | Unit Commitment, линеаризация выпуклой кривой расхода |
| 4 | Итерация 2: EMS с гистерезисом | Два класса моделей, optimality gap, точка перелома стратегий |
| 5 | Итерация 3: три сценария и Monte Carlo | Годовая экстраполяция, зависание конечного автомата |
| 6 | Итерация 4: разбор дефектов EMS | Шесть исправлений, маржинальная стоимость хранения |
| 7 | Итерация 5: 2 КГУ, правило 90%, безлимитная сеть | Дискретный состав, разблокировка запертой мощности |
| 8 | Итерация 6: кросс-валидация методики | Помесячный пик, тарифы Казахстана, 10 годовых сценариев |
| 9 | Итерация 7: финальный отчёт на 2 КГУ | Сутки, неделя, год |
| 10 | Итерация 8: три КГУ | Восемь составов, перенастройка порога |
| 11 | Интерактивная визуализация | Артефакт на React, количественная вуаль |
| 12 | Кросс-валидация | Семь слоёв проверок, найденные ошибки |
| 13 | Сводка всех формул | Одним списком |
| 14 | Реестр найденных дефектов | Что сломалось и почему |
| 15 | Что осталось незакрытым | Список для следующего шага |
| A–H | Приложения | Полные исходники и выводы прогонов |

---

# 1. Основы: мощность и энергия

## 1.1 Различие, на котором держится всё остальное

Есть два разных вопроса, которые можно задать о любом потоке:

1. **Насколько быстро он течёт прямо сейчас** — это *скорость*, rate
2. **Сколько вытекло всего** — это *количество*, amount

Скорость и количество никогда не являются одной и той же величиной. Нельзя
ответить на вопрос «сколько ты проехал» словами «80 км/ч». Скорость это rate,
расстояние это amount, а связывает их время.

Электричество устроено ровно так же:

| Величина | Тип | Что отвечает | Чем определяется в проекте |
|---|---|---|---|
| Ватт (Вт), киловатт (кВт) | скорость | Насколько быстро энергия движется в этот момент | Сечение кабелей, инверторы, генераторы |
| Ватт-час (Вт·ч), киловатт-час (кВт·ч) | количество | Сколько энергии реально прошло | Батареи, топливо, счёт за электроэнергию |

## 1.2 Что такое ватт физически

Энергия измеряется в **джоулях**. Джоуль это примерно та энергия, которая нужна,
чтобы поднять небольшое яблоко на один метр против силы тяжести.

**Ватт это один джоуль в секунду.** Это всё определение. «В секунду» спрятано
внутри названия, и именно поэтому единица путает.

```
1 Вт   = 1 Дж/с      = одно яблоко на метр вверх, каждую секунду
1 кВт  = 1000 Дж/с   = тысяча яблок в секунду
1 кВт·ч = 1000 Дж/с x 3600 с = 3 600 000 джоулей
```

Киловатт-час это не «киловатт часов». Это буквально умножение `кВт x ч`, ровно
как `км/ч x ч = км`. Если держать в голове `мощность x время = энергия`, связь
всегда восстанавливается без запоминания.

## 1.3 Физические якоря для 1 кВт

- **1 кВт ≈ 1.34 лошадиной силы.** Один киловатт это примерно одна работающая
  лошадь. Это не метафора, а точный пересчёт единиц: Джеймс Уатт определил
  лошадиную силу, наблюдая за лошадьми.
- **Тренированный человек на велосипеде держит около 100 Вт** на длинной
  дистанции. Значит **1 кВт = десять спортсменов, крутящих педали изо всех сил.**
  Каждый раз, когда работает электрочайник, в воду уходит мощность десяти
  велосипедистов.

## 1.4 Бытовые приборы как масштабная линейка

| Прибор | Мощность | За сколько часов в сутки | Энергия в сутки |
|---|---:|---:|---:|
| Светодиодная лампа | 10 Вт | 5 | 0.05 кВт·ч |
| Ноутбук | 50 Вт | 8 | 0.40 кВт·ч |
| Телевизор | 100 Вт | 4 | 0.40 кВт·ч |
| Холодильник (компрессор включён) | 150 Вт | ~8 | 1.20 кВт·ч |
| Стиральная машина | 500 Вт | 1 | 0.50 кВт·ч |
| Микроволновка | 900 Вт | 0.3 | 0.27 кВт·ч |
| Кондиционер | 1500 Вт | 6 | 9.00 кВт·ч |
| Электрочайник | 2000 Вт | 0.08 | 0.17 кВт·ч |

Главный вывод из таблицы: **чайник вдвое мощнее отметки 1 кВт, но потребляет
энергии в семь раз меньше холодильника**, который в семь раз слабее по мощности.
Прибор с бОльшим числом киловатт расходует меньше киловатт-часов.

Отсюда и берётся необходимость двух чисел:

- **Мощность определяет, какими должны быть провода и инвертор.**
- **Энергия определяет, каким должен быть аккумулятор или топливный бак.**

Это ровно та развилка, вокруг которой построена вся дальнейшая модель:
инвертор и КГУ считаются в кВт, батарея в кВт·ч.

## 1.5 кВт·п, третья единица

**кВт·п = киловатт-пик (kWp).** Это по-прежнему кВт, по-прежнему скорость, но
измеренная при фиксированных лабораторных условиях, которых на крыше не бывает:
1000 Вт/м² света, температура ячейки ровно 25 °C, стандартный спектр. Эти
условия называются **STC, Standard Test Conditions**.

Аналогия: это максимальная скорость в паспорте автомобиля. Величина настоящая,
измеренная, честная основа для сравнения одной машины с другой, но ездить на
ней практически никогда не приходится. Существует она для того, чтобы панель,
сделанную в одной стране, можно было честно сравнить с панелью из другой.

Почему реальные панели почти никогда не выходят на свой номинал:

| Причина | Механизм | Потеря |
|---|---|---|
| Температура | Ячейки на солнце греются до 55–65 °C вместо 25 °C, кремний теряет ~0.3–0.4% выработки на каждый градус выше 25 | 10–15% |
| Угол | Свет падает под идеальным углом лишь мгновение, остальное время косо | переменная |
| Атмосфера | 1000 Вт/м² это ясное небо около солнечного полудня | переменная |
| Тракт | Провода, пыль, преобразование в инверторе | несколько процентов каждое |

## 1.6 Удельная выработка

Профессионалы оперируют **кВт·ч на кВт·п в год**. Величина делит выработку на
размер системы, поэтому крышу в Гамбурге можно сравнить с крышей в Алматы.

Цифра около 1000 кВт·ч/кВт·п для Германии имеет наглядное толкование: разделить
на 365 дней и получить примерно **2.7 часа в сутки**. Это «эквивалентные часы
полного солнца» — сколько часов панель должна была бы работать точно на своём
номинале, чтобы выдать ту же энергию, что реальный кривой день.

| Регион | Эквивалентных часов в сутки | кВт·ч/кВт·п в год |
|---|---:|---:|
| Северная Европа | ~2.7 | ~1000 |
| Южная Европа | ~4.0 | ~1460 |
| Алматы (оценка, требует проверки по PVGIS) | ~3.8–4.4 | ~1400–1600 |

Через коэффициент использования установленной мощности то же самое:
`1400 кВт·ч/кВт·п ÷ 8760 ч ≈ 16%`. Один и тот же факт в третьем костюме.

## 1.7 Четыре единицы рядом

| Единица | Тип | На какой вопрос отвечает | Что по ней подбирают |
|---|---|---|---|
| кВт | скорость | Насколько быстро энергия движется сейчас | Провода, автоматы, инверторы, генераторы |
| кВт·ч | количество | Сколько энергии прошло за период | Батареи, топливо, счёт за свет |
| кВт·п | скорость при фиксированных условиях | Каков паспортный потолок этой панели | Сравнение модулей, площадь крыши, CAPEX |
| кВт·ч/кВт·п | количество на скорость | Насколько продуктивна эта площадка | Прогноз выработки, окупаемость |

## 1.8 Ошибка «МВт/ч», встретившаяся трижды

Во всех трёх присланных техзаданиях подряд стояло «потребление 1 МВт/ч»,
«700 кВт/ч», «1.5 МВт/ч».

**Такой размерности в этом смысле не существует.** МВт/ч это скорость изменения
мощности — сколько мегаватт прибавляется каждый час. Это про ramp rate турбины,
а не про потребление. Правильно либо **1 МВт** (мощность, мгновенный расход),
либо **1 МВт·ч за час** (энергия за интервал).

Для часового шага они численно совпадают, и именно поэтому ошибку так легко не
заметить. Но в коде она превращается в реальные баги при переходе на
15-минутный или 5-минутный шаг, где `мощность != энергия за интервал`.

---

# 2. Слой единиц измерения

## 2.1 Принцип

Задача MILP **инвариантна к масштабу**. Решателю всё равно, 1550 у него на входе
или 1.55, оптимум тот же. Ломается не математика, а человек: когда часть чисел в
кВт, а часть в МВт, ошибка в 1000 раз даёт результат, который выглядит абсолютно
правдоподобно.

Поэтому конвертация делается **один раз на границе**, а не по месту
использования.

## 2.2 Реализация

```python
POWER_UNIT = "MW"                                  # "kW" или "MW"
S = {"kW": 1.0, "MW": 1000.0}[POWER_UNIT]          # сколько кВт в одном POWER_UNIT
E_UNIT = POWER_UNIT + "h"

def p(x_kw):
    \"\"\"Мощность или энергия: кВт -> POWER_UNIT. Показатель степени +1, делим.\"\"\"
    return x_kw / S

def per_p(x_per_kw):
    \"\"\"Величина 'на единицу мощности' (у.е./кВт*ч, м3/кВт*ч). Показатель -1, умножаем.\"\"\"
    return x_per_kw * S

def per_p2(x_per_kw2):
    \"\"\"Величина 'на квадрат мощности' (м3/(кВт^2*ч)). Показатель -2, умножаем на S^2.\"\"\"
    return x_per_kw2 * S * S
```

Три функции, а не одна, потому что коэффициент масштабирования зависит от
**размерности** параметра. Правило простое: если у величины в знаменателе стоит
мощность, множитель переворачивается.

| Параметр | Размерность | Показатель по мощности | Преобразование кВт → МВт |
|---|---|---:|---|
| Номинал агрегата | кВт | +1 | делить на S |
| Ёмкость батареи | кВт·ч | +1 | делить на S |
| Постоянная составляющая расхода `A` | м³/ч | 0 | не менять |
| Наклон расхода `B` | м³/кВт·ч | −1 | умножать на S |
| Квадратичный член `C` | м³/(кВт²·ч) | −2 | умножать на S² |
| Тариф | ₸/кВт·ч | −1 | умножать на S |

## 2.3 Проверка инвариантности

Прогон одной и той же задачи в двух единицах:

```
POWER_UNIT = "MW":  затраты за сутки 1348.30 у.е.
POWER_UNIT = "kW":  затраты за сутки 1348.30 у.е.
```

Расхождение только в 15-м знаке, от допуска решателя. Слой работает.

---

# 3. Итерация 1: MILP на трёх КГУ и BESS

## 3.1 Постановка

Три КГУ по 0.5 МВт, техминимум 50% номинала, нелинейный расход топлива,
двухставочный тариф сети, батарея 1 МВт·ч / 0.3 МВт с КПД цикла 88%.
Найти диспетчеризацию на 24 часа, минимизирующую затраты.

## 3.2 Почему чистый LP не подходит

Исходное ТЗ предполагало `scipy.optimize` или `pulp` в режиме линейного
программирования. Задача так не решается, и это принципиально:

- Решение «включать агрегат или нет» **бинарное**. Условие «либо ноль, либо не
  ниже 250 кВт» это разрыв в области допустимых значений, одним неравенством
  его не выразить.
- Стоимость пуска платится **за событие**, а не за киловатт. Нужна переменная
  `su[g,t] ∈ {0,1}`.

Отсюда классическая формулировка **Unit Commitment** — MILP, смешанное
целочисленное линейное программирование.

## 3.3 Переменные решения

| Переменная | Тип | Смысл |
|---|---|---|
| `P[g,t]` | непрерывная ≥ 0 | Выдача агрегата g в час t |
| `u[g,t]` | бинарная | Агрегат включён |
| `su[g,t]` | бинарная | В этот час произошёл пуск |
| `sd[g,t]` | бинарная | В этот час произошёл останов |
| `F[g,t]` | непрерывная ≥ 0 | Расход газа агрегата g в час t |
| `grid[t]` | непрерывная ≥ 0 | Закуп из сети |
| `spill[t]` | непрерывная ≥ 0 | Сброс избытка |
| `ch[t]`, `dis[t]` | непрерывные ≥ 0 | Заряд и разряд батареи |
| `soc[t]` | непрерывная | Остаток ёмкости |

## 3.4 Ограничения

**Баланс мощности каждый час:**

```
sum_g P[g,t] + grid[t] + dis[t] == LOAD[t] + ch[t] + spill[t]
```

**Границы мощности агрегата.** Умножение на `u` и есть связь «выключен => ровно
ноль, включён => не ниже техминимума»:

```python
m += P[g][t] <= P_NOM * u[g][t]
m += P[g][t] >= P_MIN * u[g][t]
```

**Логика пуска и останова:**

```python
prev = U_INIT[g] if t == 1 else u[g][t - 1]
m += u[g][t] - prev == su[g][t] - sd[g][t]
m += su[g][t] + sd[g][t] <= 1
```

**Минимальное время работы и стоянки:**

```python
for k in range(t, min(t + MIN_UP, 25)):
    m += u[g][k] >= su[g][t]
for k in range(t, min(t + MIN_DOWN, 25)):
    m += 1 - u[g][k] >= sd[g][t]
```

**Динамика заряда.** Деление на ETA при разряде потому, что чтобы отдать одну
единицу в сеть, из ячеек надо вынуть `1/ETA`:

```python
prev_soc = SOC_INIT if t == 1 else soc[t - 1]
m += soc[t] == prev_soc + ETA * ch[t] - dis[t] / ETA
m += soc[24] == SOC_INIT      # суточная цикличность
```

Условие цикличности обязательно, иначе модель «сжульничает»: разрядит батарею в
ноль и не вернёт.

## 3.5 Линеаризация нелинейного расхода топлива

Расход газа: `FUEL(P) = a·u + b·P + c·P²`.

Слагаемое `a` тратится всегда, пока агрегат включён, независимо от выдачи.
Значит удельный расход `FUEL/P = a/P + b + c·P` на малой мощности взлетает по
гиперболе. **Это и есть физика холостого хода.**

Красивый приём: **эта функция выпуклая, а выпуклая функция равна максимуму своих
касательных.** В задаче минимизации достаточно записать `F >= каждая
касательная` — решатель сам прижмёт `F` к нижней границе, то есть к самой кривой.
Ни одной дополнительной бинарной переменной не нужно:

```python
for pk in (P_MIN, (P_MIN + P_NOM) / 2, P_NOM):
    m += F[g][t] >= (FUEL_A * u[g][t]
                     + (FUEL_B + 2 * FUEL_C * pk) * P[g][t]
                     - FUEL_C * pk * pk * u[g][t])
```

Умножение свободного члена на `u` обязательно, иначе выключенный агрегат
«ест» топливо.

## 3.6 Целевая функция

```python
m += pulp.lpSum(
    GAS_PRICE * pulp.lpSum(F[g][t] for g in G)
    + START_COST * pulp.lpSum(su[g][t] for g in G)
    + TARIFF[t] * grid[t]
    + DEG_COST * (ch[t] + dis[t])
    for t in T
)
```

## 3.7 Результаты

| Показатель | Без батареи | С батареей | Разница |
|---|---:|---:|---:|
| Затраты за сутки, у.е. | 1348.30 | 1316.88 | 31.43 |
| Покупка из сети, МВт·ч | 0.28 | 0.00 | −0.28 |
| Сброшено вхолостую, МВт·ч | 0.250 | 0.000 | −0.250 |
| **Затраты за год, у.е.** | **492 130** | **480 660** | **11 470** |

Экономия 2.33%. При CAPEX батареи 1 МВт·ч / 0.3 МВт порядка 250–350 тыс. у.е.
срок окупаемости выходит за пределы её ресурса.

**Это честный отрицательный результат, и он важнее красивого положительного.**
На этом объекте BESS не окупается на арбитраже, потому что маржинальная
себестоимость КГУ (45.2 у.е./МВт·ч) ниже даже ночного тарифа сети (70).
Арбитраж «купил ночью — отдал днём» здесь мёртв: `70 / 0.88 = 79.5 > 45.2`.

## 3.8 Где именно КГУ уходили вхолостую

Только в часы 13–14, межсменный провал нагрузки. Механизм:

1. Провал длится 2 часа, а `MIN_DOWN = 4 ч` (двигатель после останова не может
   сразу пуститься: тепловой цикл, продувка).
2. В час 15 нагрузка уже 1.35 МВт, две установки её не покрывают.
3. Значит третью гасить нельзя, она вынуждена работать на техминимуме.
4. Сумма трёх минимумов 0.75 МВт превышает нагрузку 0.60 МВт, избыток 0.15 МВт
   идёт в сброс.

Батарея исправляет это идеально: избыток не выбрасывается, а запасается.

## 3.9 Отклонения от ТЗ и их обоснование

**Техминимум поднят с 100 до 250 кВт (20% → 50%).** Газопоршневая машина ниже
40–50% длительно не работает: падает температура выхлопа, растёт
нагарообразование, летит ресурс. Плюс при `P_MIN = 100 кВт` эффект избытка
физически не возникает вообще, и главный вопрос ТЗ теряет смысл.

**Тепло не смоделировано.** Установки когенерационные, их реальная экономика
определяется не только электричеством: отпуск тепла даёт кредит, а при
отсутствии теплопотребления возникает принудительный сброс на аварийные
радиаторы. Полная модель требует второго баланса — теплового, с буферной
ёмкостью.

---

# 4. Итерация 2: EMS с гистерезисом и два класса моделей

## 4.1 Вопрос, который здесь решался

Пришло второе ТЗ с жёсткой стратегией: батарея основной источник, при SoC ≤ 30%
включаются КГУ и заряжают её до 100%, потом гаснут. Требовалось объединить это с
MILP из первой итерации.

**Ответ: их нельзя сложить в одну модель, они соединяются слоями.**

## 4.2 Два класса моделей

| | MILP | EMS с гистерезисом |
|---|---|---|
| Вопрос | Что было бы **лучшим**? | Что **сделает** реальный контроллер? |
| Знает будущее | Да, все 24 часа сразу | Нет, только прошлое |
| Тип | Оптимизация | Причинная симуляция |
| Куда идёт | В отчёт, в ТЭО | В контроллер на объекте |

MILP физически нереализуем как алгоритм управления: он выбирает режим часа 5,
зная нагрузку часа 20. На объекте такого знания нет. А rule-based EMS реализуем,
но не оптимален.

## 4.3 Архитектура соединения

```
Общий слой параметров и единиц (один на всех)
        |
        +-- Слой A: СИМУЛЯТОРЫ (rule-based, причинные)   <- это продукт
        |     +-- load_following
        |     +-- cycle_charging      <- стратегия из ТЗ
        |     +-- hybrid
        |
        +-- Слой B: MILP-ОПТИМИЗАТОР                      <- это линейка

        Разница между A и B = optimality gap = цена стратегии управления
```

**Optimality gap и есть KPI стратегии управления.** Не «сколько сэкономила
батарея», а «насколько мой контроллер хуже теоретического предела».

## 4.4 Точка перелома стратегий

Формула, которая решает весь спор между Load Following и Cycle Charging:

```python
SFC_BREAKEVEN = sfc(P_NOM) / RTE      # 257.5 / 0.88 = 292.6 м3/МВт*ч
```

Пропустить киловатт-час через батарею стоит `1/RTE = 1.136`, то есть **на 13.6%
больше топлива**. Значит запасать энергию выгодно **только если прямая выработка
идёт с удельным расходом хуже порога**.

Реальные цифры системы:

| Точка | Удельный расход | Вывод |
|---|---:|---|
| Номинал 0.5 МВт | 257.5 м³/МВт·ч | прямая выработка |
| Техминимум 0.25 МВт | 277.75 м³/МВт·ч | прямая выработка |
| **Порог** | **292.6 м³/МВт·ч** | |

Даже в худшей точке (277.75) прямая выработка выгоднее батареи. Потеря на
part-load здесь 7.9%, а потеря на КПД накопителя 13.6%. **Штраф за неэффективный
режим меньше штрафа за накопитель, поэтому батарею вообще не надо включать в
контур снабжения.**

## 4.5 Результаты сравнения четырёх стратегий

Профиль 0.7–1.3 МВт, батарея 5 МВт·ч:

| Стратегия | у.е./сутки | Газ, м³ | Сброс | gap, % |
|---|---:|---:|---:|---:|
| Load Following (без BESS) | 1304.00 | 6120 | 0 | 5.03 |
| **Cycle Charging (ТЗ)** | **1627.09** | 6412 | 0.734 | **31.06** |
| Гибрид (порог по sfc) | 1304.00 | 6120 | 0 | 5.03 |
| MILP-эталон | 1241.52 | 6096 | 0 | 0.00 |

Стратегия «батарея основной источник» **дороже базовой на 323 у.е./сутки, то
есть на 118 тыс. у.е. в год.** Плюс 6.9 пусков в сутки против 2.0, то есть
~2500 пусков в год вместо 730 — межремонтный ресурс, порезанный втрое.

## 4.6 Когда идея Cycle Charging становится правильной

Она не ошибочна, просто у неё есть условие применимости. В литературе это
**Cycle Charging против Load Following** (терминология HOMER).

Прогон на профиле с глубоким ночным провалом (ночью завод почти стоит, 0.15 МВт):

| Стратегия | у.е./сутки | gap, % |
|---|---:|---:|
| Load Following | 1047.01 | 13.24 |
| Cycle Charging | 1294.35 | 40.00 |
| Гибрид | 1054.35 | 14.04 |
| MILP-эталон | 924.56 | 0.00 |

Ночью один КГУ на 0.15 МВт даёт удельный расход 308.2 — выше порога 292.6.
В эти часы прямая выработка действительно невыгодна. Gap базовой стратегии
подскочил с 5% до 13.2%: оборудование перестало подходить профилю, и это видно
именно по gap.

## 4.7 Правило синтеза

```python
if sfc(загрузка_агрегата) > sfc(P_NOM) / RTE:
    # прямая выработка дороже, чем через батарею -> разряжаем
else:
    # прямая выработка дешевле -> батарею не трогаем
```

Правило причинное, считается по текущему часу, зашивается в контроллер. Но
опирается на физику топливной кривой, а не на произвольные «30%». Порог 30% из
ТЗ это число, взятое из ниоткуда; порог по sfc выводится из паспорта двигателя и
паспорта батареи.

## 4.8 Policy extraction

MILP используется как **тюнер, а не как алгоритм**. Гоняем MILP на годовом
профиле, смотрим, при каких условиях он включает и выключает агрегаты, и
извлекаем из этого пороги для rule-based контроллера. Оптимизатор проектирует
правила, правила работают на объекте.

## 4.9 Что это значит для сайзинга

При подборе оборудования внутренний цикл прогоняет тысячи вариантов
конфигураций, и на каждом нужен диспетчер. Полный MILP на 8760 часов в таком
цикле не выживет по времени. Поэтому:

- **внутри цикла подбора** — быстрый rule-based диспетчер (путь HOMER)
- **на финальной конфигурации** — один прогон MILP как валидация и источник
  верхней границы экономии (путь REopt)

Разница между ними и есть та величина, которую честно показывают заказчику:
«вот теоретический предел, вот что реально даст автоматика».

---

# 5. Итерация 3: три сценария, Monte Carlo, годовая экстраполяция

## 5.1 Калибровка топливной кривой по паспортным точкам

ТЗ задавало: на 100% загрузке удельный расход `SFC_NOM`, на 20% загрузке он на
40% выше. Это не приблизительное указание, а **полное определение аффинной
модели** — система из двух уравнений с двумя неизвестными.

```
fuel(P) = A + B*P                 (м3/ч)
sfc(P)  = A/P + B                 (м3/кВт*ч)

sfc(0.2*Pном) / sfc(Pном) = 1.40
```

Вывод:

```
alpha = A/Pn
(alpha/r + B) = PENALTY * (alpha + B)
alpha * (1/r - PENALTY) = B * (PENALTY - 1)
```

```python
_k = (PL_PENALTY - 1.0) / (1.0 / PL_RATIO - PL_PENALTY)      # B = alpha / _k
ALPHA = SFC_NOM / (1.0 + 1.0 / _k)
FUEL_A = ALPHA * P_NOM                 # м3/ч, постоянная составляющая
FUEL_B = ALPHA / _k                    # м3 на единицу энергии
```

Результат: `fuel(P) = 12.50 + 0.225*P`. Проверка в коде: `sfc(500) = 0.2500`,
`sfc(100) = 0.3500`, отношение ровно 1.40.

## 5.2 Порог перелома в этой конфигурации

```
sfc(P_NOM)/RTE = 0.2841 м3/кВт*ч
P_BREAKEVEN    = 211.5 кВт (42.3% номинала)
```

Решается из `A/P + B = порог`: `12.5/P + 0.225 = 0.2841` → `P = 211.5`.

Ниже 42.3% загрузки заряжать батарею выгоднее, выше — питать напрямую.

## 5.3 Конечный автомат EMS первой версии

```
DISCHARGE:  SoC > 30%  -> вся нагрузка от батареи, КГУ выключены
            переход в CHARGE, когда SoC упирается в 30%
CHARGE:     КГУ на эффективной мощности, избыток в батарею
            переход в DISCHARGE, когда SoC = 100%
```

## 5.4 Monte Carlo

Один случайный день ничего не доказывает: результат зависит от того, какие числа
выпали. Гонялось по 300 реализаций на каждый сценарий, с переносом состояния
батареи между сутками.

Средний расход газа, м³/сутки:

| Сценарий | Без BESS | EMS из ТЗ | Порог по sfc |
|---|---:|---:|---:|
| Номинальный (хаос) | 2618.7 ±42 | 2572.0 **±205** | 2492.9 ±47 |
| Пиковый | 9000.0 | 8998.7 | 9000.0 |
| Разгрузка 30% | 1920.4 | **2010.2** | 1921.4 |

СКО 205 у EMS в хаотичном дне против 47 у остальных. Стратегия не просто чуть
хуже — она **непредсказуема**. Для эксплуатации это плохо само по себе: нельзя
планировать закупку газа.

В режиме разгрузки EMS **хуже базового варианта** на 90 м³/сутки. При 300 кВт
один агрегат работает на 60% загрузки, `sfc = 0.2667 < 0.2841`, прямая
выработка выгоднее, а стратегия всё равно тащит энергию через батарею.

## 5.5 Годовая экстраполяция (200 / 65 / 100 = 365 дней)

| | Газ, м³/год | у.е./год | К базе |
|---|---:|---:|---:|
| Без батареи | 1 300 783 | 260 157 | 0.00% |
| **EMS из ТЗ** | 1 300 336 | 260 067 | **0.03%** |
| Порог по sfc | 1 275 717 | 255 143 | 1.93% |

Экономия стратегии из ТЗ — **89 у.е. в год.** Не 89 тысяч. Восемьдесят девять.

Почему почти ноль, хотя ночью батарея действительно помогает: **днём она столько
же и теряет.** Ночью агрегат сидит на техминимуме 100 кВт (`sfc = 0.35`),
батарея выигрывает. Днём при 700 кВт агрегаты работают на 70% (`sfc ≈ 0.268`), и
каждый киловатт-час через накопитель — чистый убыток 13.6%.

## 5.6 Фатальный дефект: зависание автомата в пиковом дне

Диагностика на трёх сутках подряд с переносом состояния:

```
сутки  час   нагр  разряд  заряд   SoC%  КГУ сум  состояние
    1    1   1500    1500      0   46.7        0  DISCHARGE
    1    2   1500       0      0   46.7     1500     CHARGE
    1   24   1500       0      0   46.7     1500     CHARGE
    3   24   1500       0      0   46.7     1500     CHARGE
```

Установленная мощность КГУ 3 × 0.5 = **1.5 МВт**, а пиковая нагрузка тоже
**1.5 МВт**. Избытка мощности физически не остаётся, значит Условие 3 («избыток
направляется на зарядку») даёт ноль. Батарея разряжается один раз в первый час и
**навсегда застревает на 46.7%**: до 100% дозарядиться не может, а разряжаться ей
запрещено Условием 2.

**Это дефект самой логики: условие выхода из состояния недостижимо.** В любом
конечном автомате это надо проверять формально — для каждого состояния должен
существовать достижимый переход.

Лечение: третье состояние `CHP_ONLY`, либо резервирование мощности (4 агрегата
вместо 3), либо снижение порога отключения со 100% до 80–85%.

## 5.7 Ещё три замечания к постановке

**Противоречие в сценарии разгрузки.** «30% от номинала» и «около 300 кВт» это
разные числа: 30% от 1500 = 450. Параметр `LOW_LEVEL` выведен наружу.

**Хаос на часовом шаге это не хаос.** `rng.uniform(600, 800)` раз в час
сглаживает именно то, что важно. Реальные броски нагрузки на заводе это секунды
и минуты: пуск компрессора, включение сварочного поста. Именно они определяют
требуемую мощность инвертора и способность агрегата держать частоту (ramp rate).
На часовом усреднении они исчезают полностью. Модель молча предполагает, что
двигатель мгновенно переходит с 100 на 700 кВт, чего он не умеет.

**Собственные нужды BESS недооценивают.** 0.1% ёмкости в час это 3 кВт·ч/ч,
72 кВт·ч в сутки, 26 МВт·ч в год. Больше, чем вся годовая экономия от EMS в
старой версии.

---

# 6. Итерация 4: разбор дефектов EMS

## 6.1 Найденный корень проблемы

В состоянии CHARGE алгоритм запрашивал максимальную мощность заряда `BESS_P`
(1500 кВт). Переменная `need = load + target_charge` достигала 1800 кВт
(300 + 1500), расчёт `need / P_NOM` заставлял включать все три КГУ на полную
мощность. Батарея мгновенно заряжалась, система уходила в DISCHARGE, неся 12%
потерь на цикл.

Диагноз верный, но корень оказался глубже.

## 6.2 Маржинальная стоимость хранения

Раньше порог считался как `sfc(P_NOM) / RTE = 0.2841`. Это цена энергии от
**отдельно запущенного** агрегата. Но если агрегат уже крутится под нагрузку
завода, постоянная составляющая `FUEL_A` уже оплачена. Каждый дополнительный
киловатт-час сверх текущей точки стоит **только `FUEL_B`**:

```python
CHARGE_MARGINAL = FUEL_B / RTE        # 0.225 / 0.88 = 0.2557 м3/кВт*ч
```

| Порог | Значение | Следствие |
|---|---:|---|
| `sfc(P_NOM)/RTE` | 0.2841 | заряжать почти никогда не выгодно |
| **`FUEL_B/RTE`** | **0.2557** | заряжать выгодно почти всегда при наличии запаса |

Разница принципиальная и является главным исправлением этой итерации.

## 6.3 Шесть внесённых исправлений

| № | Что | Как |
|---|---|---|
| 1 | `target_charge` не требует максимума | Забирает только свободный запас работающих агрегатов |
| 2 | Лишние КГУ не пускаются ради заряда | Состав определяется только нагрузкой завода |
| 3 | Переходы автомата в конце шага | По фактическому SoC, а не по прогнозу |
| 4 | Peak shaving | При `L > BESS_P` КГУ добирают разницу, а не пишется дефицит |
| 5 | Паразитная нагрузка | 0.1% ёмкости в час списывается всегда |
| 6 | Min-up-time 2 часа | Наработка каждого агрегата переносится между сутками |

## 6.4 Побочно найденный дефект: двойной учёт сброса

Внутренний `spill` из `commit()` был посчитан относительно `p_chp_need`, а не
относительно всей нагрузки завода. Без поправки техминимум вынужденного
агрегата попадал в сброс, хотя завод его целиком потребляет. Сброс стал
считаться только по фактическому балансу часа.

## 6.5 Результат исправлений

Сценарий разгрузки 300 кВт:

```
без батареи          1904.5 м3/сут
EMS старый           1993.1 м3/сут   (хуже базы на +88.7)
EMS исправленный     1511.3 м3/сут   (к базе -393.1)

Число работающих КГУ в час, старый:  [0, 3]
Число работающих КГУ в час, новый:   [0, 1]
```

Было 0 или 3 агрегата, стало 0 или 1. Вместо проигрыша 4.7% получилась экономия
20.6% на этом сценарии. Годовая экономия выросла с 89 до **2 677 у.е./год**.

## 6.6 Побочный эффект исправления тайминга

Раньше автомат зависал в CHARGE на 46.7% и просто ничего не делал. С паразитной
нагрузкой 3 кВт·ч/ч зависание стало **опасным**: SoC поехал бы вниз до нуля, а
глубокий разряд LFP до 0% это необратимая деградация.

Пришлось добавить третье состояние `CHP_ONLY`: если два часа подряд заряжать
нечем, батарея выводится из контура.

```
старый   SoC в конце 3-х суток:  46.7%  состояние: CHARGE
новый    SoC в конце 3-х суток:  23.0%  состояние: CHP_ONLY
```

Настоящее решение здесь не программное: при нагрузке 1.5 МВт и установленной
мощности 1.5 МВт заряжать батарею физически нечем.

## 6.7 Проверка новой физики

```
min-up-time: длительности непрерывной работы, ч -> [9]   (все >= 2)
паразитная нагрузка: 3.0 кВт*ч/ч = 72 кВт*ч/сут = 2.4% ёмкости в сутки
peak shaving: нагрузка 1800 кВт -> разряд 1500 + КГУ 300, дефицит 0
максимальная невязка почасового баланса мощности: 0.00e+00 кВт
```

**Проверку баланса стоит держать в тестах постоянно:**

```
нагрузка − разряд + заряд − генерация + сброс − дефицит = 0
```

в каждом часу. Она ловит именно те ошибки, которые находятся вручную: двойной
учёт мощности не проходит через неё незамеченным.

---

# 7. Итерация 5: два КГУ, правило 90%, безлимитная сеть

## 7.1 Смена постановки

| Параметр | Было | Стало |
|---|---|---|
| Агрегатов | 3 × 0.5 МВт | Jenbacher 1.067 МВт + TEDOM 1.2 МВт |
| Регулирование | плавное от техминимума | **либо выключен, либо 90–100% номинала** |
| Сеть | ограниченная | **безлимитная**, дефицита не бывает |
| Батарея | 5 МВт·ч / 0.3 МВт | 5.5 МВт·ч / 2.5 МВт, буфер 30% |
| Тарифы | газ в м³ | КГУ 22 ₸/кВт·ч, сеть 60 ₸/кВт·ч |

## 7.2 Дискретность вместо непрерывности

Правило 90% делает мощность КГУ **дискретной**. Непрерывного регулирования от
нуля больше нет: агрегат либо выключен, либо в узкой полосе.

| Состав | Полоса суммарной мощности |
|---|---|
| Jenbacher + TEDOM | 2040.3 – 2267.0 кВт |
| TEDOM | 1080.0 – 1200.0 кВт |
| Jenbacher | 960.3 – 1067.0 кВт |
| Всё выключено | 0 |

Технический минимум 50% из ТЗ здесь **недостижим**: правило 90% строго жёстче и
всегда срабатывает раньше.

## 7.3 Merit order решает всё

Порядок предпочтения задан тарифами однозначно:

```
КГУ            22 ₸/кВт*ч   <-- самый дешёвый, брать максимум
BESS (разряд)  25 ₸/кВт*ч   <-- 22 / 0.88, энергия из КГУ через накопитель
Сеть           60 ₸/кВт*ч   <-- самый дорогой, брать в последнюю очередь
```

Никакой оптимизации не нужно. Из допустимых комбинаций всегда берём самую
мощную. Весь `commit` сводится к одному циклу:

```python
def commit(p_absorb, st):
    for c in COMBOS:                       # отсортированы по убыванию p_hi
        if c["p_lo"] <= p_absorb + 1e-9 and combo_allowed(c, st):
            total = min(c["p_hi"], p_absorb)
            base = sum(UNITS[i]["p_nom"] for i in c["idx"])
            return [total * UNITS[i]["p_nom"] / base if i in c["idx"] else 0.0
                    for i in range(NG)]
    return [0.0] * NG
```

`p_absorb` это то, что система физически **способна поглотить**: нагрузка завода
плюс то, что примет батарея. Больше выдавать нельзя, сброса электрической
мощности нет, экспорт в сеть не предусмотрен.

Деление пропорционально номиналам держит оба агрегата на одинаковой
относительной загрузке, поэтому оба гарантированно выше своих 90%.

## 7.4 Правило пикового режима

Первый прогон вскрыл ловушку: при жадном разряде батарея сливалась ночью на
копеечные 250 кВт и приходила к утреннему пику 5.5 МВт наполовину пустой.

Требование «ночью не разряжаться» делается **причинным**, без прогноза:

```python
peak_mode = L > P_CHP_MAX + 1e-9      # 2267 кВт
if not peak_mode:
    dis = 0.0
```

Контроллер сравнивает текущий час с паспортом КГУ.

## 7.5 Ограничитель скорости заряда

Годовой прогон вскрыл ещё один дефект. Батарея жадно набивалась за 3–4 ночных
часа, после чего у Jenbacher пропадало место для сброса излишка, он глох, и
таймер `MIN_DOWN` запирал его на 3 часа ровно к утреннему подъёму.

**Свободная ёмкость накопителя — это ресурс, который держит КГУ в работе, и
тратить его весь сразу нельзя.**

| CHARGE_RATE_CAP, кВт | Экономия, ₸/год |
|---:|---:|
| 300 | 66 862 073 |
| **700** | **70 577 336** |
| 1200 | 70 085 254 |
| 2500 | 68 936 648 |

## 7.6 Главный механизм экономии оказался не арбитражом

Выработка Jenbacher: **10 638 кВт·ч без батареи против 19 206 с батареей.** Рост
на 80%.

Батарея зарабатывает не столько на арбитраже, сколько на том, что
**разблокирует запертую мощность КГУ**. Без накопителя Jenbacher нельзя
запустить при нагрузке ниже 2040 кВт — его минимальные 960 кВт просто некуда
деть. С накопителем излишек уходит в заряд, и агрегат работает 18 часов вместо 8.

**Именно правило 90%, которое выглядит как ограничение, и создаёт ценность
батареи.** В системе с плавным регулированием от нуля этого эффекта не было бы
вовсе — что и наблюдалось во всех предыдущих итерациях.

---

# 8. Итерация 6: кросс-валидация методики

## 8.1 Ошибка: пик считался по суткам, а платят по месяцу

Промышленные потребители платят две составляющие: за потреблённую энергию и за
пиковую мощность, причём вторая определяется как наивысшая мощность за расчётный
период. Типовой расчётный цикл для платы за мощность — календарный месяц.

**Суточный максимум экономического смысла не имеет: тридцать хороших дней не
компенсируют один плохой час.**

## 8.2 Следствие для контроллера

Если пик месяца уже зафиксирован, разряжать накопитель ниже этого уровня не
нужно: покупная мощность всё равно не опустится ниже текущего максимума.

```python
if st.month != month[t]:                 # начался новый расчётный период
    st.month, st.month_peak = month[t], 0.0
...
eff_target = max(target, st.month_peak)  # ниже уже зафиксированного смысла нет
dis = min(max(0.0, gap - eff_target), avail)
```

## 8.3 Тариф сети под большим вопросом

Весь бизнес-кейс держится на спреде 60 − 22 = 38 ₸. По разным регионам и группам
потребителей Казахстана публикуемые тарифы для юрлиц расходятся примерно от
25 до 67 ₸/кВт·ч.

| Тариф сети, ₸/кВт·ч | Спред | Экономия, ₸/год | Окупаемость |
|---:|---:|---:|---:|
| 25 | 3 | 4 274 904 | **95.4 года** |
| 30 | 8 | 13 512 296 | 30.2 |
| 35 | 13 | 22 749 688 | 17.9 |
| 40 | 18 | 31 987 080 | 12.8 |
| 45 | 23 | 41 224 472 | 9.9 |
| 50 | 28 | 50 461 864 | 8.1 |
| **60** | 38 | 68 936 648 | **5.9** |
| 70 | 48 | 87 411 432 | 4.7 |

**Проект живёт только при тарифе сети выше примерно 45 ₸/кВт·ч.** Это цифра
номер один, которую надо подтвердить по договору с ЭСО, включая НДС, передачу и
снабжение.

## 8.4 Рынок мощности в Казахстане

С 2019 года действует рынок электрической мощности. Потребители, являющиеся
субъектами оптового рынка, ежемесячно оплачивают услугу единого закупщика по
обеспечению готовности мощности к несению нагрузки. Максимум за расчётный
период определяется методом частичного усреднения по контрольным периодам
календарного месяца.

Практический вывод раздваивается:

- **Розница через ЭСО** — плата за мощность уже размазана внутри ₸/кВт·ч, срез
  пика не даёт ни тенге напрямую, правильная стратегия это чистый арбитраж.
- **Оптовый рынок** — максимум месяца оплачивается отдельно, срез пика начинает
  приносить деньги.

Модель считает порог: **срез пика окупается при плате за мощность выше
2 810 ₸ за кВт максимума месяца.**

## 8.5 Десять независимых годовых сценариев

Все нормированы под конверт из ТЗ (средняя 2.1–2.55 МВт, пик 4.6–5.8 МВт),
различаются формой: сменность, недельный ритм, сезонность, шум, частота коротких
пиков.

| # | Сценарий | Средняя | Пик | Год | Экономия, ₸/год | % | Окуп. |
|---:|---|---:|---:|---:|---:|---:|---:|
| 1 | Две смены, непрерывно | 2.40 | 5.50 | 21.0 ГВт·ч | 68 936 648 | 11.1 | 5.9 |
| 2 | Три смены, ровный ритм | 2.30 | 5.20 | 20.2 | 25 799 393 | 4.9 | 15.8 |
| 3 | Одна дневная смена | 2.10 | 5.50 | 18.4 | 67 563 306 | 9.1 | 6.0 |
| 4 | Летний пик | 2.45 | 5.60 | 21.5 | 61 776 619 | 9.1 | 6.6 |
| 5 | Зимний пик | 2.45 | 5.60 | 21.5 | 53 930 361 | 8.1 | 7.6 |
| 6 | Частые короткие пики | 2.25 | 5.50 | 19.7 | 89 726 985 | 15.1 | 4.5 |
| 7 | Ровная базовая нагрузка | 2.55 | 4.60 | 22.3 | 5 019 210 | **0.8** | **81.3** |
| 8 | Останов на выходные | 2.20 | 5.50 | 19.3 | 45 680 210 | 6.7 | 8.9 |
| 9 | Кампанийное производство | 2.15 | 5.40 | 18.8 | 56 845 367 | 9.7 | 7.2 |
| 10 | Высокая хаотичность | 2.30 | 5.80 | 20.2 | 123 274 517 | **18.8** | **3.3** |

**Среднее 9.3%, но разброс окупаемости от 3.3 до 81.3 лет.**

Это главный результат прогона. Одна суточная цифра (3.1 года из ранней итерации)
была не просто оптимистичной — она была случайной. **Окупаемость определяется
формой профиля, а не средней нагрузкой.**

Закономерность: чем ровнее нагрузка, тем бесполезнее батарея. Сценарий 7
(размах день/ночь всего 1.4 раза) — 81 год. Сценарий 10 — 3.3 года. Механизм тот
же: батарея зарабатывает на разблокировке запертой мощности КГУ, а запирается
мощность только там, где нагрузка проваливается ниже 2040 кВт.

## 8.6 Генератор годовых профилей

Слои: суточный цикл + недельный ритм + сезонность + кампанийность + шум + пики.

```python
# --- суточный цикл: форма зависит от сменности ---
if a["shifts"] == 1:
    shape = np.where((hod >= 7) & (hod < 18), 1.0, 0.0)
elif a["shifts"] == 2:
    shape = np.where((hod >= 6) & (hod < 22), 1.0, 0.0)
    shape = np.where((hod >= 13) & (hod < 15), 0.45, shape)   # межсменка
else:
    shape = np.full(8760, 0.85)
    shape = np.where((hod >= 8) & (hod < 20), 1.0, shape)
smooth = np.convolve(np.r_[shape[-3:], shape, shape[:3]],
                     np.ones(5) / 5, mode="same")[3:-3]        # сглаживаем фронты

x = a["base"] * (a["night"] + (a["day_amp"] - a["night"]) * smooth)
x = x * np.where(dow >= 5, a["weekend"], 1.0)                  # недельный ритм
ph = a.get("season_phase", 0.0)
x = x * (1.0 + a["season"] * np.cos(2 * np.pi * doy / 365.0 + ph))
if a.get("campaign"):
    x = x * np.where((doy // 7) % 4 == 3, 0.55, 1.0)
x = x * (1.0 + rng.normal(0, a["noise"], 8760))
hit = rng.random(8760) < a["spike_p"]
x = np.where(hit, x * rng.uniform(1.0, a["spike"] / a["day_amp"] + 0.6, 8760), x)
```

Нормировка под конверт обязательна, иначе архетипы разъезжаются по средней в
два раза и сравнивать их между собой становится бессмысленно:

```python
x = x * (a["target_mean"] * 1000.0 / x.mean())
x = np.minimum(x, a["target_peak"] * 1000.0)   # срезаем нефизичные выбросы
x = x * (a["target_mean"] * 1000.0 / x.mean())
x = np.minimum(x, a["target_peak"] * 1000.0)
```

---

# 9. Итерация 7: финальный отчёт на двух КГУ

## 9.1 Методика после кросс-валидации

1. Считается полный год, 8760 часов.
2. Типичные сутки и неделя **вырезаются** из годового прогона, а не считаются
   отдельно. В них приходит фактический SoC батареи и накопленный максимум
   расчётного месяца.
3. Пик считается помесячно.
4. Контроллер среза пика помнит максимум текущего месяца.

**Выбор типичных суток:** будний день в первых числах месяца (чтобы контроллер
среза пика стартовал с чистого расчётного периода) с суточным потреблением ближе
всего к медиане года.

```python
daily = LOAD.reshape(365, 24).sum(axis=1)
med = np.median(daily)
cand = [d for d in range(365) if IDX[d * 24].dayofweek < 5 and IDX[d * 24].day <= 3]
DAY = min(cand, key=lambda d: abs(daily[d] - med))
```

Результат: 01.09.2026, вторник, 56 212 кВт·ч при медиане 57 412.

**Выбор недели:** понедельник, первое число месяца. 1 июня 2026 подходит.

## 9.2 Сводная таблица, 2 КГУ

| Показатель | A. Без батареи | B. Арбитраж | C. Срез пика |
|---|---:|---:|---:|
| **ТИПИЧНЫЕ СУТКИ** | | | |
| Нагрузка завода, кВт·ч | 56 212 | 56 212 | 56 212 |
| Выработка КГУ, кВт·ч | 46 655 | 53 625 | 48 666 |
| Заряд BESS, кВт·ч | 0 | 4 082 | 508 |
| Разряд BESS, кВт·ч | 0 | 3 612 | 447 |
| Закуп из сети, кВт·ч | 9 557 | **3 058** | 7 607 |
| Пик закупа, кВт | 861 | 861 | **600** |
| Затраты, ₸ | 1 599 828 | **1 363 234** | 1 527 088 |
| Экономия к A, ₸ | — | **236 594** | 72 741 |
| **НЕДЕЛЯ** | | | |
| Нагрузка завода, кВт·ч | 365 658 | 365 658 | 365 658 |
| Выработка КГУ, кВт·ч | 313 540 | 351 890 | 313 895 |
| Заряд BESS, кВт·ч | 0 | 18 650 | 355 |
| Разряд BESS, кВт·ч | 0 | 16 416 | 312 |
| Закуп из сети, кВт·ч | 52 118 | **16 002** | 51 806 |
| Пик закупа, кВт | 1 453 | **727** | 1 271 |
| Затраты, ₸ | 10 024 956 | **8 701 707** | 10 014 018 |
| Экономия к A, ₸ | — | **1 323 248** | 10 938 |
| **ГОД** | | | |
| Выработка КГУ, МВт·ч | 16 945 | 18 939 | 17 293 |
| Закуп из сети, МВт·ч | 4 079 | **2 215** | 3 754 |
| Доля сети в питании, % | 19.4 | **10.5** | 17.9 |
| Годовой максимум закупа, кВт | 3 233 | 3 233 | **1 403** |
| Сумма 12 месячных максимумов, кВт | 34 952 | 34 320 | **13 436** |
| Пусков КГУ за год | 401 | **227** | 398 |
| Затраты, ₸/год | 623 555 059 | **552 977 723** | 611 646 415 |
| Экономия к A, ₸/год | — | **70 577 336** | 11 908 644 |
| Экономия, % | — | **11.3%** | 1.9% |
| Простая окупаемость, лет | — | **5.8** | 34.3 |

CAPEX BESS: $850 000 = 408 000 000 ₸ по курсу 480.

## 9.3 Помесячный максимум закупа, кВт

| Месяц | Без BESS | Арбитраж | Срез пика | Срез |
|---:|---:|---:|---:|---:|
| 1 | 2 344 | 2 344 | 929 | 60.3% |
| 2 | 3 233 | 3 233 | 929 | 71.3% |
| 4 | 3 233 | 3 233 | 1 050 | 67.5% |
| 6 | 1 461 | 1 190 | 1 271 | 13.0% |
| 9 | 3 233 | 3 203 | 986 | 69.5% |
| 12 | 3 233 | 3 233 | 1 403 | 56.6% |
| **ГОД** | **3 233** | **3 233** | **1 403** | **56.6%** |

Арбитраж **почти не снижает месячный максимум**, хотя за неделю выглядел
эффектным. Батарея к моменту годового пика оказывается разряженной. Два режима
конфликтуют, и суточная модель этого не показывала вообще.

## 9.4 Три вещи, которые видно только на годовом горизонте

**Сутки врут в обе стороны.** На типичных сутках арбитраж даёт 236 594 ₸
экономии, что при умножении на 365 дало бы 86.4 млн ₸/год. Фактический годовой
результат 70.6 млн. Разница 18%.

**Два режима конфликтуют.** Арбитраж снижает закуп вдвое, но годовой максимум не
трогает вообще. Срез пика валит сумму двенадцати месячных максимумов втрое, но
экономит в шесть раз меньше денег.

**При заданных тарифах выбор однозначен: арбитраж.** Плата за мощность равна
нулю, значит 21 516 кВт срезанных максимумов не стоят ничего.

Побочный бонус арбитража: пусков КГУ за год **401 → 227**, почти вдвое меньше.

---

# 10. Итерация 8: три КГУ

## 10.1 Изменение парка

| Агрегат | Номинал | Рабочая полоса (90–100%) |
|---|---:|---|
| Jenbacher | 1 067 кВт | 960.3 – 1 067.0 |
| TEDOM | 1 200 кВт | 1 080.0 – 1 200.0 |
| **КГУ-3** | **1 100 кВт** | **990.0 – 1 100.0** |
| **Суммарно** | **3 367 кВт** | |

Комбинаций состава стало восемь вместо четырёх:

| Состав | Полоса суммарной мощности |
|---|---|
| Jenbacher + TEDOM + КГУ-3 | 3 030.3 – 3 367.0 |
| TEDOM + КГУ-3 | 2 070.0 – 2 300.0 |
| Jenbacher + TEDOM | 2 040.3 – 2 267.0 |
| Jenbacher + КГУ-3 | 1 950.3 – 2 167.0 |
| TEDOM | 1 080.0 – 1 200.0 |
| КГУ-3 | 990.0 – 1 100.0 |
| Jenbacher | 960.3 – 1 067.0 |

Дырка между 1 200 и 1 950 кВт осталась, но верхний диапазон стал плотнее.

## 10.2 Правило пикового режима перестало масштабироваться

При первом прогоне вариант B дал таблицу, **побайтово совпадающую с вариантом
A** — батарея за сутки не сделала ни одного движения.

```
Часов в году выше 3367 кВт (новый порог): 332 из 8760 = 3.8%
Часов в году выше 2267 кВт (старый порог): 5492 из 8760 = 62.7%
```

Правило «разряд только когда нагрузка выше суммарной мощности КГУ» разумно само
по себе, но привязка порога к мощности парка не масштабируется. Порог вынесен в
параметр `PEAK_TRIGGER`:

| PEAK_TRIGGER, кВт | Закуп, МВт·ч/год | Экономия, ₸/год | Окупаемость |
|---:|---:|---:|---:|
| 3 367 (= мощность парка) | 2 377 | 17 569 061 | **23.2 года** |
| 2 600 | 863 | 71 688 066 | 5.7 |
| 2 267 | 659 | 77 149 572 | 5.3 |
| 2 000 | 622 | 78 041 016 | 5.2 |
| 1 700 | 423 | 83 598 286 | 4.9 |
| **1 400** | **133** | **91 093 306** | **4.5** |

Разница между слепым следованием формуле и подобранным порогом — **73.5 млн ₸ в
год**.

## 10.3 Сводная таблица, 3 КГУ

| Показатель | A. Без батареи | B. Арбитраж | C. Срез пика |
|---|---:|---:|---:|
| **ТИПИЧНЫЕ СУТКИ** | | | |
| Нагрузка завода, кВт·ч | 56 212 | 56 212 | 56 212 |
| Выработка КГУ, кВт·ч | 47 912 | 57 168 | 47 912 |
| Заряд BESS, кВт·ч | 0 | 5 151 | 0 |
| Разряд BESS, кВт·ч | 0 | 3 888 | 136 |
| Закуп из сети, кВт·ч | 8 300 | **307** | 8 164 |
| Пик закупа, кВт | 836 | **307** | 700 |
| Затраты, ₸ | 1 552 066 | **1 276 094** | 1 543 882 |
| Экономия к A, ₸ | — | **275 973** | 8 184 |
| **НЕДЕЛЯ** | | | |
| Нагрузка завода, кВт·ч | 365 658 | 365 658 | 365 658 |
| Выработка КГУ, кВт·ч | 325 710 | 361 715 | 328 772 |
| Заряд BESS, кВт·ч | 0 | 29 822 | 4 446 |
| Разряд BESS, кВт·ч | 0 | 25 562 | 3 902 |
| Закуп из сети, кВт·ч | 39 948 | **8 204** | 37 430 |
| Пик закупа, кВт | 914 | 1 395 | **700** |
| Затраты, ₸ | 9 562 503 | **8 449 963** | 9 478 771 |
| Экономия к A, ₸ | — | **1 112 540** | 83 732 |
| **ГОД** | | | |
| Выработка КГУ, МВт·ч | 18 201 | 21 099 | 18 525 |
| Закуп из сети, МВт·ч | 2 823 | **133** | 2 518 |
| Доля сети в питании, % | 13.4 | **0.6** | 12.0 |
| Годовой максимум закупа, кВт | 3 200 | 3 517 | **1 294** |
| Сумма 12 месячных максимумов, кВт | 30 306 | 18 709 | **9 441** |
| Пусков КГУ за год | 1 477 | **1 913** | 1 486 |
| Затраты, ₸/год | 591 937 472 | **500 844 166** | 580 897 459 |
| Экономия к A, ₸/год | — | **91 093 306** | 11 040 013 |
| Экономия, % | — | **15.4%** | 1.9% |
| **Простая окупаемость, лет** | — | **4.5** | 37.0 |

## 10.4 Сравнение двух и трёх агрегатов

| Показатель | 2 КГУ (2 267 кВт) | 3 КГУ (3 367 кВт) |
|---|---:|---:|
| Затраты без батареи, ₸/год | 623 555 059 | **591 937 472** |
| Закуп из сети без батареи, МВт·ч | 4 079 | **2 823** |
| Затраты с батареей, ₸/год | 552 977 723 | **500 844 166** |
| Закуп из сети с батареей, МВт·ч | 2 215 | **133** |
| Доля сети с батареей, % | 10.5 | **0.6** |
| Экономия от BESS, ₸/год | 70 577 336 | **91 093 306** |
| Окупаемость BESS, лет | 5.8 | **4.5** |

Третий агрегат сам по себе экономит **31.6 млн ₸/год**, а вместе с батареей
завод почти уходит от сети: **0.6% питания вместо 13.4%**. Батарея и третий
агрегат не конкурируют, а усиливают друг друга: третий агрегат закрывает верхние
диапазоны, батарея закрывает провалы.

## 10.5 Две проблемы конфигурации с тремя КГУ

**Пуски выросли: 1 477 → 1 913 за год.** Это на 30% больше базового варианта и
заметно хуже, чем при двух агрегатах (227 пусков в варианте B). Восемь
комбинаций дают алгоритму слишком много свободы, `MIN_UP = 2` и `MIN_DOWN = 3`
уже не сдерживают. 1 913 пусков в год это порядка пяти пусков в сутки на парк.
Нужен гистерезис на смену состава: не переключаться, если выигрыш ниже стоимости
пуска.

**Годовой максимум закупа у арбитража вырос: 3 200 → 3 517 кВт.** Батарея в
режиме арбитража к годовому пику приходит разряженной, и без неё третий агрегат
отработал бы лучше. Арбитраж делает пик **хуже**, чем вариант вообще без батареи.

---

# 11. Интерактивная визуализация

## 11.1 Состав артефакта

React + recharts, всё в одном файле `bess_profile.jsx`.

- Две панели: конфигурация 2 КГУ и 3 КГУ
- Переключатель сценария: A (без батареи) / B (арбитраж) / C (срез пика)
- Переключатель периода: сутки (24 точки) / 7 дней (168 точек)
- Точки — нагрузка завода, столбцы — только накопитель (вверх заряд, вниз
  разряд), пунктир — суммарная мощность КГУ
- Зелёная и оранжевая вуаль — деньги
- Внизу два блока отчёта с полными таблицами

## 11.2 Количественная вуаль

Первая версия рисовала полосу от столбца накопителя до линии нагрузки:
`band = [min(bess, 0), load]`. Красиво, но с экономией не связано:

```
час  1: вуаль 1373 кВт высотой   экономия часа  −8 826 ₸   (вуаль есть, деньги теряются)
час  8: вуаль 2852 кВт высотой   экономия часа  +17 520 ₸
час 20: вуаль 3128 кВт высотой   экономия часа        0 ₸   (вуаль максимальная, экономии ноль)
```

Корреляция высоты с экономией по восьми наборам: **от −0.07 до 0.72**. Местами
связь отрицательная. При этом легенда утверждала «зона экономии».

Замена на количественную:

```js
const SPREAD = KZT_GRID - KZT_CHP;   // 38 ₸/кВт*ч
const saveE = (b[1]*KZT_CHP + b[5]*KZT_GRID) - (chp*KZT_CHP + grid*KZT_GRID);
const h = saveE / SPREAD;
bandPos: h >= 0 ? [load - h, load] : null,
bandNeg: h <  0 ? [load, load - h] : null,
```

Теперь **площадь вуали в кВт·ч, умноженная на 38 ₸/кВт·ч, в точности равна
деньгам.** Проверено по всем восьми наборам, расхождение 0.0000 ₸:

| Набор | Площадь вуали | × 38 ₸ | Сумма почасовой экономии |
|---|---:|---:|---:|
| chp2.day.B | 6 225 кВт·ч | 236 540 ₸ | 236 540 ₸ |
| chp2.day.C | 1 914 кВт·ч | 72 720 ₸ | 72 720 ₸ |
| chp2.week.B | 34 818 кВт·ч | 1 323 102 ₸ | 1 323 102 ₸ |
| chp2.week.C | 287 кВт·ч | 10 888 ₸ | 10 888 ₸ |
| chp3.day.B | 7 264 кВт·ч | 276 030 ₸ | 276 030 ₸ |
| chp3.day.C | 215 кВт·ч | 8 160 ₸ | 8 160 ₸ |
| chp3.week.B | 29 275 кВт·ч | 1 112 454 ₸ | 1 112 454 ₸ |
| chp3.week.C | 2 201 кВт·ч | 83 656 ₸ | 83 656 ₸ |

**Убыточные часы стали видны.** В недельном арбитраже с тремя КГУ **54 часа из
168** дают отрицательную экономию, вуаль уходит выше линии и красится в
оранжевый.

## 11.3 Разделение экономии на составляющие

Пусковые расходы дают скачки по 15 000 ₸ на один час; если бы они вошли в высоту
вуали, та стала бы рваной. Разделение:

```js
const saveE = (b[1]*KZT_CHP + b[5]*KZT_GRID) - (chp*KZT_CHP + grid*KZT_GRID);
const saveS = (b[6] - starts) * KZT_START;
```

Вуаль показывает энергетическую составляющую, пуски идут в подсказку и в итог.
У трёх КГУ пуски **съедают экономию**: за неделю энергия даёт +1 112 454 ₸, а
пуски отнимают −330 000 ₸.

## 11.4 Поправка на незамкнутость периода по энергии

Период не замкнут по энергии: если накопитель на конце полнее, чем в начале, эта
энергия ещё не отработала.

```js
const dCur = (rows[rows.length - 1].soc / 100) * cfg.cap - cur.soc0;
const dBas = (bas.rows[bas.rows.length - 1][4] / 100) * cfg.cap - bas.soc0;
const dSoc = dCur - dBas;
const socLo = dSoc * cfg.eta * KZT_CHP;    // если заместит собственную выработку
const socHi = dSoc * cfg.eta * KZT_GRID;   // если заместит сеть
const totLo = saveEnergy + saveStarts + Math.min(socLo, socHi);
const totHi = saveEnergy + saveStarts + Math.max(socLo, socHi);
```

Итог показывается диапазоном, а не одним числом.

| Набор | Энергия | Пуски | ΔSoC | Итого |
|---|---:|---:|---:|---:|
| chp2.day.B | 236 540 | +15 000 | −18 кВт·ч | 250 499…251 158 |
| chp2.week.B | 1 323 102 | +45 000 | −5 кВт·ч | 1 367 843…1 368 007 |
| chp3.day.B | 276 030 | **−60 000** | +687 кВт·ч | 230 204…254 686 |
| chp3.week.B | 1 112 454 | **−330 000** | +727 кВт·ч | 797 462…823 384 |

## 11.5 Схема данных

Строки лежат плоскими массивами ради компактности: 12 наборов (2 конфигурации ×
3 сценария × 2 периода) занимают 29 КБ вместо примерно 90 КБ в формате объектов.

```
rows[i] = [нагрузка, выработка КГУ, заряд, разряд, SoC %, закуп из сети, пусков]
gen[i]  = [выработка агрегата 1, агрегата 2, ...]      кВт
soc0    = состояние заряда ДО первого часа окна        кВт*ч
names   = ["Jenbacher", "TEDOM", ...]
cap, eta, pchp, target, dayDate, weekDate
```

## 11.6 Предупреждение о масштабировании, вшитое в интерфейс

| Набор | Сутки × 365 | Неделя × 52.14 | Годовой прогон | Ошибка суток |
|---|---:|---:|---:|---:|
| 2 КГУ, арбитраж | 86 337 100 | 68 990 318 | 70 577 336 | **+22.3%** |
| 2 КГУ, срез пика | 26 542 800 | 567 731 | 11 908 644 | **+122.9%** |
| 3 КГУ, арбитраж | 100 750 950 | 58 006 530 | 91 093 306 | **+10.6%** |
| 3 КГУ, срез пика | 2 978 400 | 4 362 063 | 11 040 013 | **−73.0%** |

Недельное окно ближе к правде, суточное врёт до двух с лишним раз.

---

# 12. Кросс-валидация

## 12.1 Семь слоёв проверок

| № | Что проверяется | Метод | Результат |
|---|---|---|---|
| 1 | Данные артефакта против моделей | Повторный независимый прогон, построчное сравнение | все 12 наборов совпали |
| 2 | Почасовой баланс мощности | `нагрузка + заряд = КГУ + разряд + сеть` | макс. невязка 1 кВт |
| 3 | Динамика SoC | `soc[t] = soc[t−1] + заряд·η − разряд/η` | макс. 5.0 кВт·ч при шаге округления 2.8 |
| 4 | Экономия против отчётных таблиц | Сумма почасовых разниц | −146…+57 ₸ из сотен тысяч |
| 5 | Поагрегатная выработка | `sum(gen) == chp` | макс. 1 кВт |
| 6 | Правило 90% | Минимальная загрузка работающего агрегата | 90.8% и 90.0% |
| 7 | Годовая сводка | Все восемь показателей против `economics()` | совпало до единицы |

## 12.2 Рендер-тест интерфейса

Babel + `react-dom/server`: JSX компилируется, компонент рендерится в
статическую разметку. Прогоняются **все 36 комбинаций состояний** — три режима
панелей × два периода × три режима отчёта × два состояния переключателя часов.

```
[OK ] панели B/day · отчёт A/свёрнут: строк 134, 211 831 симв.
[OK ] панели B/day · отчёт B/168ч:   строк 472, 883 411 симв.
...
Все 36 комбинаций состояний отрендерились без ошибок
```

Плюс статическая проверка использований переменной `g` против объявлений во всех
функциях таблиц — именно она показывает пропущенное объявление сразу.

## 12.3 Ошибка «g is not defined»

В функции `weekTable` не была объявлена переменная `g`. В соседних `dayTable` и
`hourWeekTable` строка есть, здесь пропущена, а ниже её уже используют:

```js
const gseg = g.slice(k * 24, k * 24 + 24);   // g не существует
```

Синтаксически код валиден, поэтому парсер молчал. Ошибка срабатывала только в
момент рендера недельной таблицы. Исправление:

```js
function weekTable(cfg, md) {
  const r = cfg.week[md].rows;
  const g = cfg.week[md].gen;             // выработка каждого агрегата по часам
```

## 12.4 Найденная нестыковка в методике расчёта затрат

В годовых таблицах затраты включали стоимость пусков, а в суточных и недельных —
нет. Артефакт теперь считает пуски везде.

Следствие для трёх КГУ: суточная экономия падает с 276 030 до **216 030 ₸** —
шесть пусков за сутки против двух в базовом варианте съедают 60 000 ₸.

| Конфигурация | Период | A | B | C |
|---|---|---:|---:|---:|
| 2 КГУ | сутки | 1 614 852 | 1 363 312 (+251 540) | 1 542 132 (+72 720) |
| 2 КГУ | неделя | 10 174 982 | 8 806 880 (+1 368 102) | 10 164 094 (+10 888) |
| 3 КГУ | сутки | 1 582 146 | 1 366 116 (+216 030) | 1 573 986 (+8 160) |
| 3 КГУ | неделя | 9 907 522 | 9 125 068 (+782 454) | 9 898 866 (+8 656) |

## 12.5 Пики и пуски по всем наборам

| Набор | Пик сети, кВт | Пусков |
|---|---:|---:|
| chp2.day.A | 861 | 1 |
| chp2.day.B | 861 | 0 |
| chp2.day.C | 600 | 1 |
| chp2.week.A | 1 453 | 10 |
| chp2.week.B | 727 | 7 |
| chp2.week.C | 1 271 | 10 |
| chp3.day.A | 836 | 2 |
| chp3.day.B | 307 | 6 |
| chp3.day.C | 700 | 2 |
| chp3.week.A | 914 | 23 |
| chp3.week.B | **1 395** | **45** |
| chp3.week.C | 700 | 28 |

---

# 13. Сводка всех формул

## 13.1 Единицы

```
1 Вт = 1 Дж/с
1 кВт·ч = 3 600 000 Дж
мощность x время = энергия
1 кВт ≈ 1.34 л.с. ≈ десять велосипедистов
```

## 13.2 Топливная кривая

```
Аффинная:      fuel(P) = A + B·P                       м3/ч
Квадратичная:  fuel(P) = A·u + B·P + C·P²              м3/ч
Удельный:      sfc(P)  = fuel(P)/P = A/P + B + C·P     м3/кВт·ч
```

Калибровка по двум паспортным точкам (загрузка `r` от номинала, штраф `k`):

```
alpha = A/Pn
_k    = (k − 1) / (1/r − k)
ALPHA = SFC_NOM / (1 + 1/_k)
A     = ALPHA · Pn
B     = ALPHA / _k
```

## 13.3 Линеаризация выпуклой кривой (эпиграф касательными)

```
F[g,t] >= A·u[g,t] + (B + 2·C·pk)·P[g,t] − C·pk²·u[g,t]     для каждой точки pk
```

## 13.4 Пороги перелома стратегий

```
Отдельно запущенный агрегат:  SFC_BREAKEVEN   = sfc(P_NOM) / RTE
Дозагрузка работающего:       CHARGE_MARGINAL = B / RTE
Точка перелома по мощности:   P_BREAKEVEN     = A / (SFC_BREAKEVEN − B)
```

Значения в разных конфигурациях:

| Конфигурация | sfc(Pном) | Порог | P_BREAKEVEN |
|---|---:|---:|---:|
| 3 КГУ по 0.5 МВт, техмин 50% | 257.5 м³/МВт·ч | 292.6 | не достигается |
| 3 КГУ по 0.5 МВт, техмин 20% | 0.2500 м³/кВт·ч | 0.2841 | 211.5 кВт (42.3%) |
| Маржинальная дозагрузка | — | 0.2557 | — |

## 13.5 Батарея

```
ETA = sqrt(RTE)                                  симметрично на заряд и разряд
soc[t] = soc[t−1] + ch[t]·ETA − dis[t]/ETA
Полезная ёмкость = CAP · (1 − доля буфера)
Стоимость разрядного кВт·ч = CHP_COST / RTE
Собственные нужды = 0.001 · CAP каждый час
```

## 13.6 Баланс мощности

```
sum_g P[g,t] + grid[t] + dis[t] == LOAD[t] + ch[t] + spill[t]
```

Тест-инвариант для каждого часа:

```
нагрузка + заряд − КГУ − разряд − сеть == 0
```

## 13.7 Правило 90% и дискретный состав

```
Полоса комбинации:  p_lo = sum(0.9 · Pном_i),  p_hi = sum(Pном_i)
Допустима, если:    p_lo <= p_absorb  и  не нарушает min-up / min-down
p_absorb = нагрузка + min(BESS_PWR, CHARGE_RATE_CAP, свободная ёмкость)
Дележ:              gen_i = total · Pном_i / sum(Pном_j)
```

## 13.8 Экономика

```
Затраты = КГУ·CHP_COST + сеть·GRID_COST + пуски·START_COST + пик_месяца·DEMAND_CHARGE
Экономия часа (энергия) = (chpA·22 + gridA·60) − (chp·22 + grid·60)
Экономия часа (пуски)   = (startsA − starts) · 15000
Поправка ΔSoC           = ΔSoC · ETA · [CHP_COST … GRID_COST]
Высота вуали            = экономия_энергии / (GRID_COST − CHP_COST)
Простая окупаемость     = CAPEX / экономия_за_год
Порог платы за мощность = Δзатрат_год / Δсумма_12_максимумов / 12
```

## 13.9 Помесячный учёт пика

```
При смене месяца:  month_peak = 0
Каждый час:        month_peak = max(month_peak, grid)
Уставка разряда:   eff_target = max(target, month_peak)
Разряд:            dis = min(max(0, gap − eff_target), avail)
```

## 13.10 Удельная выработка солнечной установки

```
Специфический выход = кВт·ч / кВт·п в год
Эквивалентные часы  = выход / 365
Коэффициент использования = выход / 8760
```

---

# 14. Реестр найденных дефектов

| № | Где | Дефект | Последствие | Исправление |
|---|---|---|---|---|
| 1 | Все три ТЗ | Размерность «МВт/ч» вместо МВт | Скрытая ошибка при переходе на подчасовой шаг | Слой единиц с проверкой размерностей |
| 2 | ТЗ №1 | Техминимум 20% для газопоршневой машины | Эффект избытка физически не возникает, вопрос ТЗ теряет смысл | Поднят до 50%, параметр выведен наружу |
| 3 | EMS v1 | `want = BESS_P` вслепую | Запуск всех КГУ ради заряда, перерасход 4.7% | Только свободный запас работающих агрегатов |
| 4 | EMS v1 | Переходы автомата до расчёта часа | Батарея не отдавала остаток до порога | Переходы в конце шага по фактическому SoC |
| 5 | EMS v1 | Порог сравнения `sfc(P_NOM)/RTE` | Заряд почти никогда не выгоден | Маржинальный порог `FUEL_B/RTE` |
| 6 | EMS v1 | Недостижимое условие выхода из CHARGE | Автомат зависает навсегда в пиковом дне | Третье состояние `CHP_ONLY` |
| 7 | EMS v2 | Двойной учёт сброса в DISCHARGE | Завышенный `spill` в отчётах | Сброс только по фактическому балансу часа |
| 8 | 2 КГУ | Жадный разряд ночью | К утреннему пику батарея пустая | Правило пикового режима |
| 9 | 2 КГУ | Жадный заряд ночью | Jenbacher глохнет и запирается MIN_DOWN | `CHARGE_RATE_CAP`, подобран 700 кВт |
| 10 | Методика | Пик считался по суткам | Экономически бессмысленная метрика | Помесячный максимум |
| 11 | Методика | Контроллер не помнил максимум месяца | Заряд тратился впустую | `eff_target = max(target, month_peak)` |
| 12 | Отчёты | Суточные затраты без пусков, годовые с пусками | Расхождение горизонтов | Единый метод `money()` |
| 13 | 3 КГУ | `PEAK_TRIGGER` привязан к мощности парка | Батарея простаивает 96.2% времени | Вынесен в параметр, подобран 1400 кВт |
| 14 | Артефакт | Вуаль не связана с экономией | Легенда врала: корреляция от −0.07 до 0.72 | `h = saveE / SPREAD` |
| 15 | Артефакт | `g` не объявлена в `weekTable` | Runtime error при рендере | `const g = cfg.week[md].gen;` |
| 16 | Артефакт | Ось Y зашита в `[-1000, 3600]` | Обрезание в недельном виде | Расчёт границ от данных |

---

# 15. Что осталось незакрытым

## 15.1 Критично для решения

**Тариф сети.** Весь бизнес-кейс держится на спреде 38 ₸. Проект живёт только
при тарифе выше примерно 45 ₸/кВт·ч. Нужна выписка из договора с ЭСО с НДС,
передачей и снабжением.

**Статус на рынке мощности.** От него зависит выбор между арбитражем и срезом
пика, а это разные уставки контроллера. Порог: плата выше 2 810 ₸ за кВт
максимума месяца.

**Реальные профили вместо синтетики.** Десять сценариев показывают разброс
окупаемости от 3.3 до 81.3 лет. Формат CSV `timestamp, load_kw`, 8760 строк,
функция `load_from_csv` готова.

## 15.2 Технические доработки модели

**Тепловой контур.** Установки когенерационные, экономика определяется не только
электричеством. Нужен второй баланс с буферной ёмкостью.

**Гистерезис на смену состава.** У трёх КГУ 1 913 пусков в год, около пяти в
сутки на парк. Правило: не переключаться, если выигрыш ниже стоимости пуска.

**Деградация ячеек и LCOS.** Сейчас считается простая окупаемость. При ~300
полных циклах в год за 5.8 лет накапливается ~1 700 циклов и заметная потеря
ёмкости.

**Подчасовой шаг.** Реальные броски нагрузки это секунды и минуты. На часовом
усреднении они исчезают, а именно они определяют мощность инвертора и ramp rate.

**Отказ агрегата и надёжность.** Ценность батареи как источника бесперебойного
питания сейчас не измеряется вообще. Метрики LOLP / LPSP / EENS или штраф VOLL.

**Экспорт излишков в сеть.** Модель считает, что экспорт запрещён, из-за чего
Jenbacher простаивает ночью при полной батарее. Разрешение экспорта по любой
положительной цене изменило бы картину.

**Устойчивость подобранных порогов.** `PEAK_TRIGGER = 1400` и
`CHARGE_RATE_CAP = 700` подобраны под один профиль. Нужен прогон по всем десяти
сценариям: если оптимум сильно пляшет, нужен адаптивный порог.

---

# Приложение A. Исходники моделей

## A.1 Итерация 1: MILP на трёх КГУ

**`chp_bess_dispatch.py`** — Unit Commitment, PuLP + CBC

```python
"""
Оптимизация суточной диспетчеризации 3 КГУ + BESS для промышленного предприятия.

Модель: MILP (Mixed Integer Linear Programming), решатель CBC через PuLP.
Почему MILP, а не чистый LP: решение "включать / не включать агрегат" - бинарное,
а стоимость пуска нельзя выразить непрерывной переменной. Это классическая
задача Unit Commitment.

Запуск:  python3 chp_bess_dispatch.py
"""

import pulp

# =============================================================================
# БЛОК 1. СЛОЙ ЕДИНИЦ ИЗМЕРЕНИЯ
# =============================================================================
# Все физические данные ниже ОБЪЯВЛЕНЫ в кВт и кВт*ч - это каноническая
# внутренняя единица. POWER_UNIT задаёт только то, в чём мы хотим ВИДЕТЬ
# результат и в чём удобнее думать про этот конкретный объект.
#
# Ключевая мысль: сама задача MILP инвариантна к масштабу. Решателю всё равно,
# 1550 у него на входе или 1.55 - оптимум тот же. Ломается не математика,
# а человек: когда часть чисел в кВт, а часть в МВт, ошибка в 1000 раз даёт
# результат, который выглядит абсолютно правдоподобно. Поэтому конвертация
# делается ОДИН раз на границе, а не по месту использования.

POWER_UNIT = "MW"                                  # "kW" или "MW"
S = {"kW": 1.0, "MW": 1000.0}[POWER_UNIT]          # сколько кВт в одном POWER_UNIT
E_UNIT = POWER_UNIT + "h"
DP = 0 if POWER_UNIT == "kW" else 3                # знаков после запятой при выводе


def p(x_kw):
    """Мощность или энергия: кВт -> POWER_UNIT. Показатель степени +1, делим."""
    return x_kw / S


def per_p(x_per_kw):
    """Величина 'на единицу мощности' (у.е./кВт*ч, м3/кВт*ч). Показатель -1, умножаем."""
    return x_per_kw * S


def per_p2(x_per_kw2):
    """Величина 'на квадрат мощности' (м3/(кВт^2*ч)). Показатель -2, умножаем на S^2."""
    return x_per_kw2 * S * S


# =============================================================================
# БЛОК 2. ИСХОДНЫЕ ДАННЫЕ (всё объявлено в кВт / кВт*ч / у.е. за кВт*ч)
# =============================================================================
T = list(range(1, 25))
G = [1, 2, 3]

# --- Профиль нагрузки завода, кВт ---------------------------------------------
# Двухсменное производство: ночной минимум, два пика и провал в межсменку 13-14 ч.
# Провал в середине дня - не случайность, он специально нужен, чтобы модель
# столкнулась с проблемой "минимума технологического состава" (см. выводы).
LOAD_KW = [550, 520, 500, 500, 550, 700, 950, 1200, 1400, 1550, 1550, 1450,
           650, 600, 1350, 1500, 1550, 1550, 1400, 1200, 1000, 850, 620, 580]
LOAD = {t: p(LOAD_KW[t - 1]) for t in T}

# --- Когенерационные установки ------------------------------------------------
P_NOM = p(500.0)          # номинал каждой КГУ, кВт
P_MIN = p(250.0)          # технический минимум = 50% номинала.
                          # В ТЗ было 100 кВт (20%), но газопоршневой двигатель
                          # ниже ~40-50% нагрузки не работает длительно: падает
                          # температура выхлопа, растёт нагарообразование.
                          # Параметр оставлен свободным - можно вернуть 100.

# Расход газа, м3/ч:  FUEL(P) = a*u + b*P + c*P^2
# Именно это и есть та "нелинейность", о которой говорит ТЗ. Слагаемое a
# тратится всегда, пока агрегат включён, независимо от выдаваемой мощности.
# Поэтому удельный расход  FUEL/P = a/P + b + c*P  на малой мощности взлетает
# (гипербола a/P) - физически это и есть "холостой ход".
FUEL_A = 12.0                    # м3/ч постоянная составляющая
FUEL_B = per_p(0.226)            # м3 на единицу энергии
FUEL_C = per_p2(1.5e-5)          # м3 на квадрат мощности в час
GAS_PRICE = 0.20                 # у.е./м3
START_COST = 40.0                # у.е. за один пуск (износ + пусковое топливо)
MIN_UP = 3                       # минимальное время работы после пуска, ч
MIN_DOWN = 4                     # минимальное время стоянки после останова, ч
U_INIT = {1: 1, 2: 1, 3: 0}      # состояние на начало суток

# --- Внешняя сеть -------------------------------------------------------------
DAY_HOURS = set(range(8, 23))    # 8:00-22:00 - дневная зона
TARIFF_DAY = per_p(0.160)        # у.е. за единицу энергии
TARIFF_NIGHT = per_p(0.070)
GRID_MAX = p(800.0)              # ограничение по вводу от сети
TARIFF = {t: (TARIFF_DAY if t in DAY_HOURS else TARIFF_NIGHT) for t in T}

# --- Аккумулятор (BESS) -------------------------------------------------------
BESS_CAP = p(1000.0)             # ёмкость
BESS_P = p(300.0)                # максимальная мощность заряда/разряда
RTE = 0.88                       # round-trip efficiency
ETA = RTE ** 0.5                 # раскладываем на заряд и разряд симметрично
SOC_MIN = 0.10 * BESS_CAP
SOC_MAX = 0.95 * BESS_CAP
SOC_INIT = 0.20 * BESS_CAP
DEG_COST = per_p(0.005)          # плата за оборот энергии (деградация ячеек)


# =============================================================================
# БЛОК 3. ПОСТРОЕНИЕ И РЕШЕНИЕ МОДЕЛИ
# =============================================================================
def solve(with_bess: bool):
    m = pulp.LpProblem("dispatch", pulp.LpMinimize)

    # --- Переменные решения ---------------------------------------------------
    P = pulp.LpVariable.dicts("P", (G, T), lowBound=0)          # выдача КГУ
    u = pulp.LpVariable.dicts("u", (G, T), cat="Binary")        # вкл/выкл
    su = pulp.LpVariable.dicts("su", (G, T), cat="Binary")      # пуск
    sd = pulp.LpVariable.dicts("sd", (G, T), cat="Binary")      # останов
    F = pulp.LpVariable.dicts("F", (G, T), lowBound=0)          # расход газа
    grid = pulp.LpVariable.dicts("grid", T, lowBound=0, upBound=GRID_MAX)
    spill = pulp.LpVariable.dicts("spill", T, lowBound=0)       # сброс избытка

    bp = BESS_P if with_bess else 0.0
    ch = pulp.LpVariable.dicts("ch", T, lowBound=0, upBound=bp)
    dis = pulp.LpVariable.dicts("dis", T, lowBound=0, upBound=bp)
    soc = pulp.LpVariable.dicts("soc", T, lowBound=SOC_MIN, upBound=SOC_MAX)

    for t in T:
        # --- Баланс мощности: приход = расход, каждый час -------------------
        m += (pulp.lpSum(P[g][t] for g in G) + grid[t] + dis[t]
              == LOAD[t] + ch[t] + spill[t]), f"balance_{t}"

        for g in G:
            # --- Границы мощности агрегата ---------------------------------
            # Умножение на u - это и есть связь "выключен => ровно ноль,
            # включён => не ниже технического минимума". Одним неравенством
            # такое не выразить, нужна бинарная переменная.
            m += P[g][t] <= P_NOM * u[g][t]
            m += P[g][t] >= P_MIN * u[g][t]

            # --- Линеаризация выпуклой кривой расхода ----------------------
            # FUEL(P) выпуклая, значит она равна максимуму своих касательных.
            # В задаче МИНИМИЗАЦИИ достаточно записать F >= каждая касательная:
            # решатель сам прижмёт F к нижней границе, то есть к самой кривой.
            # Никаких дополнительных бинарных переменных не требуется.
            for pk in (P_MIN, (P_MIN + P_NOM) / 2, P_NOM):
                m += F[g][t] >= (FUEL_A * u[g][t]
                                 + (FUEL_B + 2 * FUEL_C * pk) * P[g][t]
                                 - FUEL_C * pk * pk * u[g][t])

            # --- Логика пуска / останова ------------------------------------
            prev = U_INIT[g] if t == 1 else u[g][t - 1]
            m += u[g][t] - prev == su[g][t] - sd[g][t]
            m += su[g][t] + sd[g][t] <= 1

            # --- Минимальное время работы и стоянки -------------------------
            for k in range(t, min(t + MIN_UP, 25)):
                m += u[g][k] >= su[g][t]
            for k in range(t, min(t + MIN_DOWN, 25)):
                m += 1 - u[g][k] >= sd[g][t]

        # --- Динамика заряда аккумулятора -----------------------------------
        # SoC[t] = SoC[t-1] + заряд*КПД - разряд/КПД.
        # Деление на ETA при разряде - потому что чтобы отдать 1 единицу
        # в сеть, из ячеек надо вынуть 1/ETA.
        prev_soc = SOC_INIT if t == 1 else soc[t - 1]
        m += soc[t] == prev_soc + ETA * ch[t] - dis[t] / ETA

    # Суточная цикличность: утро следующих суток должно начинаться так же,
    # иначе модель "сжульничает" - разрядит батарею в ноль и не вернёт.
    m += soc[24] == SOC_INIT

    # --- Целевая функция ----------------------------------------------------
    m += pulp.lpSum(
        GAS_PRICE * pulp.lpSum(F[g][t] for g in G)
        + START_COST * pulp.lpSum(su[g][t] for g in G)
        + TARIFF[t] * grid[t]
        + DEG_COST * (ch[t] + dis[t])
        for t in T
    )

    m.solve(pulp.PULP_CBC_CMD(msg=0))

    rows = []
    for t in T:
        gen = {g: P[g][t].value() for g in G}
        fuel = sum(F[g][t].value() for g in G)
        starts = sum(su[g][t].value() for g in G)
        cost = (GAS_PRICE * fuel + START_COST * starts + TARIFF[t] * grid[t].value()
                + DEG_COST * (ch[t].value() + dis[t].value()))
        rows.append(dict(t=t, load=LOAD[t], g1=gen[1], g2=gen[2], g3=gen[3],
                         grid=grid[t].value(), spill=spill[t].value(),
                         ch=ch[t].value(), dis=dis[t].value(),
                         soc=soc[t].value(), fuel=fuel, starts=starts, cost=cost))
    return pulp.LpStatus[m.status], rows


# =============================================================================
# БЛОК 4. ВЫВОД
# =============================================================================
def f(x):
    return f"{x:.{DP}f}"


def table1(rows):
    print(f"\nТАБЛИЦА 1. БЕЗ БАТАРЕИ   (мощность в {POWER_UNIT})\n")
    h = (f"{'Час':>4} {'Нагр.':>8} {'КГУ1':>8} {'КГУ2':>8} {'КГУ3':>8} "
         f"{'Сеть':>8} {'Сброс':>8} {'Затр.,у.е.':>11}")
    print(h); print("-" * len(h))
    for r in rows:
        print(f"{r['t']:>4} {f(r['load']):>8} {f(r['g1']):>8} {f(r['g2']):>8} "
              f"{f(r['g3']):>8} {f(r['grid']):>8} {f(r['spill']):>8} "
              f"{r['cost']:>11.2f}")


def table2(rows):
    print(f"\nТАБЛИЦА 2. С БАТАРЕЕЙ   (мощность в {POWER_UNIT}, SoC в {E_UNIT})\n")
    h = (f"{'Час':>4} {'Нагр.':>8} {'КГУ1':>8} {'КГУ2':>8} {'КГУ3':>8} "
         f"{'Сеть':>8} {'Заряд':>8} {'Разряд':>8} {'SoC':>8} {'Затр.,у.е.':>11}")
    print(h); print("-" * len(h))
    for r in rows:
        print(f"{r['t']:>4} {f(r['load']):>8} {f(r['g1']):>8} {f(r['g2']):>8} "
              f"{f(r['g3']):>8} {f(r['grid']):>8} {f(-r['ch']):>8} "
              f"{f(r['dis']):>8} {f(r['soc']):>8} {r['cost']:>11.2f}")


def summary(r1, r2):
    c1 = sum(r['cost'] for r in r1)
    c2 = sum(r['cost'] for r in r2)
    print("\n" + "=" * 62)
    print("ИТОГОВЫЙ ЭКОНОМИЧЕСКИЙ АНАЛИЗ")
    print("=" * 62)
    for name, rr, c in (("Без батареи", r1, c1), ("С батареей", r2, c2)):
        print(f"\n{name}:")
        print(f"  Затраты за сутки          {c:12.2f} у.е.")
        print(f"  Расход газа               {sum(r['fuel'] for r in rr):12.1f} м3")
        print(f"  Покупка из сети           {sum(r['grid'] for r in rr):12.2f} {E_UNIT}")
        print(f"  Сброшено в холостую       {sum(r['spill'] for r in rr):12.3f} {E_UNIT}")
        print(f"  Пусков КГУ                {sum(r['starts'] for r in rr):12.0f}")
    d = c1 - c2
    print(f"\nЭкономия за сутки:            {d:12.2f} у.е.  ({d / c1 * 100:.2f}%)")
    print(f"Экономия за год (365 дней):    {d * 365:12.2f} у.е.")
    print(f"  Год без батареи:             {c1 * 365:12.2f} у.е.")
    print(f"  Год с батареей:              {c2 * 365:12.2f} у.е.")


if __name__ == "__main__":
    st1, r1 = solve(with_bess=False)
    st2, r2 = solve(with_bess=True)
    print(f"Статус решения: сценарий 1 = {st1}, сценарий 2 = {st2}")
    table1(r1)
    table2(r2)
    summary(r1, r2)
```

## A.2 Итерация 2: два слоя моделей

**`ems_lab.py`** — симуляторы + MILP-эталон, optimality gap

```python
"""
EMS Lab: сравнение стратегий управления 3 КГУ + BESS.

АРХИТЕКТУРА - два принципиально разных слоя:

  Слой A. СИМУЛЯТОР (rule-based).  Отвечает на вопрос "что СДЕЛАЕТ реальный
          контроллер". Причинный, шаг за шагом, знает только прошлое.
          Это то, что поедет на объект.

  Слой B. ОПТИМИЗАТОР (MILP).      Отвечает на вопрос "что БЫЛО БЫ лучшим".
          Видит все 24 часа сразу. На объекте так не бывает.
          Это эталон, линейка, а не алгоритм управления.

Разница между A и B = цена твоей стратегии управления (optimality gap).
Именно её и надо минимизировать, подбирая правила в слое A.

Запуск:  python3 ems_lab.py
"""

import pulp

# =============================================================================
# СЛОЙ 0. ЕДИНИЦЫ И ПАРАМЕТРЫ (общие для симулятора и оптимизатора)
# =============================================================================
POWER_UNIT = "MW"
S = {"kW": 1.0, "MW": 1000.0}[POWER_UNIT]
E_UNIT = POWER_UNIT + "h"

def p(x_kw):      return x_kw / S          # мощность/энергия: степень +1
def per_p(x):     return x * S             # на единицу энергии: степень -1
def per_p2(x):    return x * S * S         # на квадрат мощности: степень -2

T = list(range(1, 25))
NG = 3

LOAD_KW = [750, 720, 700, 700, 730, 800, 900, 1050, 1180, 1280, 1300, 1220,
           850, 800, 1150, 1250, 1300, 1250, 1100, 1000, 920, 850, 800, 770]
LOAD = [p(x) for x in LOAD_KW]

P_NOM   = p(500.0)
P_MIN   = p(250.0)                 # 50% номинала - предел для газопоршневой машины
FUEL_A  = 12.0                     # м3/ч, постоянная составляющая (пока агрегат включён)
FUEL_B  = per_p(0.226)             # м3 на единицу энергии
FUEL_C  = per_p2(1.5e-5)           # м3 на квадрат мощности
GAS     = 0.20                     # у.е./м3
START_C = 40.0                     # у.е. за пуск

BESS_CAP  = p(5000.0)              # 5 МВт*ч
BESS_PCH  = p(1000.0)              # предел заряда
BESS_PDIS = p(1500.0)              # предел разряда: ОБЯЗАН быть >= пика нагрузки,
                                   # иначе "батарея держит завод" физически невозможно
RTE  = 0.88
ETA  = RTE ** 0.5                  # симметрично на заряд и разряд
SOC_LO  = 0.30 * BESS_CAP          # порог включения КГУ по ТЗ
SOC_HI  = 1.00 * BESS_CAP          # порог отключения КГУ по ТЗ
DEG     = per_p(0.005)             # у.е. за единицу оборота энергии (деградация)

GRID_MAX  = p(500.0)
DAY_HOURS = set(range(8, 23))
TARIFF    = [per_p(0.160) if t in DAY_HOURS else per_p(0.070) for t in T]


def fuel_h(pw):
    """Расход газа, м3/ч, для ОДНОГО работающего агрегата при мощности pw."""
    if pw <= 1e-9:
        return 0.0
    return FUEL_A + FUEL_B * pw + FUEL_C * pw * pw


def sfc(pw):
    """Удельный расход, м3 на единицу энергии. Гипербола FUEL_A/pw - тот самый
    'холостой ход': чем ниже загрузка, тем дороже каждый киловатт-час."""
    return fuel_h(pw) / pw if pw > 1e-9 else float("inf")


# Точка перелома стратегий, выводится аналитически.
# Пропустить энергию через батарею стоит 1/RTE = 1.136 раза дороже.
# Значит запасать в батарею выгодно ТОЛЬКО если прямая выработка идёт с
# удельным расходом хуже, чем sfc(P_NOM) / RTE.
SFC_BREAKEVEN = sfc(P_NOM) / RTE


# =============================================================================
# СЛОЙ A. СИМУЛЯТОРЫ (rule-based, причинные)
# =============================================================================
def commit_load_following(load):
    """Классический Load Following: включаем минимально возможное число
    агрегатов и делим нагрузку между ними равномерно."""
    n = 0
    while n * P_NOM < load - 1e-9 and n < NG:
        n += 1
    n = max(n, 1)
    if n * P_MIN > load + 1e-9:        # техминимум выше нагрузки -> избыток
        per = P_MIN
        spill = n * P_MIN - load
    else:
        per = load / n
        spill = 0.0
    return n, per, spill


def sim_load_following(days=40):
    """Стратегия 1: батареи нет. КГУ следят за нагрузкой."""
    prev_on = 1
    log = []
    for d in range(days):
        for t in T:
            load = LOAD[t - 1]
            n, per, spill = commit_load_following(load)
            starts = max(0, n - prev_on)
            prev_on = n
            gas = n * fuel_h(per)
            cost = GAS * gas + START_C * starts
            log.append(dict(day=d, t=t, load=load, gen=[per] * n + [0.0] * (NG - n),
                            n=n, per=per, spill=spill, grid=0.0, ch=0.0, dis=0.0,
                            soc=0.0, gas=gas, starts=starts, cost=cost))
    return log


def sim_cycle_charging(days=40):
    """Стратегия 2: EMS из твоего ТЗ (в литературе - Cycle Charging).

    Гистерезис: разряд до 30% -> КГУ на номинале питают завод И заряжают
    батарею -> при 100% КГУ гасятся. Батарея - основной источник.
    """
    soc = SOC_HI
    mode = "DIS"
    prev_on = 0
    log = []
    for d in range(days):
        for t in T:
            load = LOAD[t - 1]
            # --- переключение режима: строго по SoC, как в ТЗ ---------------
            if mode == "DIS":
                need = min(load, BESS_PDIS) / ETA          # сколько уйдёт из ячеек
                if soc - need < SOC_LO - 1e-9:
                    mode = "CHG"
            else:
                if soc >= SOC_HI - 1e-9:
                    mode = "DIS"

            n = per = spill = ch = dis = grid = 0.0
            if mode == "DIS":
                dis = min(load, BESS_PDIS)
                grid = max(0.0, load - dis)                # страховка при нехватке
                soc -= dis / ETA
            else:
                # КГУ на ОПТИМАЛЬНОЙ мощности. Для нашей кривой удельный расход
                # монотонно падает до номинала, значит оптимум = P_NOM.
                n = NG
                per = P_NOM
                surplus = n * P_NOM - load
                room = (SOC_HI - soc) / ETA                # сколько можно принять
                ch = max(0.0, min(surplus, BESS_PCH, room))
                spill = surplus - ch                       # остальное в сброс
                soc += ch * ETA

            starts = max(0, n - prev_on)
            prev_on = n
            gas = n * fuel_h(per) if n else 0.0
            cost = GAS * gas + START_C * starts + TARIFF[t - 1] * grid + DEG * (ch + dis)
            log.append(dict(day=d, t=t, load=load, gen=[per] * int(n) + [0.0] * (NG - int(n)),
                            n=n, per=per, spill=spill, grid=grid, ch=ch, dis=dis,
                            soc=soc, gas=gas, starts=starts, cost=cost, mode=mode))
    return log


def sim_hybrid(days=40):
    """Стратегия 3: СИНТЕЗ. Load Following - базовый режим. Батарея вступает
    в игру не 'всегда', а только там, где прямая выработка была бы ХУЖЕ порога
    SFC_BREAKEVEN, то есть где потеря на part-load превышает потерю на КПД
    накопителя. Это правило, а не оптимизация - его можно зашить в контроллер.
    """
    soc = 0.5 * BESS_CAP
    prev_on = 1
    log = []
    for d in range(days):
        for t in T:
            load = LOAD[t - 1]
            n, per, spill0 = commit_load_following(load)

            ch = dis = 0.0
            # Правило 1: если состав вынужден работать неэффективно (или в сброс),
            # разряжаем батарею и снимаем часть нагрузки с КГУ.
            if (spill0 > 0 or sfc(per) > SFC_BREAKEVEN) and soc > 0.15 * BESS_CAP:
                dis = min(BESS_PDIS, load, (soc - 0.15 * BESS_CAP) * ETA)
                n, per, spill0 = commit_load_following(max(load - dis, P_MIN))
                dis = load - n * per if n * per < load else 0.0
                soc -= dis / ETA
            # Правило 2: есть свободная мощность на эффективном агрегате и место
            # в батарее - добираем до номинала и складываем разницу.
            elif per < P_NOM and soc < 0.90 * BESS_CAP:
                head = min(n * (P_NOM - per), BESS_PCH, (0.90 * BESS_CAP - soc) / ETA)
                if head > 1e-6 and sfc(per + head / n) < sfc(per):
                    per = per + head / n
                    ch = head
                    soc += ch * ETA

            starts = max(0, n - prev_on)
            prev_on = n
            gas = n * fuel_h(per)
            cost = (GAS * gas + START_C * starts + DEG * (ch + dis))
            log.append(dict(day=d, t=t, load=load, gen=[per] * n + [0.0] * (NG - n),
                            n=n, per=per, spill=spill0, grid=0.0, ch=ch, dis=dis,
                            soc=soc, gas=gas, starts=starts, cost=cost))
    return log


# =============================================================================
# СЛОЙ B. ОПТИМИЗАТОР (MILP) - эталон, недостижимый предел
# =============================================================================
def optimize_milp():
    G = list(range(NG))
    m = pulp.LpProblem("bench", pulp.LpMinimize)
    P = pulp.LpVariable.dicts("P", (G, T), lowBound=0)
    u = pulp.LpVariable.dicts("u", (G, T), cat="Binary")
    su = pulp.LpVariable.dicts("su", (G, T), cat="Binary")
    F = pulp.LpVariable.dicts("F", (G, T), lowBound=0)
    grid = pulp.LpVariable.dicts("gr", T, lowBound=0, upBound=GRID_MAX)
    spill = pulp.LpVariable.dicts("sp", T, lowBound=0)
    ch = pulp.LpVariable.dicts("ch", T, lowBound=0, upBound=BESS_PCH)
    dis = pulp.LpVariable.dicts("di", T, lowBound=0, upBound=BESS_PDIS)
    soc = pulp.LpVariable.dicts("so", T, lowBound=0.10 * BESS_CAP, upBound=BESS_CAP)

    for t in T:
        m += (pulp.lpSum(P[g][t] for g in G) + grid[t] + dis[t]
              == LOAD[t - 1] + ch[t] + spill[t])
        for g in G:
            m += P[g][t] <= P_NOM * u[g][t]
            m += P[g][t] >= P_MIN * u[g][t]
            for pk in (P_MIN, (P_MIN + P_NOM) / 2, P_NOM):
                m += F[g][t] >= (FUEL_A * u[g][t] + (FUEL_B + 2 * FUEL_C * pk) * P[g][t]
                                 - FUEL_C * pk * pk * u[g][t])
            m += su[g][t] >= u[g][t] - (1 if t == 1 else u[g][t - 1])
        prev = 0.5 * BESS_CAP if t == 1 else soc[t - 1]
        m += soc[t] == prev + ETA * ch[t] - dis[t] / ETA
    m += soc[24] == 0.5 * BESS_CAP

    m += pulp.lpSum(GAS * pulp.lpSum(F[g][t] for g in G)
                    + START_C * pulp.lpSum(su[g][t] for g in G)
                    + TARIFF[t - 1] * grid[t] + DEG * (ch[t] + dis[t]) for t in T)
    m.solve(pulp.PULP_CBC_CMD(msg=0))

    log = []
    for t in T:
        gen = sorted((P[g][t].value() for g in G), reverse=True)
        log.append(dict(day=0, t=t, load=LOAD[t - 1], gen=gen,
                        n=sum(round(u[g][t].value()) for g in G),
                        spill=spill[t].value(), grid=grid[t].value(),
                        ch=ch[t].value(), dis=dis[t].value(), soc=soc[t].value(),
                        gas=sum(F[g][t].value() for g in G),
                        starts=sum(su[g][t].value() for g in G),
                        cost=(GAS * sum(F[g][t].value() for g in G)
                              + START_C * sum(su[g][t].value() for g in G)
                              + TARIFF[t - 1] * grid[t].value()
                              + DEG * (ch[t].value() + dis[t].value()))))
    return pulp.LpStatus[m.status], log


# =============================================================================
# ОТЧЁТ
# =============================================================================
def steady(log, tail_days=10):
    """Rule-based симулятор не обязан замыкать сутки по энергии: SoC в конце
    может не совпасть с началом. Поэтому крутим много суток и берём среднее
    по последним - это установившийся режим, а не переходный процесс."""
    days = sorted({r["day"] for r in log})[-tail_days:]
    sel = [r for r in log if r["day"] in days]
    n = len(days)
    return {k: sum(r[k] for r in sel) / n for k in ("cost", "gas", "spill", "grid", "starts")}


def last_day(log):
    d = max(r["day"] for r in log)
    return [r for r in log if r["day"] == d]


if __name__ == "__main__":
    dp = 0 if POWER_UNIT == "kW" else 3
    print(f"Единицы: {POWER_UNIT} / {E_UNIT}")
    print(f"Удельный расход на номинале  sfc(P_NOM) = {sfc(P_NOM):.4f} м3/{E_UNIT}")
    print(f"Удельный расход на минимуме  sfc(P_MIN) = {sfc(P_MIN):.4f} м3/{E_UNIT}")
    print(f"Порог перелома стратегий  sfc(P_NOM)/RTE = {SFC_BREAKEVEN:.4f} м3/{E_UNIT}")
    print(f"Суточное потребление завода: {sum(LOAD):.3f} {E_UNIT}\n")

    runs = [("1. Load Following (без BESS)", steady(sim_load_following())),
            ("2. Cycle Charging (EMS из ТЗ)", steady(sim_cycle_charging())),
            ("3. Гибрид (порог по sfc)", steady(sim_hybrid()))]
    st, mlog = optimize_milp()
    runs.append((f"4. MILP-эталон ({st})",
                 {k: sum(r[k] for r in mlog) for k in ("cost", "gas", "spill", "grid", "starts")}))

    base = runs[0][1]["cost"]
    best = runs[-1][1]["cost"]
    print(f"{'Стратегия':<32}{'у.е./сут':>10}{'газ,м3':>10}{'сброс':>9}{'пуски':>7}{'gap,%':>8}")
    print("-" * 76)
    for name, r in runs:
        gap = (r["cost"] - best) / best * 100
        print(f"{name:<32}{r['cost']:>10.2f}{r['gas']:>10.1f}{r['spill']:>9.3f}"
              f"{r['starts']:>7.1f}{gap:>8.2f}")
    print("-" * 76)
    for name, r in runs[1:]:
        d = base - r["cost"]
        print(f"{name:<32} к базе: {d:+8.2f} у.е./сут   {d * 365:+11.0f} у.е./год")

    print("\n\nТАБЛИЦА 2. Cycle Charging (EMS из ТЗ), установившиеся сутки\n")
    h = (f"{'Час':>4}{'Нагр.':>8}{'Разряд':>8}{'SoC,%':>8}{'КГУ':>6}"
         f"{'КГУ1':>8}{'КГУ2':>8}{'КГУ3':>8}{'Заряд':>8}{'Газ,м3':>9}{'у.е.':>8}")
    print(h); print("-" * len(h))
    for r in last_day(sim_cycle_charging()):
        g = r["gen"]
        print(f"{r['t']:>4}{r['load']:>8.{dp}f}{r['dis']:>8.{dp}f}"
              f"{r['soc'] / BESS_CAP * 100:>8.1f}{'Вкл' if r['n'] else 'Выкл':>6}"
              f"{g[0]:>8.{dp}f}{g[1]:>8.{dp}f}{g[2]:>8.{dp}f}"
              f"{r['ch']:>8.{dp}f}{r['gas']:>9.1f}{r['cost']:>8.2f}")
```

## A.3 Итерация 3: три сценария и Monte Carlo

**`microgrid_ems.py`** — FSM, три профиля, годовая экстраполяция

```python
"""
Микроэнергосистема: 3 КГУ + BESS. Симулятор EMS-стратегии (Finite State Machine)
с тремя сценариями суток, Monte Carlo и годовой экстраполяцией.

Структура:
    БЛОК 1  единицы измерения
    БЛОК 2  топливная кривая (калибруется по паспортным точкам)
    БЛОК 3  генераторы профилей нагрузки + загрузка из CSV/Excel
    БЛОК 4  конечный автомат EMS (стратегия из ТЗ)
    БЛОК 5  базовый сценарий без батареи (Load Following)
    БЛОК 6  улучшенная стратегия (порог по удельному расходу)
    БЛОК 7  Monte Carlo и годовая экстраполяция

Запуск:  python3 microgrid_ems.py
"""

import numpy as np
import pandas as pd

# =============================================================================
# БЛОК 1. ЕДИНИЦЫ ИЗМЕРЕНИЯ
# =============================================================================
# Каноническая внутренняя единица - кВт и кВт*ч. POWER_UNIT влияет только на
# отображение и на то, в чём удобно вводить данные.
POWER_UNIT = "kW"
S = {"kW": 1.0, "MW": 1000.0}[POWER_UNIT]
E_UNIT = POWER_UNIT + "h"

def p(x_kw):    return x_kw / S        # мощность/энергия      (степень +1)
def per_p(x):   return x * S           # на единицу энергии    (степень -1)

# =============================================================================
# БЛОК 2. ОБОРУДОВАНИЕ И ТОПЛИВНАЯ КРИВАЯ
# =============================================================================
NG      = 3
P_NOM   = p(500.0)
P_MIN   = p(100.0)                     # 20% номинала, как задано в ТЗ
GAS     = 0.20                         # у.е./м3

# --- Калибровка кривой расхода по двум паспортным точкам ---------------------
# ТЗ задаёт: на 100% загрузке удельный расход = SFC_NOM,
#            на 20%  загрузке удельный расход на 40% выше.
# Модель расхода:  fuel(P) = A + B*P   (м3/ч)
# Тогда удельный:  sfc(P)  = A/P + B
# Два уравнения, два неизвестных - система решается аналитически, гадать не нужно.
SFC_NOM   = per_p(0.250)               # м3 на единицу энергии при P_NOM
PL_RATIO  = 0.20                       # доля номинала во второй точке
PL_PENALTY = 1.40                      # во сколько раз хуже удельный расход

# sfc(r*Pn)/sfc(Pn) = PENALTY  =>  (A/(r*Pn) + B)/(A/Pn + B) = PENALTY
# Обозначим alpha = A/Pn:  (alpha/r + B) = PENALTY*(alpha + B)
# =>  alpha*(1/r - PENALTY) = B*(PENALTY - 1)
_k = (PL_PENALTY - 1.0) / (1.0 / PL_RATIO - PL_PENALTY)      # B = alpha / _k
ALPHA = SFC_NOM / (1.0 + 1.0 / _k)
FUEL_A = ALPHA * P_NOM                 # м3/ч, постоянная составляющая
FUEL_B = ALPHA / _k                    # м3 на единицу энергии


def fuel_h(pw):
    """Расход газа одного РАБОТАЮЩЕГО агрегата, м3/ч."""
    return FUEL_A + FUEL_B * pw if pw > 1e-9 else 0.0


def sfc(pw):
    """Удельный расход. Слагаемое FUEL_A/pw - это и есть 'холостой ход':
    постоянная составляющая делится на всё меньшую выработку."""
    return fuel_h(pw) / pw if pw > 1e-9 else float("inf")


# --- Батарея ------------------------------------------------------------------
BESS_CAP  = p(3000.0)
BESS_P    = p(1500.0)                  # и заряд, и разряд
RTE       = 0.88                       # в ТЗ не задан, взят типовой для LFP
ETA       = RTE ** 0.5
SOC_LO    = 0.30 * BESS_CAP            # порог включения КГУ по ТЗ
SOC_HI    = 1.00 * BESS_CAP            # порог отключения КГУ по ТЗ

# --- Точка перелома стратегий -------------------------------------------------
# Пропустить энергию через батарею дороже в 1/RTE раз. Значит запасать выгодно,
# только если ПРЯМАЯ выработка идёт с удельным расходом хуже этого порога.
SFC_BREAKEVEN = SFC_NOM / RTE
P_BREAKEVEN = FUEL_A / (SFC_BREAKEVEN - FUEL_B)     # решаем A/P + B = порог


# =============================================================================
# БЛОК 3. ПРОФИЛИ НАГРУЗКИ
# =============================================================================
def load_nominal(rng):
    """Хаотичный день: 08:00-20:00 около 700, ночью около 100."""
    x = np.empty(24)
    for h in range(24):
        if 8 <= h < 20:
            x[h] = rng.uniform(600, 800)
        else:
            x[h] = rng.uniform(80, 120)
    return p(x)


def load_peak(rng):
    """Пиковый день: стабильные 1500 кВт круглосуточно."""
    return p(np.full(24, 1500.0))


def load_low(rng):
    """Разгрузка: около 300 кВт.
    ВНИМАНИЕ: в ТЗ сказано '30% от номинала' и одновременно 'около 300 кВт'.
    Это разные числа: 30% от 1500 = 450. Здесь взято 300 как написано явно;
    поменяй LOW_LEVEL, если имелось в виду 450."""
    LOW_LEVEL = 300.0
    return p(rng.uniform(LOW_LEVEL * 0.9, LOW_LEVEL * 1.1, 24))


SCENARIOS = {"nominal": (load_nominal, 200), "peak": (load_peak, 65), "low": (load_low, 100)}


def load_from_file(path, column="load_kw"):
    """Подстановка реальных исторических данных.
    Ожидается CSV/Excel с колонкой почасовой нагрузки в кВт.
    Возвращает список суточных массивов по 24 значения."""
    df = pd.read_excel(path) if str(path).endswith((".xlsx", ".xls")) else pd.read_csv(path)
    v = df[column].to_numpy(dtype=float)
    n = len(v) // 24
    return [p(v[i * 24:(i + 1) * 24]) for i in range(n)]


# =============================================================================
# БЛОК 4. КОНЕЧНЫЙ АВТОМАТ EMS (стратегия из ТЗ)
# =============================================================================
def commit(load, target_charge=0.0):
    """Сколько агрегатов включить и с какой мощностью.
    Возвращает (n, мощность каждого, сброс)."""
    need = load + target_charge
    n = max(1, int(np.ceil(need / P_NOM - 1e-9)))
    n = min(n, NG)
    per = min(P_NOM, max(P_MIN, need / n))
    spill = max(0.0, n * per - load - target_charge)
    return n, per, spill


def sim_ems(load, soc0=None, state0="DISCHARGE"):
    """Стратегия из ТЗ. Два состояния, переходы строго по SoC.

    DISCHARGE:  SoC > 30%  -> вся нагрузка от батареи, КГУ выключены
                переход в CHARGE, когда SoC упирается в 30%
    CHARGE:     КГУ на эффективной мощности, избыток в батарею
                переход в DISCHARGE, когда SoC = 100%
    """
    soc = SOC_HI if soc0 is None else soc0
    st = state0
    rows = []
    for h in range(24):
        L = load[h]

        # --- переходы автомата (проверяются ДО расчёта часа) ----------------
        if st == "DISCHARGE":
            if soc - min(L, BESS_P) / ETA < SOC_LO - 1e-9:
                st = "CHARGE"
        elif soc >= SOC_HI - 1e-9:
            st = "DISCHARGE"

        gen = [0.0] * NG
        dis = ch = spill = 0.0
        deficit = 0.0

        if st == "DISCHARGE":
            dis = min(L, BESS_P, max(0.0, (soc - SOC_LO) * ETA))
            deficit = L - dis                    # если батарея не тянет пик
            soc -= dis / ETA
        else:
            room = max(0.0, (SOC_HI - soc) / ETA)
            want = min(BESS_P, room)
            n, per, spill = commit(L, want)
            for i in range(n):
                gen[i] = per
            surplus = n * per - L
            ch = max(0.0, min(surplus, BESS_P, room))
            spill = surplus - ch
            soc += ch * ETA

        gas = sum(fuel_h(g) for g in gen)
        rows.append(dict(hour=h + 1, load=L, dis=dis, ch=ch, soc_pct=soc / BESS_CAP * 100,
                         g1=gen[0], g2=gen[1], g3=gen[2], spill=spill, deficit=deficit,
                         gas=gas, cost=GAS * gas, state=st))
    return pd.DataFrame(rows), soc, st


# =============================================================================
# БЛОК 5. БАЗОВЫЙ СЦЕНАРИЙ БЕЗ БАТАРЕИ
# =============================================================================
def sim_no_bess(load):
    """Load Following: КГУ следят за нагрузкой, при её падении уходят
    в техминимум и сбрасывают избыток."""
    rows = []
    for h in range(24):
        L = load[h]
        n, per, spill = commit(L)
        gen = [per] * n + [0.0] * (NG - n)
        gas = sum(fuel_h(g) for g in gen)
        rows.append(dict(hour=h + 1, load=L, dis=0.0, ch=0.0, soc_pct=0.0,
                         g1=gen[0], g2=gen[1], g3=gen[2], spill=spill, deficit=0.0,
                         gas=gas, cost=GAS * gas, state="LF"))
    return pd.DataFrame(rows)


# =============================================================================
# БЛОК 6. УЛУЧШЕННАЯ СТРАТЕГИЯ: ПОРОГ ПО УДЕЛЬНОМУ РАСХОДУ
# =============================================================================
def sim_threshold(load, soc0=None):
    """Батарея включается НЕ всегда, а только когда прямая выработка была бы
    неэффективнее, чем через накопитель: загрузка агрегата ниже P_BREAKEVEN
    или есть вынужденный сброс. В остальное время КГУ питают завод напрямую
    и попутно добирают заряд до эффективной точки."""
    soc = 0.5 * BESS_CAP if soc0 is None else soc0
    rows = []
    for h in range(24):
        L = load[h]
        n, per, spill = commit(L)
        gen = [0.0] * NG
        dis = ch = 0.0

        if (spill > 1e-9 or per < P_BREAKEVEN) and soc > SOC_LO:
            dis = min(L, BESS_P, (soc - SOC_LO) * ETA)
            soc -= dis / ETA
            n = 0
            spill = 0.0
            if L - dis > 1e-9:                    # батарея закрыла не всё
                n, per, spill = commit(L - dis)
                for i in range(n):
                    gen[i] = per
        else:
            for i in range(n):
                gen[i] = per
            room = max(0.0, (0.95 * BESS_CAP - soc) / ETA)
            head = min(n * (P_NOM - per), BESS_P, room)
            if head > 1e-6:
                per_new = per + head / n
                for i in range(n):
                    gen[i] = per_new
                ch = head
                soc += ch * ETA

        gas = sum(fuel_h(g) for g in gen)
        rows.append(dict(hour=h + 1, load=L, dis=dis, ch=ch, soc_pct=soc / BESS_CAP * 100,
                         g1=gen[0], g2=gen[1], g3=gen[2], spill=spill, deficit=0.0,
                         gas=gas, cost=GAS * gas, state="TH"))
    return pd.DataFrame(rows), soc


# =============================================================================
# БЛОК 7. MONTE CARLO И ГОДОВАЯ ЭКСТРАПОЛЯЦИЯ
# =============================================================================
def monte_carlo(n_days=300, seed=42):
    """Один случайный день ничего не доказывает: результат зависит от того,
    какие числа выпали. Гоняем много реализаций и берём среднее со СКО.
    Состояние батареи переносится из суток в сутки - иначе автомат вечно
    стартует со 100% и модель льстит батарее."""
    rng = np.random.default_rng(seed)
    out = {}
    for name, (gen_load, days) in SCENARIOS.items():
        acc = {"no_bess": [], "ems": [], "thr": [], "spill_nb": [], "spill_ems": [],
               "def_ems": [], "starts_ems": []}
        soc_e, st_e, soc_t = SOC_HI, "DISCHARGE", 0.5 * BESS_CAP
        for _ in range(n_days):
            L = gen_load(rng)
            nb = sim_no_bess(L)
            de, soc_e, st_e = sim_ems(L, soc_e, st_e)
            dt, soc_t = sim_threshold(L, soc_t)
            acc["no_bess"].append(nb.gas.sum())
            acc["ems"].append(de.gas.sum())
            acc["thr"].append(dt.gas.sum())
            acc["spill_nb"].append(nb.spill.sum())
            acc["spill_ems"].append(de.spill.sum())
            acc["def_ems"].append(de.deficit.sum())
            on = (de[["g1", "g2", "g3"]] > 0).sum(axis=1).to_numpy()
            acc["starts_ems"].append(int(np.clip(np.diff(np.r_[0, on]), 0, None).sum()))
        out[name] = {k: (float(np.mean(v)), float(np.std(v))) for k, v in acc.items()}
        out[name]["days"] = days
    return out


def annual(mc):
    rows = []
    for name, r in mc.items():
        d = r["days"]
        rows.append(dict(scenario=name, days=d,
                         gas_nb=r["no_bess"][0] * d, gas_ems=r["ems"][0] * d,
                         gas_thr=r["thr"][0] * d,
                         spill_nb=r["spill_nb"][0] * d, spill_ems=r["spill_ems"][0] * d,
                         deficit_ems=r["def_ems"][0] * d, starts_ems=r["starts_ems"][0] * d))
    df = pd.DataFrame(rows)
    df.loc["ИТОГО"] = df.sum(numeric_only=True)
    df.loc["ИТОГО", "scenario"] = "ИТОГО за год"
    return df


if __name__ == "__main__":
    pd.set_option("display.width", 200)
    print(f"Единицы: {POWER_UNIT} / {E_UNIT}\n")
    print("КАЛИБРОВКА ТОПЛИВНОЙ КРИВОЙ (по двум точкам из ТЗ)")
    print(f"  fuel(P) = {FUEL_A:.3f} + {FUEL_B:.5f} * P   м3/ч")
    print(f"  sfc(100% = {P_NOM:.0f}) = {sfc(P_NOM):.4f} м3/{E_UNIT}")
    print(f"  sfc( 20% = {P_MIN:.0f}) = {sfc(P_MIN):.4f}  -> "
          f"{sfc(P_MIN) / sfc(P_NOM):.2f}x  (проверка: должно быть 1.40)")
    print(f"\nПОРОГ ПЕРЕЛОМА СТРАТЕГИЙ")
    print(f"  sfc(P_NOM)/RTE = {SFC_BREAKEVEN:.4f} м3/{E_UNIT}")
    print(f"  P_BREAKEVEN    = {P_BREAKEVEN:.1f} {POWER_UNIT} "
          f"({P_BREAKEVEN / P_NOM * 100:.1f}% номинала)")
    print(f"  Ниже этой загрузки выгоднее заряжать батарею, выше - питать напрямую.\n")

    mc = monte_carlo()
    print("MONTE CARLO, средний расход газа за сутки, м3 (в скобках СКО)")
    print(f"{'Сценарий':<12}{'без BESS':>18}{'EMS из ТЗ':>18}{'порог по sfc':>18}"
          f"{'сброс без BESS':>16}{'дефицит EMS':>14}")
    print("-" * 96)
    for n_, r in mc.items():
        print(f"{n_:<12}{r['no_bess'][0]:>11.1f} ±{r['no_bess'][1]:<5.1f}"
              f"{r['ems'][0]:>11.1f} ±{r['ems'][1]:<5.1f}"
              f"{r['thr'][0]:>11.1f} ±{r['thr'][1]:<5.1f}"
              f"{r['spill_nb'][0]:>16.1f}{r['def_ems'][0]:>14.1f}")

    df = annual(mc)
    print("\n\nГОДОВАЯ ЭКСТРАПОЛЯЦИЯ")
    print(df.to_string(index=False, float_format=lambda x: f"{x:,.0f}"))
    tot = df.loc["ИТОГО"]
    print(f"\n{'':<28}{'газ, м3/год':>16}{'у.е./год':>16}{'к базе':>14}")
    print("-" * 74)
    base = tot.gas_nb
    for lbl, key in (("Без батареи", "gas_nb"), ("EMS из ТЗ", "gas_ems"),
                     ("Порог по sfc", "gas_thr")):
        g = tot[key]
        print(f"{lbl:<28}{g:>16,.0f}{g * GAS:>16,.0f}"
              f"{(base - g) / base * 100:>13.2f}%")
    print(f"\nЭкономия EMS из ТЗ:     {(base - tot.gas_ems) * GAS:>12,.0f} у.е./год")
    print(f"Экономия порога по sfc: {(base - tot.gas_thr) * GAS:>12,.0f} у.е./год")
    print(f"Недоотпуск EMS из ТЗ:   {tot.deficit_ems:>12,.0f} {E_UNIT}/год  "
          f"<-- нагрузка, которую система НЕ ЗАКРЫЛА")
```

## A.4 Итерация 4: исправленный EMS

**`microgrid_ems_v2.py`** — шесть исправлений, CHP_ONLY, min-up-time

```python
"""
Микроэнергосистема: 3 КГУ + BESS. Симулятор EMS-стратегии (Finite State Machine)
с тремя сценариями суток, Monte Carlo и годовой экстраполяцией.

Структура:
    БЛОК 1  единицы измерения
    БЛОК 2  топливная кривая (калибруется по паспортным точкам)
    БЛОК 3  генераторы профилей нагрузки + загрузка из CSV/Excel
    БЛОК 4  конечный автомат EMS (стратегия из ТЗ)
    БЛОК 5  базовый сценарий без батареи (Load Following)
    БЛОК 6  улучшенная стратегия (порог по удельному расходу)
    БЛОК 7  Monte Carlo и годовая экстраполяция

Запуск:  python3 microgrid_ems.py
"""

import numpy as np
import pandas as pd

# =============================================================================
# БЛОК 1. ЕДИНИЦЫ ИЗМЕРЕНИЯ
# =============================================================================
# Каноническая внутренняя единица - кВт и кВт*ч. POWER_UNIT влияет только на
# отображение и на то, в чём удобно вводить данные.
POWER_UNIT = "kW"
S = {"kW": 1.0, "MW": 1000.0}[POWER_UNIT]
E_UNIT = POWER_UNIT + "h"

def p(x_kw):    return x_kw / S        # мощность/энергия      (степень +1)
def per_p(x):   return x * S           # на единицу энергии    (степень -1)

# =============================================================================
# БЛОК 2. ОБОРУДОВАНИЕ И ТОПЛИВНАЯ КРИВАЯ
# =============================================================================
NG      = 3
P_NOM   = p(500.0)
P_MIN   = p(100.0)                     # 20% номинала, как задано в ТЗ
GAS     = 0.20                         # у.е./м3

# --- Калибровка кривой расхода по двум паспортным точкам ---------------------
# ТЗ задаёт: на 100% загрузке удельный расход = SFC_NOM,
#            на 20%  загрузке удельный расход на 40% выше.
# Модель расхода:  fuel(P) = A + B*P   (м3/ч)
# Тогда удельный:  sfc(P)  = A/P + B
# Два уравнения, два неизвестных - система решается аналитически, гадать не нужно.
SFC_NOM   = per_p(0.250)               # м3 на единицу энергии при P_NOM
PL_RATIO  = 0.20                       # доля номинала во второй точке
PL_PENALTY = 1.40                      # во сколько раз хуже удельный расход

# sfc(r*Pn)/sfc(Pn) = PENALTY  =>  (A/(r*Pn) + B)/(A/Pn + B) = PENALTY
# Обозначим alpha = A/Pn:  (alpha/r + B) = PENALTY*(alpha + B)
# =>  alpha*(1/r - PENALTY) = B*(PENALTY - 1)
_k = (PL_PENALTY - 1.0) / (1.0 / PL_RATIO - PL_PENALTY)      # B = alpha / _k
ALPHA = SFC_NOM / (1.0 + 1.0 / _k)
FUEL_A = ALPHA * P_NOM                 # м3/ч, постоянная составляющая
FUEL_B = ALPHA / _k                    # м3 на единицу энергии


def fuel_h(pw):
    """Расход газа одного РАБОТАЮЩЕГО агрегата, м3/ч."""
    return FUEL_A + FUEL_B * pw if pw > 1e-9 else 0.0


def sfc(pw):
    """Удельный расход. Слагаемое FUEL_A/pw - это и есть 'холостой ход':
    постоянная составляющая делится на всё меньшую выработку."""
    return fuel_h(pw) / pw if pw > 1e-9 else float("inf")


# --- Батарея ------------------------------------------------------------------
BESS_CAP  = p(3000.0)
BESS_P    = p(1500.0)                  # и заряд, и разряд
RTE       = 0.88                       # в ТЗ не задан, взят типовой для LFP
ETA       = RTE ** 0.5
SOC_LO    = 0.30 * BESS_CAP            # порог включения КГУ по ТЗ
SOC_HI    = 1.00 * BESS_CAP            # порог отключения КГУ по ТЗ

# --- Точка перелома стратегий -------------------------------------------------
# Пропустить энергию через батарею дороже в 1/RTE раз. Значит запасать выгодно,
# только если ПРЯМАЯ выработка идёт с удельным расходом хуже этого порога.
SFC_BREAKEVEN = SFC_NOM / RTE
P_BREAKEVEN = FUEL_A / (SFC_BREAKEVEN - FUEL_B)     # решаем A/P + B = порог


# =============================================================================
# БЛОК 3. ПРОФИЛИ НАГРУЗКИ
# =============================================================================
def load_shift(rng):
    """Сменный режим: дневная смена 08:00-20:00 около 700 кВт,
    ночной провал до охраны и освещения около 100 кВт."""
    x = np.empty(24)
    for h in range(24):
        if 8 <= h < 20:
            x[h] = rng.uniform(600, 800)
        else:
            x[h] = rng.uniform(80, 120)
    return p(x)


def load_continuous(rng):
    """Непрерывное производство: стабильные 1500 кВт круглосуточно."""
    return p(np.full(24, 1500.0))


def load_reduced(rng):
    """Пониженная активность: около 300 кВт.
    ВНИМАНИЕ: в ТЗ сказано '30% от номинала' и одновременно 'около 300 кВт'.
    Это разные числа: 30% от 1500 = 450. Здесь взято 300 как написано явно;
    поменяй LOW_LEVEL, если имелось в виду 450."""
    LOW_LEVEL = 300.0
    return p(rng.uniform(LOW_LEVEL * 0.9, LOW_LEVEL * 1.1, 24))


SCENARIOS = {
    # ключ: (генератор профиля, дней в году, читаемое название)
    "shift":      (load_shift, 200, "Сменный режим — дневная смена и ночной провал"),
    "continuous": (load_continuous, 65, "Непрерывное производство — полная мощность круглосуточно"),
    "reduced":    (load_reduced, 100, "Пониженная активность — работа на 30%"),
}


def load_from_file(path, column="load_kw"):
    """Подстановка реальных исторических данных.
    Ожидается CSV/Excel с колонкой почасовой нагрузки в кВт.
    Возвращает список суточных массивов по 24 значения."""
    df = pd.read_excel(path) if str(path).endswith((".xlsx", ".xls")) else pd.read_csv(path)
    v = df[column].to_numpy(dtype=float)
    n = len(v) // 24
    return [p(v[i * 24:(i + 1) * 24]) for i in range(n)]


# =============================================================================
# БЛОК 4. КОНЕЧНЫЙ АВТОМАТ EMS (исправленная версия)
# =============================================================================
MIN_UP_HOURS = 2                        # минимальное время работы после пуска
PARASITIC    = 0.001 * BESS_CAP         # собственные нужды BESS (HVAC), кВт*ч/ч
STUCK_HOURS  = 2                        # сколько часов бесплодного CHARGE до выхода
SOC_RESERVE  = 0.05 * BESS_CAP          # ниже этого BMS размыкает контактор
ALLOW_EXTRA_UNIT_FOR_CHARGE = False     # разрешать ли пуск лишнего КГУ ради заряда

# --- Маржинальная стоимость хранения -----------------------------------------
# КЛЮЧЕВОЕ ИСПРАВЛЕНИЕ. Раньше сравнивали sfc(P_NOM)/RTE = 0.2841 - это цена
# энергии, произведённой ОТДЕЛЬНО ЗАПУЩЕННЫМ агрегатом. Но если агрегат уже
# работает под нагрузку завода, постоянная составляющая FUEL_A уже оплачена.
# Каждый дополнительный кВт*ч сверх текущей точки стоит только FUEL_B.
# Поэтому "дозагрузка" работающего агрегата в батарею стоит FUEL_B / RTE,
# а не sfc(P_NOM) / RTE. Разница принципиальная:
#     sfc(P_NOM)/RTE = 0.2841  -> заряжать почти никогда не выгодно
#     FUEL_B/RTE     = 0.2557  -> заряжать выгодно почти всегда, когда есть запас
CHARGE_MARGINAL = FUEL_B / RTE


def commit(p_serve, n_forced=0):
    """Распределить требуемую мощность p_serve между агрегатами.

    p_serve  - сколько ДОЛЖНЫ выдать КГУ суммарно (нагрузка + желаемый заряд)
    n_forced - сколько агрегатов ОБЯЗАНЫ работать из-за min-up-time

    Возвращает (n, [мощность каждого], сброс).
    Сброс возникает, когда суммарный техминимум включённых агрегатов выше
    того, что от них требуется, и деть избыток некуда.
    """
    n_need = int(np.ceil(p_serve / P_NOM - 1e-9)) if p_serve > 1e-9 else 0
    n = min(NG, max(n_need, n_forced))
    if n == 0:
        return 0, [0.0] * NG, 0.0

    per = p_serve / n
    spill = 0.0
    if per < P_MIN:                     # техминимум выше требуемого
        per = P_MIN
        spill = n * P_MIN - p_serve
    elif per > P_NOM:                   # мощности не хватает, дефицит наверху
        per = P_NOM
    return n, [per] * n + [0.0] * (NG - n), spill


class EmsState:
    """Состояние автомата, переносимое между сутками.
    Раньше переносились только soc и режим. Теперь ещё и наработка каждого
    агрегата - без неё min-up-time не работает на границе суток."""
    def __init__(self, soc=None, mode="DISCHARGE"):
        self.soc = SOC_HI if soc is None else soc
        self.mode = mode
        self.on_h = [0] * NG            # часов подряд в работе, 0 = остановлен
        self.stuck = 0                  # счётчик бесплодных часов в CHARGE


def sim_ems(load, state=None):
    """Исправленный конечный автомат.

    Состояния:
      DISCHARGE  батарея питает завод; КГУ подхватывают только пик выше BESS_P
                 либо дорабатывают min-up-time
      CHARGE     КГУ питают завод и ДОЗАГРУЖАЮТСЯ в батарею своим свободным
                 запасом мощности; лишние агрегаты ради заряда не пускаются
      CHP_ONLY   аварийный выход из тупика: заряжать нечем (нагрузка съедает
                 всю установленную мощность), батарея выведена из контура

    Переходы вычисляются В КОНЦЕ шага по фактическому SoC, а не в начале
    по прогнозу - иначе в час переключения батарея вслепую переставала
    разряжаться, не отдав остаток до SOC_LO.
    """
    st = state if state is not None else EmsState()
    rows = []

    for h in range(len(load)):
        L = load[h]

        # --- 1. Собственные нужды BESS. Списываются КАЖДЫЙ час, в любом
        #        состоянии, включая простой. Контейнерный BESS греет и
        #        охлаждает себя всегда, пока подключён.
        # Ниже аварийного резерва реальный BMS размыкает контактор и снимает
        # питание с климатики: иначе батарея убила бы себя разрядом в ноль.
        # Без этой проверки модель показывала SoC = 0.0% сутками подряд, что
        # для LFP означает необратимую деградацию, а не режим работы.
        if st.soc > SOC_RESERVE:
            st.soc = max(SOC_RESERVE, st.soc - PARASITIC)

        # --- 2. Кто обязан работать по min-up-time ---------------------------
        n_forced = sum(1 for x in st.on_h if 0 < x < MIN_UP_HOURS)

        gen = [0.0] * NG
        dis = ch = spill = deficit = 0.0

        # =====================================================================
        if st.mode == "DISCHARGE":
            # Сколько батарея реально может отдать в этот час.
            avail = min(BESS_P, max(0.0, (st.soc - SOC_LO) * ETA))

            # PEAK SHAVING: если нагрузка выше мощности разряда, КГУ не пишут
            # дефицит, а запускаются и добирают разницу.
            p_chp_need = max(0.0, L - avail)
            n, gen, _ = commit(p_chp_need, n_forced)
            p_chp = sum(gen)

            # Батарея закрывает то, что осталось после КГУ.
            dis = min(avail, max(0.0, L - p_chp))
            st.soc -= dis / ETA

            # Сброс считаем ТОЛЬКО по фактическому балансу часа. Внутренний
            # spill из commit() здесь не годится: он посчитан относительно
            # p_chp_need, а не относительно всей нагрузки завода, и без этой
            # поправки техминимум вынужденного агрегата попадал бы в сброс,
            # хотя завод его целиком потребляет.
            surplus = p_chp + dis - L
            if surplus > 1e-9:
                room = max(0.0, (SOC_HI - st.soc) / ETA)
                ch = min(surplus, BESS_P, room)
                st.soc += ch * ETA
                spill = surplus - ch
            deficit = max(0.0, L - p_chp - dis)

        # =====================================================================
        elif st.mode == "CHARGE":
            # ИСПРАВЛЕНИЕ 1 и 2: состав определяется ТОЛЬКО нагрузкой завода.
            # Батарея не требует BESS_P вслепую - она забирает лишь свободный
            # запас мощности уже работающих агрегатов.
            n_load, gen_base, _ = commit(L, n_forced)
            per_base = gen_base[0] if n_load else 0.0
            headroom = n_load * P_NOM - max(L, n_load * P_MIN)

            room = max(0.0, (SOC_HI - st.soc) / ETA)

            # Экономический фильтр: дозагружаться в батарею имеет смысл,
            # только если её энергия потом заменит выработку ХУЖЕ, чем
            # обходится хранение по маржинальной цене.
            gate = CHARGE_MARGINAL < sfc(per_base) if n_load else False
            want = min(headroom, BESS_P, room) if gate else 0.0

            if ALLOW_EXTRA_UNIT_FOR_CHARGE and want < room and n_load < NG:
                # Пуск лишнего агрегата окупается, только если ПОЛНАЯ его
                # стоимость через батарею дешевле худшей прямой выработки.
                if sfc(P_NOM) / RTE < sfc(P_MIN):
                    want = min(room, BESS_P, (n_load + 1) * P_NOM - L)

            n, gen, spill = commit(L + want, n_forced)
            p_chp = sum(gen)
            ch = min(max(0.0, p_chp - L), BESS_P, room)
            st.soc += ch * ETA
            spill = max(0.0, p_chp - L - ch)
            deficit = max(0.0, L - p_chp)
            st.stuck = st.stuck + 1 if ch <= 1e-9 else 0

        # =====================================================================
        else:                                    # CHP_ONLY, батарея в обходе
            n, gen, spill = commit(L, n_forced)
            deficit = max(0.0, L - sum(gen))

        # --- 3. Наработка агрегатов (для min-up-time следующего часа) --------
        for i in range(NG):
            st.on_h[i] = st.on_h[i] + 1 if gen[i] > 1e-9 else 0

        # --- 4. ПЕРЕХОДЫ. Считаются в КОНЦЕ шага по фактическому SoC ---------
        if st.mode == "DISCHARGE":
            if st.soc <= SOC_LO + 1e-9:
                st.mode, st.stuck = "CHARGE", 0
        elif st.mode == "CHARGE":
            if st.soc >= SOC_HI - 1e-9:
                st.mode = "DISCHARGE"
            elif st.stuck >= STUCK_HOURS:
                # Тупик: нагрузка съедает всю установленную мощность, свободного
                # запаса для заряда нет. Без этого выхода автомат зависал бы
                # в CHARGE навсегда, а паразитная нагрузка утащила бы SoC в ноль.
                st.mode = "CHP_ONLY"
        else:
            if L + P_MIN < NG * P_NOM and st.soc < SOC_HI:
                st.mode, st.stuck = "CHARGE", 0

        gas = sum(fuel_h(g) for g in gen)
        rows.append(dict(hour=h + 1, load=L, dis=dis, ch=ch,
                         soc_pct=st.soc / BESS_CAP * 100,
                         g1=gen[0], g2=gen[1], g3=gen[2], spill=spill,
                         deficit=deficit, gas=gas, cost=GAS * gas, state=st.mode))
    return pd.DataFrame(rows), st


# =============================================================================
# БЛОК 5. БАЗОВЫЙ СЦЕНАРИЙ БЕЗ БАТАРЕИ
# =============================================================================
def sim_no_bess(load):
    """Load Following: КГУ следят за нагрузкой, при её падении уходят
    в техминимум и сбрасывают избыток."""
    rows = []
    for h in range(len(load)):
        L = load[h]
        n, gen, spill = commit(L)
        gas = sum(fuel_h(g) for g in gen)
        rows.append(dict(hour=h + 1, load=L, dis=0.0, ch=0.0, soc_pct=0.0,
                         g1=gen[0], g2=gen[1], g3=gen[2], spill=spill, deficit=0.0,
                         gas=gas, cost=GAS * gas, state="LF"))
    return pd.DataFrame(rows)


# =============================================================================
# БЛОК 6. УЛУЧШЕННАЯ СТРАТЕГИЯ: ПОРОГ ПО УДЕЛЬНОМУ РАСХОДУ
# =============================================================================
def sim_threshold(load, soc0=None):
    """Батарея включается НЕ всегда, а только когда прямая выработка была бы
    неэффективнее, чем через накопитель: загрузка агрегата ниже P_BREAKEVEN
    или есть вынужденный сброс. В остальное время КГУ питают завод напрямую
    и попутно добирают заряд до эффективной точки."""
    soc = 0.5 * BESS_CAP if soc0 is None else soc0
    rows = []
    for h in range(len(load)):
        L = load[h]
        # Собственные нужды BESS: эта стратегия тоже работает С батареей,
        # значит и HVAC контейнера она обязана оплачивать. Без этой строки
        # сравнение с EMS было бы нечестным в её пользу.
        # Ниже аварийного резерва реальный BMS размыкает контактор и снимает
        # питание с климатики: иначе батарея убила бы себя разрядом в ноль.
        # Без этой проверки модель показывала SoC = 0.0% сутками подряд, что
        # для LFP означает необратимую деградацию, а не режим работы.
        if soc > SOC_RESERVE:
            soc = max(SOC_RESERVE, soc - PARASITIC)
        n, gen_base, spill = commit(L)
        per = gen_base[0] if n else 0.0
        gen = [0.0] * NG
        dis = ch = 0.0

        if (spill > 1e-9 or per < P_BREAKEVEN) and soc > SOC_LO:
            dis = min(L, BESS_P, (soc - SOC_LO) * ETA)
            soc -= dis / ETA
            n = 0
            spill = 0.0
            if L - dis > 1e-9:                    # батарея закрыла не всё
                n, gen, spill = commit(L - dis)
                per = gen[0] if n else 0.0
        else:
            for i in range(n):
                gen[i] = per
            room = max(0.0, (0.95 * BESS_CAP - soc) / ETA)
            head = min(n * (P_NOM - per), BESS_P, room)
            if head > 1e-6:
                per_new = per + head / n
                for i in range(n):
                    gen[i] = per_new
                ch = head
                soc += ch * ETA

        gas = sum(fuel_h(g) for g in gen)
        rows.append(dict(hour=h + 1, load=L, dis=dis, ch=ch, soc_pct=soc / BESS_CAP * 100,
                         g1=gen[0], g2=gen[1], g3=gen[2], spill=spill, deficit=0.0,
                         gas=gas, cost=GAS * gas, state="TH"))
    return pd.DataFrame(rows), soc


# =============================================================================
# БЛОК 7. MONTE CARLO И ГОДОВАЯ ЭКСТРАПОЛЯЦИЯ
# =============================================================================
def monte_carlo(n_days=300, seed=42):
    """Один случайный день ничего не доказывает: результат зависит от того,
    какие числа выпали. Гоняем много реализаций и берём среднее со СКО.
    Состояние батареи переносится из суток в сутки - иначе автомат вечно
    стартует со 100% и модель льстит батарее."""
    rng = np.random.default_rng(seed)
    out = {}
    for name, (gen_load, days, _title) in SCENARIOS.items():
        acc = {"no_bess": [], "ems": [], "thr": [], "spill_nb": [], "spill_ems": [],
               "def_ems": [], "starts_ems": []}
        ems_state, soc_t = EmsState(), 0.5 * BESS_CAP
        for _ in range(n_days):
            L = gen_load(rng)
            nb = sim_no_bess(L)
            de, ems_state = sim_ems(L, ems_state)
            dt, soc_t = sim_threshold(L, soc_t)
            acc["no_bess"].append(nb.gas.sum())
            acc["ems"].append(de.gas.sum())
            acc["thr"].append(dt.gas.sum())
            acc["spill_nb"].append(nb.spill.sum())
            acc["spill_ems"].append(de.spill.sum())
            acc["def_ems"].append(de.deficit.sum())
            on = (de[["g1", "g2", "g3"]] > 0).sum(axis=1).to_numpy()
            acc["starts_ems"].append(int(np.clip(np.diff(np.r_[0, on]), 0, None).sum()))
        out[name] = {k: (float(np.mean(v)), float(np.std(v))) for k, v in acc.items()}
        out[name]["days"] = days
    return out


def annual(mc):
    rows = []
    for name, r in mc.items():
        d = r["days"]
        rows.append(dict(scenario=name, days=d,
                         gas_nb=r["no_bess"][0] * d, gas_ems=r["ems"][0] * d,
                         gas_thr=r["thr"][0] * d,
                         spill_nb=r["spill_nb"][0] * d, spill_ems=r["spill_ems"][0] * d,
                         deficit_ems=r["def_ems"][0] * d, starts_ems=r["starts_ems"][0] * d))
    df = pd.DataFrame(rows)
    df.loc["ИТОГО"] = df.sum(numeric_only=True)
    df.loc["ИТОГО", "scenario"] = "ИТОГО за год"
    return df


if __name__ == "__main__":
    pd.set_option("display.width", 200)
    print(f"Единицы: {POWER_UNIT} / {E_UNIT}\n")
    print("КАЛИБРОВКА ТОПЛИВНОЙ КРИВОЙ (по двум точкам из ТЗ)")
    print(f"  fuel(P) = {FUEL_A:.3f} + {FUEL_B:.5f} * P   м3/ч")
    print(f"  sfc(100% = {P_NOM:.0f}) = {sfc(P_NOM):.4f} м3/{E_UNIT}")
    print(f"  sfc( 20% = {P_MIN:.0f}) = {sfc(P_MIN):.4f}  -> "
          f"{sfc(P_MIN) / sfc(P_NOM):.2f}x  (проверка: должно быть 1.40)")
    print(f"\nПОРОГ ПЕРЕЛОМА СТРАТЕГИЙ")
    print(f"  sfc(P_NOM)/RTE = {SFC_BREAKEVEN:.4f} м3/{E_UNIT}")
    print(f"  P_BREAKEVEN    = {P_BREAKEVEN:.1f} {POWER_UNIT} "
          f"({P_BREAKEVEN / P_NOM * 100:.1f}% номинала)")
    print(f"  Ниже этой загрузки выгоднее заряжать батарею, выше - питать напрямую.\n")

    mc = monte_carlo()
    print("MONTE CARLO, средний расход газа за сутки, м3 (в скобках СКО)")
    print(f"{'Сценарий':<12}{'без BESS':>18}{'EMS из ТЗ':>18}{'порог по sfc':>18}"
          f"{'сброс без BESS':>16}{'дефицит EMS':>14}")
    print("-" * 96)
    for n_, r in mc.items():
        print(f"{n_:<12}{r['no_bess'][0]:>11.1f} ±{r['no_bess'][1]:<5.1f}"
              f"{r['ems'][0]:>11.1f} ±{r['ems'][1]:<5.1f}"
              f"{r['thr'][0]:>11.1f} ±{r['thr'][1]:<5.1f}"
              f"{r['spill_nb'][0]:>16.1f}{r['def_ems'][0]:>14.1f}")

    df = annual(mc)
    print("\n\nГОДОВАЯ ЭКСТРАПОЛЯЦИЯ")
    print(df.to_string(index=False, float_format=lambda x: f"{x:,.0f}"))
    tot = df.loc["ИТОГО"]
    print(f"\n{'':<28}{'газ, м3/год':>16}{'у.е./год':>16}{'к базе':>14}")
    print("-" * 74)
    base = tot.gas_nb
    for lbl, key in (("Без батареи", "gas_nb"), ("EMS из ТЗ", "gas_ems"),
                     ("Порог по sfc", "gas_thr")):
        g = tot[key]
        print(f"{lbl:<28}{g:>16,.0f}{g * GAS:>16,.0f}"
              f"{(base - g) / base * 100:>13.2f}%")
    print(f"\nЭкономия EMS из ТЗ:     {(base - tot.gas_ems) * GAS:>12,.0f} у.е./год")
    print(f"Экономия порога по sfc: {(base - tot.gas_thr) * GAS:>12,.0f} у.е./год")
    print(f"Недоотпуск EMS из ТЗ:   {tot.deficit_ems:>12,.0f} {E_UNIT}/год  "
          f"<-- нагрузка, которую система НЕ ЗАКРЫЛА")
```

## A.5 Итерация 5: два КГУ, суточная модель

**`factory_2chp.py`** — правило 90%, дискретный состав, безлимитная сеть

```python
"""
Микросеть завода: 2 КГУ + BESS + безлимитная городская сеть.

Ключевое отличие от предыдущих версий: сеть безлимитная, поэтому физического
дефицита не бывает никогда. Задача чисто экономическая - минимизировать
затраты и срезать пиковый закуп из сети.

Порядок предпочтения источников (merit order) задан тарифами:
    КГУ            22 ₸/кВт*ч   <-- самый дешёвый, брать максимум
    BESS (разряд)  25 ₸/кВт*ч   <-- 22 / 0.88, энергия из КГУ через накопитель
    Сеть           60 ₸/кВт*ч   <-- самый дорогой, брать в последнюю очередь

Запуск:  python3 factory_2chp.py
"""

import numpy as np
import pandas as pd

# =============================================================================
# БЛОК 1. ПАРК ГЕНЕРАЦИИ
# =============================================================================
# Каждый агрегат описан отдельно: разные номиналы, значит разные рабочие полосы.
# ПРАВИЛО 90%: агрегат либо выключен, либо загружен не ниже 90% своего номинала.
# Это делает мощность КГУ ДИСКРЕТНОЙ - непрерывного регулирования от нуля нет.
UNITS = [
    dict(name="Jenbacher", p_nom=1067.0, eff_frac=0.90),
    dict(name="TEDOM",     p_nom=1200.0, eff_frac=0.90),
]
for u in UNITS:
    u["p_lo"] = u["p_nom"] * u["eff_frac"]      # нижняя граница рабочей полосы
NG = len(UNITS)
P_CHP_MAX = sum(u["p_nom"] for u in UNITS)      # 2267.0 кВт

# Технический минимум 50% из ТЗ здесь недостижим: правило 90% строго жёстче
# и всегда срабатывает раньше. Параметр оставлен для документации.
TECH_MIN_FRAC = 0.50

UNIT_AVAILABLE = [True] * 2   # доступность агрегата (False = в ремонте)
MIN_UP_H   = 2      # часов работы после пуска, защита от short-cycling
MIN_DOWN_H = 3      # часов стоянки после останова
START_COST = 15000.0   # ₸ за пуск (износ, пусковой расход)

# =============================================================================
# БЛОК 2. НАКОПИТЕЛЬ
# =============================================================================
BESS_CAP   = 5500.0                 # кВт*ч полная ёмкость
SOC_FLOOR  = 0.30 * BESS_CAP        # 1650 кВт*ч, буфер защиты ячеек
BESS_USE   = BESS_CAP - SOC_FLOOR   # 3850 кВт*ч полезной ёмкости
BESS_PWR   = 2500.0                 # кВт, предел инвертора на заряд и разряд
RTE        = 0.88
ETA        = RTE ** 0.5             # симметрично на заряд и разряд
BESS_CAPEX = 850_000.0              # $

# =============================================================================
# БЛОК 3. ТАРИФЫ
# =============================================================================
CHP_COST  = 22.0        # ₸/кВт*ч, себестоимость собственной выработки
GRID_COST = 60.0        # ₸/кВт*ч, закуп из города
BESS_COST = CHP_COST / RTE          # 25.0 ₸/кВт*ч, справочно
DEMAND_CHARGE = 0.0     # ₸ за кВт заявленной мощности в месяц (см. разбор ниже)
USD_KZT = 480.0

# =============================================================================
# БЛОК 4. ПРОФИЛЬ НАГРУЗКИ
# =============================================================================
# Синтетика под заданные ориентиры: средняя ~2.3-2.4 МВт, пик 5.5 МВт.
# Заменяется реальными данными через load_from_csv().
LOAD = np.array([1450, 1400, 1380, 1380, 1420, 1480, 1800, 2600, 3600, 5500,
                 4200, 3400, 2000, 1900, 3000, 4200, 3600, 2700, 2200, 1800,
                 1650, 1520, 1440, 1400], dtype=float)


def load_from_csv(path, column="load_kw"):
    """Подстановка реальных почасовых данных, кВт."""
    df = pd.read_csv(path)
    v = df[column].to_numpy(dtype=float)
    return [v[i * 24:(i + 1) * 24] for i in range(len(v) // 24)]


# =============================================================================
# БЛОК 5. ДИСПЕТЧЕРИЗАЦИЯ КГУ (дискретный выбор состава)
# =============================================================================
# Комбинаций всего 2^2 = 4. Их можно просто перебрать - никакой оптимизации
# не нужно. Для каждой комбинации известна допустимая полоса суммарной
# мощности [p_lo, p_hi]; внутри полосы мощность непрерывна.
COMBOS = []
for mask in range(1 << NG):
    idx = [i for i in range(NG) if mask >> i & 1]
    COMBOS.append(dict(mask=mask, idx=idx,
                       p_lo=sum(UNITS[i]["p_lo"] for i in idx),
                       p_hi=sum(UNITS[i]["p_nom"] for i in idx)))
# Сортируем по убыванию максимальной мощности: КГУ всегда дешевле сети,
# значит из допустимых комбинаций всегда берём ту, что даёт больше выработки.
COMBOS.sort(key=lambda c: -c["p_hi"])


class State:
    """Наработка и простой каждого агрегата. Нужны для min-up / min-down,
    переносятся между сутками."""
    def __init__(self, soc=None):
        self.soc = BESS_CAP if soc is None else soc
        self.on_h = [0] * NG
        self.off_h = [99] * NG      # на старте ограничений на пуск нет


def combo_allowed(c, st):
    """Проверка short-cycling: нельзя гасить недоработавший агрегат
    и нельзя пускать недостоявший."""
    for i in range(NG):
        want_on = bool(c["mask"] >> i & 1)
        if want_on and st.off_h[i] and st.off_h[i] < MIN_DOWN_H:
            return False
        if not want_on and st.on_h[i] and st.on_h[i] < MIN_UP_H:
            return False
    return True


def commit(p_absorb, st):
    """Выбрать состав КГУ и мощность каждого агрегата.

    p_absorb - сколько система физически способна ПОГЛОТИТЬ:
               нагрузка завода плюс то, что примет батарея.
               Выдавать больше нельзя: сброса электрической мощности нет,
               экспорт в сеть не предусмотрен.

    Логика: КГУ (22 ₸) всегда дешевле сети (60 ₸), поэтому берём самую
    мощную из ДОПУСТИМЫХ комбинаций. Комбинация допустима, если её нижняя
    граница помещается в p_absorb и не нарушает min-up / min-down.
    """
    for c in COMBOS:
        if any(not UNIT_AVAILABLE[i] for i in c["idx"]):
            continue                       # агрегат выведен в ремонт
        if c["p_lo"] <= p_absorb + 1e-9 and combo_allowed(c, st):
            total = min(c["p_hi"], p_absorb)
            gen = [0.0] * NG
            if c["idx"]:
                # Делим пропорционально номиналам: тогда оба агрегата
                # оказываются на одинаковой относительной загрузке и оба
                # гарантированно выше своих 90%.
                base = sum(UNITS[i]["p_nom"] for i in c["idx"])
                for i in c["idx"]:
                    gen[i] = total * UNITS[i]["p_nom"] / base
            return gen
    return [0.0] * NG


# =============================================================================
# БЛОК 6. КОНЕЧНЫЙ АВТОМАТ EMS
# =============================================================================
def step(L, st, grid_target=None, allow_discharge=True):
    """Один час симуляции. Возвращает словарь строки таблицы.

    grid_target - уставка потолка закупа из сети, кВт.
        None  = жадный арбитраж: разряжать, как только сеть вообще нужна.
        число = классический peak shaving: разряжать ровно столько, чтобы
                закуп не превысил уставку, остальное беречь на настоящий пик.
    """
    # --- 1. Сколько батарея готова принять в этот час --------------------
    room = max(0.0, (BESS_CAP - st.soc) / ETA)
    ch_cap = min(BESS_PWR, room)

    # --- 2. Состав КГУ. Поглотить можем нагрузку плюс приём батареи -------
    gen = commit(L + ch_cap, st)
    chp = sum(gen)

    # --- 3. Излишек выработки идёт в батарею ------------------------------
    ch = min(max(0.0, chp - L), BESS_PWR, room)
    st.soc += ch * ETA

    # --- 4. Дефицит закрывается батареей, потом сетью ---------------------
    gap = max(0.0, L - chp)
    avail = min(BESS_PWR, max(0.0, (st.soc - SOC_FLOOR) * ETA))

    # ПРАВИЛО ПИКОВОГО РЕЖИМА. Батарея разряжается ТОЛЬКО когда нагрузка
    # превысила суммарную мощность КГУ. Ночью, когда до сети остаётся
    # 200-600 кВт, разряжать нельзя: эта мелочь опустошит накопитель, и к
    # утреннему пику в 5.5 МВт он придёт пустым. Правило причинное -
    # прогноза не требует, сравнивает текущий час с паспортом КГУ.
    peak_mode = L > P_CHP_MAX + 1e-9
    if not allow_discharge or not peak_mode:
        dis = 0.0
    elif grid_target is None:
        dis = min(gap, avail)                       # жадный арбитраж
    else:
        dis = min(max(0.0, gap - grid_target), avail)   # держим потолок
    st.soc -= dis / ETA
    grid = gap - dis

    # --- 5. Наработка агрегатов для следующего часа ------------------------
    starts = 0
    for i in range(NG):
        if gen[i] > 1e-9:
            if st.on_h[i] == 0:
                starts += 1
            st.on_h[i] += 1
            st.off_h[i] = 0
        else:
            st.off_h[i] += 1
            st.on_h[i] = 0

    cost = chp * CHP_COST + grid * GRID_COST + starts * START_COST
    return dict(hour=0, load=L, jenbacher=gen[0], tedom=gen[1], ch=ch, dis=dis,
                grid=grid, soc_pct=st.soc / BESS_CAP * 100, chp=chp,
                starts=starts, cost=cost)


def simulate(load, with_bess=True, grid_target=None, days=1, st=None):
    """Прогон суток. days > 1 - выход на установившийся режим: rule-based
    автомат не обязан замыкать сутки по энергии, и первый день всегда
    переходный."""
    st = st or State()
    rows = []
    for d in range(days):
        rows = []
        for h in range(24):
            if with_bess:
                r = step(load[h], st, grid_target)
            else:
                # Без батареи: приёма нет, разряда нет.
                gen = commit(load[h], st)
                chp = sum(gen)
                starts = 0
                for i in range(NG):
                    if gen[i] > 1e-9:
                        if st.on_h[i] == 0:
                            starts += 1
                        st.on_h[i] += 1; st.off_h[i] = 0
                    else:
                        st.off_h[i] += 1; st.on_h[i] = 0
                grid = max(0.0, load[h] - chp)
                r = dict(hour=0, load=load[h], jenbacher=gen[0], tedom=gen[1],
                         ch=0.0, dis=0.0, grid=grid, soc_pct=100.0, chp=chp,
                         starts=starts, cost=chp * CHP_COST + grid * GRID_COST
                         + starts * START_COST)
            r["hour"] = h + 1
            rows.append(r)
    return pd.DataFrame(rows), st


# =============================================================================
# БЛОК 7. ВЫВОД
# =============================================================================
def show(df, title):
    print(f"\n{title}\n")
    h = (f"{'Час':>4}{'Нагрузка':>10}{'Jenbacher':>11}{'TEDOM':>9}"
         f"{'Заряд +':>9}{'Разряд -':>10}{'Сеть':>9}{'SoC,%':>8}")
    print(h); print("-" * len(h))
    for _, r in df.iterrows():
        print(f"{int(r.hour):>4}{r.load:>10.0f}{r.jenbacher:>11.1f}{r.tedom:>9.1f}"
              f"{r.ch:>9.0f}{-r.dis if r.dis else 0:>10.0f}{r.grid:>9.0f}"
              f"{r.soc_pct:>8.1f}")
    print("-" * len(h))
    print(f"{'ИТОГО':>4}{df.load.sum():>10.0f}{df.jenbacher.sum():>11.0f}"
          f"{df.tedom.sum():>9.0f}{df.ch.sum():>9.0f}{-df.dis.sum():>10.0f}"
          f"{df.grid.sum():>9.0f}")


def sweep_target(load, days=8):
    """Подбор уставки потолка закупа. Причинный контроллер не знает будущего,
    поэтому уставка задаётся заранее - её и подбираем."""
    best = []
    for tgt in list(np.arange(0, 3400, 100)) + [None]:
        st = State()
        for _ in range(days):
            df, st = simulate(load, True, tgt, 1, st)
        best.append((tgt, df.cost.sum(), df.grid.max(), df.grid.sum()))
    return best


if __name__ == "__main__":
    print("=" * 78)
    print("ПАРАМЕТРЫ СИСТЕМЫ")
    print("=" * 78)
    for u in UNITS:
        print(f"  {u['name']:<12} номинал {u['p_nom']:>7.1f} кВт   "
              f"рабочая полоса {u['p_lo']:>7.1f} - {u['p_nom']:>7.1f} кВт")
    print(f"  Суммарно КГУ                {P_CHP_MAX:>7.1f} кВт")
    print(f"  BESS  ёмкость {BESS_CAP:.0f} кВт*ч, полезная {BESS_USE:.0f} кВт*ч, "
          f"инвертор {BESS_PWR:.0f} кВт")
    print(f"  Тарифы: КГУ {CHP_COST:.0f} ₸   BESS {BESS_COST:.2f} ₸   "
          f"сеть {GRID_COST:.0f} ₸ за кВт*ч")
    print(f"  Профиль: средняя {LOAD.mean():.0f} кВт, пик {LOAD.max():.0f} кВт, "
          f"сутки {LOAD.sum():.0f} кВт*ч")

    print("\nДОПУСТИМЫЕ СОСТАВЫ КГУ (полосы суммарной мощности)")
    for c in COMBOS:
        nm = " + ".join(UNITS[i]["name"] for i in c["idx"]) or "все выключены"
        print(f"  {nm:<26} {c['p_lo']:>8.1f} - {c['p_hi']:>8.1f} кВт")

    d0, _ = simulate(LOAD, with_bess=False)
    show(d0, "ТАБЛИЦА 1. БЕЗ БАТАРЕИ")

    st = State()
    for _ in range(8):
        d1, st = simulate(LOAD, True, None, 1, st)
    show(d1, "ТАБЛИЦА 2. С БАТАРЕЕЙ, жадный арбитраж (разряд при первой нужде)")

    sw = sweep_target(LOAD)
    fin = [x for x in sw if x[0] is not None]
    best_peak = min(fin, key=lambda x: (x[2], x[1]))
    best_cost = min(sw, key=lambda x: x[1])

    st = State()
    for _ in range(8):
        d2, st = simulate(LOAD, True, best_peak[0], 1, st)
    show(d2, f"ТАБЛИЦА 3. С БАТАРЕЕЙ, peak shaving с уставкой "
             f"{best_peak[0]:.0f} кВт")

    print("\n" + "=" * 78)
    print("ФИНАНСОВЫЙ ИТОГ ЗА СУТКИ")
    print("=" * 78)
    hdr = f"{'Вариант':<34}{'КГУ, ₸':>13}{'Сеть, ₸':>13}{'Всего, ₸':>13}{'Пик сети':>11}"
    print(hdr); print("-" * len(hdr))
    for name, d in (("Без батареи", d0),
                    ("С батареей, арбитраж", d1),
                    (f"С батареей, peak shaving {best_peak[0]:.0f}", d2)):
        print(f"{name:<34}{d.chp.sum() * CHP_COST:>13,.0f}"
              f"{d.grid.sum() * GRID_COST:>13,.0f}{d.cost.sum():>13,.0f}"
              f"{d.grid.max():>11,.0f}")
    print("-" * len(hdr))
    for name, d in (("арбитраж", d1), (f"peak shaving {best_peak[0]:.0f}", d2)):
        sav = d0.cost.sum() - d.cost.sum()
        print(f"Экономия ({name}): {sav:>12,.0f} ₸/сут   "
              f"{sav * 365:>14,.0f} ₸/год")
    print(f"\nМаксимальный пиковый закуп из сети за сутки:")
    print(f"  без батареи                 {d0.grid.max():>9,.0f} кВт")
    print(f"  с батареей, арбитраж        {d1.grid.max():>9,.0f} кВт")
    print(f"  с батареей, peak shaving    {d2.grid.max():>9,.0f} кВт  "
          f"(срез {(1 - d2.grid.max() / d0.grid.max()) * 100:.1f}%)")

    sav_y = (d0.cost.sum() - d1.cost.sum()) * 365
    capex = BESS_CAPEX * USD_KZT
    print(f"\nCAPEX BESS: ${BESS_CAPEX:,.0f} = {capex:,.0f} ₸")
    print(f"Простая окупаемость на арбитраже: {capex / sav_y:.1f} лет")

    print("\nПУСКИ КГУ ЗА СУТКИ (защита от short-cycling)")
    for name, d in (("без батареи", d0), ("арбитраж", d1), ("peak shaving", d2)):
        print(f"  {name:<16}{d.starts.sum():.0f} пусков")

    # --- Сколько должна стоить плата за мощность, чтобы peak shaving окупился --
    dpeak = d1.grid.max() - d2.grid.max()
    dcost = (d2.cost.sum() - d1.cost.sum()) * 365
    print("\nПЛАТА ЗА ЗАЯВЛЕННУЮ МОЩНОСТЬ")
    print(f"  peak shaving срезает ещё {dpeak:,.0f} кВт пика,")
    print(f"  но обходится дороже арбитража на {dcost:,.0f} ₸/год.")
    print(f"  Порог окупаемости: тариф на мощность выше "
          f"{dcost / (dpeak * 12):,.0f} ₸ за кВт в месяц.")
    print(f"  Сейчас DEMAND_CHARGE = {DEMAND_CHARGE:.0f} ₸/кВт/мес, "
          f"то есть пик в модели ничего не стоит.")
```

## A.6 Итерация 6-7: годовая модель на двух КГУ

**`factory_year.py`** — 8760 часов, 10 сценариев, помесячный пик

```python
"""
Годовая модель микросети завода: 2 КГУ + BESS + безлимитная сеть.

ЧТО ИСПРАВЛЕНО ПО СРАВНЕНИЮ С СУТОЧНОЙ ВЕРСИЕЙ:

1. ПИК СЧИТАЕТСЯ ПОМЕСЯЧНО, а не посуточно. Расчётный период по счёту -
   календарный месяц, значит стоимость мощности определяет ОДИН худший час
   месяца. Суточный максимум не имеет экономического смысла: 30 хороших
   дней не компенсируют один плохой.

2. КОНТРОЛЛЕР ЗНАЕТ ТЕКУЩИЙ МАКСИМУМ МЕСЯЦА. Если пик месяца уже
   зафиксирован на уровне X, разряжать батарею ниже X бессмысленно - счёт
   всё равно выставят по X. Эта энергия сохраняется для следующего месяца
   или для энергетического арбитража. Стандартный приём в онлайн-алгоритмах
   demand charge management.

3. РАСЧЁТ ИДЁТ ПО ПОЛНОМУ ГОДУ (8760 часов), а не по одному типовому дню.
   Переходные процессы батареи и сезонность типовым днём не ловятся.

Корневая логика диспетчеризации КГУ не менялась: правило 90%, дискретный
выбор состава, merit order КГУ -> BESS -> сеть.

Запуск:  python3 factory_year.py
"""

import numpy as np
import pandas as pd
import os

RNG_MASTER = 20260824
OUT = "/mnt/user-data/outputs"

# =============================================================================
# БЛОК 1. ОБОРУДОВАНИЕ (без изменений)
# =============================================================================
UNITS = [
    dict(name="Jenbacher", p_nom=1067.0, eff_frac=0.90),
    dict(name="TEDOM",     p_nom=1200.0, eff_frac=0.90),
]
for u in UNITS:
    u["p_lo"] = u["p_nom"] * u["eff_frac"]
NG = len(UNITS)
P_CHP_MAX = sum(u["p_nom"] for u in UNITS)          # 2267.0 кВт

MIN_UP_H, MIN_DOWN_H = 2, 3
START_COST = 15000.0

BESS_CAP  = 5500.0
SOC_FLOOR = 0.30 * BESS_CAP
BESS_PWR  = 2500.0
# Ограничитель скорости заряда. Без него батарея набивается за 3-4 ночных часа,
# после чего у КГУ пропадает место для сброса излишка, агрегат глохнет и
# запирается таймером MIN_DOWN ровно к утреннему подъёму нагрузки.
# Свободная ёмкость накопителя - это ресурс, который держит КГУ в работе.
CHARGE_RATE_CAP = 700.0
RTE  = 0.88
ETA  = RTE ** 0.5
BESS_CAPEX_USD = 850_000.0
USD_KZT = 480.0

CHP_COST  = 22.0        # ₸/кВт*ч
GRID_COST = 60.0        # ₸/кВт*ч
DEMAND_CHARGE = 0.0     # ₸ за кВт максимума месяца; 0 = чисто энергетический тариф

# =============================================================================
# БЛОК 2. ГЕНЕРАТОР ГОДОВЫХ ПРОФИЛЕЙ (10 независимых сценариев)
# =============================================================================
# Каждый сценарий - свой архетип завода. Общее только одно: порядок величин
# из ТЗ (средняя около 2.3 МВт, пик до 5.5 МВт). Всё остальное - форма
# суточного цикла, недельный ритм, сезонность, шум - различается.
ARCHETYPES = [
    dict(id=1,  target_mean=2.4, target_peak=5.5, name="Две смены, непрерывно",   base=2.30, day_amp=1.30, night=0.62,
         weekend=0.90, season=0.10, noise=0.06, spike_p=0.010, spike=2.10, shifts=2),
    dict(id=2,  target_mean=2.3, target_peak=5.2, name="Три смены, ровный ритм",  base=2.45, day_amp=0.75, night=0.85,
         weekend=0.95, season=0.08, noise=0.05, spike_p=0.008, spike=1.90, shifts=3),
    dict(id=3,  target_mean=2.1, target_peak=5.5, name="Одна дневная смена",      base=1.95, day_amp=1.85, night=0.35,
         weekend=0.40, season=0.12, noise=0.07, spike_p=0.012, spike=2.30, shifts=1),
    dict(id=4,  target_mean=2.45, target_peak=5.6, name="Летний пик (охлаждение)", base=2.35, day_amp=1.25, night=0.60,
         weekend=0.85, season=0.28, noise=0.06, spike_p=0.010, spike=2.05, shifts=2,
         season_phase=0.0),
    dict(id=5,  target_mean=2.45, target_peak=5.6, name="Зимний пик (отопление)",  base=2.40, day_amp=1.15, night=0.68,
         weekend=0.88, season=0.26, noise=0.06, spike_p=0.009, spike=2.00, shifts=2,
         season_phase=np.pi),
    dict(id=6,  target_mean=2.25, target_peak=5.5, name="Частые короткие пики",    base=2.20, day_amp=1.10, night=0.58,
         weekend=0.85, season=0.09, noise=0.09, spike_p=0.045, spike=2.35, shifts=2),
    dict(id=7,  target_mean=2.55, target_peak=4.6, name="Ровная базовая нагрузка", base=2.55, day_amp=0.40, night=0.92,
         weekend=0.97, season=0.06, noise=0.03, spike_p=0.004, spike=1.75, shifts=3),
    dict(id=8,  target_mean=2.2, target_peak=5.5, name="Останов на выходные",     base=2.40, day_amp=1.35, night=0.55,
         weekend=0.22, season=0.11, noise=0.06, spike_p=0.011, spike=2.15, shifts=2),
    dict(id=9,  target_mean=2.15, target_peak=5.4, name="Кампанийное производство", base=2.25, day_amp=1.20, night=0.60,
         weekend=0.80, season=0.10, noise=0.07, spike_p=0.013, spike=2.20, shifts=2,
         campaign=True),
    dict(id=10, target_mean=2.3, target_peak=5.8, name="Высокая хаотичность",     base=2.30, day_amp=1.15, night=0.60,
         weekend=0.82, season=0.10, noise=0.16, spike_p=0.030, spike=2.25, shifts=2),
]


def make_year(a, seed):
    """8760 часов, кВт. Слои: суточный цикл + недельный + сезонный + шум + пики."""
    rng = np.random.default_rng(seed)
    hours = np.arange(8760)
    hod = hours % 24
    dow = (hours // 24 + 3) % 7          # 2026-01-01 - четверг, значит смещение 3
    doy = hours // 24

    # --- суточный цикл: форма зависит от сменности ---------------------------
    if a["shifts"] == 1:
        shape = np.where((hod >= 7) & (hod < 18), 1.0, 0.0)
    elif a["shifts"] == 2:
        shape = np.where((hod >= 6) & (hod < 22), 1.0, 0.0)
        shape = np.where((hod >= 13) & (hod < 15), 0.45, shape)   # межсменка
    else:
        shape = np.full(8760, 0.85)
        shape = np.where((hod >= 8) & (hod < 20), 1.0, shape)
    smooth = np.convolve(np.r_[shape[-3:], shape, shape[:3]],
                         np.ones(5) / 5, mode="same")[3:-3]        # сглаживаем фронты

    x = a["base"] * (a["night"] + (a["day_amp"] - a["night"]) * smooth)

    # --- недельный ритм ------------------------------------------------------
    x = x * np.where(dow >= 5, a["weekend"], 1.0)

    # --- сезонность ----------------------------------------------------------
    ph = a.get("season_phase", 0.0)
    x = x * (1.0 + a["season"] * np.cos(2 * np.pi * doy / 365.0 + ph))

    # --- кампанийность: три недели работы, одна тихая ------------------------
    if a.get("campaign"):
        x = x * np.where((doy // 7) % 4 == 3, 0.55, 1.0)

    # --- шум и короткие пики -------------------------------------------------
    x = x * (1.0 + rng.normal(0, a["noise"], 8760))
    hit = rng.random(8760) < a["spike_p"]
    x = np.where(hit, x * rng.uniform(1.0, a["spike"] / a["day_amp"] + 0.6, 8760), x)

    x = np.clip(x, 0.25, None) * 1000.0                            # МВт -> кВт

    # --- Нормировка под конверт из ТЗ ----------------------------------------
    # Сценарии должны различаться ФОРМОЙ, но описывать один и тот же завод.
    # Без этого шага архетипы разъезжаются по средней в два раза, и сравнивать
    # их между собой становится бессмысленно.
    x = x * (a["target_mean"] * 1000.0 / x.mean())
    x = np.minimum(x, a["target_peak"] * 1000.0)   # срезаем нефизичные выбросы
    x = x * (a["target_mean"] * 1000.0 / x.mean())
    x = np.minimum(x, a["target_peak"] * 1000.0)
    return np.round(x, 1)


def build_scenarios():
    os.makedirs(OUT, exist_ok=True)
    out = {}
    for i, a in enumerate(ARCHETYPES):
        v = make_year(a, RNG_MASTER + i * 7919)
        idx = pd.date_range("2026-01-01", periods=8760, freq="h")
        df = pd.DataFrame({"timestamp": idx, "load_kw": v})
        path = f"{OUT}/load_year_{a['id']:02d}.csv"
        df.to_csv(path, index=False)
        out[a["id"]] = dict(name=a["name"], load=v, month=idx.month.to_numpy(), path=path)
    return out


# =============================================================================
# БЛОК 3. ДИСПЕТЧЕРИЗАЦИЯ КГУ (корневая логика без изменений)
# =============================================================================
COMBOS = []
for mask in range(1 << NG):
    idx = [i for i in range(NG) if mask >> i & 1]
    COMBOS.append(dict(mask=mask, idx=idx,
                       p_lo=sum(UNITS[i]["p_lo"] for i in idx),
                       p_hi=sum(UNITS[i]["p_nom"] for i in idx)))
COMBOS.sort(key=lambda c: -c["p_hi"])


class State:
    def __init__(self):
        self.soc = BESS_CAP
        self.on_h = [0] * NG
        self.off_h = [99] * NG
        self.month = None
        self.month_peak = 0.0      # текущий зафиксированный максимум месяца


def combo_allowed(c, st):
    for i in range(NG):
        want_on = bool(c["mask"] >> i & 1)
        if want_on and st.off_h[i] and st.off_h[i] < MIN_DOWN_H:
            return False
        if not want_on and st.on_h[i] and st.on_h[i] < MIN_UP_H:
            return False
    return True


def commit(p_absorb, st):
    for c in COMBOS:
        if c["p_lo"] <= p_absorb + 1e-9 and combo_allowed(c, st):
            total = min(c["p_hi"], p_absorb)
            gen = [0.0] * NG
            if c["idx"]:
                base = sum(UNITS[i]["p_nom"] for i in c["idx"])
                for i in c["idx"]:
                    gen[i] = total * UNITS[i]["p_nom"] / base
            return gen
    return [0.0] * NG


# =============================================================================
# БЛОК 4. ГОДОВОЙ ПРОГОН
# =============================================================================
def run(load, month, mode="bess", target=None):
    """mode: 'none' - без батареи, 'bess' - арбитраж, 'peak' - срез пика.

    target - базовая уставка потолка закупа, кВт (только для mode='peak').
    """
    st = State()
    n = len(load)
    rec = dict(load=load, jen=np.zeros(n), ted=np.zeros(n), ch=np.zeros(n),
               dis=np.zeros(n), grid=np.zeros(n), soc=np.zeros(n),
               chp=np.zeros(n), starts=np.zeros(n))

    for t in range(n):
        L = load[t]
        if st.month != month[t]:                 # начался новый расчётный период
            st.month, st.month_peak = month[t], 0.0

        if mode == "none":
            gen = commit(L, st)
            chp = sum(gen)
            ch = dis = 0.0
            grid = max(0.0, L - chp)
        else:
            room = max(0.0, (BESS_CAP - st.soc) / ETA)
            cap = min(BESS_PWR, CHARGE_RATE_CAP, room)
            gen = commit(L + cap, st)
            chp = sum(gen)
            ch = min(max(0.0, chp - L), cap)
            st.soc += ch * ETA

            gap = max(0.0, L - chp)
            avail = min(BESS_PWR, max(0.0, (st.soc - SOC_FLOOR) * ETA))
            peak_mode = L > P_CHP_MAX + 1e-9     # разряд только выше мощности КГУ

            if not peak_mode:
                dis = 0.0
            elif mode == "bess":
                dis = min(gap, avail)
            else:
                # Потолок не может быть ниже уже зафиксированного максимума
                # месяца: счёт всё равно выставят по нему, экономить смысла нет.
                eff_target = max(target, st.month_peak)
                dis = min(max(0.0, gap - eff_target), avail)
            st.soc -= dis / ETA
            grid = gap - dis

        st.month_peak = max(st.month_peak, grid)
        for i in range(NG):
            if gen[i] > 1e-9:
                if st.on_h[i] == 0:
                    rec["starts"][t] += 1
                st.on_h[i] += 1; st.off_h[i] = 0
            else:
                st.off_h[i] += 1; st.on_h[i] = 0

        rec["jen"][t], rec["ted"][t] = gen[0], gen[1]
        rec["ch"][t], rec["dis"][t], rec["grid"][t] = ch, dis, grid
        rec["soc"][t], rec["chp"][t] = st.soc, chp

    df = pd.DataFrame(rec)
    df["month"] = month
    return df


def economics(df):
    """Помесячный счёт: энергия за месяц + максимум месяца."""
    m = df.groupby("month").agg(grid_kwh=("grid", "sum"), chp_kwh=("chp", "sum"),
                                peak=("grid", "max"), starts=("starts", "sum"))
    m["cost"] = (m.chp_kwh * CHP_COST + m.grid_kwh * GRID_COST
                 + m.peak * DEMAND_CHARGE + m.starts * START_COST)
    return dict(cost=m.cost.sum(), chp_kwh=m.chp_kwh.sum(), grid_kwh=m.grid_kwh.sum(),
                peak_year=m.peak.max(), peak_sum=m.peak.sum(), peak_mean=m.peak.mean(),
                starts=m.starts.sum(), monthly=m)


# =============================================================================
# БЛОК 5. НЕДЕЛЬНЫЕ ТАБЛИЦЫ
# =============================================================================
def week_table(df, start_h, title):
    w = df.iloc[start_h:start_h + 168].reset_index(drop=True)
    lines = [f"\n### {title}\n",
             "| День | Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |",
             "|---:|---:|---:|---:|---:|---:|---:|---:|---:|"]
    dn = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]
    for i, r in w.iterrows():
        lines.append(f"| {dn[i // 24]} | {i % 24 + 1} | {r.load:.0f} | {r.jen:.0f} | "
                     f"{r.ted:.0f} | {r.ch:.0f} | {-r.dis if r.dis else 0:.0f} | "
                     f"{r.grid:.0f} | {r.soc / BESS_CAP * 100:.1f} |")
    lines.append(f"\n**Итого за неделю:** нагрузка {w.load.sum():,.0f} кВт·ч, "
                 f"КГУ {w.chp.sum():,.0f}, заряд {w.ch.sum():,.0f}, "
                 f"разряд {w.dis.sum():,.0f}, сеть {w.grid.sum():,.0f} кВт·ч, "
                 f"пик сети {w.grid.max():,.0f} кВт\n")
    return "\n".join(lines), w


def week_summary(w):
    d = w.copy(); d["day"] = d.index // 24
    g = d.groupby("day").agg(load=("load", "sum"), chp=("chp", "sum"),
                             ch=("ch", "sum"), dis=("dis", "sum"),
                             grid=("grid", "sum"), peak=("grid", "max"))
    return g


if __name__ == "__main__":
    print("Генерация 10 годовых сценариев...")
    SC = build_scenarios()
    for k, v in SC.items():
        L = v["load"]
        print(f"  {k:>2}. {v['name']:<28} средняя {L.mean() / 1000:.2f} МВт   "
              f"пик {L.max() / 1000:.2f} МВт   год {L.sum() / 1e6:.2f} ГВт*ч")

    print("\n" + "=" * 100)
    print("ГОДОВОЙ ПРОГОН ПО ВСЕМ 10 СЦЕНАРИЯМ")
    print("=" * 100)
    hdr = (f"{'#':>3} {'Сценарий':<28}{'без BESS, ₸':>16}{'с BESS, ₸':>16}"
           f"{'экономия':>14}{'%':>7}{'окуп.,лет':>11}")
    print(hdr); print("-" * len(hdr))
    rows = []
    for k, v in SC.items():
        e0 = economics(run(v["load"], v["month"], "none"))
        e1 = economics(run(v["load"], v["month"], "bess"))
        sav = e0["cost"] - e1["cost"]
        pay = BESS_CAPEX_USD * USD_KZT / sav if sav > 0 else float("inf")
        rows.append(dict(id=k, name=v["name"], c0=e0["cost"], c1=e1["cost"], sav=sav,
                         pct=sav / e0["cost"] * 100, pay=pay,
                         pk0=e0["peak_year"], pk1=e1["peak_year"],
                         chp0=e0["chp_kwh"], chp1=e1["chp_kwh"],
                         g0=e0["grid_kwh"], g1=e1["grid_kwh"]))
        print(f"{k:>3} {v['name']:<28}{e0['cost']:>16,.0f}{e1['cost']:>16,.0f}"
              f"{sav:>14,.0f}{sav / e0['cost'] * 100:>7.1f}{pay:>11.1f}")
    R = pd.DataFrame(rows)
    print("-" * len(hdr))
    print(f"{'':>3} {'СРЕДНЕЕ ПО 10 СЦЕНАРИЯМ':<28}{R.c0.mean():>16,.0f}"
          f"{R.c1.mean():>16,.0f}{R.sav.mean():>14,.0f}{R.pct.mean():>7.1f}"
          f"{R.pay.mean():>11.1f}")
    print(f"{'':>3} {'РАЗБРОС (мин..макс окуп.)':<28}{'':>16}{'':>16}{'':>14}{'':>7}"
          f"{R.pay.min():>5.1f}..{R.pay.max():<5.1f}")

    # --- Недельные таблицы по сценарию 1 -------------------------------------
    v = SC[1]
    d0 = run(v["load"], v["month"], "none")
    d1 = run(v["load"], v["month"], "bess")
    # Уставку не выдумываем, а подбираем: считаем сумму 12 месячных максимумов
    # (именно она определяет счёт за мощность) и годовые затраты.
    sweep = []
    for tg in range(400, 2200, 200):
        e = economics(run(v["load"], v["month"], "peak", target=float(tg)))
        sweep.append((tg, e["peak_sum"], e["cost"], e["peak_year"]))
    TG = min(sweep, key=lambda r: (r[1], r[2]))[0]
    print("\nПОДБОР УСТАВКИ СРЕЗА ПИКА (сценарий 1)")
    print(f"{'уставка, кВт':>14}{'сумма 12 макс., кВт':>22}{'годовой макс.':>16}{'затраты, ₸':>18}")
    for tg, ps, c, py in sweep:
        mark = "  <-- выбрано" if tg == TG else ""
        print(f"{tg:>14}{ps:>22,.0f}{py:>16,.0f}{c:>18,.0f}{mark}")
    d2 = run(v["load"], v["month"], "peak", target=float(TG))
    # Берём неделю в начале мая, стартующую строго с понедельника.
    idx_all = pd.date_range("2026-01-01", periods=8760, freq="h")
    # Берём неделю, которая начинается ПЕРВОГО числа месяца и с понедельника:
    # 1 июня 2026 - понедельник. Так контроллер среза пика стартует с чистого
    # расчётного периода, и его работа видна с первого часа.
    mondays = [i for i in range(0, 8760 - 168, 24)
               if idx_all[i].dayofweek == 0 and idx_all[i].day == 1]
    START = mondays[0]
    txt = [f"# Недельные профили, сценарий 1 «{v['name']}»\n",
           f"Неделя начинается с часа {START} (день {START // 24 + 1} года).\n"]
    for df, ttl in ((d0, "ТАБЛИЦА 1. БЕЗ БАТАРЕИ"),
                    (d1, "ТАБЛИЦА 2. С БАТАРЕЕЙ, энергетический арбитраж"),
                    (d2, f"ТАБЛИЦА 3. С БАТАРЕЕЙ, срез пика с уставкой {TG} кВт")):
        t, w = week_table(df, START, ttl)
        txt.append(t)
    open(f"{OUT}/week_tables.md", "w").write("\n".join(txt))

    print("\n" + "=" * 100)
    print("НЕДЕЛЯ, СВОДКА ПО СУТКАМ (сценарий 1, полные почасовые таблицы в week_tables.md)")
    print("=" * 100)
    dn = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]
    for ttl, df in (("БЕЗ БАТАРЕИ", d0), ("АРБИТРАЖ", d1), (f"СРЕЗ ПИКА {TG}", d2)):
        w = df.iloc[START:START + 168].reset_index(drop=True)
        g = week_summary(w)
        print(f"\n{ttl}")
        print(f"{'День':>5}{'Нагрузка':>11}{'КГУ':>10}{'Заряд':>9}{'Разряд':>9}"
              f"{'Сеть':>10}{'Пик сети':>10}")
        for i, r in g.iterrows():
            print(f"{dn[i]:>5}{r.load:>11,.0f}{r.chp:>10,.0f}{r.ch:>9,.0f}"
                  f"{r.dis:>9,.0f}{r.grid:>10,.0f}{r.peak:>10,.0f}")
        print(f"{'ИТОГ':>5}{g.load.sum():>11,.0f}{g.chp.sum():>10,.0f}"
              f"{g.ch.sum():>9,.0f}{g.dis.sum():>9,.0f}{g.grid.sum():>10,.0f}"
              f"{g.peak.max():>10,.0f}")

    # --- Помесячные пики -----------------------------------------------------
    print("\n" + "=" * 100)
    print("ПОМЕСЯЧНЫЙ МАКСИМУМ ЗАКУПА ИЗ СЕТИ, кВт (сценарий 1)")
    print("=" * 100)
    m0 = economics(d0)["monthly"]; m1 = economics(d1)["monthly"]
    m2 = economics(d2)["monthly"]
    print(f"{'Месяц':>6}{'без BESS':>12}{'арбитраж':>12}{'срез пика':>12}{'срез, %':>10}")
    for mm in range(1, 13):
        a, b, c = m0.peak[mm], m1.peak[mm], m2.peak[mm]
        print(f"{mm:>6}{a:>12,.0f}{b:>12,.0f}{c:>12,.0f}{(1 - c / a) * 100:>10.1f}")
    print(f"{'ГОД':>6}{m0.peak.max():>12,.0f}{m1.peak.max():>12,.0f}"
          f"{m2.peak.max():>12,.0f}{(1 - m2.peak.max() / m0.peak.max()) * 100:>10.1f}")

    # --- Порог платы за мощность ---------------------------------------------
    e1, e2 = economics(d1), economics(d2)
    dpk = e1["peak_sum"] - e2["peak_sum"]        # суммарно по 12 месяцам
    dc = e2["cost"] - e1["cost"]
    print("\n" + "=" * 100)
    print("КОГДА СРЕЗ ПИКА ВЫГОДНЕЕ АРБИТРАЖА")
    print("=" * 100)
    print(f"  Срез пика убирает {dpk:,.0f} кВт суммарно по 12 месячным максимумам,")
    print(f"  но обходится дороже арбитража на {dc:,.0f} ₸/год.")
    if dpk > 0:
        print(f"  Порог: плата за мощность выше {dc / dpk:,.0f} ₸ за кВт максимума месяца.")
    print(f"  Сейчас DEMAND_CHARGE = {DEMAND_CHARGE:.0f} ₸/кВт/мес.")

    # --- Чувствительность к цене сети ----------------------------------------
    print("\n" + "=" * 100)
    print("ЧУВСТВИТЕЛЬНОСТЬ К ТАРИФУ СЕТИ (сценарий 1, окупаемость BESS)")
    print("=" * 100)
    print(f"{'Тариф сети, ₸/кВт*ч':>22}{'спред к КГУ':>14}{'экономия, ₸/год':>20}{'окуп., лет':>13}")
    import copy
    for gp in (25, 30, 35, 40, 45, 50, 60, 70):
        globals()["GRID_COST"] = float(gp)
        a = economics(run(v["load"], v["month"], "none"))["cost"]
        b = economics(run(v["load"], v["month"], "bess"))["cost"]
        s = a - b
        pay = BESS_CAPEX_USD * USD_KZT / s if s > 0 else float("inf")
        print(f"{gp:>22}{gp - CHP_COST:>14}{s:>20,.0f}{pay:>13.1f}")
    globals()["GRID_COST"] = 60.0
    print(f"\nФайлы: 10 годовых CSV (load_year_01..10.csv) и week_tables.md в outputs.")
```

## A.7 Итерация 8: годовая модель на трёх КГУ

**`factory_year3.py`** — восемь составов, PEAK_TRIGGER

```python
"""
Годовая модель микросети завода: 2 КГУ + BESS + безлимитная сеть.

ЧТО ИСПРАВЛЕНО ПО СРАВНЕНИЮ С СУТОЧНОЙ ВЕРСИЕЙ:

1. ПИК СЧИТАЕТСЯ ПОМЕСЯЧНО, а не посуточно. Расчётный период по счёту -
   календарный месяц, значит стоимость мощности определяет ОДИН худший час
   месяца. Суточный максимум не имеет экономического смысла: 30 хороших
   дней не компенсируют один плохой.

2. КОНТРОЛЛЕР ЗНАЕТ ТЕКУЩИЙ МАКСИМУМ МЕСЯЦА. Если пик месяца уже
   зафиксирован на уровне X, разряжать батарею ниже X бессмысленно - счёт
   всё равно выставят по X. Эта энергия сохраняется для следующего месяца
   или для энергетического арбитража. Стандартный приём в онлайн-алгоритмах
   demand charge management.

3. РАСЧЁТ ИДЁТ ПО ПОЛНОМУ ГОДУ (8760 часов), а не по одному типовому дню.
   Переходные процессы батареи и сезонность типовым днём не ловятся.

Корневая логика диспетчеризации КГУ не менялась: правило 90%, дискретный
выбор состава, merit order КГУ -> BESS -> сеть.

Запуск:  python3 factory_year.py
"""

import numpy as np
import pandas as pd
import os

RNG_MASTER = 20260824
OUT = "/mnt/user-data/outputs"

# =============================================================================
# БЛОК 1. ОБОРУДОВАНИЕ (без изменений)
# =============================================================================
UNITS = [
    dict(name="Jenbacher", p_nom=1067.0, eff_frac=0.90),
    dict(name="TEDOM",     p_nom=1200.0, eff_frac=0.90),
    dict(name="КГУ-3",     p_nom=1100.0, eff_frac=0.90),
]
for u in UNITS:
    u["p_lo"] = u["p_nom"] * u["eff_frac"]
NG = len(UNITS)
P_CHP_MAX = sum(u["p_nom"] for u in UNITS)          # 2267.0 кВт

MIN_UP_H, MIN_DOWN_H = 2, 3
START_COST = 15000.0

BESS_CAP  = 5500.0
SOC_FLOOR = 0.30 * BESS_CAP
BESS_PWR  = 2500.0
# Ограничитель скорости заряда. Без него батарея набивается за 3-4 ночных часа,
# после чего у КГУ пропадает место для сброса излишка, агрегат глохнет и
# запирается таймером MIN_DOWN ровно к утреннему подъёму нагрузки.
# Свободная ёмкость накопителя - это ресурс, который держит КГУ в работе.
CHARGE_RATE_CAP = 700.0
# Порог пикового режима: ниже него батарея не разряжается (бережёт заряд).
# По корневой логике равен суммарной мощности КГУ. С третьим агрегатом парк
# вырос до 3367 кВт, и порог автоматически поднялся - батарея почти замерзает.
PEAK_TRIGGER = 1400.0     # подобрано перебором для парка из трёх КГУ
RTE  = 0.88
ETA  = RTE ** 0.5
BESS_CAPEX_USD = 850_000.0
USD_KZT = 480.0

CHP_COST  = 22.0        # ₸/кВт*ч
GRID_COST = 60.0        # ₸/кВт*ч
DEMAND_CHARGE = 0.0     # ₸ за кВт максимума месяца; 0 = чисто энергетический тариф

# =============================================================================
# БЛОК 2. ГЕНЕРАТОР ГОДОВЫХ ПРОФИЛЕЙ (10 независимых сценариев)
# =============================================================================
# Каждый сценарий - свой архетип завода. Общее только одно: порядок величин
# из ТЗ (средняя около 2.3 МВт, пик до 5.5 МВт). Всё остальное - форма
# суточного цикла, недельный ритм, сезонность, шум - различается.
ARCHETYPES = [
    dict(id=1,  target_mean=2.4, target_peak=5.5, name="Две смены, непрерывно",   base=2.30, day_amp=1.30, night=0.62,
         weekend=0.90, season=0.10, noise=0.06, spike_p=0.010, spike=2.10, shifts=2),
    dict(id=2,  target_mean=2.3, target_peak=5.2, name="Три смены, ровный ритм",  base=2.45, day_amp=0.75, night=0.85,
         weekend=0.95, season=0.08, noise=0.05, spike_p=0.008, spike=1.90, shifts=3),
    dict(id=3,  target_mean=2.1, target_peak=5.5, name="Одна дневная смена",      base=1.95, day_amp=1.85, night=0.35,
         weekend=0.40, season=0.12, noise=0.07, spike_p=0.012, spike=2.30, shifts=1),
    dict(id=4,  target_mean=2.45, target_peak=5.6, name="Летний пик (охлаждение)", base=2.35, day_amp=1.25, night=0.60,
         weekend=0.85, season=0.28, noise=0.06, spike_p=0.010, spike=2.05, shifts=2,
         season_phase=0.0),
    dict(id=5,  target_mean=2.45, target_peak=5.6, name="Зимний пик (отопление)",  base=2.40, day_amp=1.15, night=0.68,
         weekend=0.88, season=0.26, noise=0.06, spike_p=0.009, spike=2.00, shifts=2,
         season_phase=np.pi),
    dict(id=6,  target_mean=2.25, target_peak=5.5, name="Частые короткие пики",    base=2.20, day_amp=1.10, night=0.58,
         weekend=0.85, season=0.09, noise=0.09, spike_p=0.045, spike=2.35, shifts=2),
    dict(id=7,  target_mean=2.55, target_peak=4.6, name="Ровная базовая нагрузка", base=2.55, day_amp=0.40, night=0.92,
         weekend=0.97, season=0.06, noise=0.03, spike_p=0.004, spike=1.75, shifts=3),
    dict(id=8,  target_mean=2.2, target_peak=5.5, name="Останов на выходные",     base=2.40, day_amp=1.35, night=0.55,
         weekend=0.22, season=0.11, noise=0.06, spike_p=0.011, spike=2.15, shifts=2),
    dict(id=9,  target_mean=2.15, target_peak=5.4, name="Кампанийное производство", base=2.25, day_amp=1.20, night=0.60,
         weekend=0.80, season=0.10, noise=0.07, spike_p=0.013, spike=2.20, shifts=2,
         campaign=True),
    dict(id=10, target_mean=2.3, target_peak=5.8, name="Высокая хаотичность",     base=2.30, day_amp=1.15, night=0.60,
         weekend=0.82, season=0.10, noise=0.16, spike_p=0.030, spike=2.25, shifts=2),
]


def make_year(a, seed):
    """8760 часов, кВт. Слои: суточный цикл + недельный + сезонный + шум + пики."""
    rng = np.random.default_rng(seed)
    hours = np.arange(8760)
    hod = hours % 24
    dow = (hours // 24 + 3) % 7          # 2026-01-01 - четверг, значит смещение 3
    doy = hours // 24

    # --- суточный цикл: форма зависит от сменности ---------------------------
    if a["shifts"] == 1:
        shape = np.where((hod >= 7) & (hod < 18), 1.0, 0.0)
    elif a["shifts"] == 2:
        shape = np.where((hod >= 6) & (hod < 22), 1.0, 0.0)
        shape = np.where((hod >= 13) & (hod < 15), 0.45, shape)   # межсменка
    else:
        shape = np.full(8760, 0.85)
        shape = np.where((hod >= 8) & (hod < 20), 1.0, shape)
    smooth = np.convolve(np.r_[shape[-3:], shape, shape[:3]],
                         np.ones(5) / 5, mode="same")[3:-3]        # сглаживаем фронты

    x = a["base"] * (a["night"] + (a["day_amp"] - a["night"]) * smooth)

    # --- недельный ритм ------------------------------------------------------
    x = x * np.where(dow >= 5, a["weekend"], 1.0)

    # --- сезонность ----------------------------------------------------------
    ph = a.get("season_phase", 0.0)
    x = x * (1.0 + a["season"] * np.cos(2 * np.pi * doy / 365.0 + ph))

    # --- кампанийность: три недели работы, одна тихая ------------------------
    if a.get("campaign"):
        x = x * np.where((doy // 7) % 4 == 3, 0.55, 1.0)

    # --- шум и короткие пики -------------------------------------------------
    x = x * (1.0 + rng.normal(0, a["noise"], 8760))
    hit = rng.random(8760) < a["spike_p"]
    x = np.where(hit, x * rng.uniform(1.0, a["spike"] / a["day_amp"] + 0.6, 8760), x)

    x = np.clip(x, 0.25, None) * 1000.0                            # МВт -> кВт

    # --- Нормировка под конверт из ТЗ ----------------------------------------
    # Сценарии должны различаться ФОРМОЙ, но описывать один и тот же завод.
    # Без этого шага архетипы разъезжаются по средней в два раза, и сравнивать
    # их между собой становится бессмысленно.
    x = x * (a["target_mean"] * 1000.0 / x.mean())
    x = np.minimum(x, a["target_peak"] * 1000.0)   # срезаем нефизичные выбросы
    x = x * (a["target_mean"] * 1000.0 / x.mean())
    x = np.minimum(x, a["target_peak"] * 1000.0)
    return np.round(x, 1)


def build_scenarios():
    os.makedirs(OUT, exist_ok=True)
    out = {}
    for i, a in enumerate(ARCHETYPES):
        v = make_year(a, RNG_MASTER + i * 7919)
        idx = pd.date_range("2026-01-01", periods=8760, freq="h")
        df = pd.DataFrame({"timestamp": idx, "load_kw": v})
        path = f"{OUT}/load_year_{a['id']:02d}.csv"
        df.to_csv(path, index=False)
        out[a["id"]] = dict(name=a["name"], load=v, month=idx.month.to_numpy(), path=path)
    return out


# =============================================================================
# БЛОК 3. ДИСПЕТЧЕРИЗАЦИЯ КГУ (корневая логика без изменений)
# =============================================================================
COMBOS = []
for mask in range(1 << NG):
    idx = [i for i in range(NG) if mask >> i & 1]
    COMBOS.append(dict(mask=mask, idx=idx,
                       p_lo=sum(UNITS[i]["p_lo"] for i in idx),
                       p_hi=sum(UNITS[i]["p_nom"] for i in idx)))
COMBOS.sort(key=lambda c: -c["p_hi"])


class State:
    def __init__(self):
        self.soc = BESS_CAP
        self.on_h = [0] * NG
        self.off_h = [99] * NG
        self.month = None
        self.month_peak = 0.0      # текущий зафиксированный максимум месяца


def combo_allowed(c, st):
    for i in range(NG):
        want_on = bool(c["mask"] >> i & 1)
        if want_on and st.off_h[i] and st.off_h[i] < MIN_DOWN_H:
            return False
        if not want_on and st.on_h[i] and st.on_h[i] < MIN_UP_H:
            return False
    return True


def commit(p_absorb, st):
    for c in COMBOS:
        if c["p_lo"] <= p_absorb + 1e-9 and combo_allowed(c, st):
            total = min(c["p_hi"], p_absorb)
            gen = [0.0] * NG
            if c["idx"]:
                base = sum(UNITS[i]["p_nom"] for i in c["idx"])
                for i in c["idx"]:
                    gen[i] = total * UNITS[i]["p_nom"] / base
            return gen
    return [0.0] * NG


# =============================================================================
# БЛОК 4. ГОДОВОЙ ПРОГОН
# =============================================================================
def run(load, month, mode="bess", target=None):
    """mode: 'none' - без батареи, 'bess' - арбитраж, 'peak' - срез пика.

    target - базовая уставка потолка закупа, кВт (только для mode='peak').
    """
    st = State()
    n = len(load)
    rec = dict(load=load, ch=np.zeros(n), dis=np.zeros(n), grid=np.zeros(n),
               soc=np.zeros(n), chp=np.zeros(n), starts=np.zeros(n))
    for i in range(NG):
        rec[f"g{i}"] = np.zeros(n)

    for t in range(n):
        L = load[t]
        if st.month != month[t]:                 # начался новый расчётный период
            st.month, st.month_peak = month[t], 0.0

        if mode == "none":
            gen = commit(L, st)
            chp = sum(gen)
            ch = dis = 0.0
            grid = max(0.0, L - chp)
        else:
            room = max(0.0, (BESS_CAP - st.soc) / ETA)
            cap = min(BESS_PWR, CHARGE_RATE_CAP, room)
            gen = commit(L + cap, st)
            chp = sum(gen)
            ch = min(max(0.0, chp - L), cap)
            st.soc += ch * ETA

            gap = max(0.0, L - chp)
            avail = min(BESS_PWR, max(0.0, (st.soc - SOC_FLOOR) * ETA))
            trig = P_CHP_MAX if PEAK_TRIGGER is None else PEAK_TRIGGER
            peak_mode = L > trig + 1e-9          # разряд только выше порога

            if not peak_mode:
                dis = 0.0
            elif mode == "bess":
                dis = min(gap, avail)
            else:
                # Потолок не может быть ниже уже зафиксированного максимума
                # месяца: счёт всё равно выставят по нему, экономить смысла нет.
                eff_target = max(target, st.month_peak)
                dis = min(max(0.0, gap - eff_target), avail)
            st.soc -= dis / ETA
            grid = gap - dis

        st.month_peak = max(st.month_peak, grid)
        for i in range(NG):
            if gen[i] > 1e-9:
                if st.on_h[i] == 0:
                    rec["starts"][t] += 1
                st.on_h[i] += 1; st.off_h[i] = 0
            else:
                st.off_h[i] += 1; st.on_h[i] = 0

        for i in range(NG):
            rec[f"g{i}"][t] = gen[i]
        rec["ch"][t], rec["dis"][t], rec["grid"][t] = ch, dis, grid
        rec["soc"][t], rec["chp"][t] = st.soc, chp

    df = pd.DataFrame(rec)
    df["month"] = month
    return df


def economics(df):
    """Помесячный счёт: энергия за месяц + максимум месяца."""
    m = df.groupby("month").agg(grid_kwh=("grid", "sum"), chp_kwh=("chp", "sum"),
                                peak=("grid", "max"), starts=("starts", "sum"))
    m["cost"] = (m.chp_kwh * CHP_COST + m.grid_kwh * GRID_COST
                 + m.peak * DEMAND_CHARGE + m.starts * START_COST)
    return dict(cost=m.cost.sum(), chp_kwh=m.chp_kwh.sum(), grid_kwh=m.grid_kwh.sum(),
                peak_year=m.peak.max(), peak_sum=m.peak.sum(), peak_mean=m.peak.mean(),
                starts=m.starts.sum(), monthly=m)


# =============================================================================
# БЛОК 5. НЕДЕЛЬНЫЕ ТАБЛИЦЫ
# =============================================================================
def week_table(df, start_h, title):
    w = df.iloc[start_h:start_h + 168].reset_index(drop=True)
    lines = [f"\n### {title}\n",
             "| День | Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |",
             "|---:|---:|---:|---:|---:|---:|---:|---:|---:|"]
    dn = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]
    for i, r in w.iterrows():
        lines.append(f"| {dn[i // 24]} | {i % 24 + 1} | {r.load:.0f} | {r.jen:.0f} | "
                     f"{r.ted:.0f} | {r.ch:.0f} | {-r.dis if r.dis else 0:.0f} | "
                     f"{r.grid:.0f} | {r.soc / BESS_CAP * 100:.1f} |")
    lines.append(f"\n**Итого за неделю:** нагрузка {w.load.sum():,.0f} кВт·ч, "
                 f"КГУ {w.chp.sum():,.0f}, заряд {w.ch.sum():,.0f}, "
                 f"разряд {w.dis.sum():,.0f}, сеть {w.grid.sum():,.0f} кВт·ч, "
                 f"пик сети {w.grid.max():,.0f} кВт\n")
    return "\n".join(lines), w


def week_summary(w):
    d = w.copy(); d["day"] = d.index // 24
    g = d.groupby("day").agg(load=("load", "sum"), chp=("chp", "sum"),
                             ch=("ch", "sum"), dis=("dis", "sum"),
                             grid=("grid", "sum"), peak=("grid", "max"))
    return g


if __name__ == "__main__":
    print("Генерация 10 годовых сценариев...")
    SC = build_scenarios()
    for k, v in SC.items():
        L = v["load"]
        print(f"  {k:>2}. {v['name']:<28} средняя {L.mean() / 1000:.2f} МВт   "
              f"пик {L.max() / 1000:.2f} МВт   год {L.sum() / 1e6:.2f} ГВт*ч")

    print("\n" + "=" * 100)
    print("ГОДОВОЙ ПРОГОН ПО ВСЕМ 10 СЦЕНАРИЯМ")
    print("=" * 100)
    hdr = (f"{'#':>3} {'Сценарий':<28}{'без BESS, ₸':>16}{'с BESS, ₸':>16}"
           f"{'экономия':>14}{'%':>7}{'окуп.,лет':>11}")
    print(hdr); print("-" * len(hdr))
    rows = []
    for k, v in SC.items():
        e0 = economics(run(v["load"], v["month"], "none"))
        e1 = economics(run(v["load"], v["month"], "bess"))
        sav = e0["cost"] - e1["cost"]
        pay = BESS_CAPEX_USD * USD_KZT / sav if sav > 0 else float("inf")
        rows.append(dict(id=k, name=v["name"], c0=e0["cost"], c1=e1["cost"], sav=sav,
                         pct=sav / e0["cost"] * 100, pay=pay,
                         pk0=e0["peak_year"], pk1=e1["peak_year"],
                         chp0=e0["chp_kwh"], chp1=e1["chp_kwh"],
                         g0=e0["grid_kwh"], g1=e1["grid_kwh"]))
        print(f"{k:>3} {v['name']:<28}{e0['cost']:>16,.0f}{e1['cost']:>16,.0f}"
              f"{sav:>14,.0f}{sav / e0['cost'] * 100:>7.1f}{pay:>11.1f}")
    R = pd.DataFrame(rows)
    print("-" * len(hdr))
    print(f"{'':>3} {'СРЕДНЕЕ ПО 10 СЦЕНАРИЯМ':<28}{R.c0.mean():>16,.0f}"
          f"{R.c1.mean():>16,.0f}{R.sav.mean():>14,.0f}{R.pct.mean():>7.1f}"
          f"{R.pay.mean():>11.1f}")
    print(f"{'':>3} {'РАЗБРОС (мин..макс окуп.)':<28}{'':>16}{'':>16}{'':>14}{'':>7}"
          f"{R.pay.min():>5.1f}..{R.pay.max():<5.1f}")

    # --- Недельные таблицы по сценарию 1 -------------------------------------
    v = SC[1]
    d0 = run(v["load"], v["month"], "none")
    d1 = run(v["load"], v["month"], "bess")
    # Уставку не выдумываем, а подбираем: считаем сумму 12 месячных максимумов
    # (именно она определяет счёт за мощность) и годовые затраты.
    sweep = []
    for tg in range(400, 2200, 200):
        e = economics(run(v["load"], v["month"], "peak", target=float(tg)))
        sweep.append((tg, e["peak_sum"], e["cost"], e["peak_year"]))
    TG = min(sweep, key=lambda r: (r[1], r[2]))[0]
    print("\nПОДБОР УСТАВКИ СРЕЗА ПИКА (сценарий 1)")
    print(f"{'уставка, кВт':>14}{'сумма 12 макс., кВт':>22}{'годовой макс.':>16}{'затраты, ₸':>18}")
    for tg, ps, c, py in sweep:
        mark = "  <-- выбрано" if tg == TG else ""
        print(f"{tg:>14}{ps:>22,.0f}{py:>16,.0f}{c:>18,.0f}{mark}")
    d2 = run(v["load"], v["month"], "peak", target=float(TG))
    # Берём неделю в начале мая, стартующую строго с понедельника.
    idx_all = pd.date_range("2026-01-01", periods=8760, freq="h")
    # Берём неделю, которая начинается ПЕРВОГО числа месяца и с понедельника:
    # 1 июня 2026 - понедельник. Так контроллер среза пика стартует с чистого
    # расчётного периода, и его работа видна с первого часа.
    mondays = [i for i in range(0, 8760 - 168, 24)
               if idx_all[i].dayofweek == 0 and idx_all[i].day == 1]
    START = mondays[0]
    txt = [f"# Недельные профили, сценарий 1 «{v['name']}»\n",
           f"Неделя начинается с часа {START} (день {START // 24 + 1} года).\n"]
    for df, ttl in ((d0, "ТАБЛИЦА 1. БЕЗ БАТАРЕИ"),
                    (d1, "ТАБЛИЦА 2. С БАТАРЕЕЙ, энергетический арбитраж"),
                    (d2, f"ТАБЛИЦА 3. С БАТАРЕЕЙ, срез пика с уставкой {TG} кВт")):
        t, w = week_table(df, START, ttl)
        txt.append(t)
    open(f"{OUT}/week_tables.md", "w").write("\n".join(txt))

    print("\n" + "=" * 100)
    print("НЕДЕЛЯ, СВОДКА ПО СУТКАМ (сценарий 1, полные почасовые таблицы в week_tables.md)")
    print("=" * 100)
    dn = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]
    for ttl, df in (("БЕЗ БАТАРЕИ", d0), ("АРБИТРАЖ", d1), (f"СРЕЗ ПИКА {TG}", d2)):
        w = df.iloc[START:START + 168].reset_index(drop=True)
        g = week_summary(w)
        print(f"\n{ttl}")
        print(f"{'День':>5}{'Нагрузка':>11}{'КГУ':>10}{'Заряд':>9}{'Разряд':>9}"
              f"{'Сеть':>10}{'Пик сети':>10}")
        for i, r in g.iterrows():
            print(f"{dn[i]:>5}{r.load:>11,.0f}{r.chp:>10,.0f}{r.ch:>9,.0f}"
                  f"{r.dis:>9,.0f}{r.grid:>10,.0f}{r.peak:>10,.0f}")
        print(f"{'ИТОГ':>5}{g.load.sum():>11,.0f}{g.chp.sum():>10,.0f}"
              f"{g.ch.sum():>9,.0f}{g.dis.sum():>9,.0f}{g.grid.sum():>10,.0f}"
              f"{g.peak.max():>10,.0f}")

    # --- Помесячные пики -----------------------------------------------------
    print("\n" + "=" * 100)
    print("ПОМЕСЯЧНЫЙ МАКСИМУМ ЗАКУПА ИЗ СЕТИ, кВт (сценарий 1)")
    print("=" * 100)
    m0 = economics(d0)["monthly"]; m1 = economics(d1)["monthly"]
    m2 = economics(d2)["monthly"]
    print(f"{'Месяц':>6}{'без BESS':>12}{'арбитраж':>12}{'срез пика':>12}{'срез, %':>10}")
    for mm in range(1, 13):
        a, b, c = m0.peak[mm], m1.peak[mm], m2.peak[mm]
        print(f"{mm:>6}{a:>12,.0f}{b:>12,.0f}{c:>12,.0f}{(1 - c / a) * 100:>10.1f}")
    print(f"{'ГОД':>6}{m0.peak.max():>12,.0f}{m1.peak.max():>12,.0f}"
          f"{m2.peak.max():>12,.0f}{(1 - m2.peak.max() / m0.peak.max()) * 100:>10.1f}")

    # --- Порог платы за мощность ---------------------------------------------
    e1, e2 = economics(d1), economics(d2)
    dpk = e1["peak_sum"] - e2["peak_sum"]        # суммарно по 12 месяцам
    dc = e2["cost"] - e1["cost"]
    print("\n" + "=" * 100)
    print("КОГДА СРЕЗ ПИКА ВЫГОДНЕЕ АРБИТРАЖА")
    print("=" * 100)
    print(f"  Срез пика убирает {dpk:,.0f} кВт суммарно по 12 месячным максимумам,")
    print(f"  но обходится дороже арбитража на {dc:,.0f} ₸/год.")
    if dpk > 0:
        print(f"  Порог: плата за мощность выше {dc / dpk:,.0f} ₸ за кВт максимума месяца.")
    print(f"  Сейчас DEMAND_CHARGE = {DEMAND_CHARGE:.0f} ₸/кВт/мес.")

    # --- Чувствительность к цене сети ----------------------------------------
    print("\n" + "=" * 100)
    print("ЧУВСТВИТЕЛЬНОСТЬ К ТАРИФУ СЕТИ (сценарий 1, окупаемость BESS)")
    print("=" * 100)
    print(f"{'Тариф сети, ₸/кВт*ч':>22}{'спред к КГУ':>14}{'экономия, ₸/год':>20}{'окуп., лет':>13}")
    import copy
    for gp in (25, 30, 35, 40, 45, 50, 60, 70):
        globals()["GRID_COST"] = float(gp)
        a = economics(run(v["load"], v["month"], "none"))["cost"]
        b = economics(run(v["load"], v["month"], "bess"))["cost"]
        s = a - b
        pay = BESS_CAPEX_USD * USD_KZT / s if s > 0 else float("inf")
        print(f"{gp:>22}{gp - CHP_COST:>14}{s:>20,.0f}{pay:>13.1f}")
    globals()["GRID_COST"] = 60.0
    print(f"\nФайлы: 10 годовых CSV (load_year_01..10.csv) и week_tables.md в outputs.")
```

---

# Приложение B. Генераторы отчётов

## B.1 Отчёт на двух КГУ

**`report_final.py`** — сутки, неделя, год; выбор типичных суток

```python
"""
Итоговый отчёт: 2 КГУ + BESS + безлимитная сеть.

МЕТОДИКА (после кросс-валидации):
  1. Считается полный год, 8760 часов.
  2. Типичные сутки и неделя ВЫРЕЗАЮТСЯ из годового прогона, а не считаются
     отдельно. Это принципиально: в них приходит фактический SoC батареи и
     накопленный максимум расчётного месяца.
  3. Пик считается ПОМЕСЯЧНО - расчётный период по счёту календарный месяц.
  4. Контроллер среза пика помнит максимум текущего месяца и не разряжается
     ниже него: счёт всё равно выставят по уже зафиксированному значению.

ТАРИФЫ - как задано: КГУ 22 ₸, сеть 60 ₸, разряд BESS 22/0.88 = 25.0 ₸.
"""

import numpy as np
import pandas as pd
import importlib.util

spec = importlib.util.spec_from_file_location("fy", "factory_year.py")
fy = importlib.util.module_from_spec(spec)
spec.loader.exec_module(fy)

OUT = "/mnt/user-data/outputs"
DN = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]

fy.CHP_COST, fy.GRID_COST, fy.DEMAND_CHARGE = 22.0, 60.0, 0.0

SC = fy.build_scenarios()
v = SC[1]
LOAD, MONTH = v["load"], v["month"]
IDX = pd.date_range("2026-01-01", periods=8760, freq="h")

# --- подбор уставки среза пика ------------------------------------------------
sweep = []
for tg in range(300, 2100, 100):
    e = fy.economics(fy.run(LOAD, MONTH, "peak", target=float(tg)))
    sweep.append((tg, e["peak_sum"], e["cost"], e["peak_year"]))
TG = min(sweep, key=lambda r: (r[1], r[2]))[0]

RUNS = [("A. Без батареи", fy.run(LOAD, MONTH, "none")),
        ("B. С батареей, арбитраж", fy.run(LOAD, MONTH, "bess")),
        (f"C. С батареей, срез пика {TG} кВт", fy.run(LOAD, MONTH, "peak", float(TG)))]

# --- выбор типичных суток -----------------------------------------------------
# Будний день, в первых числах месяца (чтобы контроллер среза пика стартовал
# с чистого расчётного периода), с суточным потреблением ближе всего к медиане.
daily = LOAD.reshape(365, 24).sum(axis=1)
med = np.median(daily)
cand = [d for d in range(365) if IDX[d * 24].dayofweek < 5 and IDX[d * 24].day <= 3]
DAY = min(cand, key=lambda d: abs(daily[d] - med))
D0 = DAY * 24

# --- выбор недели: понедельник, первое число месяца ---------------------------
WK = [i for i in range(0, 8760 - 168, 24)
      if IDX[i].dayofweek == 0 and IDX[i].day == 1][0]

HDR = ("| Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |",
       "|---:|---:|---:|---:|---:|---:|---:|---:|")


def day_rows(df, s):
    w = df.iloc[s:s + 24].reset_index(drop=True)
    out = list(HDR)
    for i, r in w.iterrows():
        out.append(f"| {i + 1} | {r.load:.0f} | {r.jen:.0f} | {r.ted:.0f} | "
                   f"{r.ch:.0f} | {-r.dis if r.dis else 0:.0f} | {r.grid:.0f} | "
                   f"{r.soc / fy.BESS_CAP * 100:.1f} |")
    out.append(f"| **Итог** | **{w.load.sum():,.0f}** | {w.jen.sum():,.0f} | "
               f"{w.ted.sum():,.0f} | {w.ch.sum():,.0f} | {-w.dis.sum():,.0f} | "
               f"**{w.grid.sum():,.0f}** | пик {w.grid.max():,.0f} |")
    return out, w


def week_daily(df, s):
    w = df.iloc[s:s + 168].reset_index(drop=True)
    w = w.assign(day=w.index // 24)
    g = w.groupby("day").agg(load=("load", "sum"), jen=("jen", "sum"),
                             ted=("ted", "sum"), ch=("ch", "sum"),
                             dis=("dis", "sum"), grid=("grid", "sum"),
                             peak=("grid", "max"), soc=("soc", "last"))
    out = ["| День | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |",
           "|---:|---:|---:|---:|---:|---:|---:|---:|---:|"]
    for i, r in g.iterrows():
        out.append(f"| {DN[i]} | {r.load:,.0f} | {r.jen:,.0f} | {r.ted:,.0f} | "
                   f"{r.ch:,.0f} | {-r.dis if r.dis else 0:,.0f} | {r.grid:,.0f} | "
                   f"{r.peak:,.0f} | {r.soc / fy.BESS_CAP * 100:.1f} |")
    out.append(f"| **Итог** | **{g.load.sum():,.0f}** | {g.jen.sum():,.0f} | "
               f"{g.ted.sum():,.0f} | {g.ch.sum():,.0f} | {-g.dis.sum():,.0f} | "
               f"**{g.grid.sum():,.0f}** | **{g.peak.max():,.0f}** | |")
    return out, w


def week_hourly(df, s):
    w = df.iloc[s:s + 168].reset_index(drop=True)
    out = ["| День | Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |",
           "|---:|---:|---:|---:|---:|---:|---:|---:|---:|"]
    for i, r in w.iterrows():
        out.append(f"| {DN[i // 24]} | {i % 24 + 1} | {r.load:.0f} | {r.jen:.0f} | "
                   f"{r.ted:.0f} | {r.ch:.0f} | {-r.dis if r.dis else 0:.0f} | "
                   f"{r.grid:.0f} | {r.soc / fy.BESS_CAP * 100:.1f} |")
    return out


def money(w):
    return w.chp.sum() * fy.CHP_COST + w.grid.sum() * fy.GRID_COST


# =============================================================================
MD = [f"# Расчёт микросети: 2 КГУ + BESS + сеть\n",
      f"Тарифы: КГУ **{fy.CHP_COST:.0f} ₸/кВт·ч**, сеть **{fy.GRID_COST:.0f} ₸/кВт·ч**, "
      f"разряд BESS **{fy.CHP_COST / fy.RTE:.2f} ₸/кВт·ч**.\n",
      f"Типичные сутки: {IDX[D0]:%d.%m.%Y} ({DN[IDX[D0].dayofweek]}). "
      f"Неделя: с {IDX[WK]:%d.%m.%Y}.\n",
      f"Сутки и неделя вырезаны из годового прогона (8760 ч), а не посчитаны отдельно.\n"]

print("=" * 92)
print(f"ТИПИЧНЫЕ СУТКИ: {IDX[D0]:%d.%m.%Y} ({DN[IDX[D0].dayofweek]}), "
      f"потребление {daily[DAY]:,.0f} кВт*ч при медиане {med:,.0f}")
print("=" * 92)
DAYW = {}
MD.append("\n## Типичные сутки\n")
for name, df in RUNS:
    rows, w = day_rows(df, D0)
    DAYW[name] = w
    MD.append(f"\n### {name}\n"); MD += rows
    print(f"\n{name}")
    print("\n".join(rows))

print("\n" + "=" * 92)
print(f"НЕДЕЛЯ с {IDX[WK]:%d.%m.%Y} (понедельник, первое число расчётного месяца)")
print("=" * 92)
WKW = {}
MD.append("\n## Неделя, сводка по суткам\n")
for name, df in RUNS:
    rows, w = week_daily(df, WK)
    WKW[name] = w
    MD.append(f"\n### {name}\n"); MD += rows
    print(f"\n{name}")
    print("\n".join(rows))

MD.append("\n## Неделя, полный почасовой профиль (168 часов)\n")
for name, df in RUNS:
    MD.append(f"\n### {name}\n"); MD += week_hourly(df, WK)

# =============================================================================
ECO = {name: fy.economics(df) for name, df in RUNS}
base = RUNS[0][0]

sm = ["| Показатель | A. Без батареи | B. Арбитраж | C. Срез пика |",
      "|---|---:|---:|---:|"]


def row(lbl, f, fmt="{:,.0f}"):
    vals = [f(n) for n, _ in RUNS]
    return f"| {lbl} | " + " | ".join(fmt.format(x) for x in vals) + " |"


sm.append("| **ТИПИЧНЫЕ СУТКИ** | | | |")
sm.append(row("Нагрузка завода, кВт·ч", lambda n: DAYW[n].load.sum()))
sm.append(row("Выработка КГУ, кВт·ч", lambda n: DAYW[n].chp.sum()))
sm.append(row("Заряд BESS, кВт·ч", lambda n: DAYW[n].ch.sum()))
sm.append(row("Разряд BESS, кВт·ч", lambda n: DAYW[n].dis.sum()))
sm.append(row("Закуп из сети, кВт·ч", lambda n: DAYW[n].grid.sum()))
sm.append(row("Пик закупа, кВт", lambda n: DAYW[n].grid.max()))
sm.append(row("Затраты, ₸", lambda n: money(DAYW[n])))
sm.append(row("Экономия к варианту A, ₸", lambda n: money(DAYW[base]) - money(DAYW[n])))

sm.append("| **НЕДЕЛЯ** | | | |")
sm.append(row("Нагрузка завода, кВт·ч", lambda n: WKW[n].load.sum()))
sm.append(row("Выработка КГУ, кВт·ч", lambda n: WKW[n].chp.sum()))
sm.append(row("Заряд BESS, кВт·ч", lambda n: WKW[n].ch.sum()))
sm.append(row("Разряд BESS, кВт·ч", lambda n: WKW[n].dis.sum()))
sm.append(row("Закуп из сети, кВт·ч", lambda n: WKW[n].grid.sum()))
sm.append(row("Пик закупа, кВт", lambda n: WKW[n].grid.max()))
sm.append(row("Затраты, ₸", lambda n: money(WKW[n])))
sm.append(row("Экономия к варианту A, ₸", lambda n: money(WKW[base]) - money(WKW[n])))

sm.append("| **ГОД** | | | |")
sm.append(row("Выработка КГУ, МВт·ч", lambda n: ECO[n]["chp_kwh"] / 1000))
sm.append(row("Закуп из сети, МВт·ч", lambda n: ECO[n]["grid_kwh"] / 1000))
sm.append(row("Доля сети в питании, %", lambda n: ECO[n]["grid_kwh"]
              / LOAD.sum() * 100, "{:.1f}"))
sm.append(row("Годовой максимум закупа, кВт", lambda n: ECO[n]["peak_year"]))
sm.append(row("Сумма 12 месячных максимумов, кВт", lambda n: ECO[n]["peak_sum"]))
sm.append(row("Пусков КГУ за год", lambda n: ECO[n]["starts"]))
sm.append(row("Затраты, ₸/год", lambda n: ECO[n]["cost"]))
sm.append(row("Экономия к варианту A, ₸/год",
              lambda n: ECO[base]["cost"] - ECO[n]["cost"]))
sm.append(row("Экономия, %", lambda n: (ECO[base]["cost"] - ECO[n]["cost"])
              / ECO[base]["cost"] * 100, "{:.1f}"))
capex = fy.BESS_CAPEX_USD * fy.USD_KZT
sm.append(row("Простая окупаемость, лет",
              lambda n: (capex / (ECO[base]["cost"] - ECO[n]["cost"]))
              if ECO[base]["cost"] - ECO[n]["cost"] > 0 else float("nan"), "{:.1f}"))

print("\n" + "=" * 92)
print("СВОДНАЯ ТАБЛИЦА")
print("=" * 92)
print("\n".join(sm))
MD.append("\n## Сводная таблица\n"); MD += sm

MD.append(f"\n\nCAPEX BESS: ${fy.BESS_CAPEX_USD:,.0f} = {capex:,.0f} ₸ "
          f"(курс {fy.USD_KZT:.0f} ₸/$).\n")
open(f"{OUT}/report_final.md", "w").write("\n".join(MD))
print(f"\nCAPEX BESS: ${fy.BESS_CAPEX_USD:,.0f} = {capex:,.0f} ₸")
print("Полный отчёт с почасовой неделей: report_final.md")
```

## B.2 Отчёт на трёх КГУ

**`report_final3.py`** — обобщён под произвольное число агрегатов

```python
"""
Итоговый отчёт: 2 КГУ + BESS + безлимитная сеть.

МЕТОДИКА (после кросс-валидации):
  1. Считается полный год, 8760 часов.
  2. Типичные сутки и неделя ВЫРЕЗАЮТСЯ из годового прогона, а не считаются
     отдельно. Это принципиально: в них приходит фактический SoC батареи и
     накопленный максимум расчётного месяца.
  3. Пик считается ПОМЕСЯЧНО - расчётный период по счёту календарный месяц.
  4. Контроллер среза пика помнит максимум текущего месяца и не разряжается
     ниже него: счёт всё равно выставят по уже зафиксированному значению.

ТАРИФЫ - как задано: КГУ 22 ₸, сеть 60 ₸, разряд BESS 22/0.88 = 25.0 ₸.
"""

import numpy as np
import pandas as pd
import importlib.util

spec = importlib.util.spec_from_file_location("fy", "factory_year3.py")
fy = importlib.util.module_from_spec(spec)
spec.loader.exec_module(fy)

OUT = "/mnt/user-data/outputs"
DN = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]

fy.CHP_COST, fy.GRID_COST, fy.DEMAND_CHARGE = 22.0, 60.0, 0.0
fy.CHARGE_RATE_CAP = 700.0

SC = fy.build_scenarios()
v = SC[1]
LOAD, MONTH = v["load"], v["month"]
IDX = pd.date_range("2026-01-01", periods=8760, freq="h")

# --- подбор уставки среза пика ------------------------------------------------
sweep = []
for tg in range(200, 2600, 100):
    e = fy.economics(fy.run(LOAD, MONTH, "peak", target=float(tg)))
    sweep.append((tg, e["peak_sum"], e["cost"], e["peak_year"]))
TG = min(sweep, key=lambda r: (r[1], r[2]))[0]

RUNS = [("A. Без батареи", fy.run(LOAD, MONTH, "none")),
        ("B. С батареей, арбитраж", fy.run(LOAD, MONTH, "bess")),
        (f"C. С батареей, срез пика {TG} кВт", fy.run(LOAD, MONTH, "peak", float(TG)))]

# --- выбор типичных суток -----------------------------------------------------
# Будний день, в первых числах месяца (чтобы контроллер среза пика стартовал
# с чистого расчётного периода), с суточным потреблением ближе всего к медиане.
daily = LOAD.reshape(365, 24).sum(axis=1)
med = np.median(daily)
cand = [d for d in range(365) if IDX[d * 24].dayofweek < 5 and IDX[d * 24].day <= 3]
DAY = min(cand, key=lambda d: abs(daily[d] - med))
D0 = DAY * 24

# --- выбор недели: понедельник, первое число месяца ---------------------------
WK = [i for i in range(0, 8760 - 168, 24)
      if IDX[i].dayofweek == 0 and IDX[i].day == 1][0]

GN = [u["name"] for u in fy.UNITS]
GC = [f"g{i}" for i in range(fy.NG)]
HDR = ("| Час | Нагрузка | " + " | ".join(GN) + " | Заряд + | Разряд − | Сеть | SoC,% |",
       "|---:|" + "---:|" * (len(GN) + 1) + "---:|---:|---:|---:|")


def day_rows(df, s):
    w = df.iloc[s:s + 24].reset_index(drop=True)
    out = list(HDR)
    for i, r in w.iterrows():
        g = " | ".join(f"{r[c]:.0f}" for c in GC)
        out.append(f"| {i + 1} | {r.load:.0f} | {g} | "
                   f"{r.ch:.0f} | {-r.dis if r.dis else 0:.0f} | {r.grid:.0f} | "
                   f"{r.soc / fy.BESS_CAP * 100:.1f} |")
    gs = " | ".join(f"{w[c].sum():,.0f}" for c in GC)
    out.append(f"| **Итог** | **{w.load.sum():,.0f}** | {gs} | "
               f"{w.ch.sum():,.0f} | {-w.dis.sum():,.0f} | "
               f"**{w.grid.sum():,.0f}** | пик {w.grid.max():,.0f} |")
    return out, w


def week_daily(df, s):
    w = df.iloc[s:s + 168].reset_index(drop=True)
    w = w.assign(day=w.index // 24)
    agg = dict(load=("load", "sum"), ch=("ch", "sum"), dis=("dis", "sum"),
               grid=("grid", "sum"), peak=("grid", "max"), soc=("soc", "last"))
    for c in GC:
        agg[c] = (c, "sum")
    g = w.groupby("day").agg(**agg)
    out = ["| День | Нагрузка | " + " | ".join(GN) + " | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |",
           "|---:|" + "---:|" * (len(GN) + 1) + "---:|---:|---:|---:|---:|"]
    for i, r in g.iterrows():
        gg = " | ".join(f"{r[c]:,.0f}" for c in GC)
        out.append(f"| {DN[i]} | {r.load:,.0f} | {gg} | "
                   f"{r.ch:,.0f} | {-r.dis if r.dis else 0:,.0f} | {r.grid:,.0f} | "
                   f"{r.peak:,.0f} | {r.soc / fy.BESS_CAP * 100:.1f} |")
    gs = " | ".join(f"{g[c].sum():,.0f}" for c in GC)
    out.append(f"| **Итог** | **{g.load.sum():,.0f}** | {gs} | "
               f"{g.ch.sum():,.0f} | {-g.dis.sum():,.0f} | "
               f"**{g.grid.sum():,.0f}** | **{g.peak.max():,.0f}** | |")
    return out, w


def week_hourly(df, s):
    w = df.iloc[s:s + 168].reset_index(drop=True)
    out = ["| День | Час | Нагрузка | " + " | ".join(GN) + " | Заряд + | Разряд − | Сеть | SoC,% |",
           "|---:|---:|" + "---:|" * (len(GN) + 1) + "---:|---:|---:|---:|"]
    for i, r in w.iterrows():
        gg = " | ".join(f"{r[c]:.0f}" for c in GC)
        out.append(f"| {DN[i // 24]} | {i % 24 + 1} | {r.load:.0f} | {gg} | "
                   f"{r.ch:.0f} | {-r.dis if r.dis else 0:.0f} | "
                   f"{r.grid:.0f} | {r.soc / fy.BESS_CAP * 100:.1f} |")
    return out


def money(w):
    return w.chp.sum() * fy.CHP_COST + w.grid.sum() * fy.GRID_COST


# =============================================================================
MD = [f"# Расчёт микросети: 2 КГУ + BESS + сеть\n",
      f"Тарифы: КГУ **{fy.CHP_COST:.0f} ₸/кВт·ч**, сеть **{fy.GRID_COST:.0f} ₸/кВт·ч**, "
      f"разряд BESS **{fy.CHP_COST / fy.RTE:.2f} ₸/кВт·ч**.\n",
      f"Типичные сутки: {IDX[D0]:%d.%m.%Y} ({DN[IDX[D0].dayofweek]}). "
      f"Неделя: с {IDX[WK]:%d.%m.%Y}.\n",
      f"Сутки и неделя вырезаны из годового прогона (8760 ч), а не посчитаны отдельно.\n"]

print("=" * 92)
print(f"ТИПИЧНЫЕ СУТКИ: {IDX[D0]:%d.%m.%Y} ({DN[IDX[D0].dayofweek]}), "
      f"потребление {daily[DAY]:,.0f} кВт*ч при медиане {med:,.0f}")
print("=" * 92)
DAYW = {}
MD.append("\n## Типичные сутки\n")
for name, df in RUNS:
    rows, w = day_rows(df, D0)
    DAYW[name] = w
    MD.append(f"\n### {name}\n"); MD += rows
    print(f"\n{name}")
    print("\n".join(rows))

print("\n" + "=" * 92)
print(f"НЕДЕЛЯ с {IDX[WK]:%d.%m.%Y} (понедельник, первое число расчётного месяца)")
print("=" * 92)
WKW = {}
MD.append("\n## Неделя, сводка по суткам\n")
for name, df in RUNS:
    rows, w = week_daily(df, WK)
    WKW[name] = w
    MD.append(f"\n### {name}\n"); MD += rows
    print(f"\n{name}")
    print("\n".join(rows))

MD.append("\n## Неделя, полный почасовой профиль (168 часов)\n")
for name, df in RUNS:
    MD.append(f"\n### {name}\n"); MD += week_hourly(df, WK)

# =============================================================================
ECO = {name: fy.economics(df) for name, df in RUNS}
base = RUNS[0][0]

sm = ["| Показатель | A. Без батареи | B. Арбитраж | C. Срез пика |",
      "|---|---:|---:|---:|"]


def row(lbl, f, fmt="{:,.0f}"):
    vals = [f(n) for n, _ in RUNS]
    return f"| {lbl} | " + " | ".join(fmt.format(x) for x in vals) + " |"


sm.append("| **ТИПИЧНЫЕ СУТКИ** | | | |")
sm.append(row("Нагрузка завода, кВт·ч", lambda n: DAYW[n].load.sum()))
sm.append(row("Выработка КГУ, кВт·ч", lambda n: DAYW[n].chp.sum()))
sm.append(row("Заряд BESS, кВт·ч", lambda n: DAYW[n].ch.sum()))
sm.append(row("Разряд BESS, кВт·ч", lambda n: DAYW[n].dis.sum()))
sm.append(row("Закуп из сети, кВт·ч", lambda n: DAYW[n].grid.sum()))
sm.append(row("Пик закупа, кВт", lambda n: DAYW[n].grid.max()))
sm.append(row("Затраты, ₸", lambda n: money(DAYW[n])))
sm.append(row("Экономия к варианту A, ₸", lambda n: money(DAYW[base]) - money(DAYW[n])))

sm.append("| **НЕДЕЛЯ** | | | |")
sm.append(row("Нагрузка завода, кВт·ч", lambda n: WKW[n].load.sum()))
sm.append(row("Выработка КГУ, кВт·ч", lambda n: WKW[n].chp.sum()))
sm.append(row("Заряд BESS, кВт·ч", lambda n: WKW[n].ch.sum()))
sm.append(row("Разряд BESS, кВт·ч", lambda n: WKW[n].dis.sum()))
sm.append(row("Закуп из сети, кВт·ч", lambda n: WKW[n].grid.sum()))
sm.append(row("Пик закупа, кВт", lambda n: WKW[n].grid.max()))
sm.append(row("Затраты, ₸", lambda n: money(WKW[n])))
sm.append(row("Экономия к варианту A, ₸", lambda n: money(WKW[base]) - money(WKW[n])))

sm.append("| **ГОД** | | | |")
sm.append(row("Выработка КГУ, МВт·ч", lambda n: ECO[n]["chp_kwh"] / 1000))
sm.append(row("Закуп из сети, МВт·ч", lambda n: ECO[n]["grid_kwh"] / 1000))
sm.append(row("Доля сети в питании, %", lambda n: ECO[n]["grid_kwh"]
              / LOAD.sum() * 100, "{:.1f}"))
sm.append(row("Годовой максимум закупа, кВт", lambda n: ECO[n]["peak_year"]))
sm.append(row("Сумма 12 месячных максимумов, кВт", lambda n: ECO[n]["peak_sum"]))
sm.append(row("Пусков КГУ за год", lambda n: ECO[n]["starts"]))
sm.append(row("Затраты, ₸/год", lambda n: ECO[n]["cost"]))
sm.append(row("Экономия к варианту A, ₸/год",
              lambda n: ECO[base]["cost"] - ECO[n]["cost"]))
sm.append(row("Экономия, %", lambda n: (ECO[base]["cost"] - ECO[n]["cost"])
              / ECO[base]["cost"] * 100, "{:.1f}"))
capex = fy.BESS_CAPEX_USD * fy.USD_KZT
sm.append(row("Простая окупаемость, лет",
              lambda n: (capex / (ECO[base]["cost"] - ECO[n]["cost"]))
              if ECO[base]["cost"] - ECO[n]["cost"] > 0 else float("nan"), "{:.1f}"))

print("\n" + "=" * 92)
print("СВОДНАЯ ТАБЛИЦА")
print("=" * 92)
print("\n".join(sm))
MD.append("\n## Сводная таблица\n"); MD += sm

MD.append(f"\n\nCAPEX BESS: ${fy.BESS_CAPEX_USD:,.0f} = {capex:,.0f} ₸ "
          f"(курс {fy.USD_KZT:.0f} ₸/$).\n")
open(f"{OUT}/report_final_3chp.md", "w").write("\n".join(MD))
print(f"\nCAPEX BESS: ${fy.BESS_CAPEX_USD:,.0f} = {capex:,.0f} ₸")
print("Полный отчёт с почасовой неделей: report_final.md")
```

## B.3 Табличный вывод первой итерации

**`mkreport.py`** — markdown-таблицы MILP-модели

```python
import importlib.util, io, contextlib
spec = importlib.util.spec_from_file_location("m", "chp_bess_dispatch.py")
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
st1, r1 = m.solve(False); st2, r2 = m.solve(True)
f = m.f
L = []
L.append("# Оптимизация диспетчеризации 3 КГУ + BESS\n")
L.append(f"Единицы: мощность в {m.POWER_UNIT}, энергия в {m.E_UNIT}, затраты в у.е.\n")
L.append("## Таблица 1. Без батареи\n")
L.append("| Час | Потребление | КГУ 1 | КГУ 2 | КГУ 3 | Сеть | Сброс/холостой ход | Затраты за час |")
L.append("|---:|---:|---:|---:|---:|---:|---:|---:|")
for r in r1:
    L.append(f"| {r['t']} | {f(r['load'])} | {f(r['g1'])} | {f(r['g2'])} | {f(r['g3'])} | {f(r['grid'])} | {f(r['spill'])} | {r['cost']:.2f} |")
c1 = sum(r['cost'] for r in r1)
L.append(f"| **Итог** | | | | | {f(sum(r['grid'] for r in r1))} | {f(sum(r['spill'] for r in r1))} | **{c1:.2f}** |\n")
L.append("## Таблица 2. С батареей\n")
L.append("| Час | Потребление | КГУ 1 | КГУ 2 | КГУ 3 | Сеть | Заряд (-) | Разряд (+) | SoC | Затраты за час |")
L.append("|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|")
for r in r2:
    L.append(f"| {r['t']} | {f(r['load'])} | {f(r['g1'])} | {f(r['g2'])} | {f(r['g3'])} | {f(r['grid'])} | {f(-r['ch'])} | {f(r['dis'])} | {f(r['soc'])} | {r['cost']:.2f} |")
c2 = sum(r['cost'] for r in r2)
L.append(f"| **Итог** | | | | | {f(sum(r['grid'] for r in r2))} | | | | **{c2:.2f}** |\n")
d = c1 - c2
L.append("## Экономический итог\n")
L.append("| Показатель | Без батареи | С батареей | Разница |")
L.append("|---|---:|---:|---:|")
L.append(f"| Затраты за сутки, у.е. | {c1:.2f} | {c2:.2f} | {d:.2f} |")
L.append(f"| Расход газа, м3/сутки | {sum(r['fuel'] for r in r1):.1f} | {sum(r['fuel'] for r in r2):.1f} | {sum(r['fuel'] for r in r1)-sum(r['fuel'] for r in r2):.1f} |")
L.append(f"| Покупка из сети, {m.E_UNIT}/сутки | {f(sum(r['grid'] for r in r1))} | {f(sum(r['grid'] for r in r2))} | |")
L.append(f"| Сброшено в холостую, {m.E_UNIT}/сутки | {f(sum(r['spill'] for r in r1))} | {f(sum(r['spill'] for r in r2))} | |")
L.append(f"| **Затраты за год (365 дн.), у.е.** | **{c1*365:.0f}** | **{c2*365:.0f}** | **{d*365:.0f}** |")
L.append(f"\nЭкономический эффект от внедрения BESS: **{d*365:.0f} у.е./год** ({d/c1*100:.2f}%).\n")
open("/mnt/user-data/outputs/results.md","w").write("\n".join(L))
print("ok", c1, c2)
```

## B.4 Диагностика и суточные таблицы третьей итерации

**`tables.py`** — таблицы по трём сценариям, диагностика зависания

```python
import importlib.util, numpy as np, pandas as pd
spec = importlib.util.spec_from_file_location("m","microgrid_ems.py"); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
rng = np.random.default_rng(7)
out=["# Симуляция EMS: 3 КГУ + BESS\n",
     f"Топливная кривая: fuel(P) = {m.FUEL_A:.2f} + {m.FUEL_B:.5f}*P м3/ч. "
     f"sfc(500)={m.sfc(m.P_NOM):.4f}, sfc(100)={m.sfc(m.P_MIN):.4f} (x{m.sfc(m.P_MIN)/m.sfc(m.P_NOM):.2f}).\n",
     f"Порог перелома: {m.P_BREAKEVEN:.1f} кВт ({m.P_BREAKEVEN/m.P_NOM*100:.1f}% номинала).\n"]
diag={}
for name,(gl,days) in m.SCENARIOS.items():
    L = gl(rng)
    df,soc,st = m.sim_ems(L)
    nb = m.sim_no_bess(L)
    diag[name]=(df,nb)
    out.append(f"\n## Сценарий «{name}» — EMS из ТЗ\n")
    out.append("| Час | Потребление | Разряд (-) | Заряд (+) | SoC,% | КГУ1 | КГУ2 | КГУ3 | Холостой/сброс | Топливо,у.е. | Состояние |")
    out.append("|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|:--|")
    for _,r in df.iterrows():
        out.append(f"| {int(r.hour)} | {r.load:.0f} | {-r.dis:.0f} | {r.ch:.0f} | {r.soc_pct:.1f} | "
                   f"{r.g1:.0f} | {r.g2:.0f} | {r.g3:.0f} | {r.spill:.0f} | {r.cost:.2f} | {r.state} |")
    out.append(f"\nЗа сутки: топливо **{df.gas.sum():.0f} м3** ({df.cost.sum():.2f} у.е.), "
               f"сброс {df.spill.sum():.0f} кВт·ч, недоотпуск {df.deficit.sum():.0f} кВт·ч. "
               f"Без батареи: {nb.gas.sum():.0f} м3 ({nb.cost.sum():.2f} у.е.), сброс {nb.spill.sum():.0f} кВт·ч.\n")
open("/mnt/user-data/outputs/ems_tables.md","w").write("\n".join(out))

print("=== ДИАГНОСТИКА ПИКОВОГО ДНЯ (три подряд суток, состояние переносится) ===\n")
soc, st = m.SOC_HI, "DISCHARGE"
print(f"{'сутки':>6}{'час':>5}{'нагр':>7}{'разряд':>8}{'заряд':>7}{'SoC%':>7}{'КГУ сум':>9}{'состояние':>11}")
for d in range(3):
    L = m.load_peak(rng)
    df, soc, st = m.sim_ems(L, soc, st)
    for _,r in df.iterrows():
        if d==0 and int(r.hour)<=4 or int(r.hour) in (1,12,24):
            print(f"{d+1:>6}{int(r.hour):>5}{r.load:>7.0f}{r.dis:>8.0f}{r.ch:>7.0f}"
                  f"{r.soc_pct:>7.1f}{r.g1+r.g2+r.g3:>9.0f}{r.state:>11}")
print(f"\nSoC в конце третьих суток: {soc/m.BESS_CAP*100:.1f}%  состояние: {st}")
```

## B.5 Шаблон CSV для реальных данных

**`csv_demo.py`** — проверка load_from_file

```python
import importlib.util, numpy as np, pandas as pd
spec = importlib.util.spec_from_file_location("m","microgrid_ems.py"); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
rng = np.random.default_rng(1)
rows=[]
for d in range(7):
    for h,v in enumerate(m.load_nominal(rng)):
        rows.append({"day":d+1,"hour":h+1,"load_kw":round(float(v),1)})
pd.DataFrame(rows).to_csv("/mnt/user-data/outputs/load_template.csv", index=False)
days = m.load_from_file("/mnt/user-data/outputs/load_template.csv")
print(f"Шаблон CSV создан: {len(days)} суток по 24 часа, загрузка через load_from_file() работает.")
print(f"Первые сутки, среднее {np.mean(days[0]):.0f} кВт, макс {np.max(days[0]):.0f} кВт")
```

---

# Приложение C. Скрипты чувствительности и подбора

## C.1 Порог перелома стратегий по техминимуму

**`sens.py`**

```python
import importlib.util
spec = importlib.util.spec_from_file_location("m", "ems_lab.py"); m = importlib.util.module_from_spec(spec)
spec.loader.exec_module(m)

print("Порог перелома: батарея выгодна, только если sfc(загрузка) > sfc(P_NOM)/RTE\n")
print(f"{'P_MIN, % ном.':>14}{'P_MIN, MW':>11}{'sfc(P_MIN)':>12}{'порог':>9}{'BESS полезна?':>15}")
print("-"*61)
for frac in (0.20, 0.25, 0.30, 0.35, 0.40, 0.45, 0.50):
    pm = frac * m.P_NOM
    s = m.sfc(pm)
    print(f"{frac*100:>13.0f}%{pm:>11.3f}{s:>12.2f}{m.SFC_BREAKEVEN:>9.2f}{('ДА' if s > m.SFC_BREAKEVEN else 'нет'):>15}")

print("\n\nПрогон трёх стратегий при P_MIN = 20% номинала (как в исходном ТЗ):\n")
m.P_MIN = 0.20 * m.P_NOM
m.SFC_BREAKEVEN = m.sfc(m.P_NOM) / m.RTE
rows = [("Load Following (без BESS)", m.steady(m.sim_load_following())),
        ("Cycle Charging (EMS из ТЗ)", m.steady(m.sim_cycle_charging())),
        ("Гибрид (порог по sfc)", m.steady(m.sim_hybrid()))]
st, ml = m.optimize_milp()
rows.append(("MILP-эталон", {k: sum(r[k] for r in ml) for k in ("cost","gas","spill","grid","starts")}))
best = rows[-1][1]["cost"]; base = rows[0][1]["cost"]
print(f"{'Стратегия':<30}{'у.е./сут':>10}{'сброс':>9}{'gap,%':>8}{'к базе,у.е./год':>18}")
print("-"*75)
for n_, r in rows:
    print(f"{n_:<30}{r['cost']:>10.2f}{r['spill']:>9.3f}{(r['cost']-best)/best*100:>8.2f}{(base-r['cost'])*365:>18.0f}")
```

## C.2 Профиль с глубоким ночным провалом

**`sens2.py`**

```python
import importlib.util
spec = importlib.util.spec_from_file_location("m", "ems_lab.py"); m = importlib.util.module_from_spec(spec)
spec.loader.exec_module(m)

# Профиль с ГЛУБОКИМ ночным провалом: завод почти стоит, остаётся освещение и охрана.
m.LOAD_KW = [180,150,150,150,180,300,700,1100,1300,1300,1300,1250,
             500,450,1200,1300,1300,1250,1000,700,400,250,200,180]
m.LOAD = [m.p(x) for x in m.LOAD_KW]
m.P_MIN = 0.20 * m.P_NOM
m.SFC_BREAKEVEN = m.sfc(m.P_NOM) / m.RTE
print(f"Суточное потребление: {sum(m.LOAD):.3f} MWh")
print(f"Ночная загрузка одного КГУ: 0.150 MW -> sfc = {m.sfc(m.p(150)):.1f} м3/MWh")
print(f"Порог sfc(P_NOM)/RTE = {m.SFC_BREAKEVEN:.1f}  -> прямая выработка НЕВЫГОДНА\n")
rows = [("Load Following (без BESS)", m.steady(m.sim_load_following())),
        ("Cycle Charging (EMS из ТЗ)", m.steady(m.sim_cycle_charging())),
        ("Гибрид (порог по sfc)", m.steady(m.sim_hybrid()))]
st, ml = m.optimize_milp()
rows.append(("MILP-эталон", {k: sum(r[k] for r in ml) for k in ("cost","gas","spill","grid","starts")}))
best = rows[-1][1]["cost"]; base = rows[0][1]["cost"]
print(f"{'Стратегия':<30}{'у.е./сут':>10}{'газ,м3':>10}{'сброс':>8}{'gap,%':>8}{'к базе,у.е./год':>18}")
print("-"*84)
for n_, r in rows:
    print(f"{n_:<30}{r['cost']:>10.2f}{r['gas']:>10.1f}{r['spill']:>8.3f}{(r['cost']-best)/best*100:>8.2f}{(base-r['cost'])*365:>18.0f}")
```

## C.3 Порог пикового режима для трёх КГУ

**`sens3.py`**

```python
import importlib.util
sp = importlib.util.spec_from_file_location("fy","factory_year3.py"); fy=importlib.util.module_from_spec(sp); sp.loader.exec_module(fy)
fy.CHP_COST, fy.GRID_COST, fy.DEMAND_CHARGE = 22.0, 60.0, 0.0
SC = fy.build_scenarios(); v = SC[1]; L, M = v["load"], v["month"]
import numpy as np
print(f"Часов в году с нагрузкой выше 3367 кВт (порог пикового режима): "
      f"{(L>3367).sum()} из 8760 = {(L>3367).mean()*100:.1f}%")
print(f"Часов выше 2267 кВт (старый порог при двух КГУ): {(L>2267).sum()} = {(L>2267).mean()*100:.1f}%\n")
e0 = fy.economics(fy.run(L, M, "none"))
print(f"{'PEAK_TRIGGER':>14}{'CHARGE_CAP':>12}{'закуп,МВт*ч':>14}{'затраты,₸/год':>17}{'экономия':>15}{'окуп.,лет':>11}")
best=(None,None,-1)
for trig in (None, 2600, 2267, 2000, 1700, 1400):
    for cap in (300, 700, 1200):
        fy.PEAK_TRIGGER = trig; fy.CHARGE_RATE_CAP=float(cap)
        e = fy.economics(fy.run(L, M, "bess"))
        sav = e0["cost"]-e["cost"]; pay = fy.BESS_CAPEX_USD*fy.USD_KZT/sav if sav>0 else 9e9
        if sav>best[2]: best=(trig,cap,sav)
        lbl = "3367 (=P_CHP)" if trig is None else str(trig)
        print(f"{lbl:>14}{cap:>12}{e['grid_kwh']/1000:>14,.0f}{e['cost']:>17,.0f}{sav:>15,.0f}{pay:>11.1f}")
print(f"\nЛучшее: порог {best[0]}, cap {best[1]}, экономия {best[2]:,.0f} ₸/год, "
      f"окупаемость {fy.BESS_CAPEX_USD*fy.USD_KZT/best[2]:.1f} лет")
```

## C.4 Подбор ограничителя скорости заряда

**`sweep_cap.py`**

```python
import importlib.util
sp = importlib.util.spec_from_file_location("fy","factory_year.py"); fy = importlib.util.module_from_spec(sp); sp.loader.exec_module(fy)
fy.CHP_COST, fy.GRID_COST = 22.0, 60.0
SC = fy.build_scenarios(); v = SC[1]
e0 = fy.economics(fy.run(v["load"], v["month"], "none"))
print(f"{'CHARGE_RATE_CAP, кВт':>22}{'закуп, МВт*ч/год':>19}{'затраты, ₸/год':>18}{'экономия':>16}{'окуп.,лет':>11}")
best=None
for cap in (300,400,500,600,700,800,1000,1200,1600,2500):
    fy.CHARGE_RATE_CAP = float(cap)
    e = fy.economics(fy.run(v["load"], v["month"], "bess"))
    sav = e0["cost"]-e["cost"]; pay = fy.BESS_CAPEX_USD*fy.USD_KZT/sav
    m = "  <--" if best is None or sav>best[1] else ""
    if best is None or sav>best[1]: best=(cap,sav)
    print(f"{cap:>22}{e['grid_kwh']/1000:>19,.0f}{e['cost']:>18,.0f}{sav:>16,.0f}{pay:>11.1f}{m}")
print(f"\nЛучшее: {best[0]} кВт, экономия {best[1]:,.0f} ₸/год")
```

## C.5 Составы и параметры трёх КГУ

**`sweep3.py`**

```python
import importlib.util
sp = importlib.util.spec_from_file_location("fy","factory_year3.py"); fy = importlib.util.module_from_spec(sp); sp.loader.exec_module(fy)
fy.CHP_COST, fy.GRID_COST, fy.DEMAND_CHARGE = 22.0, 60.0, 0.0
print("Состав парка:")
for u in fy.UNITS: print(f"  {u['name']:<12}{u['p_nom']:>8.0f} кВт   полоса {u['p_lo']:>7.1f} - {u['p_nom']:>7.1f}")
print(f"  Суммарно: {fy.P_CHP_MAX:.0f} кВт\n")
print("Допустимые составы (полосы суммарной мощности):")
for c in fy.COMBOS:
    nm = " + ".join(fy.UNITS[i]["name"] for i in c["idx"]) or "все выключены"
    print(f"  {nm:<34}{c['p_lo']:>9.1f} - {c['p_hi']:>8.1f}")
SC = fy.build_scenarios(); v = SC[1]
e0 = fy.economics(fy.run(v["load"], v["month"], "none"))
print(f"\nПодбор CHARGE_RATE_CAP:")
print(f"{'кВт':>8}{'закуп, МВт*ч':>16}{'затраты, ₸/год':>18}{'экономия':>16}{'окуп.,лет':>11}")
best=(None,-1)
for cap in (300,500,700,900,1100,1400,1800,2500):
    fy.CHARGE_RATE_CAP = float(cap)
    e = fy.economics(fy.run(v["load"], v["month"], "bess"))
    sav = e0["cost"]-e["cost"]; pay = fy.BESS_CAPEX_USD*fy.USD_KZT/sav if sav>0 else 9e9
    if sav>best[1]: best=(cap,sav)
    print(f"{cap:>8}{e['grid_kwh']/1000:>16,.0f}{e['cost']:>18,.0f}{sav:>16,.0f}{pay:>11.1f}")
print(f"\nЛучшее: {best[0]} кВт, экономия {best[1]:,.0f} ₸/год")
fy.CHARGE_RATE_CAP = float(best[0])
print(f"\nПодбор уставки среза пика:")
print(f"{'уставка':>9}{'сумма 12 макс.':>17}{'годовой макс.':>16}{'затраты, ₸':>18}")
sw=[]
for tg in range(300,2600,200):
    e = fy.economics(fy.run(v["load"], v["month"], "peak", target=float(tg)))
    sw.append((tg,e["peak_sum"],e["cost"],e["peak_year"]))
TG=min(sw,key=lambda r:(r[1],r[2]))[0]
for tg,ps,c,py in sw:
    print(f"{tg:>9}{ps:>17,.0f}{py:>16,.0f}{c:>18,.0f}{'  <-- выбрано' if tg==TG else ''}")
```

---

# Приложение D. Экспорт и валидация

## D.1 Экспорт данных в артефакт

**`export_js.py`** — плоские массивы, 29 КБ вместо 90

```python
import importlib.util, numpy as np, pandas as pd, json

def load_mod(path, name):
    sp = importlib.util.spec_from_file_location(name, path)
    m = importlib.util.module_from_spec(sp); sp.loader.exec_module(m); return m

IDX = pd.date_range("2026-01-01", periods=8760, freq="h")

def build(path, name, tg, label, units):
    fy = load_mod(path, name)
    fy.CHP_COST, fy.GRID_COST, fy.DEMAND_CHARGE = 22.0, 60.0, 0.0
    SC = fy.build_scenarios(); v = SC[1]
    L, M = v["load"], v["month"]
    runs = {"A": fy.run(L, M, "none"),
            "B": fy.run(L, M, "bess"),
            "C": fy.run(L, M, "peak", float(tg))}
    daily = L.reshape(365,24).sum(axis=1); med = np.median(daily)
    cand = [d for d in range(365) if IDX[d*24].dayofweek < 5 and IDX[d*24].day <= 3]
    DAY = min(cand, key=lambda d: abs(daily[d]-med)); D0 = DAY*24
    WK = [i for i in range(0,8760-168,24) if IDX[i].dayofweek==0 and IDX[i].day==1][0]
    def rows(df, s, n):
        w = df.iloc[s:s+n]
        return [[round(float(r.load)), round(float(r.chp)), round(float(r.ch)),
                 round(float(r.dis)), round(float(r.soc)/fy.BESS_CAP*100,1),
                 round(float(r.grid))] for _, r in w.iterrows()]
    return dict(label=label, units=units, pchp=round(fy.P_CHP_MAX), target=tg,
                day={k: rows(d, D0, 24) for k,d in runs.items()},
                week={k: rows(d, WK, 168) for k,d in runs.items()},
                dayDate=f"{IDX[D0]:%d.%m.%Y}", weekDate=f"{IDX[WK]:%d.%m.%Y}")

out = {"chp2": build("factory_year.py","fy2",600,"2 КГУ","Jenbacher 1067 + TEDOM 1200"),
       "chp3": build("factory_year3.py","fy3",700,"3 КГУ","Jenbacher 1067 + TEDOM 1200 + КГУ-3 1100")}
js = json.dumps(out, ensure_ascii=False, separators=(",",":"))
open("data_blob.js","w").write("const DATA = " + js + ";")
print("байт:", len(js))
print("день, 2 КГУ, B, первые 3 часа:", out["chp2"]["day"]["B"][:3])
print("неделя, 3 КГУ, A, первые 2 часа:", out["chp3"]["week"]["A"][:2])
print("даты:", out["chp2"]["dayDate"], out["chp2"]["weekDate"])
```

## D.2 Кросс-валидация первого уровня

**`validate.py`**

```python
import json, re, importlib.util, numpy as np, pandas as pd

src = open("/mnt/user-data/outputs/bess_profile.jsx").read()
BLOB = json.loads(re.search(r"const DATA = (\{.*?\});\n", src, re.S).group(1))
CHP, GRID, ETA = 22.0, 60.0, 0.88 ** 0.5
SPREAD = GRID - CHP
ok = lambda b: "OK " if b else "ОШИБКА "
fails = []

def check(name, cond, detail=""):
    if not cond: fails.append(name + " " + detail)
    print(f"  [{ok(cond)}] {name} {detail}")

# ---------- 1. НЕЗАВИСИМЫЙ ПЕРЕСЧЁТ ИЗ МОДЕЛЕЙ ----------
print("\n1. СВЕРКА БЛОБА С НЕЗАВИСИМЫМ ПРОГОНОМ МОДЕЛЕЙ")
IDX = pd.date_range("2026-01-01", periods=8760, freq="h")
def remake(path, nm, tg):
    sp = importlib.util.spec_from_file_location(nm, path); m = importlib.util.module_from_spec(sp); sp.loader.exec_module(m)
    m.CHP_COST, m.GRID_COST, m.DEMAND_CHARGE = 22.0, 60.0, 0.0
    v = m.build_scenarios()[1]; L, M = v["load"], v["month"]
    R = {"A": m.run(L,M,"none"), "B": m.run(L,M,"bess"), "C": m.run(L,M,"peak", float(tg))}
    daily = L.reshape(365,24).sum(axis=1)
    cand=[d for d in range(365) if IDX[d*24].dayofweek<5 and IDX[d*24].day<=3]
    D0 = min(cand,key=lambda d:abs(daily[d]-np.median(daily)))*24
    WK = [i for i in range(0,8760-168,24) if IDX[i].dayofweek==0 and IDX[i].day==1][0]
    def rows(df,s,n):
        w=df.iloc[s:s+n]
        return [[round(float(r.load)),round(float(r.chp)),round(float(r.ch)),round(float(r.dis)),
                 round(float(r.soc)/m.BESS_CAP*100,1),round(float(r.grid))] for _,r in w.iterrows()]
    return {"day":{k:rows(d,D0,24) for k,d in R.items()},
            "week":{k:rows(d,WK,168) for k,d in R.items()}}, m

for key,(p,nm,tg) in {"chp2":("factory_year.py","v2",600),"chp3":("factory_year3.py","v3",700)}.items():
    fresh,_ = remake(p,nm,tg)
    for per in ("day","week"):
        for md in ("A","B","C"):
            same = fresh[per][md] == BLOB[key][per][md]
            check(f"{key}.{per}.{md}", same, f"({len(fresh[per][md])} строк)")

# ---------- 2. ПОЧАСОВОЙ БАЛАНС МОЩНОСТИ ----------
print("\n2. БАЛАНС МОЩНОСТИ:  нагрузка + заряд = КГУ + разряд + сеть")
for key in BLOB:
    for per in ("day","week"):
        for md in ("A","B","C"):
            r = BLOB[key][per][md]
            err = max(abs(x[0]+x[2]-x[1]-x[3]-x[5]) for x in r)
            check(f"{key}.{per}.{md}", err <= 1, f"макс. невязка {err} кВт")

# ---------- 3. ДИНАМИКА SoC ----------
print("\n3. ДИНАМИКА SoC:  soc[t] = soc[t-1] + заряд*ETA - разряд/ETA")
for key in BLOB:
    cap = 5500.0
    for per in ("day","week"):
        for md in ("B","C"):
            r = BLOB[key][per][md]
            e = 0
            for i in range(1,len(r)):
                prev = r[i-1][4]/100*cap
                pred = prev + r[i][2]*ETA - r[i][3]/ETA
                e = max(e, abs(pred - r[i][4]/100*cap))
            check(f"{key}.{per}.{md}", e < 30, f"макс. расхождение {e:.1f} кВт*ч (округление до 0.1% = 5.5)")

# ---------- 4. ЭКОНОМИЯ ----------
print("\n4. ЭКОНОМИЯ: сумма почасовых разниц против отчётных цифр")
REPORT = {("chp2","day","B"):236594,("chp2","day","C"):72741,
          ("chp2","week","B"):1323248,("chp2","week","C"):10938,
          ("chp3","day","B"):275973,("chp3","day","C"):8184,
          ("chp3","week","B"):1112540,("chp3","week","C"):83732}
for (key,per,md),ref in REPORT.items():
    A = BLOB[key][per]["A"]; X = BLOB[key][per][md]
    s = sum((a[1]*CHP+a[5]*GRID)-(x[1]*CHP+x[5]*GRID) for a,x in zip(A,X))
    check(f"{key}.{per}.{md}", abs(s-ref) < max(600, ref*0.002),
          f"артефакт {s:,.0f} ₸  отчёт {ref:,.0f} ₸  разница {s-ref:+,.0f}")

# ---------- 5. ДРЕЙФ SoC ----------
print("\n5. ДРЕЙФ SoC ЗА ПЕРИОД (энергия, взятая из запаса и не оплаченная в окне)")
SOC_KZT = CHP/ETA          # ₸ за 1 кВт*ч запаса в ячейках
for key in BLOB:
    for per in ("day","week"):
        for md in ("B","C"):
            r = BLOB[key][per][md]
            soc0 = r[0][4]/100*5500 - r[0][2]*ETA + r[0][3]/ETA   # состояние ДО первого часа
            d = soc0 - r[-1][4]/100*5500
            A = BLOB[key][per]["A"]
            s = sum((a[1]*CHP+a[5]*GRID)-(x[1]*CHP+x[5]*GRID) for a,x in zip(A,r))
            print(f"      {key}.{per}.{md}: ΔSoC {d:+8.0f} кВт*ч  поправка {-d*SOC_KZT:+11,.0f} ₸  "
                  f"экономия {s:,.0f} -> {s - d*SOC_KZT:,.0f} ₸  ({(-d*SOC_KZT)/s*100:+.1f}%)")

# ---------- 6. ВУАЛЬ ----------
print("\n6. ЗЕЛЁНАЯ ВУАЛЬ: что она сейчас рисует")
for key in ("chp2",):
    for per in ("day",):
        for md in ("B",):
            A=BLOB[key][per]["A"]; X=BLOB[key][per][md]
            for i in (0,7,19):
                a,x=A[i],X[i]
                bess = x[2] if x[2]>0 else -x[3]
                sv = (a[1]*CHP+a[5]*GRID)-(x[1]*CHP+x[5]*GRID)
                print(f"      час {i+1:>2}: вуаль от {min(bess,0):>6.0f} до {x[0]:>5.0f} = "
                      f"{x[0]-min(bess,0):>5.0f} кВт   а экономия {sv:>9,.0f} ₸ = {sv/SPREAD:>6.0f} кВт*экв")
mx = 0
for key in BLOB:
    for per in ("day","week"):
        for md in ("B","C"):
            A=BLOB[key][per]["A"]; X=BLOB[key][per][md]
            for a,x in zip(A,X):
                mx = max(mx, abs(((a[1]*CHP+a[5]*GRID)-(x[1]*CHP+x[5]*GRID))/SPREAD))
print(f"      максимум |экономия/38| по всем наборам: {mx:.0f} кВт-эквивалента")

print("\n" + ("ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ" if not fails else "ПРОВАЛЕНО:\n  " + "\n  ".join(fails)))
```

## D.3 Кросс-валидация полная

**`validate2.py`** — семь слоёв проверок

```python
import json, re, importlib.util, numpy as np, pandas as pd

src = open("/mnt/user-data/outputs/bess_profile.jsx").read()
B = json.loads(re.search(r"const DATA = (\{.*?\});\n", src, re.S).group(1))
CHP, GRID, START = 22.0, 60.0, 15000.0
fails = []
def check(n, c, d=""):
    if not c: fails.append(f"{n} {d}")
    print(f"  [{'OK ' if c else 'ОШИБКА'}] {n} {d}")

IDX = pd.date_range("2026-01-01", periods=8760, freq="h")
def remake(path, nm, tg):
    sp = importlib.util.spec_from_file_location(nm, path); m = importlib.util.module_from_spec(sp); sp.loader.exec_module(m)
    m.CHP_COST, m.GRID_COST, m.DEMAND_CHARGE = 22.0, 60.0, 0.0
    v = m.build_scenarios()[1]; L, M = v["load"], v["month"]
    R = {"A": m.run(L,M,"none"), "B": m.run(L,M,"bess"), "C": m.run(L,M,"peak", float(tg))}
    daily = L.reshape(365,24).sum(axis=1)
    cand = [d for d in range(365) if IDX[d*24].dayofweek<5 and IDX[d*24].day<=3]
    D0 = min(cand, key=lambda d: abs(daily[d]-np.median(daily)))*24
    WK = [i for i in range(0,8760-168,24) if IDX[i].dayofweek==0 and IDX[i].day==1][0]
    def pack(df,s,n):
        w = df.iloc[s:s+n]
        rows = [[round(float(r.load)),round(float(r.chp)),round(float(r.ch)),round(float(r.dis)),
                 round(float(r.soc)/m.BESS_CAP*100,1),round(float(r.grid)),int(r.starts)] for _,r in w.iterrows()]
        soc0 = round(float(df.soc.iloc[s-1]),1) if s>0 else m.BESS_CAP
        return {"rows":rows,"soc0":soc0}
    return ({"day":{k:pack(d,D0,24) for k,d in R.items()},
             "week":{k:pack(d,WK,168) for k,d in R.items()}}, m,
            {k:m.economics(d) for k,d in R.items()})

print("\n1. СВЕРКА С НЕЗАВИСИМЫМ ПРОГОНОМ МОДЕЛЕЙ")
ECO = {}
for key,(p,nm,tg) in {"chp2":("factory_year.py","w2",600),"chp3":("factory_year3.py","w3",700)}.items():
    fresh, mod, eco = remake(p,nm,tg); ECO[key]=eco
    check(f"{key}.cap/eta", abs(B[key]["cap"]-mod.BESS_CAP)<1e-6 and abs(B[key]["eta"]-mod.ETA)<1e-4,
          f"cap {B[key]['cap']} eta {B[key]['eta']:.5f}")
    for per in ("day","week"):
        for md in ("A","B","C"):
            a,b = fresh[per][md], B[key][per][md]
            same = a["rows"]==b["rows"] and abs(a["soc0"]-b["soc0"])<0.15
            check(f"{key}.{per}.{md}", same, f"{len(b['rows'])} строк, soc0 {b['soc0']}")

print("\n2. ПОЧАСОВОЙ БАЛАНС:  нагрузка + заряд = КГУ + разряд + сеть")
for key in ("chp2","chp3"):
    for per in ("day","week"):
        for md in ("A","B","C"):
            r=B[key][per][md]["rows"]
            e=max(abs(x[0]+x[2]-x[1]-x[3]-x[5]) for x in r)
            check(f"{key}.{per}.{md}", e<=1, f"макс. невязка {e} кВт")

print("\n3. ДИНАМИКА SoC:  soc[t] = soc[t-1] + заряд*ETA - разряд/ETA")
for key in ("chp2","chp3"):
    cap, eta = B[key]["cap"], B[key]["eta"]
    for per in ("day","week"):
        for md in ("B","C"):
            d=B[key][per][md]; r=d["rows"]; prev=d["soc0"]; e=0
            for x in r:
                pred = prev + x[2]*eta - x[3]/eta
                cur = x[4]/100*cap
                e = max(e, abs(pred-cur)); prev = cur
            check(f"{key}.{per}.{md}", e<6, f"макс. {e:.1f} кВт*ч (шаг округления SoC = {cap*0.0005:.1f})")

print("\n4. ЭКОНОМИЯ ПО ЭНЕРГИИ против отчётных таблиц")
REF={("chp2","day","B"):236594,("chp2","day","C"):72741,("chp2","week","B"):1323248,
     ("chp2","week","C"):10938,("chp3","day","B"):275973,("chp3","day","C"):8184,
     ("chp3","week","B"):1112540,("chp3","week","C"):83732}
for (key,per,md),ref in REF.items():
    A=B[key][per]["A"]["rows"]; X=B[key][per][md]["rows"]
    s=sum((a[1]*CHP+a[5]*GRID)-(x[1]*CHP+x[5]*GRID) for a,x in zip(A,X))
    check(f"{key}.{per}.{md}", abs(s-ref)<max(700,ref*0.003), f"{s:,.0f} ₸ vs отчёт {ref:,.0f} ₸ ({s-ref:+,.0f})")

print("\n5. СОСТАВЛЯЮЩИЕ 'ЭКОНОМИЯ ИТОГО' (как считает артефакт)")
for key in ("chp2","chp3"):
    cap,eta=B[key]["cap"],B[key]["eta"]
    for per in ("day","week"):
        for md in ("B","C"):
            A=B[key][per]["A"]; X=B[key][per][md]
            eB=sum(r[1]*CHP+r[5]*GRID for r in A["rows"]); eC=sum(r[1]*CHP+r[5]*GRID for r in X["rows"])
            sB=sum(r[6] for r in A["rows"]); sC=sum(r[6] for r in X["rows"])
            dC=X["rows"][-1][4]/100*cap-X["soc0"]; dB=A["rows"][-1][4]/100*cap-A["soc0"]
            dS=dC-dB
            lo=eB-eC+(sB-sC)*START+min(dS*eta*CHP,dS*eta*GRID)
            hi=eB-eC+(sB-sC)*START+max(dS*eta*CHP,dS*eta*GRID)
            print(f"      {key}.{per}.{md}: энергия {eB-eC:>12,.0f}  пуски {(sB-sC)*START:>+9,.0f}  "
                  f"ΔSoC {dS:>+7.0f} кВт*ч -> {min(dS*eta*CHP,dS*eta*GRID):>+9,.0f}…{max(dS*eta*CHP,dS*eta*GRID):>+9,.0f}"
                  f"   ИТОГО {lo:>12,.0f}…{hi:,.0f} ₸")

print("\n6. МАСШТАБИРУЕМОСТЬ НА ГОД (суточное окно × 365 против годового прогона)")
for key in ("chp2","chp3"):
    a=ECO[key]["A"]["cost"]
    for md in ("B","C"):
        yr=a-ECO[key][md]["cost"]
        A=B[key]["day"]["A"]["rows"]; X=B[key]["day"][md]["rows"]
        day=sum((p[1]*CHP+p[5]*GRID)-(q[1]*CHP+q[5]*GRID) for p,q in zip(A,X))
        wk=sum((p[1]*CHP+p[5]*GRID)-(q[1]*CHP+q[5]*GRID)
               for p,q in zip(B[key]["week"]["A"]["rows"],B[key]["week"][md]["rows"]))
        print(f"      {key}.{md}: сутки×365 {day*365:>14,.0f}   неделя×52.14 {wk*52.142857:>14,.0f}   "
              f"годовой прогон {yr:>14,.0f} ₸   ошибка суток {(day*365/yr-1)*100:>+6.1f}%")

print("\n7. ЧТО РИСУЕТ ВУАЛЬ band = [min(bess,0), load]")
key,per,md="chp2","day","B"
A=B[key][per]["A"]["rows"]; X=B[key][per][md]["rows"]
for i in (0,5,7,19):
    a,x=A[i],X[i]; bess=x[2] if x[2]>0 else -x[3]
    sv=(a[1]*CHP+a[5]*GRID)-(x[1]*CHP+x[5]*GRID)
    print(f"      час {i+1:>2}: вуаль {x[0]-min(bess,0):>5.0f} кВт высотой   экономия часа {sv:>9,.0f} ₸"
          f"   отношение {(x[0]-min(bess,0))/max(sv,1):>7.3f}")
corr=[]
for key in ("chp2","chp3"):
    for per in ("day","week"):
        for md in ("B","C"):
            A=B[key][per]["A"]["rows"]; X=B[key][per][md]["rows"]
            h=[x[0]-min(x[2] if x[2]>0 else -x[3],0) for x in X]
            s=[(a[1]*CHP+a[5]*GRID)-(x[1]*CHP+x[5]*GRID) for a,x in zip(A,X)]
            corr.append(np.corrcoef(h,s)[0,1] if np.std(s)>0 else float("nan"))
print(f"      корреляция высоты вуали с экономией часа по 8 наборам: "
      f"{np.nanmin(corr):.2f} … {np.nanmax(corr):.2f}")

print("\n" + ("ВСЕ ЧИСЛОВЫЕ ПРОВЕРКИ ПРОЙДЕНЫ" if not fails else "ПРОВАЛЕНО:\n  "+"\n  ".join(fails)))
```

## D.4 Проверка данных отчёта

**`verify_report.py`** — поагрегатная выработка, правило 90%, годовая сводка

```python
import json,re,importlib.util,numpy as np,pandas as pd
s=open("bess_work.jsx").read()
B=json.loads(re.search(r"const DATA = (\{.*?\});\n",s,re.S).group(1))
S=json.loads(re.search(r"const SUMMARY = (\{.*?\});\n",s,re.S).group(1))
CHP,GRID,ST=22.,60.,15000.
bad=[]
def ck(n,c,d=""):
    if not c: bad.append(n+" "+d)
    print(f"  [{'OK ' if c else 'ОШИБКА'}] {n} {d}")

print("1. ПОАГРЕГАТНАЯ ВЫРАБОТКА: сумма gen == chp в строке")
for k in ("chp2","chp3"):
    for p in ("day","week"):
        for m in ("A","B","C"):
            d=B[k][p][m]; e=max(abs(sum(g)-r[1]) for g,r in zip(d["gen"],d["rows"]))
            ck(f"{k}.{p}.{m}", e<=1, f"макс. расхождение {e} кВт, агрегатов {len(d['gen'][0])}")

print("\n2. ПРАВИЛО 90%: работающий агрегат не ниже 0.9 номинала")
NOM={"chp2":[1067,1200],"chp3":[1067,1200,1100]}
for k in ("chp2","chp3"):
    worst=1e9; who=None
    for p in ("day","week"):
        for m in ("A","B","C"):
            for g in B[k][p][m]["gen"]:
                for j,v in enumerate(g):
                    if v>1e-6 and v/NOM[k][j]<worst: worst, who = v/NOM[k][j], (p,m,j,v)
    ck(f"{k}", worst>=0.899, f"минимальная загрузка {worst*100:.1f}% ({B[k]['names'][who[2]]}, {who[3]:.0f} кВт)")

print("\n3. ГОДОВАЯ СВОДКА против прогона моделей")
IDX=pd.date_range("2026-01-01",periods=8760,freq="h")
for k,(path,nm,tg) in {"chp2":("factory_year.py","z2",600),"chp3":("factory_year3.py","z3",700)}.items():
    sp=importlib.util.spec_from_file_location(nm,path); mo=importlib.util.module_from_spec(sp); sp.loader.exec_module(mo)
    mo.CHP_COST,mo.GRID_COST,mo.DEMAND_CHARGE=22.,60.,0.
    v=mo.build_scenarios()[1]; L,M=v["load"],v["month"]
    R={"A":mo.run(L,M,"none"),"B":mo.run(L,M,"bess"),"C":mo.run(L,M,"peak",float(tg))}
    E={x:mo.economics(d) for x,d in R.items()}
    ck(f"{k}.нагрузка за год", abs(S[k]["load"]-L.sum()/1000)<1, f"{S[k]['load']} против {L.sum()/1000:.0f} МВт*ч")
    for m in ("A","B","C"):
        e=E[m]; g=S[k][m]
        ok=(abs(g["chp"]-e["chp_kwh"]/1000)<1 and abs(g["grid"]-e["grid_kwh"]/1000)<1
            and abs(g["cost"]-e["cost"])<1 and abs(g["peak"]-e["peak_year"])<1
            and abs(g["peaksum"]-e["peak_sum"])<1 and abs(g["starts"]-e["starts"])<1)
        ck(f"{k}.{m} год", ok, f"КГУ {g['chp']} / сеть {g['grid']} / затраты {g['cost']:,} / пик {g['peak']} / пусков {g['starts']}")
        sv=E["A"]["cost"]-e["cost"]
        ck(f"{k}.{m} экономия+окуп.", abs(g["save"]-sv)<1 and (g["pay"] is None or abs(g["pay"]-408e6/sv)<0.05),
           f"{g['save']:,} ₸/год, окуп. {g['pay']}")

print("\n4. ЗАТРАТЫ ОКНА: метод money() = энергия + пуски")
for k in ("chp2","chp3"):
    for p in ("day","week"):
        c={m:sum(r[1]*CHP+r[5]*GRID+r[6]*ST for r in B[k][p][m]["rows"]) for m in "ABC"}
        print(f"      {k}.{p}: A {c['A']:>13,.0f}   B {c['B']:>13,.0f} (эк. {c['A']-c['B']:>+11,.0f})"
              f"   C {c['C']:>13,.0f} (эк. {c['A']-c['C']:>+10,.0f}) ₸")

print("\n5. ПИКИ И ПУСКИ В ТАБЛИЦАХ")
for k in ("chp2","chp3"):
    for p in ("day","week"):
        for m in "ABC":
            r=B[k][p][m]["rows"]
            print(f"      {k}.{p}.{m}: пик сети {max(x[5] for x in r):>6,.0f} кВт, пусков {sum(x[6] for x in r):>3}")

print("\n" + ("ВСЁ СОШЛОСЬ" if not bad else "ПРОВАЛЕНО:\n  "+"\n  ".join(bad)))
```

## D.5 Проверка тождества площади вуали

**`final_check.py`**

```python
import json,re,numpy as np
s=open("/mnt/user-data/outputs/bess_profile.jsx").read()
B=json.loads(re.search(r"const DATA = (\{.*?\});\n",s,re.S).group(1))
CHP,GRID,ST=22.,60.,15000.; SPREAD=38.
print("ПРОВЕРКА ТОЖДЕСТВА 'ПЛОЩАДЬ ВУАЛИ x 38 = ДЕНЬГИ'")
for k in ("chp2","chp3"):
  for p_ in ("day","week"):
    for m in ("B","C"):
      A=B[k][p_]["A"]["rows"]; X=B[k][p_][m]["rows"]
      sE=[(a[1]*CHP+a[5]*GRID)-(x[1]*CHP+x[5]*GRID) for a,x in zip(A,X)]
      area=sum(v/SPREAD for v in sE)          # кВт*ч, знаковая площадь вуали
      print(f"  {k}.{p_}.{m}: площадь {area:>9,.0f} кВт*ч x 38 = {area*SPREAD:>12,.0f} ₸   "
            f"сумма почасовой экономии {sum(sE):>12,.0f} ₸   расхождение {area*SPREAD-sum(sE):.4f}")
print("\nПРОВЕРКА ЛЕГЕНД И ПОДПИСЕЙ")
for t,need in [("экономия · площадь × 38 ₸/кВт·ч",True),("перерасход · вуаль выше линии",True),
               ("зона экономии",False),("рубли",False),("нагрузка завода",True),
               ("заряд BESS",True),("разряд BESS",True),("Экономия итого",True),
               ("поправка на SoC",True),("Окно нельзя умножать",True)]:
    got = t in s
    print(f"  [{'OK ' if got==need else 'ОШИБКА'}] {'есть' if need else 'убрано'}: «{t}»")
```

## D.6 Рендер-тест интерфейса

**`render_test2.js`** — 36 комбинаций состояний через react-dom/server

```javascript
const fs=require("fs"), babel=require("@babel/core"), React=require("react");
const { renderToStaticMarkup } = require("react-dom/server");
const base = fs.readFileSync("bess_work.jsx","utf8");

function compile(mode, period, rmd, full) {
  let s = base
    .replace(/^import React[^\n]*\n/m, "const React=require('react');const {useState,useMemo}=React;\n")
    .replace(/^import \{[\s\S]*?\} from "recharts";\n/m, "const {ComposedChart,Bar,Line,Area,XAxis,YAxis,CartesianGrid,Tooltip,ReferenceLine,ResponsiveContainer,Cell}=require('recharts');\n")
    .replace(/export default function/, "module.exports = function")
    .replace('useState("B");\n  const [period, setPeriod] = useState("day")',
             `useState("${mode}");\n  const [period, setPeriod] = useState("${period}")`)
    .replace('const [md, setMd] = useState("B");\n  const [full, setFull] = useState(false);',
             `const [md, setMd] = useState("${rmd}");\n  const [full, setFull] = useState(${full});`);
  const out = babel.transformSync(s,{presets:[["@babel/preset-react",{runtime:"classic"}]]}).code;
  const f = "c_"+mode+period+rmd+full+".js"; fs.writeFileSync(f,out);
  delete require.cache[require.resolve("./"+f)];
  return require("./"+f);
}
let bad=0;
for (const mode of ["A","B","C"]) for (const period of ["day","week"])
  for (const rmd of ["A","B","C"]) for (const full of [false,true]) {
    try {
      const html = renderToStaticMarkup(React.createElement(compile(mode,period,rmd,full)));
      const tr=(html.match(/<tr/g)||[]).length;
      if (mode==="B"&&period==="day") console.log(`  [OK ] панели ${mode}/${period} · отчёт ${rmd}/${full?"168ч":"свёрнут"}: строк ${tr}, ${html.length} симв.`);
    } catch(e){ bad++; console.log(`  [ОШИБКА] ${mode}/${period}/${rmd}/${full}: ${e.message}`); }
  }
console.log(bad? `\nПРОВАЛЕНО: ${bad}` : "\nВсе 36 комбинаций состояний отрендерились без ошибок");
process.exit(bad?1:0);
```

---

# Приложение E. Интерактивный артефакт

Полный файл с данными: `bess_profile.jsx` (78 КБ, из них ~30 КБ машинный блоб
`DATA` и `SUMMARY`). Ниже код компонента; константы данных вырезаны, их схема
описана в разделе 11.5.


**`bess_work.jsx`** — React + recharts

```jsx
import React, { useState, useMemo } from "react";
import {
  ComposedChart, Bar, Line, Area, XAxis, YAxis, CartesianGrid,
  Tooltip, ReferenceLine, ResponsiveContainer, Cell,
} from "recharts";

const DATA = { /* см. раздел 12.3: схема данных; полный блоб в bess_profile.jsx */ };

const C = {
  paper: "#EEF1F4",
  panel: "#FFFFFF",
  ink: "#17242F",
  muted: "#5C6B79",
  rule: "#CBD5DC",
  load: "#17242F",
  charge: "#1F7A8C",
  discharge: "#C2571A",
  ceiling: "#8A97A3",
  save: "#3F8F5C",
};

const MONO = "ui-monospace, SFMono-Regular, Menlo, Consolas, monospace";
const SANS = "Inter, 'Helvetica Neue', Arial, sans-serif";
const DN = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"];
const KZT_CHP = 22;
const KZT_GRID = 60;

const nf = (v) => Math.round(v).toLocaleString("ru");

// Строки в данных лежат плоскими массивами ради компактности:
// [нагрузка, выработка КГУ, заряд, разряд, SoC, закуп из сети]
const KZT_START = 15000;
const SPREAD = KZT_GRID - KZT_CHP;   // 38 ₸/кВт*ч, разница между сетью и собственной выработкой

function expand(rows, baseRows, isWeek) {
  return rows.map((r, i) => {
    const [load, chp, ch, dis, soc, grid, starts] = r;
    const b = baseRows[i];
    // Экономию раскладываем на две части. Энергетическая - гладкая, её и рисуем
    // вуалью. Пусковая - разовые скачки по 15 000 ₸, вуаль от них стала бы
    // рваной, поэтому она идёт только в подсказку и в итог.
    const saveE = (b[1] * KZT_CHP + b[5] * KZT_GRID) - (chp * KZT_CHP + grid * KZT_GRID);
    const saveS = (b[6] - starts) * KZT_START;
    const bess = ch > 0 ? ch : -dis;
    // Высота вуали = экономия / 38 ₸. Тогда её ПЛОЩАДЬ в кВт*ч, умноженная
    // на 38 ₸/кВт*ч, в точности равна деньгам. Прежний вариант рисовал полосу
    // от столбца BESS до линии нагрузки: красиво, но с экономией не связано
    // (корреляция по восьми наборам выходила от -0.07 до 0.72).
    const h = saveE / SPREAD;
    return {
      i, load, chp, ch, dis, soc, grid, bess, starts,
      bandPos: h >= 0 ? [load - h, load] : null,
      bandNeg: h < 0 ? [load, load - h] : null,
      saveE, saveS,
      save: saveE + saveS,
      label: isWeek
        ? `${DN[Math.floor(i / 24)]} ${String(i % 24).padStart(2, "0")}:00`
        : `${String(i + 1).padStart(2, "0")}:00`,
    };
  });
}

function Tip({ active, payload, showSave }) {
  if (!active || !payload || !payload.length) return null;
  const d = payload[0].payload;
  const rows = [
    ["Нагрузка завода", `${nf(d.load)} кВт`, C.ink],
    ["Выработка КГУ", `${nf(d.chp)} кВт`, C.muted],
    d.ch > 0 ? ["Заряд BESS", `+${nf(d.ch)} кВт`, C.charge] : null,
    d.dis > 0 ? ["Разряд BESS", `−${nf(d.dis)} кВт`, C.discharge] : null,
    ["Закуп из сети", `${nf(d.grid)} кВт`, C.muted],
    ["SoC", `${d.soc}%`, C.muted],
    d.starts > 0 ? ["Пусков КГУ", `${d.starts} × 15 000 ₸`, C.discharge] : null,
    showSave ? ["Экономия, энергия", `${d.saveE >= 0 ? "" : "−"}${nf(Math.abs(d.saveE))} ₸`,
                d.saveE >= 0 ? C.save : C.discharge] : null,
    showSave && d.saveS !== 0
      ? ["Экономия, пуски", `${d.saveS >= 0 ? "" : "−"}${nf(Math.abs(d.saveS))} ₸`,
         d.saveS >= 0 ? C.save : C.discharge] : null,
  ].filter(Boolean);
  return (
    <div style={{
      background: C.panel, border: `1px solid ${C.rule}`, padding: "10px 12px",
      fontFamily: MONO, fontSize: 12, color: C.ink, minWidth: 210,
    }}>
      <div style={{ fontWeight: 600, marginBottom: 6, letterSpacing: "0.04em" }}>
        {d.label}
      </div>
      {rows.map(([k, v, col]) => (
        <div key={k} style={{ display: "flex", justifyContent: "space-between", gap: 16, lineHeight: 1.7 }}>
          <span style={{ color: C.muted }}>{k}</span>
          <span style={{ color: col, fontVariantNumeric: "tabular-nums" }}>{v}</span>
        </div>
      ))}
    </div>
  );
}

function Panel({ cfg, mode, period }) {
  const isWeek = period === "week";
  const cur = cfg[period][mode];
  const bas = cfg[period].A;
  const rows = useMemo(() => expand(cur.rows, bas.rows, isWeek), [cur, bas, isWeek]);
  const showSave = mode !== "A";

  const charged = rows.reduce((a, r) => a + r.ch, 0);
  const dischg = rows.reduce((a, r) => a + r.dis, 0);
  const grid = rows.reduce((a, r) => a + r.grid, 0);
  const peak = Math.max(...rows.map((r) => r.grid));
  const idle = rows.filter((r) => r.ch === 0 && r.dis === 0).length;

  // --- Разложение экономии -------------------------------------------------
  // 1. Энергия: разница затрат на топливо КГУ и закуп из сети.
  // 2. Пуски: батарея меняет число пусков КГУ, а пуск стоит денег.
  // 3. Поправка на SoC: период не замкнут по энергии. Если накопитель на конце
  //    полнее, чем в начале, эта энергия ещё не отработала - её нельзя
  //    записывать в экономию периода без оговорки. Диапазон: нижняя граница
  //    если запас потом заместит собственную выработку (22 ₸), верхняя -
  //    если заместит сеть (60 ₸).
  const eCur = rows.reduce((a, r) => a + r.chp * KZT_CHP + r.grid * KZT_GRID, 0);
  const eBas = bas.rows.reduce((a, r) => a + r[1] * KZT_CHP + r[5] * KZT_GRID, 0);
  const sCur = rows.reduce((a, r) => a + r.starts, 0);
  const sBas = bas.rows.reduce((a, r) => a + r[6], 0);
  const dCur = (rows[rows.length - 1].soc / 100) * cfg.cap - cur.soc0;
  const dBas = (bas.rows[bas.rows.length - 1][4] / 100) * cfg.cap - bas.soc0;
  const dSoc = dCur - dBas;
  const saveEnergy = eBas - eCur;
  const saveStarts = (sBas - sCur) * KZT_START;
  const socLo = dSoc * cfg.eta * KZT_CHP;
  const socHi = dSoc * cfg.eta * KZT_GRID;
  const totLo = saveEnergy + saveStarts + Math.min(socLo, socHi);
  const totHi = saveEnergy + saveStarts + Math.max(socLo, socHi);

  // Вуаль уходит и ниже линии нагрузки, и выше неё, поэтому границы оси
  // считаем по фактическим краям всех нарисованных элементов.
  const loadMax = Math.max(...rows.map((r) => Math.max(r.load, r.load - r.saveE / SPREAD)));
  const disMax = Math.max(...rows.map((r) => r.dis), 0);
  const veilLow = Math.min(...rows.map((r) => r.load - r.saveE / SPREAD), 0);
  const yTop = Math.ceil((loadMax * 1.06) / 500) * 500;
  const yBot = Math.min(-Math.max(500, Math.ceil((disMax * 1.25) / 500) * 500),
                        Math.floor((showSave ? veilLow : 0) / 500) * 500);
  const ticks = [];
  for (let v = yBot; v <= yTop; v += 500) ticks.push(v);

  const xTicks = isWeek
    ? [0, 24, 48, 72, 96, 120, 144]
    : rows.filter((r) => r.i % 2 === 0).map((r) => r.i);

  const stats = [
    ["Заряд", `${nf(charged)} кВт·ч`],
    ["Разряд", `${nf(dischg)} кВт·ч`],
    ["Закуп из сети", `${nf(grid)} кВт·ч`],
    ["Пик закупа", `${nf(peak)} кВт`],
    ["Простой BESS", `${idle} из ${rows.length} ч`],
    ["Экономия итого", showSave ? `${nf(totLo)}…${nf(totHi)} ₸` : "—"],
  ];

  return (
    <section style={{ background: C.panel, border: `1px solid ${C.rule}`, marginBottom: 28 }}>
      <header style={{
        padding: "16px 20px 12px", borderBottom: `1px solid ${C.rule}`,
        display: "flex", flexWrap: "wrap", alignItems: "baseline", gap: 12,
      }}>
        <h2 style={{
          margin: 0, fontFamily: SANS, fontSize: 19, fontWeight: 650,
          letterSpacing: "-0.015em", color: C.ink,
        }}>
          {cfg.label}
        </h2>
        <span style={{ fontFamily: MONO, fontSize: 11.5, color: C.muted, letterSpacing: "0.02em" }}>
          {cfg.units} = {nf(cfg.pchp)} кВт
        </span>
      </header>

      <div style={{ padding: "18px 8px 4px 0" }}>
        <ResponsiveContainer width="100%" height={isWeek ? 420 : 380}>
          <ComposedChart data={rows} margin={{ top: 8, right: 18, left: 8, bottom: 26 }}>
            <CartesianGrid stroke={C.rule} strokeDasharray="2 4" vertical={false} />
            <XAxis
              dataKey="i" tick={{ fill: C.muted, fontSize: 11, fontFamily: MONO }}
              tickLine={false} axisLine={{ stroke: C.rule }}
              ticks={xTicks} interval={0}
              tickFormatter={(v) => (isWeek ? DN[Math.floor(v / 24)] : String(v + 1))}
              label={{ value: isWeek ? "дни недели" : "час суток",
                       position: "insideBottom", offset: -14,
                       fill: C.muted, fontSize: 11.5, fontFamily: MONO }}
            />
            <YAxis
              domain={[yBot, yTop]} ticks={ticks}
              tick={{ fill: C.muted, fontSize: 11, fontFamily: MONO }}
              tickLine={false} axisLine={false} width={54}
              tickFormatter={(v) => v.toLocaleString("ru")}
              label={{ value: "кВт", angle: -90, position: "insideLeft", offset: 10,
                       fill: C.muted, fontSize: 11.5, fontFamily: MONO }}
            />
            <Tooltip content={<Tip showSave={showSave} />} cursor={{ fill: "rgba(23,36,47,0.05)" }} />

            {showSave && (
              <Area dataKey="bandPos" isAnimationActive={false} stroke="none"
                    fill={C.save} fillOpacity={0.18} activeDot={false} connectNulls={false} />
            )}
            {showSave && (
              <Area dataKey="bandNeg" isAnimationActive={false} stroke="none"
                    fill={C.discharge} fillOpacity={0.16} activeDot={false} connectNulls={false} />
            )}

            <ReferenceLine y={0} stroke={C.ink} strokeWidth={1} />
            <ReferenceLine
              y={cfg.pchp} stroke={C.ceiling} strokeDasharray="6 4" strokeWidth={1.25}
              label={{ value: `потолок КГУ ${nf(cfg.pchp)} кВт`,
                       position: "insideTopRight", fill: C.ceiling,
                       fontSize: 11, fontFamily: MONO, dy: -6 }}
            />
            {isWeek && [24, 48, 72, 96, 120, 144].map((d) => (
              <ReferenceLine key={d} x={d} stroke={C.rule} strokeWidth={1} />
            ))}

            <Bar dataKey="bess" barSize={isWeek ? 3 : 17} isAnimationActive={false}>
              {rows.map((r, i) => (
                <Cell key={i} fill={r.bess >= 0 ? C.charge : C.discharge} />
              ))}
            </Bar>

            <Line
              type="linear" dataKey="load" stroke={C.load}
              strokeWidth={isWeek ? 1.1 : 1.6} isAnimationActive={false}
              dot={isWeek ? { r: 1.3, fill: C.load, stroke: "none" }
                          : { r: 3.2, fill: C.panel, stroke: C.load, strokeWidth: 1.6 }}
              activeDot={{ r: 5, fill: C.load, stroke: C.panel, strokeWidth: 2 }}
            />
          </ComposedChart>
        </ResponsiveContainer>
      </div>

      <div style={{ display: "flex", flexWrap: "wrap", borderTop: `1px solid ${C.rule}` }}>
        {stats.map(([k, v], i) => (
          <div key={k} style={{
            flex: "1 1 130px", padding: "12px 16px",
            borderLeft: i === 0 ? "none" : `1px solid ${C.rule}`,
          }}>
            <div style={{ fontFamily: MONO, fontSize: 10.5, color: C.muted,
                          textTransform: "uppercase", letterSpacing: "0.07em" }}>
              {k}
            </div>
            <div style={{ fontFamily: MONO, fontSize: k === "Экономия итого" ? 13 : 15,
                          color: k === "Экономия итого" && v !== "—"
                            ? (totHi > 0 ? C.save : C.discharge) : C.ink,
                          marginTop: 4, fontVariantNumeric: "tabular-nums" }}>
              {v}
            </div>
          </div>
        ))}
      </div>

      {showSave && (
        <div style={{ borderTop: `1px solid ${C.rule}`, padding: "10px 16px",
                      fontFamily: MONO, fontSize: 11.5, color: C.muted,
                      display: "flex", flexWrap: "wrap", gap: 18 }}>
          <span>энергия <b style={{ color: saveEnergy >= 0 ? C.save : C.discharge,
                                    fontWeight: 500 }}>
            {saveEnergy >= 0 ? "+" : "−"}{nf(Math.abs(saveEnergy))} ₸</b></span>
          <span>пуски {sCur} против {sBas} <b style={{
            color: saveStarts >= 0 ? C.save : C.discharge, fontWeight: 500 }}>
            {saveStarts >= 0 ? "+" : "−"}{nf(Math.abs(saveStarts))} ₸</b></span>
          <span>поправка на SoC {dSoc >= 0 ? "+" : "−"}{nf(Math.abs(dSoc))} кВт·ч <b style={{
            color: dSoc >= 0 ? C.save : C.discharge, fontWeight: 500 }}>
            {nf(Math.min(socLo, socHi))}…{nf(Math.max(socLo, socHi))} ₸</b></span>
        </div>
      )}
    </section>
  );
}

function Switch({ items, value, onChange }) {
  return (
    <div style={{ display: "flex", border: `1px solid ${C.rule}`, width: "fit-content" }}>
      {items.map(([k, label], i) => (
        <button
          key={k} onClick={() => onChange(k)}
          style={{
            fontFamily: MONO, fontSize: 12, letterSpacing: "0.04em",
            padding: "9px 18px", cursor: "pointer",
            border: "none", borderLeft: i === 0 ? "none" : `1px solid ${C.rule}`,
            background: value === k ? C.ink : C.panel,
            color: value === k ? C.paper : C.muted,
          }}
        >
          {label}
        </button>
      ))}
    </div>
  );
}


/* ══════════════════════════════════════════════════════════════════════════
   ОТЧЁТЫ. Таблицы строятся из того же массива DATA, что и графики выше,
   поэтому разойтись с ними физически не могут. Отдельно подставляются
   только годовые итоги: они берутся из полного прогона на 8760 часов,
   который в браузер не выгружается.
   ══════════════════════════════════════════════════════════════════════════ */

const SUMMARY = { /* см. раздел 12.3: схема данных; полный блоб в bess_profile.jsx */ };

const MODE_NAME = { A: "Без батареи", B: "Арбитраж", C: "Срез пика" };

const th = {
  fontFamily: MONO, fontSize: 10.5, color: C.muted, fontWeight: 400,
  textTransform: "uppercase", letterSpacing: "0.06em",
  padding: "7px 9px", textAlign: "right", whiteSpace: "nowrap",
  borderBottom: `1px solid ${C.rule}`,
};
const td = {
  fontFamily: MONO, fontSize: 12, color: C.ink, padding: "5px 9px",
  textAlign: "right", fontVariantNumeric: "tabular-nums", whiteSpace: "nowrap",
};

function Table({ head, rows, foot }) {
  return (
    <div style={{ overflowX: "auto" }}>
      <table style={{ borderCollapse: "collapse", width: "100%", minWidth: 560 }}>
        <thead>
          <tr>{head.map((h, i) => (
            <th key={i} style={{ ...th, textAlign: i === 0 ? "left" : "right" }}>{h}</th>
          ))}</tr>
        </thead>
        <tbody>
          {rows.map((r, i) => (
            <tr key={i} style={{ background: i % 2 ? "rgba(23,36,47,0.022)" : "transparent" }}>
              {r.map((c, j) => (
                <td key={j} style={{ ...td, textAlign: j === 0 ? "left" : "right",
                                     color: j === 0 ? C.muted : C.ink }}>{c}</td>
              ))}
            </tr>
          ))}
          {foot && (
            <tr>{foot.map((c, j) => (
              <td key={j} style={{ ...td, textAlign: j === 0 ? "left" : "right",
                                   borderTop: `1px solid ${C.ink}`, fontWeight: 600 }}>{c}</td>
            ))}</tr>
          )}
        </tbody>
      </table>
    </div>
  );
}

function Sub({ children }) {
  return (
    <h3 style={{ margin: "26px 0 10px", fontFamily: SANS, fontSize: 14.5, fontWeight: 620,
                 letterSpacing: "-0.01em", color: C.ink }}>{children}</h3>
  );
}

// Деньги считаем ровно так же, как в панелях выше: энергия плюс пуски.
// В прежних текстовых отчётах суточные и недельные затраты пуски не включали,
// из-за чего они расходились с годовыми. Здесь метод один на все три горизонта.
function money(rows) {
  return rows.reduce((a, r) => a + r[1] * KZT_CHP + r[5] * KZT_GRID + r[6] * KZT_START, 0);
}
function totals(rows) {
  const s = (k) => rows.reduce((a, r) => a + r[k], 0);
  return { load: s(0), chp: s(1), ch: s(2), dis: s(3), grid: s(5), starts: s(6),
           peak: Math.max(...rows.map((r) => r[5])), cost: money(rows) };
}

function dayTable(cfg, md) {
  const r = cfg.day[md].rows;
  const g = cfg.day[md].gen;              // выработка каждого агрегата по часам
  const head = ["Час", "Нагрузка", ...cfg.names, "Заряд +", "Разряд −", "Сеть", "SoC,%"];
  const rows = r.map((x, i) => [String(i + 1), nf(x[0]), ...g[i].map((v) => nf(v)),
    nf(x[2]), x[3] ? "−" + nf(x[3]) : "0", nf(x[5]), x[4].toFixed(1)]);
  const t = totals(r);
  const gs = cfg.names.map((_, j) => g.reduce((a, row) => a + row[j], 0));
  const foot = ["Итог", nf(t.load), ...gs.map((v) => nf(v)),
                nf(t.ch), "−" + nf(t.dis), nf(t.grid), "пик " + nf(t.peak)];
  return <Table head={head} rows={rows} foot={foot} />;
}

function weekTable(cfg, md) {
  const r = cfg.week[md].rows;
  const g = cfg.week[md].gen;             // выработка каждого агрегата по часам
  const head = ["День", "Нагрузка", ...cfg.names, "Заряд +", "Разряд −", "Сеть",
                "Пик сети", "Пусков", "SoC,%"];
  const rows = DN.map((d, k) => {
    const seg = r.slice(k * 24, k * 24 + 24);
    const gseg = g.slice(k * 24, k * 24 + 24);
    const t = totals(seg);
    const gs = cfg.names.map((_, j) => gseg.reduce((a, row) => a + row[j], 0));
    return [d, nf(t.load), ...gs.map((v) => nf(v)), nf(t.ch),
            t.dis ? "−" + nf(t.dis) : "0", nf(t.grid),
            nf(t.peak), String(t.starts), seg[23][4].toFixed(1)];
  });
  const t = totals(r);
  const gtot = cfg.names.map((_, j) => g.reduce((a, row) => a + row[j], 0));
  const foot = ["Итог", nf(t.load), ...gtot.map((v) => nf(v)), nf(t.ch), "−" + nf(t.dis),
                nf(t.grid), nf(t.peak), String(t.starts), ""];
  return <Table head={head} rows={rows} foot={foot} />;
}

function hourWeekTable(cfg, md) {
  const r = cfg.week[md].rows;
  const g = cfg.week[md].gen;
  const head = ["День", "Час", "Нагрузка", ...cfg.names, "Заряд +", "Разряд −", "Сеть", "SoC,%"];
  const rows = r.map((x, i) => [DN[Math.floor(i / 24)], String((i % 24) + 1), nf(x[0]),
    ...g[i].map((v) => nf(v)), nf(x[2]), x[3] ? "−" + nf(x[3]) : "0",
    nf(x[5]), x[4].toFixed(1)]);
  return <Table head={head} rows={rows} />;
}

function summaryTable(cfg, sum) {
  const cols = ["A", "B", "C"];
  const dT = cols.map((m) => totals(cfg.day[m].rows));
  const wT = cols.map((m) => totals(cfg.week[m].rows));
  const dS = (i) => (i === 0 ? "—" : nf(dT[0].cost - dT[i].cost) + " ₸");
  const wS = (i) => (i === 0 ? "—" : nf(wT[0].cost - wT[i].cost) + " ₸");
  const head = ["Показатель", "A. Без батареи", "B. Арбитраж", "C. Срез пика"];
  const rows = [
    ["ТИПИЧНЫЕ СУТКИ", "", "", ""],
    ["Нагрузка завода, кВт·ч", ...dT.map((t) => nf(t.load))],
    ["Выработка КГУ, кВт·ч", ...dT.map((t) => nf(t.chp))],
    ["Заряд BESS, кВт·ч", ...dT.map((t) => nf(t.ch))],
    ["Разряд BESS, кВт·ч", ...dT.map((t) => nf(t.dis))],
    ["Закуп из сети, кВт·ч", ...dT.map((t) => nf(t.grid))],
    ["Пик закупа, кВт", ...dT.map((t) => nf(t.peak))],
    ["Пусков КГУ", ...dT.map((t) => String(t.starts))],
    ["Затраты, ₸", ...dT.map((t) => nf(t.cost))],
    ["Экономия к A, ₸", ...cols.map((_, i) => dS(i))],
    ["НЕДЕЛЯ", "", "", ""],
    ["Нагрузка завода, кВт·ч", ...wT.map((t) => nf(t.load))],
    ["Выработка КГУ, кВт·ч", ...wT.map((t) => nf(t.chp))],
    ["Заряд BESS, кВт·ч", ...wT.map((t) => nf(t.ch))],
    ["Разряд BESS, кВт·ч", ...wT.map((t) => nf(t.dis))],
    ["Закуп из сети, кВт·ч", ...wT.map((t) => nf(t.grid))],
    ["Пик закупа, кВт", ...wT.map((t) => nf(t.peak))],
    ["Пусков КГУ", ...wT.map((t) => String(t.starts))],
    ["Затраты, ₸", ...wT.map((t) => nf(t.cost))],
    ["Экономия к A, ₸", ...cols.map((_, i) => wS(i))],
    ["ГОД (полный прогон 8760 ч)", "", "", ""],
    ["Выработка КГУ, МВт·ч", ...cols.map((m) => nf(sum[m].chp))],
    ["Закуп из сети, МВт·ч", ...cols.map((m) => nf(sum[m].grid))],
    ["Доля сети в питании, %", ...cols.map((m) => sum[m].share.toFixed(1))],
    ["Годовой максимум закупа, кВт", ...cols.map((m) => nf(sum[m].peak))],
    ["Сумма 12 месячных максимумов, кВт", ...cols.map((m) => nf(sum[m].peaksum))],
    ["Пусков КГУ за год", ...cols.map((m) => nf(sum[m].starts))],
    ["Затраты, ₸/год", ...cols.map((m) => nf(sum[m].cost))],
    ["Экономия к A, ₸/год", ...cols.map((m) => (sum[m].save ? nf(sum[m].save) : "—"))],
    ["Экономия, %", ...cols.map((m) => (sum[m].save ? sum[m].pct.toFixed(1) + "%" : "—"))],
    ["Простая окупаемость, лет", ...cols.map((m) => (sum[m].pay ? sum[m].pay.toFixed(1) : "—"))],
  ];
  return <Table head={head} rows={rows} />;
}

function ReportBlock({ cfg, sum }) {
  const [md, setMd] = useState("B");
  const [full, setFull] = useState(false);
  return (
    <section style={{ background: C.panel, border: `1px solid ${C.rule}`, marginBottom: 28 }}>
      <header style={{ padding: "16px 20px 12px", borderBottom: `1px solid ${C.rule}` }}>
        <h2 style={{ margin: 0, fontFamily: SANS, fontSize: 19, fontWeight: 650,
                     letterSpacing: "-0.015em", color: C.ink }}>
          Отчёт · {cfg.label}
        </h2>
        <div style={{ fontFamily: MONO, fontSize: 11.5, color: C.muted, marginTop: 5 }}>
          {cfg.units} = {nf(cfg.pchp)} кВт · уставка среза пика {nf(cfg.target)} кВт ·
          сутки {cfg.dayDate} · неделя с {cfg.weekDate}
        </div>
      </header>

      <div style={{ padding: "16px 20px 22px" }}>
        <div style={{ display: "flex", gap: 14, flexWrap: "wrap" }}>
          <Switch items={[["A", "Без батареи"], ["B", "Арбитраж"], ["C", "Срез пика"]]}
                  value={md} onChange={setMd} />
          <Switch items={[[false, "Свернуть часы"], [true, "168 часов"]]}
                  value={full} onChange={setFull} />
        </div>

        <Sub>Типичные сутки · {MODE_NAME[md]}</Sub>
        {dayTable(cfg, md)}

        <Sub>Неделя, сводка по суткам · {MODE_NAME[md]}</Sub>
        {weekTable(cfg, md)}

        {full && <><Sub>Неделя, почасовой профиль · {MODE_NAME[md]}</Sub>
          {hourWeekTable(cfg, md)}</>}

        <Sub>Сводная таблица</Sub>
        {summaryTable(cfg, sum)}

        <div style={{ marginTop: 12, fontFamily: MONO, fontSize: 11, color: C.muted,
                      lineHeight: 1.6 }}>
          Затраты включают топливо КГУ ({KZT_CHP} ₸/кВт·ч), закуп из сети
          ({KZT_GRID} ₸/кВт·ч) и пуски ({nf(KZT_START)} ₸ за пуск) на всех трёх
          горизонтах. CAPEX накопителя {nf(sum.capex)} ₸. Плата за заявленную
          мощность принята нулевой, поэтому строки с пиками на деньги не влияют.
        </div>
      </div>
    </section>
  );
}

export default function BessProfile() {
  const [mode, setMode] = useState("B");
  const [period, setPeriod] = useState("day");

  const modes = [["A", "Без батареи"], ["B", "Арбитраж"], ["C", "Срез пика"]];
  const periods = [["day", "Сутки"], ["week", "7 дней"]];
  const isWeek = period === "week";
  const showSave = mode !== "A";

  return (
    <div style={{ background: C.paper, minHeight: "100%", padding: "28px 20px 36px" }}>
      <div style={{ maxWidth: 900, margin: "0 auto" }}>
        <div style={{ marginBottom: 22 }}>
          <div style={{ fontFamily: MONO, fontSize: 11, color: C.muted,
                        letterSpacing: "0.14em", textTransform: "uppercase" }}>
            {isWeek
              ? `Неделя с ${DATA.chp2.weekDate} · сценарий 1`
              : `Типичные сутки · ${DATA.chp2.dayDate} · сценарий 1`}
          </div>
          <h1 style={{ margin: "8px 0 0", fontFamily: SANS, fontSize: 27, fontWeight: 680,
                       letterSpacing: "-0.025em", color: C.ink, lineHeight: 1.15 }}>
            Работа накопителя против нагрузки завода
          </h1>
          <p style={{ margin: "10px 0 0", fontFamily: SANS, fontSize: 14.5,
                      color: C.muted, lineHeight: 1.55, maxWidth: 660 }}>
            Точками показана нагрузка завода по часам. Столбцами — только накопитель:
            вверх заряд, вниз разряд. Пунктир отмечает суммарную мощность КГУ.
            Вуаль показывает деньги: её высота равна экономии часа, делённой на
            спред 38 ₸/кВт·ч, поэтому площадь вуали, умноженная на 38, и есть
            тенге. Зелёная — выигрыш, оранжевая над линией — перерасход.
          </p>
        </div>

        <div style={{ display: "flex", gap: 14, flexWrap: "wrap", marginBottom: 22 }}>
          <Switch items={modes} value={mode} onChange={setMode} />
          <Switch items={periods} value={period} onChange={setPeriod} />
        </div>

        <Panel cfg={DATA.chp2} mode={mode} period={period} />
        <Panel cfg={DATA.chp3} mode={mode} period={period} />

        <div style={{ border: `1px solid ${C.rule}`, background: C.panel,
                      padding: "12px 16px", marginBottom: 22,
                      fontFamily: MONO, fontSize: 11.5, color: C.muted, lineHeight: 1.6 }}>
          Окно нельзя умножать на 365 или на 52. Проверка против полного годового
          прогона: суточная экономия в режиме арбитража завышает год на 22% при
          двух КГУ и на 11% при трёх, а в режиме среза пика ошибается в разы
          (+123% и −73%). Причина в том, что накопитель не замыкает сутки по
          энергии, а контроллер среза пика помнит максимум расчётного месяца.
          Годовые цифры берутся только из годового прогона.
        </div>

        <div style={{ display: "flex", flexWrap: "wrap", gap: 20, alignItems: "center",
                      fontFamily: MONO, fontSize: 11.5, color: C.muted, marginTop: 4 }}>
          <span style={{ display: "flex", alignItems: "center", gap: 7 }}>
            <span style={{ width: 22, height: 1.6, background: C.load, display: "inline-block" }} />
            нагрузка завода
          </span>
          <span style={{ display: "flex", alignItems: "center", gap: 7 }}>
            <span style={{ width: 13, height: 13, background: C.charge, display: "inline-block" }} />
            заряд BESS
          </span>
          <span style={{ display: "flex", alignItems: "center", gap: 7 }}>
            <span style={{ width: 13, height: 13, background: C.discharge, display: "inline-block" }} />
            разряд BESS
          </span>
          {showSave && (
            <span style={{ display: "flex", alignItems: "center", gap: 7 }}>
              <span style={{ width: 13, height: 13, background: C.save, opacity: 0.35,
                             display: "inline-block" }} />
              экономия · площадь × 38 ₸/кВт·ч
            </span>
          )}
          {showSave && (
            <span style={{ display: "flex", alignItems: "center", gap: 7 }}>
              <span style={{ width: 13, height: 13, background: C.discharge, opacity: 0.32,
                             display: "inline-block" }} />
              перерасход · вуаль выше линии
            </span>
          )}
        </div>

        <div style={{ marginTop: 34, paddingTop: 24, borderTop: `2px solid ${C.ink}` }}>
          <div style={{ fontFamily: MONO, fontSize: 11, color: C.muted,
                        letterSpacing: "0.14em", textTransform: "uppercase" }}>
            Отчёты
          </div>
          <h2 style={{ margin: "8px 0 20px", fontFamily: SANS, fontSize: 23, fontWeight: 660,
                       letterSpacing: "-0.02em", color: C.ink, lineHeight: 1.2 }}>
            Сутки, неделя и год по обеим конфигурациям
          </h2>
          <ReportBlock cfg={DATA.chp2} sum={SUMMARY.chp2} />
          <ReportBlock cfg={DATA.chp3} sum={SUMMARY.chp3} />
        </div>
      </div>
    </div>
  );
}
```

---

# Приложение F. Сгенерированные отчёты

## F.1 Отчёт первой итерации: MILP на трёх КГУ

#### Оптимизация диспетчеризации 3 КГУ + BESS

Единицы: мощность в MW, энергия в MWh, затраты в у.е.

##### Таблица 1. Без батареи

| Час | Потребление | КГУ 1 | КГУ 2 | КГУ 3 | Сеть | Сброс/холостой ход | Затраты за час |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 0.550 | 0.250 | 0.300 | 0.000 | 0.000 | 0.000 | 30.11 |
| 2 | 0.520 | 0.270 | 0.250 | 0.000 | 0.000 | 0.000 | 28.71 |
| 3 | 0.500 | 0.250 | 0.250 | 0.000 | 0.000 | 0.000 | 27.78 |
| 4 | 0.500 | 0.250 | 0.250 | 0.000 | 0.000 | 0.000 | 27.78 |
| 5 | 0.550 | 0.300 | 0.250 | 0.000 | 0.000 | 0.000 | 30.11 |
| 6 | 0.700 | 0.388 | 0.312 | 0.000 | 0.000 | 0.000 | 37.17 |
| 7 | 0.950 | 0.500 | 0.450 | 0.000 | 0.000 | 0.000 | 49.09 |
| 8 | 1.200 | 0.438 | 0.438 | 0.325 | 0.000 | 0.000 | 102.87 |
| 12 | 1.450 | 0.500 | 0.500 | 0.450 | 0.000 | -0.000 | 0.000 | 0.100 | 74.84 |
| 13 | 0.650 | 0.311 | 0.250 | 0.250 | 0.000 | -0.161 | 0.000 | 0.251 | 45.32 |
| 14 | 0.600 | 0.250 | 0.250 | 0.250 | 0.000 | -0.150 | 0.000 | 0.392 | 42.41 |
| 15 | 1.350 | 0.475 | 0.438 | 0.438 | 0.000 | -0.000 | 0.000 | 0.392 | 70.02 |
| 16 | 1.500 | 0.500 | 0.500 | 0.500 | 0.000 | -0.000 | 0.000 | 0.392 | 77.25 |
| 17 | 1.550 | 0.500 | 0.500 | 0.500 | 0.000 | -0.000 | 0.050 | 0.339 | 77.50 |
| 18 | 1.550 | 0.500 | 0.500 | 0.500 | 0.000 | -0.000 | 0.050 | 0.285 | 77.50 |
| 19 | 1.400 | 0.463 | 0.500 | 0.438 | 0.000 | -0.000 | 0.000 | 0.285 | 72.43 |
| 20 | 1.200 | 0.438 | 0.325 | 0.438 | 0.000 | -0.000 | 0.000 | 0.285 | 62.87 |
| 21 | 1.000 | 0.500 | 0.000 | 0.500 | 0.000 | -0.000 | 0.000 | 0.285 | 51.50 |
| 22 | 0.850 | 0.438 | 0.000 | 0.412 | 0.000 | -0.000 | 0.000 | 0.285 | 44.29 |
| 23 | 0.620 | 0.312 | 0.000 | 0.307 | 0.000 | -0.000 | 0.000 | 0.285 | 33.38 |
| 24 | 0.580 | 0.500 | 0.000 | 0.000 | 0.000 | -0.000 | 0.080 | 0.200 | 26.15 |
| **Итог** | | | | | 0.000 | | | | **1316.88** |

##### Экономический итог

| Показатель | Без батареи | С батареей | Разница |
|---|---:|---:|---:|
| Затраты за сутки, у.е. | 1348.30 | 1316.88 | 31.43 |
| Расход газа, м3/сутки | 6353.5 | 6369.4 | -15.9 |
| Покупка из сети, MWh/сутки | 0.280 | 0.000 | |
| Сброшено в холостую, MWh/сутки | 0.250 | 0.000 | |
| **Затраты за год (365 дн.), у.е.** | **492130** | **480660** | **11470** |

Экономический эффект от внедрения BESS: **11470 у.е./год** (2.33%).

## F.2 Суточные таблицы третьей итерации, три сценария

#### Симуляция EMS: 3 КГУ + BESS

Топливная кривая: fuel(P) = 12.50 + 0.22500*P м3/ч. sfc(500)=0.2500, sfc(100)=0.3500 (x1.40).

Порог перелома: 211.5 кВт (42.3% номинала).


##### Сценарий «nominal» — EMS из ТЗ

| Час | Потребление | Разряд (-) | Заряд (+) | SoC,% | КГУ1 | КГУ2 | КГУ3 | Холостой/сброс | Топливо,у.е. | Состояние |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|:--|
| 1 | 105 | -105 | 0 | 96.3 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 2 | 116 | -116 | 0 | 92.2 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 3 | 111 | -111 | 0 | 88.2 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 4 | 89 | -89 | 0 | 85.0 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 5 | 92 | -92 | 0 | 81.8 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 6 | 115 | -115 | 0 | 77.7 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 7 | 80 | -80 | 0 | 74.8 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 8 | 113 | -113 | 0 | 70.8 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 9 | 759 | -759 | 0 | 43.8 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 10 | 694 | -0 | 806 | 69.1 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 11 | 661 | -0 | 839 | 95.3 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 12 | 656 | -0 | 150 | 100.0 | 403 | 403 | 0 | 0 | 41.26 | CHARGE |
| 13 | 651 | -651 | 0 | 76.9 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 14 | 689 | -689 | 0 | 52.4 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 15 | 701 | -0 | 799 | 77.4 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 16 | 711 | -0 | 724 | 100.0 | 478 | 478 | 478 | 0 | 72.04 | CHARGE |
| 17 | 799 | -799 | 0 | 71.6 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 18 | 759 | -759 | 0 | 44.7 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 19 | 724 | -0 | 776 | 68.9 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 20 | 798 | -0 | 702 | 90.9 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 21 | 89 | -0 | 292 | 100.0 | 381 | 0 | 0 | 0 | 19.64 | CHARGE |
| 22 | 86 | -86 | 0 | 96.9 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 23 | 105 | -105 | 0 | 93.2 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 24 | 82 | -82 | 0 | 90.3 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |

За сутки: топливо **2540 м3** (507.94 у.е.), сброс 0 кВт·ч, недоотпуск 0 кВт·ч. Без батареи: 2670 м3 (533.92 у.е.), сброс 82 кВт·ч.


##### Сценарий «peak» — EMS из ТЗ

| Час | Потребление | Разряд (-) | Заряд (+) | SoC,% | КГУ1 | КГУ2 | КГУ3 | Холостой/сброс | Топливо,у.е. | Состояние |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|:--|
| 1 | 1500 | -1500 | 0 | 46.7 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 2 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 3 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 4 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 5 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 6 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 7 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 8 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 9 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 10 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 11 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 12 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 13 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 14 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 15 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 16 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 17 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 18 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 19 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 20 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 21 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 22 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 23 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 24 | 1500 | -0 | 0 | 46.7 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |

За сутки: топливо **8625 м3** (1725.00 у.е.), сброс 0 кВт·ч, недоотпуск 0 кВт·ч. Без батареи: 9000 м3 (1800.00 у.е.), сброс 0 кВт·ч.


##### Сценарий «low» — EMS из ТЗ

| Час | Потребление | Разряд (-) | Заряд (+) | SoC,% | КГУ1 | КГУ2 | КГУ3 | Холостой/сброс | Топливо,у.е. | Состояние |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|:--|
| 1 | 272 | -272 | 0 | 90.3 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 2 | 301 | -301 | 0 | 79.6 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 3 | 298 | -298 | 0 | 69.1 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 4 | 325 | -325 | 0 | 57.5 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 5 | 308 | -308 | 0 | 46.6 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 6 | 301 | -301 | 0 | 35.9 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 7 | 300 | -0 | 1200 | 73.4 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 8 | 285 | -0 | 851 | 100.0 | 378 | 378 | 378 | 0 | 58.59 | CHARGE |
| 9 | 271 | -271 | 0 | 90.4 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 10 | 282 | -282 | 0 | 80.4 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 11 | 312 | -312 | 0 | 69.3 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 12 | 282 | -282 | 0 | 59.3 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 13 | 292 | -292 | 0 | 48.9 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 14 | 270 | -270 | 0 | 39.3 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 15 | 320 | -0 | 1180 | 76.2 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 16 | 279 | -0 | 761 | 100.0 | 347 | 347 | 347 | 0 | 54.31 | CHARGE |
| 17 | 286 | -286 | 0 | 89.8 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 18 | 323 | -323 | 0 | 78.4 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 19 | 301 | -301 | 0 | 67.7 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 20 | 321 | -321 | 0 | 56.3 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 21 | 308 | -308 | 0 | 45.3 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 22 | 315 | -315 | 0 | 34.2 | 0 | 0 | 0 | 0 | 0.00 | DISCHARGE |
| 23 | 275 | -0 | 1225 | 72.4 | 500 | 500 | 500 | 0 | 75.00 | CHARGE |
| 24 | 302 | -0 | 881 | 100.0 | 395 | 395 | 395 | 0 | 60.77 | CHARGE |

За сутки: топливо **1993 м3** (398.68 у.е.), сброс 0 кВт·ч, недоотпуск 0 кВт·ч. Без батареи: 1904 м3 (380.75 у.е.), сброс 0 кВт·ч.

---

## F.2b Недельные почасовые таблицы, два КГУ (168 часов x 3 сценария)

#### Недельные профили, сценарий 1 «Две смены, непрерывно»

Неделя начинается с часа 3624 (день 152 года).


###### ТАБЛИЦА 1. БЕЗ БАТАРЕИ

| День | Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 1 | 1344 | 0 | 1200 | 0 | 0 | 144 | 100.0 |
| Пн | 2 | 1395 | 0 | 1200 | 0 | 0 | 195 | 100.0 |
| Пн | 3 | 1348 | 0 | 1200 | 0 | 0 | 148 | 100.0 |
| Пн | 4 | 1407 | 0 | 1200 | 0 | 0 | 207 | 100.0 |
| Пн | 5 | 1700 | 0 | 1200 | 0 | 0 | 500 | 100.0 |
| Пн | 6 | 1998 | 0 | 1200 | 0 | 0 | 798 | 100.0 |
| Пн | 7 | 2433 | 1067 | 1200 | 0 | 0 | 166 | 100.0 |
| Пн | 8 | 2583 | 1067 | 1200 | 0 | 0 | 316 | 100.0 |
| Вс | 9 | 2553 | 1067 | 1200 | 0 | 0 | 286 | 100.0 |
| Вс | 10 | 2388 | 1067 | 1200 | 0 | 0 | 121 | 100.0 |
| Вс | 11 | 2400 | 1067 | 1200 | 0 | 0 | 133 | 100.0 |
| Вс | 12 | 2530 | 1067 | 1200 | 0 | 0 | 263 | 100.0 |
| Вс | 13 | 2208 | 1039 | 1169 | 0 | 0 | 0 | 100.0 |
| Вс | 14 | 2217 | 1043 | 1173 | 0 | 0 | 0 | 100.0 |
| Вс | 15 | 2140 | 1007 | 1133 | 0 | 0 | 0 | 100.0 |
| Вс | 16 | 2111 | 994 | 1118 | 0 | 0 | 0 | 100.0 |
| Вс | 17 | 2466 | 1067 | 1200 | 0 | 0 | 199 | 100.0 |
| Вс | 18 | 2365 | 1067 | 1200 | 0 | 0 | 98 | 100.0 |
| Вс | 19 | 2406 | 1067 | 1200 | 0 | 0 | 140 | 100.0 |
| Вс | 20 | 2373 | 1067 | 1200 | 0 | 0 | 106 | 100.0 |
| Вс | 21 | 2500 | 1067 | 1200 | 0 | 0 | 233 | 100.0 |
| Вс | 22 | 2147 | 1011 | 1137 | 0 | 0 | 0 | 100.0 |
| Вс | 23 | 1672 | 0 | 1200 | 0 | 0 | 472 | 100.0 |
| Вс | 24 | 1370 | 0 | 1200 | 0 | 0 | 170 | 100.0 |

**Итого за неделю:** нагрузка 365,658 кВт·ч, КГУ 313,540, заряд 0, разряд 0, сеть 52,118 кВт·ч, пик сети 1,453 кВт


###### ТАБЛИЦА 2. С БАТАРЕЕЙ, энергетический арбитраж

| День | Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 1 | 1344 | 0 | 1200 | 0 | 0 | 144 | 97.9 |
| Пн | 2 | 1395 | 0 | 1200 | 0 | 0 | 195 | 97.9 |
| Пн | 3 | 1348 | 0 | 1200 | 0 | 0 | 148 | 97.9 |
| Пн | 4 | 1407 | 0 | 1200 | 0 | 0 | 207 | 97.9 |
| Пн | 5 | 1700 | 0 | 1200 | 0 | 0 | 500 | 97.9 |
| Пн | 6 | 1998 | 1000 | 1124 | 126 | 0 | 0 | 100.0 |
| Пн | 7 | 2433 | 1067 | 1200 | 0 | -166 | 0 | 96.8 |
| Пн | 8 | 2583 | 1067 | 1200 | 0 | -316 | 0 | 90.7 |
| Пн | 9 | 2735 | 1067 | 1200 | 0 | -468 | 0 | 81.6 |
| Пн | 10 | 2547 | 1067 | 1200 | 0 | -280 | 0 | 76.1 |
| Пн | 11 | 2853 | 1067 | 1200 | 0 | -586 | 0 | 64.8 |
| Пн | 12 | 2499 | 1067 | 1200 | 0 | -232 | 0 | 60.3 |
| Пн | 13 | 2564 | 1067 | 1200 | 0 | -297 | 0 | 54.5 |
| Пн | 14 | 2397 | 1067 | 1200 | 0 | -130 | 0 | 52.0 |
| Пн | 15 | 2524 | 1067 | 1200 | 0 | -257 | 0 | 47.0 |
| Пн | 16 | 2458 | 1067 | 1200 | 0 | -191 | 0 | 43.3 |
| Вс | 16 | 2111 | 1067 | 1200 | 156 | 0 | 0 | 90.1 |
| Вс | 17 | 2466 | 1067 | 1200 | 0 | -199 | 0 | 86.2 |
| Вс | 18 | 2365 | 1067 | 1200 | 0 | -98 | 0 | 84.3 |
| Вс | 19 | 2406 | 1067 | 1200 | 0 | -140 | 0 | 81.6 |
| Вс | 20 | 2373 | 1067 | 1200 | 0 | -106 | 0 | 79.6 |
| Вс | 21 | 2500 | 1067 | 1200 | 0 | -233 | 0 | 75.1 |
| Вс | 22 | 2147 | 1067 | 1200 | 120 | 0 | 0 | 77.1 |
| Вс | 23 | 1672 | 1067 | 1200 | 594 | 0 | 0 | 87.3 |
| Вс | 24 | 1370 | 997 | 1121 | 747 | 0 | 0 | 100.0 |

**Итого за неделю:** нагрузка 365,658 кВт·ч, КГУ 351,400, заряд 24,051, разряд 21,054, сеть 17,255 кВт·ч, пик сети 1,037 кВт


###### ТАБЛИЦА 3. С БАТАРЕЕЙ, срез пика с уставкой 600 кВт

| День | Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 1 | 1344 | 0 | 1200 | 0 | 0 | 144 | 100.0 |
| Пн | 2 | 1395 | 0 | 1200 | 0 | 0 | 195 | 100.0 |
| Пн | 3 | 1348 | 0 | 1200 | 0 | 0 | 148 | 100.0 |
| Пн | 4 | 1407 | 0 | 1200 | 0 | 0 | 207 | 100.0 |
| Пн | 5 | 1700 | 0 | 1200 | 0 | 0 | 500 | 100.0 |
| Пн | 6 | 1998 | 0 | 1200 | 0 | 0 | 798 | 100.0 |
| Пн | 7 | 2433 | 1067 | 1200 | 0 | 0 | 166 | 100.0 |
| Пн | 8 | 2583 | 1067 | 1200 | 0 | 0 | 316 | 100.0 |
| Пн | 9 | 2735 | 1067 | 1200 | 0 | 0 | 468 | 100.0 |
| Пн | 10 | 2547 | 1067 | 1200 | 0 | 0 | 280 | 100.0 |
| Пн | 11 | 2853 | 1067 | 1200 | 0 | 0 | 586 | 100.0 |
| Пн | 12 | 2499 | 1067 | 1200 | 0 | 0 | 232 | 100.0 |
| Пн | 13 | 2564 | 1067 | 1200 | 0 | 0 | 297 | 100.0 |
| Пн | 14 | 2397 | 1067 | 1200 | 0 | 0 | 130 | 100.0 |
| Пн | 15 | 2524 | 1067 | 1200 | 0 | 0 | 257 | 100.0 |
| Пн | 16 | 2458 | 1067 | 1200 | 0 | 0 | 191 | 100.0 |
| Пн | 17 | 2579 | 1067 | 1200 | 0 | 0 | 312 | 100.0 |
| Пн | 18 | 3041 | 1067 | 1200 | 0 | 0 | 774 | 100.0 |
| Пн | 19 | 2713 | 1067 | 1200 | 0 | 0 | 446 | 100.0 |
| Пн | 20 | 2828 | 1067 | 1200 | 0 | 0 | 562 | 100.0 |
| Пн | 21 | 2356 | 1067 | 1200 | 0 | 0 | 89 | 100.0 |
| Пн | 22 | 2074 | 976 | 1098 | 0 | 0 | 0 | 100.0 |
| Пн | 23 | 2014 | 0 | 1200 | 0 | 0 | 814 | 100.0 |
| Вс | 24 | 1370 | 0 | 1200 | 0 | 0 | 170 | 100.0 |

**Итого за неделю:** нагрузка 365,658 кВт·ч, КГУ 313,895, заряд 355, разряд 312, сеть 51,806 кВт·ч, пик сети 1,271 кВт

---

## F.3 Финальный отчёт, два КГУ

#### Расчёт микросети: 2 КГУ + BESS + сеть

Тарифы: КГУ **22 ₸/кВт·ч**, сеть **60 ₸/кВт·ч**, разряд BESS **25.00 ₸/кВт·ч**.

Типичные сутки: 01.09.2026 (Вт). Неделя: с 01.06.2026.

Сутки и неделя вырезаны из годового прогона (8760 ч), а не посчитаны отдельно.


##### Типичные сутки


###### A. Без батареи

| Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1373 | 0 | 1200 | 0 | 0 | 173 | 100.0 |
| 2 | 1374 | 0 | 1200 | 0 | 0 | 174 | 100.0 |
| 3 | 1477 | 0 | 1200 | 0 | 0 | 277 | 100.0 |
| 4 | 1473 | 0 | 1200 | 0 | 0 | 273 | 100.0 |
| 5 | 1688 | 0 | 1200 | 0 | 0 | 488 | 100.0 |
| 6 | 2180 | 1026 | 1154 | 0 | 0 | 0 | 100.0 |
| 7 | 2071 | 975 | 1096 | 0 | 0 | 0 | 100.0 |
| 8 | 2560 | 1067 | 1200 | 0 | 0 | 292 | 100.0 |
| 9 | 2799 | 1067 | 1200 | 0 | 0 | 532 | 100.0 |
| 10 | 2911 | 1067 | 1200 | 0 | 0 | 644 | 100.0 |
| 11 | 2967 | 1067 | 1200 | 0 | 0 | 700 | 100.0 |
| 12 | 2743 | 1067 | 1200 | 0 | 0 | 476 | 100.0 |
| 13 | 2775 | 1067 | 1200 | 0 | 0 | 508 | 100.0 |
| 14 | 2680 | 1067 | 1200 | 0 | 0 | 413 | 100.0 |
| 15 | 2644 | 1067 | 1200 | 0 | 0 | 377 | 100.0 |
| 16 | 2393 | 1067 | 1200 | 0 | 0 | 126 | 100.0 |
| 17 | 2479 | 1067 | 1200 | 0 | 0 | 212 | 100.0 |
| 18 | 2717 | 1067 | 1200 | 0 | 0 | 450 | 100.0 |
| 19 | 2909 | 1067 | 1200 | 0 | 0 | 642 | 100.0 |
| 20 | 3128 | 1067 | 1200 | 0 | 0 | 861 | 100.0 |
| 21 | 2603 | 1067 | 1200 | 0 | 0 | 336 | 100.0 |
| 22 | 2368 | 1067 | 1200 | 0 | 0 | 101 | 100.0 |
| 23 | 2036 | 0 | 1200 | 0 | 0 | 836 | 100.0 |
| 24 | 1866 | 0 | 1200 | 0 | 0 | 666 | 100.0 |
| **Итог** | **56,212** | 18,005 | 28,650 | 0 | -0 | **9,557** | пик 861 |

###### B. С батареей, арбитраж

| Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 
| 17 | 2479 | 1067 | 1200 | 0 | 0 | 212 | 97.2 |
| 18 | 2717 | 1067 | 1200 | 0 | 0 | 450 | 97.2 |
| 19 | 2909 | 1067 | 1200 | 0 | -42 | 600 | 96.4 |
| 20 | 3128 | 1067 | 1200 | 0 | -261 | 600 | 91.3 |
| 21 | 2603 | 1067 | 1200 | 0 | 0 | 336 | 91.3 |
| 22 | 2368 | 1067 | 1200 | 0 | 0 | 101 | 91.3 |
| 23 | 2036 | 1067 | 1200 | 231 | 0 | 0 | 95.3 |
| 24 | 1866 | 1009 | 1135 | 277 | 0 | 0 | 100.0 |
| **Итог** | **56,212** | 20,081 | 28,584 | 508 | -447 | **7,607** | пик 600 |

##### Неделя, сводка по суткам


###### A. Без батареи

| День | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 16,981 | 28,698 | 0 | 0 | 8,436 | 814 | 100.0 |
| Вт | 54,170 | 17,055 | 28,781 | 0 | 0 | 8,333 | 774 | 100.0 |
| Ср | 53,724 | 17,042 | 28,766 | 0 | 0 | 7,916 | 703 | 100.0 |
| Чт | 54,139 | 18,051 | 28,686 | 0 | 0 | 7,402 | 848 | 100.0 |
| Пт | 53,065 | 16,962 | 28,676 | 0 | 0 | 7,428 | 910 | 100.0 |
| Сб | 48,266 | 14,805 | 23,851 | 0 | 0 | 9,610 | 1,453 | 100.0 |
| Вс | 48,180 | 16,749 | 28,437 | 0 | 0 | 2,994 | 472 | 100.0 |
| **Итог** | **365,658** | 117,645 | 195,895 | 0 | -0 | **52,118** | **1,453** | |

###### B. С батареей, арбитраж

| День | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 20,232 | 28,754 | 1,172 | -3,612 | 2,688 | 562 | 46.9 |
| Вт | 54,170 | 22,261 | 28,636 | 2,703 | -2,444 | 3,531 | 574 | 45.6 |
| Ср | 53,724 | 22,309 | 28,690 | 3,034 | -2,422 | 3,337 | 654 | 50.4 |
| Чт | 54,139 | 22,350 | 28,736 | 2,602 | -2,375 | 3,280 | 532 | 48.7 |
| Пт | 53,065 | 21,334 | 28,794 | 2,041 | -1,996 | 2,982 | 727 | 44.9 |
| Сб | 48,266 | 21,284 | 28,737 | 3,814 | -1,988 | 71 | 71 | 71.4 |
| Вс | 48,180 | 21,167 | 28,605 | 3,284 | -1,579 | 113 | 73 | 96.8 |
| **Итог** | **365,658** | 150,938 | 200,952 | 18,650 | -16,416 | **16,002** | **727** | |

###### C. С батареей, срез пика 600 кВт

| День | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
|
| Вс | 14 | 2217 | 1043 | 1173 | 0 | 0 | 0 | 100.0 |
| Вс | 15 | 2140 | 1007 | 1133 | 0 | 0 | 0 | 100.0 |
| Вс | 16 | 2111 | 994 | 1118 | 0 | 0 | 0 | 100.0 |
| Вс | 17 | 2466 | 1067 | 1200 | 0 | 0 | 199 | 100.0 |
| Вс | 18 | 2365 | 1067 | 1200 | 0 | 0 | 98 | 100.0 |
| Вс | 19 | 2406 | 1067 | 1200 | 0 | 0 | 140 | 100.0 |
| Вс | 20 | 2373 | 1067 | 1200 | 0 | 0 | 106 | 100.0 |
| Вс | 21 | 2500 | 1067 | 1200 | 0 | 0 | 233 | 100.0 |
| Вс | 22 | 2147 | 1011 | 1137 | 0 | 0 | 0 | 100.0 |
| Вс | 23 | 1672 | 0 | 1200 | 0 | 0 | 472 | 100.0 |
| Вс | 24 | 1370 | 0 | 1200 | 0 | 0 | 170 | 100.0 |

##### Сводная таблица

| Показатель | A. Без батареи | B. Арбитраж | C. Срез пика |
|---|---:|---:|---:|
| **ТИПИЧНЫЕ СУТКИ** | | | |
| Нагрузка завода, кВт·ч | 56,212 | 56,212 | 56,212 |
| Выработка КГУ, кВт·ч | 46,655 | 53,625 | 48,666 |
| Заряд BESS, кВт·ч | 0 | 4,082 | 508 |
| Разряд BESS, кВт·ч | 0 | 3,612 | 447 |
| Закуп из сети, кВт·ч | 9,557 | 3,058 | 7,607 |
| Пик закупа, кВт | 861 | 861 | 600 |
| Затраты, ₸ | 1,599,828 | 1,363,234 | 1,527,088 |
| Экономия к варианту A, ₸ | 0 | 236,594 | 72,741 |
| **НЕДЕЛЯ** | | | |
| Нагрузка завода, кВт·ч | 365,658 | 365,658 | 365,658 |
| Выработка КГУ, кВт·ч | 313,540 | 351,890 | 313,895 |
| Заряд BESS, кВт·ч | 0 | 18,650 | 355 |
| Разряд BESS, кВт·ч | 0 | 16,416 | 312 |
| Закуп из сети, кВт·ч | 52,118 | 16,002 | 51,806 |
| Пик закупа, кВт | 1,453 | 727 | 1,271 |
| Затраты, ₸ | 10,024,956 | 8,701,707 | 10,014,018 |
| Экономия к варианту A, ₸ | 0 | 1,323,248 | 10,938 |
| **ГОД** | | | |
| Выработка КГУ, МВт·ч | 16,945 | 18,939 | 17,293 |
| Закуп из сети, МВт·ч | 4,079 | 2,215 | 3,754 |
| Доля сети в питании, % | 19.4 | 10.5 | 17.9 |
| Годовой максимум закупа, кВт | 3,233 | 3,233 | 1,403 |
| Сумма 12 месячных максимумов, кВт | 34,952 | 34,320 | 13,436 |
| Пусков КГУ за год | 401 | 227 | 398 |
| Затраты, ₸/год | 623,555,059 | 552,977,723 | 611,646,415 |
| Экономия к варианту A, ₸/год | 0 | 70,577,336 | 11,908,644 |
| Экономия, % | 0.0 | 11.3 | 1.9 |
| Простая окупаемость, лет | nan | 5.8 | 34.3 |


CAPEX BESS: $850,000 = 408,000,000 ₸ (курс 480 ₸/$).

##### График диспетчеризации типичных суток

![Диспетчеризация типичных суток](chart_day_2chp.png)

**Как читать.** Столбики (прямоугольники) - мощность за час. Прямоугольник, а не плавная линия, потому что шаг симуляции часовой: внутри часа мощность считается постоянной, и рисовать её кривой было бы враньём о разрешении модели.

- выше нуля - источники: КГУ по агрегатам, разряд BESS, закуп из сети
- ниже нуля - заряд BESS: мощность, уходящая из системы в накопитель
- линия с точками - фактическая нагрузка завода, точка в середине своего часа
- тонкая линия справа - SoC накопителя, %

Проверка баланса: сумма столбиков выше нуля минус столбики ниже нуля в каждом часу ложится ровно на линию нагрузки.

---

## F.4 Финальный отчёт, три КГУ

#### Расчёт микросети: 2 КГУ + BESS + сеть

Тарифы: КГУ **22 ₸/кВт·ч**, сеть **60 ₸/кВт·ч**, разряд BESS **25.00 ₸/кВт·ч**.

Типичные сутки: 01.09.2026 (Вт). Неделя: с 01.06.2026.

Сутки и неделя вырезаны из годового прогона (8760 ч), а не посчитаны отдельно.


##### Типичные сутки


###### A. Без батареи

| Час | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1373 | 0 | 1200 | 0 | 0 | 0 | 173 | 100.0 |
| 2 | 1374 | 0 | 1200 | 0 | 0 | 0 | 174 | 100.0 |
| 3 | 1477 | 0 | 1200 | 0 | 0 | 0 | 277 | 100.0 |
| 4 | 1473 | 0 | 1200 | 0 | 0 | 0 | 273 | 100.0 |
| 5 | 1688 | 0 | 1200 | 0 | 0 | 0 | 488 | 100.0 |
| 6 | 2180 | 0 | 1137 | 1042 | 0 | 0 | 0 | 100.0 |
| 7 | 2071 | 0 | 1080 | 990 | 0 | 0 | 0 | 100.0 |
| 8 | 2560 | 0 | 1200 | 1100 | 0 | 0 | 260 | 100.0 |
| 9 | 2799 | 0 | 1200 | 1100 | 0 | 0 | 499 | 100.0 |
| 10 | 2911 | 0 | 1200 | 1100 | 0 | 0 | 611 | 100.0 |
| 11 | 2967 | 0 | 1200 | 1100 | 0 | 0 | 667 | 100.0 |
| 12 | 2743 | 0 | 1200 | 1100 | 0 | 0 | 443 | 100.0 |
| 13 | 2775 | 0 | 1200 | 1100 | 0 | 0 | 475 | 100.0 |
| 14 | 2680 | 0 | 1200 | 1100 | 0 | 0 | 380 | 100.0 |
| 15 | 2644 | 0 | 1200 | 1100 | 0 | 0 | 344 | 100.0 |
| 16 | 2393 | 0 | 1200 | 1100 | 0 | 0 | 93 | 100.0 |
| 17 | 2479 | 0 | 1200 | 1100 | 0 | 0 | 179 | 100.0 |
| 18 | 2717 | 0 | 1200 | 1100 | 0 | 0 | 417 | 100.0 |
| 19 | 2909 | 0 | 1200 | 1100 | 0 | 0 | 609 | 100.0 |
| 20 | 3128 | 991 | 1115 | 1022 | 0 | 0 | 0 | 100.0 |
| 21 | 2603 | 1067 | 1200 | 0 | 0 | 0 | 336 | 100.0 |
| 22 | 2368 | 1067 | 1200 | 0 | 0 | 0 | 101 | 100.0 |
| 23 | 2036 | 0 | 1200 | 0 | 0 | 0 | 836 | 100.0 |
| 24 | 1866 | 0 | 1200 | 0 | 0 | 0 | 666 | 100.0 |
| **Итог** | **56,212** | 3,125 | 28,532 | 16,255 | 0 | -0 | **8,300** | пик 836 |

###### B. С батареей, арбитраж

| Час | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 
| 7 | 2071 | 0 | 1080 | 990 | 0 | 0 | 0 | 100.0 |
| 8 | 2560 | 0 | 1200 | 1100 | 0 | 0 | 260 | 100.0 |
| 9 | 2799 | 0 | 1200 | 1100 | 0 | 0 | 499 | 100.0 |
| 10 | 2911 | 0 | 1200 | 1100 | 0 | 0 | 611 | 100.0 |
| 11 | 2967 | 0 | 1200 | 1100 | 0 | 0 | 667 | 100.0 |
| 12 | 2743 | 0 | 1200 | 1100 | 0 | 0 | 443 | 100.0 |
| 13 | 2775 | 0 | 1200 | 1100 | 0 | 0 | 475 | 100.0 |
| 14 | 2680 | 0 | 1200 | 1100 | 0 | 0 | 380 | 100.0 |
| 15 | 2644 | 0 | 1200 | 1100 | 0 | 0 | 344 | 100.0 |
| 16 | 2393 | 0 | 1200 | 1100 | 0 | 0 | 93 | 100.0 |
| 17 | 2479 | 0 | 1200 | 1100 | 0 | 0 | 179 | 100.0 |
| 18 | 2717 | 0 | 1200 | 1100 | 0 | 0 | 417 | 100.0 |
| 19 | 2909 | 0 | 1200 | 1100 | 0 | 0 | 609 | 100.0 |
| 20 | 3128 | 991 | 1115 | 1022 | 0 | 0 | 0 | 100.0 |
| 21 | 2603 | 1067 | 1200 | 0 | 0 | 0 | 336 | 100.0 |
| 22 | 2368 | 1067 | 1200 | 0 | 0 | 0 | 101 | 100.0 |
| 23 | 2036 | 0 | 1200 | 0 | 0 | -136 | 700 | 97.4 |
| 24 | 1866 | 0 | 1200 | 0 | 0 | 0 | 666 | 97.4 |
| **Итог** | **56,212** | 3,125 | 28,532 | 16,255 | 0 | -136 | **8,164** | пик 700 |

##### Неделя, сводка по суткам


###### A. Без батареи

| День | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 7,283 | 22,566 | 17,300 | 0 | 0 | 6,967 | 914 | 100.0 |
| Вт | 54,170 | 2,039 | 25,164 | 19,669 | 0 | 0 | 7,298 | 714 | 100.0 |
| Ср | 53,724 | 1,067 | 27,540 | 17,545 | 0 | 0 | 7,572 | 670 | 100.0 |
| Чт | 54,139 | 4,150 | 28,563 | 15,235 | 0 | 0 | 6,190 | 729 | 100.0 |
| Пт | 53,065 | 4,018 | 24,958 | 19,382 | 0 | 0 | 4,707 | 694 | 100.0 |
| Сб | 48,266 | 5,211 | 20,172 | 18,366 | 0 | 0 | 4,517 | 813 | 100.0 |
| Вс | 48,180 | 0 | 28,322 | 17,161 | 0 | 0 | 2,697 | 472 | 100.0 |
| **Итог** | **365,658** | 23,767 | 177,284 | 124,658 | 0 | -0 | **39,948** | **914** | |

###### B. С батареей, арбитраж

| День | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 10,384 | 23,590 | 20,417 | 4,242 | -3,574 | 391 | 248 | 89.8 |
| Вт | 54,170 | 14,516 | 24,726 | 15,055 | 5,314 | -4,668 | 519 | 271 | 90.0 |
| Ср | 53,724 | 11,606 | 25,052 | 16,276 | 5,173 | -4,457 | 1,506 | 1,359 | 91.9 |
| Чт | 54,139 | 12,448 | 24,849 | 16,079 | 5,438 | -4,867 | 1,334 | 1,334 | 90.3 |
| Пт | 53,065 | 10,447 | 24,938 | 17,262 | 4,648 | -3,589 | 1,477 | 1,296 | 100.0 |
| Сб | 48,266 | 8,135 | 21,152 | 18,374 | 2,250 | -2,683 | 171 | 171 | 86.4 |
| Вс | 48,180 | 5,142 | 25,025 | 16,241 | 2,758 | -1,723 | 2,806 | 1,395 | 100.0 |
| **Итог** | **365,658** | 72,680 | 169,332 | 119,703 | 29,822 | -25,561 | **8,204** | **1,395** | |

###### C. С батареей, срез пика 700 кВт

| День | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 7,288 | 22,566 | 17,306 | 12 | -214 | 6,753 | 700 | 95.9 |
| Вт | 54,170 | 12,354 | 21,311 | 16,036 | 4,245 | -3,522 | 5,193 | 700 | 100.0 |
| Ср | 53,724 | 0 | 27,540 | 18,645 | 0 | 0 | 7,539 | 670 | 100.0 |
| Чт | 54,139 | 4,159 | 28,573 | 15,244 | 28 | -54 | 6,137 | 700 | 99.4 |
| Пт | 53,065 | 4,034 | 24,958 | 19,399 | 33 | 0 | 4,707 | 694 | 100.0 |
| Сб | 48,266 | 5,211 | 20,238 | 18,427 | 128 | -113 | 4,405 | 700 | 100.0 |
| Вс | 48,180 | 0 | 28,322 | 17,161 | 0 | 0 | 2,697 | 472 | 100.0 |
| **Итог** | **365,658** | 33,047 | 173,507 | 122,218 | 4,446 | -3,902 | **37,430** | **700** | |

##### Неделя, полный почасовой профиль (168 часов)


###### A. Без батареи

| День | Час | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 1 | 1344 | 0 | 1200 | 0 | 0 | 0 | 144 | 100.0 |
| Пн | 2 | 1395 | 0 | 1200 | 0 | 0 | 0 | 195 | 100.0 |
| Пн | 3 | 1348 | 0 | 1200 | 0 | 0 | 0 | 148 | 100.0 |
| Пн | 4 | 1407 | 0 | 1200 | 0 | 0 | 0 | 207 | 100.0 |
| Пн | 5 | 1700 | 0 | 1200 | 0 | 0 | 0 | 500 | 100.0 |
| Пн | 6 | 1998 | 984 | 0 | 1014 | 0 | 0 | 0 | 100.0 |
| Пн | 7 | 2433 | 1067 | 0 | 1100 | 0 | 0 | 266 | 100.0 |
| Пн | 8 | 2583 | 1067 | 0 | 1100 | 0 | 0 | 416 | 100.0 |
| Пн | 9 | 2735 | 0 | 1200 | 1100 | 0 | 0 | 435 | 100.0 |
| Пн | 10 | 2547 | 0 | 1200 | 1100 | 0 | 0 | 247 | 100.0 |
| Пн | 11 | 2853 | 0 | 1200 | 1100 | 0 | 0 | 553 | 100.0 |
| Пн | 12 | 2499 | 0 | 1200 | 1100 | 0 | 0 | 199 | 100.0 |
| Пн | 13 | 2564 | 0 | 1200 | 1100 | 0 | 0 | 264 | 100.0 |
| Чт | 8 | 2709 | 0 | 1200 | 1100 | 0 | 0 | 409 | 100.0 |
| Чт | 9 | 2646 | 0 | 1200 | 1100 | 0 | 0 | 346 | 100.0 |
| Чт | 10 | 3024 | 0 | 1200 | 1100 | 0 | 0 | 724 | 100.0 |
| Чт | 11 | 3115 | 987 | 1110 | 1018 | 0 | 0 | 0 | 100.0 |
| Чт | 12 | 2397 | 1067 | 1200 | 0 | 0 | 0 | 130 | 100.0 |
| Чт | 13 | 2799 | 1067 | 1200 | 0 | 0 | 0 | 532 | 100.0 |
| Чт | 14 | 2187 | 1029 | 1157 | 0 | 0 | 0 | 0 | 100.0 |
| Чт | 15 | 2632 | 0 | 1200 | 1100 | 0 | 0 | 332 | 100.0 |
| Чт | 16 | 2401 | 0 | 1200 | 1100 | 0 | 0 | 101 | 100.0 |
| Чт | 17 | 2648 | 0 | 1200 | 1100 | 0 | 0 | 348 | 100.0 |
| Чт | 18 | 2716 | 0 | 1200 | 1100 | 0 | 0 | 416 | 100.0 |
| Чт | 19 | 2587 | 0 | 1200 | 1100 | 0 | 0 | 287 | 100.0 |
| Чт | 20 | 2655 | 0 | 1200 | 1100 | 0 | 0 | 355 | 100.0 |
| Чт | 21 | 2535 | 0 | 1200 | 1100 | 0 | 0 | 235 | 100.0 |
| Чт | 22 | 2190 | 0 | 1142 | 1047 | 0 | 0 | 0 | 100.0 |
| Чт | 23 | 1929 | 0 | 1200 | 0 | 0 | 0 | 729 | 100.0 |
| Чт | 24 | 1583 | 0 | 1200 | 0 | 0 | 0 | 383 | 100.0 |
| Пт | 1 | 1336 | 0 | 1200 | 0 | 0 | 0 | 136 | 100.0 |
| Пт | 2 | 1245 | 0 | 1200 | 0 | 0 | 0 | 45 | 100.0 |
| Пт | 3 | 1405 | 0 | 1200 | 0 | 0 | 0 | 205 | 100.0 |
| Пт | 4 | 1296 | 0 | 1200 | 0 | 0 | 0 | 96 | 100.0 |
| Пт | 5 | 1747 | 0 | 1200 | 0 | 0 | 0 | 547 | 100.0 |
| Пт | 6 | 1963 | 967 | 0 | 997 | 0 | 0 | 0 | 100.0 |
| Пт | 7 | 1984 | 977 | 0 | 1007 | 0 | 0 | 0 | 100.0 |
| Пт | 8 | 2544 | 1067 | 0 | 1100 | 0 | 0 | 377 | 100.0 |
| Пт | 9 | 3177 | 1007 | 1132 | 1038 | 0 | 0 | 0 | 100.0 |
| Пт | 10 | 2757 | 0 | 1200 | 1100 | 0 | 0 | 457 | 100.0 |
| Пт | 11 | 2850 | 0 | 1200 | 1100 | 0 | 0 | 550 | 100.0 |
| Пт | 12 | 2564 | 0 | 1200 | 1100 | 0 | 0 | 264 | 100.0 |
| Пт | 13 | 2303 | 0 | 1200 | 1100 | 0 | 0 | 3 | 100.0 |
| Пт | 14 | 2397 | 0 | 1200 | 1100 | 0 | 0 | 97 | 100.0 |
| Пт | 15 | 2397 | 0 | 1200 | 1100 | 0 | 0 | 97 | 100.0 |
| Пт | 16 | 2204 | 0 | 1150 | 1054 | 0 | 0 | 0 | 100.0 |
| Вс | 2 | 1220 | 0 | 1200 | 0 | 0 | 0 | 20 | 100.0 |
| Вс | 3 | 1273 | 0 | 1200 | 0 | 0 | 0 | 73 | 100.0 |
| Вс | 4 | 1211 | 0 | 1200 | 0 | 0 | 0 | 11 | 100.0 |
| Вс | 5 | 1395 | 0 | 1200 | 0 | 0 | 0 | 195 | 100.0 |
| Вс | 6 | 1664 | 0 | 1200 | 0 | 0 | 0 | 464 | 100.0 |
| Вс | 7 | 2159 | 0 | 1127 | 1033 | 0 | 0 | 0 | 100.0 |
| Вс | 8 | 2200 | 0 | 1148 | 1052 | 0 | 0 | 0 | 100.0 |
| Вс | 9 | 2553 | 0 | 1200 | 1100 | 0 | 0 | 253 | 100.0 |
| Вс | 10 | 2388 | 0 | 1200 | 1100 | 0 | 0 | 88 | 100.0 |
| Вс | 11 | 2400 | 0 | 1200 | 1100 | 0 | 0 | 100 | 100.0 |
| Вс | 12 | 2530 | 0 | 1200 | 1100 | 0 | 0 | 230 | 100.0 |
| Вс | 13 | 2208 | 0 | 1152 | 1056 | 0 | 0 | 0 | 100.0 |
| Вс | 14 | 2217 | 0 | 1157 | 1060 | 0 | 0 | 0 | 100.0 |
| Вс | 15 | 2140 | 0 | 1117 | 1024 | 0 | 0 | 0 | 100.0 |
| Вс | 16 | 2111 | 0 | 1102 | 1010 | 0 | 0 | 0 | 100.0 |
| Вс | 17 | 2466 | 0 | 1200 | 1100 | 0 | 0 | 166 | 100.0 |
| Вс | 18 | 2365 | 0 | 1200 | 1100 | 0 | 0 | 65 | 100.0 |
| Вс | 19 | 2406 | 0 | 1200 | 1100 | 0 | 0 | 106 | 100.0 |
| Вс | 20 | 2373 | 0 | 1200 | 1100 | 0 | 0 | 73 | 100.0 |
| Вс | 21 | 2500 | 0 | 1200 | 1100 | 0 | 0 | 200 | 100.0 |
| Вс | 22 | 2147 | 0 | 1120 | 1027 | 0 | 0 | 0 | 100.0 |
| Вс | 23 | 1672 | 0 | 1200 | 0 | 0 | 0 | 472 | 100.0 |
| Вс | 24 | 1370 | 0 | 1200 | 0 | 0 | 0 | 170 | 100.0 |

###### B. С батареей, арбитраж

| День | Час | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 1 | 1344 | 0 | 1200 | 0 | 0 | 0 | 144 | 86.8 |
| Пн | 2 | 1395 | 0 | 1093 | 1002 | 700 | 0 | 0 | 98.7 |
| Пн | 3 | 1348 | 0 | 0 | 1100 | 0 | 0 | 248 | 98.7 |
| Пн | 4 | 1407 | 0 | 0 | 1100 | 0 | -307 | 0 | 92.8 |
| Пн | 5 | 1700 | 1046 | 0 | 1078 | 424 | 0 | 0 | 100.0 |
| Пн | 6 | 1998 | 984 | 0 | 1014 | 0 | -0 | 0 | 100.0 |
| Пн | 7 | 2433 | 0 | 1200 | 1100 | 0 | -133 | 0 | 97.4 |
| Пн | 8 | 2583 | 0 | 1200 | 1100 | 0 | -283 | 0 | 91.9 |
| Чт | 2 | 1186 | 0 | 1200 | 0 | 14 | 0 | 0 | 87.4 |
| Чт | 3 | 1272 | 971 | 0 | 1001 | 700 | 0 | 0 | 99.3 |
| Чт | 4 | 1334 | 0 | 0 | 0 | 0 | 0 | 1334 | 99.3 |
| Чт | 5 | 1589 | 0 | 0 | 0 | 0 | -1589 | 0 | 68.5 |
| Чт | 6 | 2237 | 0 | 1200 | 0 | 0 | -1037 | 0 | 48.4 |
| Чт | 7 | 2322 | 0 | 1200 | 1100 | 0 | -22 | 0 | 48.0 |
| Чт | 8 | 2709 | 1067 | 1200 | 1100 | 658 | 0 | 0 | 59.2 |
| Чт | 9 | 2646 | 1060 | 1193 | 1093 | 700 | 0 | 0 | 71.1 |
| Чт | 10 | 3024 | 1067 | 1200 | 1100 | 343 | 0 | 0 | 77.0 |
| Чт | 11 | 3115 | 1067 | 1200 | 1100 | 252 | 0 | 0 | 81.3 |
| Чт | 12 | 2397 | 981 | 1104 | 1012 | 700 | 0 | 0 | 93.2 |
| Чт | 13 | 2799 | 1013 | 1140 | 1045 | 398 | 0 | 0 | 100.0 |
| Чт | 14 | 2187 | 0 | 1141 | 1046 | 0 | 0 | 0 | 100.0 |
| Чт | 15 | 2632 | 0 | 1200 | 1100 | 0 | -332 | 0 | 93.6 |
| Чт | 16 | 2401 | 0 | 1200 | 1100 | 0 | -101 | 0 | 91.6 |
| Чт | 17 | 2648 | 995 | 1119 | 1026 | 492 | 0 | 0 | 100.0 |
| Чт | 18 | 2716 | 1067 | 1200 | 0 | 0 | -449 | 0 | 91.3 |
| Чт | 19 | 2587 | 1067 | 1200 | 0 | 0 | -320 | 0 | 85.1 |
| Чт | 20 | 2655 | 1067 | 1200 | 0 | 0 | -388 | 0 | 77.6 |
| Чт | 21 | 2535 | 1025 | 1153 | 1057 | 700 | 0 | 0 | 89.5 |
| Чт | 22 | 2190 | 0 | 1200 | 1100 | 110 | 0 | 0 | 91.4 |
| Чт | 23 | 1929 | 0 | 1200 | 1100 | 371 | 0 | 0 | 97.7 |
| Чт | 24 | 1583 | 0 | 1200 | 0 | 0 | -383 | 0 | 90.3 |
| Пт | 1 | 1336 | 0 | 1200 | 0 | 0 | 0 | 136 | 90.3 |
| Пт | 2 | 1245 | 0 | 1200 | 0 | 0 | 0 | 45 | 90.3 |
| Пт | 3 | 1405 | 972 | 0 | 1002 | 569 | 0 | 0 | 100.0 |
| Пт | 4 | 1296 | 0 | 0 | 0 | 0 | 0 | 1296 | 100.0 |
| Пт | 5 | 1747 | 0 | 0 | 0 | 0 | -1747 | 0 | 66.1 |
| Пт | 6 | 1963 | 0 | 1200 | 0 | 0 | -763 | 0 | 51.3 |
| Пт | 7 | 1984 | 0 | 1200 | 1100 | 316 | 0 | 0 | 56.7 |
| Пт | 8 | 2544 | 1028 | 1156 | 1060 | 700 | 0 | 0 | 68.7 |
| Пт | 9 | 3177 | 1067 | 1200 | 1100 | 190 | 0 | 0 | 71.9 |
| Пт | 10 | 2757 | 1067 | 1200 | 1100 | 610 | 0 | 0 | 82.3 |
| Пт | 11 | 2850 | 1067 | 1200 | 1100 | 517 | 0 | 0 | 91.1 |
| Пт | 12 | 2564 | 978 | 1100 | 1008 | 521 | 0 | 0 | 100.0 |
| Пт | 13 | 2303 | 0 | 1200 | 1100 | 0 | -3 | 0 | 99.9 |
| Пт | 14 | 2397 | 0 | 1200 | 1100 | 0 | -97 | 0 | 98.1 |
| Пт | 15 | 2397 | 0 | 1200 | 1100 | 0 | -97 | 0 | 96.2 |
| Пт | 16 | 2204 | 0 | 1200 | 1100 | 96 | 0 | 0 | 97.8 |
| Пт | 17 | 2618 | 0 | 1200 | 1100 | 0 | -318 | 0 | 91.6 |
| Пт | 18 | 2994 | 1067 | 1200 | 1100 | 373 | 0 | 0 | 98.0 |
| Пт | 19 | 2563 | 1067 | 1200 | 0 | 0 | -296 | 0 | 92.3 |
| Пт | 20 | 2453 | 1067 | 1200 | 0 | 0 | -186 | 0 | 88.7 |
| Пт | 21 | 2348 | 1067 | 1200 | 0 | 0 | -81 | 0 | 87.1 |
| Пт | 22 | 2230 | 0 | 1200 | 1100 | 70 | 0 | 0 | 88.3 |
| Пт | 23 | 2132 | 0 | 1200 | 1100 | 168 | 0 | 0 | 91.1 |
| Пт | 24 | 1555 | 0 | 1082 | 992 | 519 | 0 | 0 | 100.0 |
| Сб | 1 | 1048 | 0 | 0 | 1048 | 0 | 0 | 0 | 100.0 |
| Сб | 2 | 1271 | 0 | 0 | 1100 | 0 | 0 | 171 | 100.0 |
| Сб | 3 | 1083 | 0 | 0 | 1083 | 0 | 0 | 0 | 100.0 |
| Сб | 4 | 1117 | 0 | 1117 | 0 | 0 | 0 | 0 | 100.0 |
| Сб | 5 | 1488 | 0 | 1200 | 0 | 0 | -288 | 0 | 94.4 |
| Сб | 6 | 1746 | 975 | 1097 | 0 | 327 | 0 | 0 | 100.0 |
| Сб | 7 | 2013 | 991 | 0 | 1022 | 0 | 0 | 0 | 100.0 |
| Сб | 8 | 2286 | 1067 | 0 | 1100 | 0 | -119 | 0 | 97.7 |
| Сб | 9 | 2776 | 1067 | 0 | 1100 | 0 | -609 | 0 | 85.9 |
| Сб | 10 | 2404 | 983 | 1106 | 1014 | 700 | 0 | 0 | 97.8 |
| Сб | 11 | 2582 | 0 | 1200 | 1100 | 0 | -282 | 0 | 92.4 |
| Сб | 12 | 2431 | 0 | 1200 | 1100 | 0 | -131 | 0 | 89.8 |
| Сб | 13 | 2201 | 0 | 1200 | 1100 | 99 | 0 | 0 | 91.5 |
| Сб | 14 | 2274 | 0 | 1200 | 1100 | 26 | 0 | 0 | 91.9 |
| Сб | 15 | 2455 | 0 | 1200 | 1100 | 0 | -155 | 0 | 88.9 |
| Сб | 16 | 1955 | 0 | 1200 | 1100 | 345 | 0 | 0 | 94.8 |
| Сб | 17 | 2325 | 0 | 1200 | 1100 | 0 | -25 | 0 | 94.3 |
| Сб | 18 | 2653 | 0 | 1200 | 1100 | 0 | -353 | 0 | 87.5 |
| Сб | 19 | 2198 | 0 | 1200 | 1100 | 102 | 0 | 0 | 89.2 |
| Сб | 20 | 2454 | 978 | 1100 | 1008 | 631 | 0 | 0 | 100.0 |
| Сб | 21 | 2284 | 1067 | 1200 | 0 | 0 | -17 | 0 | 99.7 |
| Сб | 22 | 2120 | 1007 | 1132 | 0 | 19 | 0 | 0 | 100.0 |
| Сб | 23 | 1576 | 0 | 1200 | 0 | 0 | -376 | 0 | 92.7 |
| Сб | 24 | 1528 | 0 | 1200 | 0 | 0 | -328 | 0 | 86.4 |
| Вс | 1 | 1209 | 0 | 1200 | 0 | 0 | 0 | 9 | 86.4 |
| Вс | 2 | 1220 | 0 | 1200 | 0 | 0 | 0 | 20 | 86.4 |
| Вс | 3 | 1273 | 971 | 0 | 1001 | 700 | 0 | 0 | 98.3 |
| Вс | 4 | 1211 | 0 | 0 | 0 | 0 | 0 | 1211 | 98.3 |
| Вс | 5 | 1395 | 0 | 0 | 0 | 0 | 0 | 1395 | 98.3 |
| Вс | 6 | 1664 | 0 | 1200 | 0 | 0 | -464 | 0 | 89.3 |
| Вс | 7 | 2159 | 0 | 1200 | 1100 | 141 | 0 | 0 | 91.7 |
| Вс | 8 | 2200 | 0 | 1200 | 1100 | 100 | 0 | 0 | 93.4 |
| Вс | 9 | 2553 | 0 | 1200 | 1100 | 0 | -253 | 0 | 88.5 |
| Вс | 10 | 2388 | 970 | 1091 | 1000 | 672 | 0 | 0 | 100.0 |
| Вс | 11 | 2400 | 1067 | 1200 | 0 | 0 | -133 | 0 | 97.4 |
| Вс | 12 | 2530 | 1067 | 1200 | 0 | 0 | -263 | 0 | 92.3 |
| Вс | 13 | 2208 | 1067 | 1200 | 0 | 58 | 0 | 0 | 93.3 |
| Вс | 14 | 2217 | 0 | 1200 | 1100 | 83 | 0 | 0 | 94.7 |
| Вс | 15 | 2140 | 0 | 1200 | 1100 | 160 | 0 | 0 | 97.5 |
| Вс | 16 | 2111 | 0 | 1179 | 1081 | 149 | 0 | 0 | 100.0 |
| Вс | 17 | 2466 | 0 | 1200 | 1100 | 0 | -166 | 0 | 96.8 |
| Вс | 18 | 2365 | 0 | 1200 | 1100 | 0 | -65 | 0 | 95.5 |
| Вс | 19 | 2406 | 0 | 1200 | 1100 | 0 | -106 | 0 | 93.5 |
| Вс | 20 | 2373 | 0 | 1200 | 1100 | 0 | -73 | 0 | 92.0 |
| Вс | 21 | 2500 | 0 | 1200 | 1100 | 0 | -200 | 0 | 88.2 |
| Вс | 22 | 2147 | 0 | 1200 | 1100 | 153 | 0 | 0 | 90.8 |
| Вс | 23 | 1672 | 0 | 1155 | 1059 | 541 | 0 | 0 | 100.0 |
| Вс | 24 | 1370 | 0 | 1200 | 0 | 0 | 0 | 170 | 100.0 |

###### C. С батареей, срез пика 700 кВт

| День | Час | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
|
| Пт | 6 | 1963 | 983 | 0 | 1014 | 33 | 0 | 0 | 100.0 |
| Пт | 7 | 1984 | 977 | 0 | 1007 | 0 | 0 | 0 | 100.0 |
| Пт | 8 | 2544 | 1067 | 0 | 1100 | 0 | 0 | 377 | 100.0 |
| Пт | 9 | 3177 | 1007 | 1132 | 1038 | 0 | 0 | 0 | 100.0 |
| Пт | 10 | 2757 | 0 | 1200 | 1100 | 0 | 0 | 457 | 100.0 |
| Пт | 11 | 2850 | 0 | 1200 | 1100 | 0 | 0 | 550 | 100.0 |
| Пт | 12 | 2564 | 0 | 1200 | 1100 | 0 | 0 | 264 | 100.0 |
| Пт | 13 | 2303 | 0 | 1200 | 1100 | 0 | 0 | 3 | 100.0 |
| Пт | 14 | 2397 | 0 | 1200 | 1100 | 0 | 0 | 97 | 100.0 |
| Пт | 15 | 2397 | 0 | 1200 | 1100 | 0 | 0 | 97 | 100.0 |
| Пт | 16 | 2204 | 0 | 1150 | 1054 | 0 | 0 | 0 | 100.0 |
| Пт | 17 | 2618 | 0 | 1200 | 1100 | 0 | 0 | 318 | 100.0 |
| Пт | 18 | 2994 | 0 | 1200 | 1100 | 0 | 0 | 694 | 100.0 |
| Пт | 19 | 2563 | 0 | 1200 | 1100 | 0 | 0 | 263 | 100.0 |
| Пт | 20 | 2453 | 0 | 1200 | 1100 | 0 | 0 | 153 | 100.0 |
| Пт | 21 | 2348 | 0 | 1200 | 1100 | 0 | 0 | 48 | 100.0 |
| Пт | 22 | 2230 | 0 | 1163 | 1066 | 0 | 0 | 0 | 100.0 |
| Пт | 23 | 2132 | 0 | 1113 | 1020 | 0 | 0 | 0 | 100.0 |
| Пт | 24 | 1555 | 0 | 1200 | 0 | 0 | 0 | 355 | 100.0 |
| Сб | 1 | 1048 | 1048 | 0 | 0 | 0 | 0 | 0 | 100.0 |
| Сб | 2 | 1271 | 1067 | 0 | 0 | 0 | 0 | 204 | 100.0 |
| Сб | 3 | 1083 | 0 | 0 | 1083 | 0 | 0 | 0 | 100.0 |
| Сб | 4 | 1117 | 0 | 0 | 1100 | 0 | 0 | 17 | 100.0 |
| Сб | 5 | 1488 | 0 | 1200 | 0 | 0 | 0 | 288 | 100.0 |
| Сб | 6 | 1746 | 0 | 1200 | 0 | 0 | 0 | 546 | 100.0 |
| Сб | 7 | 2013 | 0 | 1200 | 0 | 0 | -113 | 700 | 97.8 |
| Сб | 8 | 2286 | 0 | 1200 | 1100 | 14 | 0 | 0 | 98.1 |
| Сб | 9 | 2776 | 0 | 1200 | 1100 | 0 | 0 | 476 | 98.1 |
| Сб | 10 | 2404 | 0 | 1200 | 1100 | 0 | 0 | 104 | 98.1 |
| Сб | 11 | 2582 | 0 | 1200 | 1100 | 0 | 0 | 282 | 98.1 |
| Сб | 12 | 2431 | 0 | 1200 | 1100 | 0 | 0 | 131 | 98.1 |
| Сб | 13 | 2201 | 0 | 1200 | 1100 | 99 | 0 | 0 | 99.8 |
| Сб | 14 | 2274 | 0 | 1194 | 1094 | 15 | 0 | 0 | 100.0 |
| Вс | 24 | 1370 | 0 | 1200 | 0 | 0 | 0 | 170 | 100.0 |

##### Сводная таблица

| Показатель | A. Без батареи | B. Арбитраж | C. Срез пика |
|---|---:|---:|---:|
| **ТИПИЧНЫЕ СУТКИ** | | | |
| Нагрузка завода, кВт·ч | 56,212 | 56,212 | 56,212 |
| Выработка КГУ, кВт·ч | 47,912 | 57,168 | 47,912 |
| Заряд BESS, кВт·ч | 0 | 5,151 | 0 |
| Разряд BESS, кВт·ч | 0 | 3,888 | 136 |
| Закуп из сети, кВт·ч | 8,300 | 307 | 8,164 |
| Пик закупа, кВт | 836 | 307 | 700 |
| Затраты, ₸ | 1,552,066 | 1,276,094 | 1,543,882 |
| Экономия к варианту A, ₸ | 0 | 275,973 | 8,184 |
| **НЕДЕЛЯ** | | | |
| Нагрузка завода, кВт·ч | 365,658 | 365,658 | 365,658 |
| Выработка КГУ, кВт·ч | 325,710 | 361,715 | 328,772 |
| Заряд BESS, кВт·ч | 0 | 29,822 | 4,446 |
| Разряд BESS, кВт·ч | 0 | 25,562 | 3,902 |
| Закуп из сети, кВт·ч | 39,948 | 8,204 | 37,430 |
| Пик закупа, кВт | 914 | 1,395 | 700 |
| Затраты, ₸ | 9,562,503 | 8,449,963 | 9,478,771 |
| Экономия к варианту A, ₸ | 0 | 1,112,540 | 83,732 |
| **ГОД** | | | |
| Выработка КГУ, МВт·ч | 18,201 | 21,099 | 18,525 |
| Закуп из сети, МВт·ч | 2,823 | 133 | 2,518 |
| Доля сети в питании, % | 13.4 | 0.6 | 12.0 |
| Годовой максимум закупа, кВт | 3,200 | 3,517 | 1,294 |
| Сумма 12 месячных максимумов, кВт | 30,306 | 18,709 | 9,441 |
| Пусков КГУ за год | 1,477 | 1,913 | 1,486 |
| Затраты, ₸/год | 591,937,472 | 500,844,166 | 580,897,459 |
| Экономия к варианту A, ₸/год | 0 | 91,093,306 | 11,040,013 |
| Экономия, % | 0.0 | 15.4 | 1.9 |
| Простая окупаемость, лет | nan | 4.5 | 37.0 |


CAPEX BESS: $850,000 = 408,000,000 ₸ (курс 480 ₸/$).

##### График диспетчеризации типичных суток

![Диспетчеризация типичных суток](chart_day_3chp.png)

**Как читать.** Столбики (прямоугольники) - мощность за час. Прямоугольник, а не плавная линия, потому что шаг симуляции часовой: внутри часа мощность считается постоянной, и рисовать её кривой было бы враньём о разрешении модели.

- выше нуля - источники: КГУ по агрегатам, разряд BESS, закуп из сети
- ниже нуля - заряд BESS: мощность, уходящая из системы в накопитель
- линия с точками - фактическая нагрузка завода, точка в середине своего часа
- тонкая линия справа - SoC накопителя, %

Проверка баланса: сумма столбиков выше нуля минус столбики ниже нуля в каждом часу ложится ровно на линию нагрузки.

---

# Приложение G. Выводы прогонов


## G.1 Сравнение стратегий и optimality gap


```text
Единицы: MW / MWh
Удельный расход на номинале  sfc(P_NOM) = 257.5000 м3/MWh
Удельный расход на минимуме  sfc(P_MIN) = 277.7500 м3/MWh
Порог перелома стратегий  sfc(P_NOM)/RTE = 292.6136 м3/MWh
Суточное потребление завода: 23.370 MWh

Стратегия                         у.е./сут    газ,м3    сброс  пуски   gap,%
----------------------------------------------------------------------------
1. Load Following (без BESS)       1304.00    6120.0    0.000    2.0    5.03
2. Cycle Charging (EMS из ТЗ)      1627.09    6411.8    0.734    6.9   31.06
3. Гибрид (порог по sfc)           1304.00    6120.0    0.000    2.0    5.03
4. MILP-эталон (Optimal)           1241.52    6096.5    0.000    0.0    0.00
----------------------------------------------------------------------------
2. Cycle Charging (EMS из ТЗ)    к базе:  -323.09 у.е./сут       -117929 у.е./год
3. Гибрид (порог по sfc)         к базе:    +0.00 у.е./сут            +0 у.е./год
4. MILP-эталон (Optimal)         к базе:   +62.47 у.е./сут        +22802 у.е./год


ТАБЛИЦА 2. Cycle Charging (EMS из ТЗ), установившиеся сутки

 Час   Нагр.  Разряд   SoC,%   КГУ    КГУ1    КГУ2    КГУ3   Заряд   Газ,м3    у.е.
-----------------------------------------------------------------------------------
   1   0.750   0.750    84.0  Выкл   0.000   0.000   0.000   0.000      0.0    3.75
   2   0.720   0.720    68.7  Выкл   0.000   0.000   0.000   0.000      0.0    3.60
   3   0.700   0.700    53.7  Выкл   0.000   0.000   0.000   0.000      0.0    3.50
   4   0.700   0.700    38.8  Выкл   0.000   0.000   0.000   0.000      0.0    3.50
   5   0.730   0.000    53.3   Вкл   0.500   0.500   0.500   0.770    386.2  201.10
   6   0.800   0.000    66.4   Вкл   0.500   0.500   0.500   0.700    386.2   80.75
   7   0.900   0.000    77.6   Вкл   0.500   0.500   0.500   0.600    386.2   80.25
   8   1.050   0.000    86.1   Вкл   0.500   0.500   0.500   0.450    386.2   79.50
   9   1.180   0.000    92.1   Вкл   0.500   0.500   0.500   0.320    386.2   78.85
  10   1.280   0.000    96.2   Вкл   0.500   0.500   0.500   0.220    386.2   78.35
  11   1.300   0.000   100.0   Вкл   0.500   0.500   0.500   0.200    386.2   78.25
  12   1.220   0.000   100.0   Вкл   0.500   0.500   0.500   0.001    386.2   77.26
  13   0.850   0.850    81.9  Выкл   0.000   0.000   0.000   0.000      0.0    4.25
  14   0.800   0.800    64.8  Выкл   0.000   0.000   0.000   0.000      0.0    4.00
  15   1.150   1.150    40.3  Выкл   0.000   0.000   0.000   0.000      0.0    5.75
  16   1.250   0.000    45.0   Вкл   0.500   0.500   0.500   0.250    386.2  198.50
  17   1.300   0.000    48.7   Вкл   0.500   0.500   0.500   0.200    386.2   78.25
  18   1.250   0.000    53.4   Вкл   0.500   0.500   0.500   0.250    386.2   78.50
  19   1.100   0.000    60.9   Вкл   0.500   0.500   0.500   0.400    386.2   79.25
  20   1.000   0.000    70.3   Вкл   0.500   0.500   0.500   0.500    386.2   79.75
  21   0.920   0.000    81.2   Вкл   0.500   0.500   0.500   0.580    386.2   80.15
  22   0.850   0.000    93.4   Вкл   0.500   0.500   0.500   0.650    386.2   80.50
  23   0.800   0.000   100.0   Вкл   0.500   0.500   0.500   0.352    386.2   79.01
  24   0.770   0.770    83.6  Выкл   0.000   0.000   0.000   0.000      0.0    3.85

==========

Порог перелома: батарея выгодна, только если sfc(загрузка) > sfc(P_NOM)/RTE

 P_MIN, % ном.  P_MIN, MW  sfc(P_MIN)    порог  BESS полезна?
-------------------------------------------------------------
           20%      0.100      347.50   292.61             ДА
           25%      0.125      323.88   292.61             ДА
           30%      0.150      308.25   292.61             ДА
           35%      0.175      297.20   292.61             ДА
           40%      0.200      289.00   292.61            нет
           45%      0.225      282.71   292.61            нет
           50%      0.250      277.75   292.61            нет


Прогон трёх стратегий при P_MIN = 20% номинала (как в исходном ТЗ):

Стратегия                       у.е./сут    сброс   gap,%   к базе,у.е./год
---------------------------------------------------------------------------
Load Following (без BESS)        1304.00    0.000    5.03                 0
Cycle Charging (EMS из ТЗ)       1627.09    0.734   31.06           -117929
Гибрид (порог по sfc)            1304.00    0.000    5.03                 0
MILP-эталон                      1241.52    0.000    0.00             22802

==========

Суточное потребление: 16.790 MWh
Ночная загрузка одного КГУ: 0.150 MW -> sfc = 308.2 м3/MWh
Порог sfc(P_NOM)/RTE = 292.6  -> прямая выработка НЕВЫГОДНА

Стратегия                       у.е./сут    газ,м3   сброс   gap,%   к базе,у.е./год
------------------------------------------------------------------------------------
Load Following (без BESS)        1047.01    4435.1   0.000   13.24                 0
Cycle Charging (EMS из ТЗ)       1294.35    5021.2   2.070   40.00            -90278
Гибрид (порог по sfc)            1054.35    4450.9   0.000   14.04             -2677
MILP-эталон                       924.56    4500.0   0.000    0.00             44695
```


## G.2 Исправленный EMS, Monte Carlo и годовая экстраполяция


```text
Единицы: kW / kWh

КАЛИБРОВКА ТОПЛИВНОЙ КРИВОЙ (по двум точкам из ТЗ)
  fuel(P) = 12.500 + 0.22500 * P   м3/ч
  sfc(100% = 500) = 0.2500 м3/kWh
  sfc( 20% = 100) = 0.3500  -> 1.40x  (проверка: должно быть 1.40)

ПОРОГ ПЕРЕЛОМА СТРАТЕГИЙ
  sfc(P_NOM)/RTE = 0.2841 м3/kWh
  P_BREAKEVEN    = 211.5 kW (42.3% номинала)
  Ниже этой загрузки выгоднее заряжать батарею, выше - питать напрямую.

MONTE CARLO, средний расход газа за сутки, м3 (в скобках СКО)
Сценарий              без BESS         EMS из ТЗ      порог по sfc  сброс без BESS   дефицит EMS
------------------------------------------------------------------------------------------------
nominal          2618.7 ±42.2      2553.6 ±316.3     2492.9 ±46.7             59.9           0.0
peak             9000.0 ±0.0       8998.4 ±27.6      9000.0 ±0.0               0.0           0.0
low              1920.4 ±19.6      1917.7 ±235.0     1921.4 ±26.2              0.0           0.0


ГОДОВАЯ ЭКСТРАПОЛЯЦИЯ
    scenario  days    gas_nb   gas_ems   gas_thr  spill_nb  spill_ems  deficit_ems  starts_ems
     nominal   200   523,747   510,729   498,573    11,983          6            0         603
        peak    65   585,000   584,896   585,000         0          0            0         195
         low   100   192,036   191,774   192,144         0          0            0         189
ИТОГО за год   365 1,300,783 1,287,399 1,275,717    11,983          6            0         987

                                 газ, м3/год        у.е./год        к базе
--------------------------------------------------------------------------
Без батареи                        1,300,783         260,157         0.00%
EMS из ТЗ                          1,287,399         257,480         1.03%
Порог по sfc                       1,275,717         255,143         1.93%

Экономия EMS из ТЗ:            2,677 у.е./год
Экономия порога по sfc:        5,013 у.е./год
Недоотпуск EMS из ТЗ:              0 kWh/год  <-- нагрузка, которую система НЕ ЗАКРЫЛА

==========

=== 1. СЦЕНАРИЙ РАЗГРУЗКИ 300 кВт: было / стало ===

без батареи          1904.5 м3/сут
EMS старый           1993.1 м3/сут   (хуже базы на +88.7)
EMS исправленный     1511.3 м3/сут   (к базе -393.1)

Число работающих КГУ в час, старый:  [0, 3]
Число работающих КГУ в час, новый:   [0, 1]


=== 2. ТУПИК В ПИКОВОМ ДНЕ: трое суток подряд ===

старый   SoC в конце 3-х суток:  46.7%  состояние: CHARGE
новый    SoC в конце 3-х суток:  23.0%  состояние: CHP_ONLY  состояния за сутки: ['CHP_ONLY']


=== 3. НОВАЯ ФИЗИКА: проверка на хаотичном дне ===

min-up-time: длительности непрерывной работы, ч -> [9]  (все должны быть >= 2)
паразитная нагрузка: 3.0 кВт*ч/ч = 72 кВт*ч/сут = 2.4% ёмкости в сутки
дефицит за сутки: 0.0 кВт*ч   сброс: 0.0 кВт*ч
максимальная невязка почасового баланса мощности: 0.00e+00 кВт  (должна быть ~0)


=== 4. PEAK SHAVING: нагрузка выше мощности разряда ===

нагрузка 1800 кВт, разряд батареи 1500 кВт, КГУ 300 кВт, дефицит 0 кВт
```


## G.3 Годовой прогон по десяти сценариям, два КГУ


```text
Генерация 10 годовых сценариев...
   1. Две смены, непрерывно        средняя 2.40 МВт   пик 5.50 МВт   год 21.02 ГВт*ч
   2. Три смены, ровный ритм       средняя 2.30 МВт   пик 5.20 МВт   год 20.15 ГВт*ч
   3. Одна дневная смена           средняя 2.10 МВт   пик 5.50 МВт   год 18.40 ГВт*ч
   4. Летний пик (охлаждение)      средняя 2.45 МВт   пик 5.60 МВт   год 21.46 ГВт*ч
   5. Зимний пик (отопление)       средняя 2.45 МВт   пик 5.60 МВт   год 21.46 ГВт*ч
   6. Частые короткие пики         средняя 2.25 МВт   пик 5.50 МВт   год 19.71 ГВт*ч
   7. Ровная базовая нагрузка      средняя 2.55 МВт   пик 4.60 МВт   год 22.34 ГВт*ч
   8. Останов на выходные          средняя 2.20 МВт   пик 5.50 МВт   год 19.27 ГВт*ч
   9. Кампанийное производство     средняя 2.15 МВт   пик 5.40 МВт   год 18.83 ГВт*ч
  10. Высокая хаотичность          средняя 2.30 МВт   пик 5.80 МВт   год 20.15 ГВт*ч

====================================================================================================
ГОДОВОЙ ПРОГОН ПО ВСЕМ 10 СЦЕНАРИЯМ
====================================================================================================
  # Сценарий                         без BESS, ₸       с BESS, ₸      экономия      %  окуп.,лет
------------------------------------------------------------------------------------------------
  1 Две смены, непрерывно            623,555,059     554,618,411    68,936,648   11.1        5.9
  2 Три смены, ровный ритм           529,803,440     504,004,047    25,799,393    4.9       15.8
  3 Одна дневная смена               741,229,882     673,666,576    67,563,306    9.1        6.0
  4 Летний пик (охлаждение)          682,180,061     620,403,442    61,776,619    9.1        6.6
  5 Зимний пик (отопление)           664,659,803     610,729,441    53,930,361    8.1        7.6
  6 Частые короткие пики             596,071,515     506,344,530    89,726,985   15.1        4.5
  7 Ровная базовая нагрузка          591,116,961     586,097,752     5,019,210    0.8       81.3
  8 Останов на выходные              682,172,540     636,492,330    45,680,210    6.7        8.9
  9 Кампанийное производство         585,644,521     528,799,154    56,845,367    9.7        7.2
 10 Высокая хаотичность              657,019,674     533,745,157   123,274,517   18.8        3.3
------------------------------------------------------------------------------------------------
    СРЕДНЕЕ ПО 10 СЦЕНАРИЯМ          635,345,346     575,490,084    59,855,262    9.3       14.7
    РАЗБРОС (мин..макс окуп.)                                                          3.3..81.3 

ПОДБОР УСТАВКИ СРЕЗА ПИКА (сценарий 1)
  уставка, кВт   сумма 12 макс., кВт   годовой макс.        затраты, ₸
           400                13,918           1,933       611,954,848
           600                13,184           1,403       611,487,912  <-- выбрано
           800                14,288           1,933       612,319,318
          1000                13,570           2,109       613,115,046
          1200                14,620           1,349       617,743,020
          1400                16,800           1,400       619,900,372
          1600                19,061           1,600       621,339,192
          1800                21,261           1,800       621,062,372
          2000                23,461           2,000       621,562,848

====================================================================================================
НЕДЕЛЯ, СВОДКА ПО СУТКАМ (сценарий 1, полные почасовые таблицы в week_tables.md)
====================================================================================================

БЕЗ БАТАРЕИ
 День   Нагрузка       КГУ    Заряд   Разряд      Сеть  Пик сети
   Пн     54,115    45,679        0        0     8,436       814
   Вт     54,170    45,837        0        0     8,333       774
   Ср     53,724    45,809        0        0     7,916       703
   Чт     54,139    46,737        0        0     7,402       848
   Пт     53,065    45,637        0        0     7,428       910
   Сб     48,266    38,656        0        0     9,610     1,453
   Вс     48,180    45,186        0        0     2,994       472
 ИТОГ    365,658   313,540        0        0    52,118     1,453

АРБИТРАЖ
 День   Нагрузка       КГУ    Заряд   Разряд      Сеть  Пик сети
   Пн     54,115    48,930    1,116    3,612     2,688       562
   Вт     54,170    51,207    3,589    3,224     3,328       714
   Ср     53,724    51,207    4,044    3,311     3,250       654
   Чт     54,139    51,207    4,078    3,674     3,336     1,037
   Пт     53,065    51,114    3,952    3,667     2,235       763
   Сб     48,266    49,878    5,245    1,988     1,646       813
   Вс     48,180    47,857    2,028    1,579       772       464
 ИТОГ    365,658   351,400   24,051   21,054    17,255     1,037

СРЕЗ ПИКА 600
 День   Нагрузка       КГУ    Заряд   Разряд      Сеть  Пик сети
   Пн     54,115    45,679        0        0     8,436       814
   Вт     54,170    45,837        0        0     8,333       774
   Ср     53,724    45,809        0        0     7,916       703
   Чт     54,139    46,776       39       34     7,367       814
   Пт     53,065    45,747      109       96     7,332       814
   Сб     48,266    38,863      207      182     9,428     1,271
   Вс     48,180    45,186        0        0     2,994       472
 ИТОГ    365,658   313,895      355      312    51,806     1,271

====================================================================================================
ПОМЕСЯЧНЫЙ МАКСИМУМ ЗАКУПА ИЗ СЕТИ, кВт (сценарий 1)
====================================================================================================
 Месяц    без BESS    арбитраж   срез пика   срез, %
     1       2,344       2,344         929      60.3
     2       3,233       3,233         929      71.3
     3       3,233       3,233       1,023      68.4
     4       3,233       3,233       1,050      67.5
     5       3,035       2,327         968      68.1
     6       1,461       1,190       1,271      13.0
     7       2,604       2,208       1,040      60.1
     8       2,878       2,878       1,061      63.1
     9       3,233       3,203         986      69.5
    10       3,233       3,112       1,299      59.8
    11       3,233       3,233       1,226      62.1
    12       3,233       3,233       1,403      56.6
   ГОД       3,233       3,233       1,403      56.6

====================================================================================================
КОГДА СРЕЗ ПИКА ВЫГОДНЕЕ АРБИТРАЖА
====================================================================================================
  Срез пика убирает 20,242 кВт суммарно по 12 месячным максимумам,
  но обходится дороже арбитража на 56,869,500 ₸/год.
  Порог: плата за мощность выше 2,810 ₸ за кВт максимума месяца.
  Сейчас DEMAND_CHARGE = 0 ₸/кВт/мес.

====================================================================================================
ЧУВСТВИТЕЛЬНОСТЬ К ТАРИФУ СЕТИ (сценарий 1, окупаемость BESS)
====================================================================================================
   Тариф сети, ₸/кВт*ч   спред к КГУ     экономия, ₸/год   окуп., лет
                    25           3.0           4,274,904         95.4
                    30           8.0          13,512,296         30.2
                    35          13.0          22,749,688         17.9
                    40          18.0          31,987,080         12.8
                    45          23.0          41,224,472          9.9
                    50          28.0          50,461,864          8.1
                    60          38.0          68,936,648          5.9
                    70          48.0          87,411,432          4.7

Файлы: 10 годовых CSV (load_year_01..10.csv) и week_tables.md в outputs.
```


## G.4 Финальный отчёт, два КГУ, консольный вывод


```text
============================================================================================
ТИПИЧНЫЕ СУТКИ: 01.09.2026 (Вт), потребление 56,212 кВт*ч при медиане 57,412
============================================================================================

A. Без батареи
| Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1373 | 0 | 1200 | 0 | 0 | 173 | 100.0 |
| 2 | 1374 | 0 | 1200 | 0 | 0 | 174 | 100.0 |
| 3 | 1477 | 0 | 1200 | 0 | 0 | 277 | 100.0 |
| 4 | 1473 | 0 | 1200 | 0 | 0 | 273 | 100.0 |
| 5 | 1688 | 0 | 1200 | 0 | 0 | 488 | 100.0 |
| 6 | 2180 | 1026 | 1154 | 0 | 0 | 0 | 100.0 |
| 7 | 2071 | 975 | 1096 | 0 | 0 | 0 | 100.0 |
| 8 | 2560 | 1067 | 1200 | 0 | 0 | 292 | 100.0 |
| 9 | 2799 | 1067 | 1200 | 0 | 0 | 532 | 100.0 |
| 10 | 2911 | 1067 | 1200 | 0 | 0 | 644 | 100.0 |
| 11 | 2967 | 1067 | 1200 | 0 | 0 | 700 | 100.0 |
| 12 | 2743 | 1067 | 1200 | 0 | 0 | 476 | 100.0 |
| 13 | 2775 | 1067 | 1200 | 0 | 0 | 508 | 100.0 |
| 14 | 2680 | 1067 | 1200 | 0 | 0 | 413 | 100.0 |
| 15 | 2644 | 1067 | 1200 | 0 | 0 | 377 | 100.0 |
| 16 | 2393 | 1067 | 1200 | 0 | 0 | 126 | 100.0 |
| 17 | 2479 | 1067 | 1200 | 0 | 0 | 212 | 100.0 |
| 18 | 2717 | 1067 | 1200 | 0 | 0 | 450 | 100.0 |
| 19 | 2909 | 1067 | 1200 | 0 | 0 | 642 | 100.0 |
| 20 | 3128 | 1067 | 1200 | 0 | 0 | 861 | 100.0 |
| 21 | 2603 | 1067 | 1200 | 0 | 0 | 336 | 100.0 |
| 22 | 2368 | 1067 | 1200 | 0 | 0 | 101 | 100.0 |
| 23 | 2036 | 0 | 1200 | 0 | 0 | 836 | 100.0 |
| 24 | 1866 | 0 | 1200 | 0 | 0 | 666 | 100.0 |
| **Итог** | **56,212** | 18,005 | 28,650 | 0 | -0 | **9,557** | пик 861 |

B. С батареей, арбитраж
| Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1373 | 976 | 1097 | 700 | 0 | 0 | 53.1 |
| 2 | 1374 | 976 | 1098 | 700 | 0 | 0 | 65.0 |
| 3 | 1477 | 1024 | 1152 | 700 | 0 | 0 | 77.0 |
| 4 | 1473 | 1023 | 1150 | 700 | 0 | 0 | 88.9 |
| 5 | 1688 | 1067 | 1200 | 579 | 0 | 0 | 98.8 |
| 6 | 2180 | 1060 | 1192 | 72 | 0 | 0 | 100.0 |
| 7 | 2071 | 975 | 1096 | 0 | 0 | 0 | 100.0 |
| 8 | 2560 | 1067 | 1200 | 0 | -292 | 0 | 94.3 |
| 9 | 2799 | 1067 | 1200 | 0 | -532 | 0 | 84.0 |
| 10 | 2911 | 1067 | 1200 | 0 | -644 | 0 | 71.5 |
| 11 | 2967 | 1067 | 1200 | 0 | -700 | 0 | 58.0 |
| 12 | 2743 | 1067 | 1200 | 0 | -476 | 0 | 48.7 |
| 13 | 2775 | 1067 | 1200 | 0 | -508 | 0 | 38.9 |
| 14 | 2680 | 1067 | 1200 | 0 | -413 | 0 | 30.9 |
| 15 | 2644 | 1067 | 1200 | 0 | -46 | 331 | 30.0 |
| 16 | 2393 | 1067 | 1200 | 0 | 0 | 126 | 30.0 |
| 17 | 2479 | 1067 | 1200 | 0 | 0 | 212 | 30.0 |
| 18 | 2717 | 1067 | 1200 | 0 | 0 | 450 | 30.0 |
| 19 | 2909 | 1067 | 1200 | 0 | 0 | 642 | 30.0 |
| 20 | 3128 | 1067 | 1200 | 0 | 0 | 861 | 30.0 |
| 21 | 2603 | 1067 | 1200 | 0 | 0 | 336 | 30.0 |
| 22 | 2368 | 1067 | 1200 | 0 | 0 | 101 | 30.0 |
| 23 | 2036 | 1067 | 1200 | 231 | 0 | 0 | 33.9 |
| 24 | 1866 | 1067 | 1200 | 401 | 0 | 0 | 40.8 |
| **Итог** | **56,212** | 25,239 | 28,385 | 4,082 | -3,612 | **3,058** | пик 861 |

C. С батареей, срез пика 600 кВт
| Час | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1373 | 0 | 1200 | 0 | 0 | 173 | 100.0 |
| 2 | 1374 | 0 | 1200 | 0 | 0 | 174 | 100.0 |
| 3 | 1477 | 0 | 1200 | 0 | 0 | 277 | 100.0 |
| 4 | 1473 | 0 | 1200 | 0 | 0 | 273 | 100.0 |
| 5 | 1688 | 0 | 1200 | 0 | 0 | 488 | 100.0 |
| 6 | 2180 | 1026 | 1154 | 0 | 0 | 0 | 100.0 |
| 7 | 2071 | 975 | 1096 | 0 | 0 | 0 | 100.0 |
| 8 | 2560 | 1067 | 1200 | 0 | 0 | 292 | 100.0 |
| 9 | 2799 | 1067 | 1200 | 0 | 0 | 532 | 100.0 |
| 10 | 2911 | 1067 | 1200 | 0 | -44 | 600 | 99.2 |
| 11 | 2967 | 1067 | 1200 | 0 | -100 | 600 | 97.2 |
| 12 | 2743 | 1067 | 1200 | 0 | 0 | 476 | 97.2 |
| 13 | 2775 | 1067 | 1200 | 0 | 0 | 508 | 97.2 |
| 14 | 2680 | 1067 | 1200 | 0 | 0 | 413 | 97.2 |
| 15 | 2644 | 1067 | 1200 | 0 | 0 | 377 | 97.2 |
| 16 | 2393 | 1067 | 1200 | 0 | 0 | 126 | 97.2 |
| 17 | 2479 | 1067 | 1200 | 0 | 0 | 212 | 97.2 |
| 18 | 2717 | 1067 | 1200 | 0 | 0 | 450 | 97.2 |
| 19 | 2909 | 1067 | 1200 | 0 | -42 | 600 | 96.4 |
| 20 | 3128 | 1067 | 1200 | 0 | -261 | 600 | 91.3 |
| 21 | 2603 | 1067 | 1200 | 0 | 0 | 336 | 91.3 |
| 22 | 2368 | 1067 | 1200 | 0 | 0 | 101 | 91.3 |
| 23 | 2036 | 1067 | 1200 | 231 | 0 | 0 | 95.3 |
| 24 | 1866 | 1009 | 1135 | 277 | 0 | 0 | 100.0 |
| **Итог** | **56,212** | 20,081 | 28,584 | 508 | -447 | **7,607** | пик 600 |

============================================================================================
НЕДЕЛЯ с 01.06.2026 (понедельник, первое число расчётного месяца)
============================================================================================

A. Без батареи
| День | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 16,981 | 28,698 | 0 | 0 | 8,436 | 814 | 100.0 |
| Вт | 54,170 | 17,055 | 28,781 | 0 | 0 | 8,333 | 774 | 100.0 |
| Ср | 53,724 | 17,042 | 28,766 | 0 | 0 | 7,916 | 703 | 100.0 |
| Чт | 54,139 | 18,051 | 28,686 | 0 | 0 | 7,402 | 848 | 100.0 |
| Пт | 53,065 | 16,962 | 28,676 | 0 | 0 | 7,428 | 910 | 100.0 |
| Сб | 48,266 | 14,805 | 23,851 | 0 | 0 | 9,610 | 1,453 | 100.0 |
| Вс | 48,180 | 16,749 | 28,437 | 0 | 0 | 2,994 | 472 | 100.0 |
| **Итог** | **365,658** | 117,645 | 195,895 | 0 | -0 | **52,118** | **1,453** | |

B. С батареей, арбитраж
| День | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 20,232 | 28,754 | 1,172 | -3,612 | 2,688 | 562 | 46.9 |
| Вт | 54,170 | 22,261 | 28,636 | 2,703 | -2,444 | 3,531 | 574 | 45.6 |
| Ср | 53,724 | 22,309 | 28,690 | 3,034 | -2,422 | 3,337 | 654 | 50.4 |
| Чт | 54,139 | 22,350 | 28,736 | 2,602 | -2,375 | 3,280 | 532 | 48.7 |
| Пт | 53,065 | 21,334 | 28,794 | 2,041 | -1,996 | 2,982 | 727 | 44.9 |
| Сб | 48,266 | 21,284 | 28,737 | 3,814 | -1,988 | 71 | 71 | 71.4 |
| Вс | 48,180 | 21,167 | 28,605 | 3,284 | -1,579 | 113 | 73 | 96.8 |
| **Итог** | **365,658** | 150,938 | 200,952 | 18,650 | -16,416 | **16,002** | **727** | |

C. С батареей, срез пика 600 кВт
| День | Нагрузка | Jenbacher | TEDOM | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 16,981 | 28,698 | 0 | 0 | 8,436 | 814 | 100.0 |
| Вт | 54,170 | 17,055 | 28,781 | 0 | 0 | 8,333 | 774 | 100.0 |
| Ср | 53,724 | 17,042 | 28,766 | 0 | 0 | 7,916 | 703 | 100.0 |
| Чт | 54,139 | 18,069 | 28,707 | 39 | -34 | 7,367 | 814 | 100.0 |
| Пт | 53,065 | 17,013 | 28,734 | 109 | -96 | 7,332 | 814 | 100.0 |
| Сб | 48,266 | 14,902 | 23,960 | 207 | -182 | 9,428 | 1,271 | 100.0 |
| Вс | 48,180 | 16,749 | 28,437 | 0 | 0 | 2,994 | 472 | 100.0 |
| **Итог** | **365,658** | 117,812 | 196,083 | 355 | -312 | **51,806** | **1,271** | |

============================================================================================
СВОДНАЯ ТАБЛИЦА
============================================================================================
| Показатель | A. Без батареи | B. Арбитраж | C. Срез пика |
|---|---:|---:|---:|
| **ТИПИЧНЫЕ СУТКИ** | | | |
| Нагрузка завода, кВт·ч | 56,212 | 56,212 | 56,212 |
| Выработка КГУ, кВт·ч | 46,655 | 53,625 | 48,666 |
| Заряд BESS, кВт·ч | 0 | 4,082 | 508 |
| Разряд BESS, кВт·ч | 0 | 3,612 | 447 |
| Закуп из сети, кВт·ч | 9,557 | 3,058 | 7,607 |
| Пик закупа, кВт | 861 | 861 | 600 |
| Затраты, ₸ | 1,599,828 | 1,363,234 | 1,527,088 |
| Экономия к варианту A, ₸ | 0 | 236,594 | 72,741 |
| **НЕДЕЛЯ** | | | |
| Нагрузка завода, кВт·ч | 365,658 | 365,658 | 365,658 |
| Выработка КГУ, кВт·ч | 313,540 | 351,890 | 313,895 |
| Заряд BESS, кВт·ч | 0 | 18,650 | 355 |
| Разряд BESS, кВт·ч | 0 | 16,416 | 312 |
| Закуп из сети, кВт·ч | 52,118 | 16,002 | 51,806 |
| Пик закупа, кВт | 1,453 | 727 | 1,271 |
| Затраты, ₸ | 10,024,956 | 8,701,707 | 10,014,018 |
| Экономия к варианту A, ₸ | 0 | 1,323,248 | 10,938 |
| **ГОД** | | | |
| Выработка КГУ, МВт·ч | 16,945 | 18,939 | 17,293 |
| Закуп из сети, МВт·ч | 4,079 | 2,215 | 3,754 |
| Доля сети в питании, % | 19.4 | 10.5 | 17.9 |
| Годовой максимум закупа, кВт | 3,233 | 3,233 | 1,403 |
| Сумма 12 месячных максимумов, кВт | 34,952 | 34,320 | 13,436 |
| Пусков КГУ за год | 401 | 227 | 398 |
| Затраты, ₸/год | 623,555,059 | 552,977,723 | 611,646,415 |
| Экономия к варианту A, ₸/год | 0 | 70,577,336 | 11,908,644 |
| Экономия, % | 0.0 | 11.3 | 1.9 |
| Простая окупаемость, лет | nan | 5.8 | 34.3 |

CAPEX BESS: $850,000 = 408,000,000 ₸
Полный отчёт с почасовой неделей: report_final.md
```


## G.5 Финальный отчёт, три КГУ, консольный вывод


```text
============================================================================================
ТИПИЧНЫЕ СУТКИ: 01.09.2026 (Вт), потребление 56,212 кВт*ч при медиане 57,412
============================================================================================

A. Без батареи
| Час | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1373 | 0 | 1200 | 0 | 0 | 0 | 173 | 100.0 |
| 2 | 1374 | 0 | 1200 | 0 | 0 | 0 | 174 | 100.0 |
| 3 | 1477 | 0 | 1200 | 0 | 0 | 0 | 277 | 100.0 |
| 4 | 1473 | 0 | 1200 | 0 | 0 | 0 | 273 | 100.0 |
| 5 | 1688 | 0 | 1200 | 0 | 0 | 0 | 488 | 100.0 |
| 6 | 2180 | 0 | 1137 | 1042 | 0 | 0 | 0 | 100.0 |
| 7 | 2071 | 0 | 1080 | 990 | 0 | 0 | 0 | 100.0 |
| 8 | 2560 | 0 | 1200 | 1100 | 0 | 0 | 260 | 100.0 |
| 9 | 2799 | 0 | 1200 | 1100 | 0 | 0 | 499 | 100.0 |
| 10 | 2911 | 0 | 1200 | 1100 | 0 | 0 | 611 | 100.0 |
| 11 | 2967 | 0 | 1200 | 1100 | 0 | 0 | 667 | 100.0 |
| 12 | 2743 | 0 | 1200 | 1100 | 0 | 0 | 443 | 100.0 |
| 13 | 2775 | 0 | 1200 | 1100 | 0 | 0 | 475 | 100.0 |
| 14 | 2680 | 0 | 1200 | 1100 | 0 | 0 | 380 | 100.0 |
| 15 | 2644 | 0 | 1200 | 1100 | 0 | 0 | 344 | 100.0 |
| 16 | 2393 | 0 | 1200 | 1100 | 0 | 0 | 93 | 100.0 |
| 17 | 2479 | 0 | 1200 | 1100 | 0 | 0 | 179 | 100.0 |
| 18 | 2717 | 0 | 1200 | 1100 | 0 | 0 | 417 | 100.0 |
| 19 | 2909 | 0 | 1200 | 1100 | 0 | 0 | 609 | 100.0 |
| 20 | 3128 | 991 | 1115 | 1022 | 0 | 0 | 0 | 100.0 |
| 21 | 2603 | 1067 | 1200 | 0 | 0 | 0 | 336 | 100.0 |
| 22 | 2368 | 1067 | 1200 | 0 | 0 | 0 | 101 | 100.0 |
| 23 | 2036 | 0 | 1200 | 0 | 0 | 0 | 836 | 100.0 |
| 24 | 1866 | 0 | 1200 | 0 | 0 | 0 | 666 | 100.0 |
| **Итог** | **56,212** | 3,125 | 28,532 | 16,255 | 0 | -0 | **8,300** | пик 836 |

B. С батареей, арбитраж
| Час | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 
| 9 | 2799 | 0 | 1200 | 1100 | 0 | 0 | 499 | 100.0 |
| 10 | 2911 | 0 | 1200 | 1100 | 0 | 0 | 611 | 100.0 |
| 11 | 2967 | 0 | 1200 | 1100 | 0 | 0 | 667 | 100.0 |
| 12 | 2743 | 0 | 1200 | 1100 | 0 | 0 | 443 | 100.0 |
| 13 | 2775 | 0 | 1200 | 1100 | 0 | 0 | 475 | 100.0 |
| 14 | 2680 | 0 | 1200 | 1100 | 0 | 0 | 380 | 100.0 |
| 15 | 2644 | 0 | 1200 | 1100 | 0 | 0 | 344 | 100.0 |
| 16 | 2393 | 0 | 1200 | 1100 | 0 | 0 | 93 | 100.0 |
| 17 | 2479 | 0 | 1200 | 1100 | 0 | 0 | 179 | 100.0 |
| 18 | 2717 | 0 | 1200 | 1100 | 0 | 0 | 417 | 100.0 |
| 19 | 2909 | 0 | 1200 | 1100 | 0 | 0 | 609 | 100.0 |
| 20 | 3128 | 991 | 1115 | 1022 | 0 | 0 | 0 | 100.0 |
| 21 | 2603 | 1067 | 1200 | 0 | 0 | 0 | 336 | 100.0 |
| 22 | 2368 | 1067 | 1200 | 0 | 0 | 0 | 101 | 100.0 |
| 23 | 2036 | 0 | 1200 | 0 | 0 | -136 | 700 | 97.4 |
| 24 | 1866 | 0 | 1200 | 0 | 0 | 0 | 666 | 97.4 |
| **Итог** | **56,212** | 3,125 | 28,532 | 16,255 | 0 | -136 | **8,164** | пик 700 |

============================================================================================
НЕДЕЛЯ с 01.06.2026 (понедельник, первое число расчётного месяца)
============================================================================================

A. Без батареи
| День | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 7,283 | 22,566 | 17,300 | 0 | 0 | 6,967 | 914 | 100.0 |
| Вт | 54,170 | 2,039 | 25,164 | 19,669 | 0 | 0 | 7,298 | 714 | 100.0 |
| Ср | 53,724 | 1,067 | 27,540 | 17,545 | 0 | 0 | 7,572 | 670 | 100.0 |
| Чт | 54,139 | 4,150 | 28,563 | 15,235 | 0 | 0 | 6,190 | 729 | 100.0 |
| Пт | 53,065 | 4,018 | 24,958 | 19,382 | 0 | 0 | 4,707 | 694 | 100.0 |
| Сб | 48,266 | 5,211 | 20,172 | 18,366 | 0 | 0 | 4,517 | 813 | 100.0 |
| Вс | 48,180 | 0 | 28,322 | 17,161 | 0 | 0 | 2,697 | 472 | 100.0 |
| **Итог** | **365,658** | 23,767 | 177,284 | 124,658 | 0 | -0 | **39,948** | **914** | |

B. С батареей, арбитраж
| День | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 10,384 | 23,590 | 20,417 | 4,242 | -3,574 | 391 | 248 | 89.8 |
| Вт | 54,170 | 14,516 | 24,726 | 15,055 | 5,314 | -4,668 | 519 | 271 | 90.0 |
| Ср | 53,724 | 11,606 | 25,052 | 16,276 | 5,173 | -4,457 | 1,506 | 1,359 | 91.9 |
| Чт | 54,139 | 12,448 | 24,849 | 16,079 | 5,438 | -4,867 | 1,334 | 1,334 | 90.3 |
| Пт | 53,065 | 10,447 | 24,938 | 17,262 | 4,648 | -3,589 | 1,477 | 1,296 | 100.0 |
| Сб | 48,266 | 8,135 | 21,152 | 18,374 | 2,250 | -2,683 | 171 | 171 | 86.4 |
| Вс | 48,180 | 5,142 | 25,025 | 16,241 | 2,758 | -1,723 | 2,806 | 1,395 | 100.0 |
| **Итог** | **365,658** | 72,680 | 169,332 | 119,703 | 29,822 | -25,561 | **8,204** | **1,395** | |

C. С батареей, срез пика 700 кВт
| День | Нагрузка | Jenbacher | TEDOM | КГУ-3 | Заряд + | Разряд − | Сеть | Пик сети | SoC,% |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Пн | 54,115 | 7,288 | 22,566 | 17,306 | 12 | -214 | 6,753 | 700 | 95.9 |
| Вт | 54,170 | 12,354 | 21,311 | 16,036 | 4,245 | -3,522 | 5,193 | 700 | 100.0 |
| Ср | 53,724 | 0 | 27,540 | 18,645 | 0 | 0 | 7,539 | 670 | 100.0 |
| Чт | 54,139 | 4,159 | 28,573 | 15,244 | 28 | -54 | 6,137 | 700 | 99.4 |
| Пт | 53,065 | 4,034 | 24,958 | 19,399 | 33 | 0 | 4,707 | 694 | 100.0 |
| Сб | 48,266 | 5,211 | 20,238 | 18,427 | 128 | -113 | 4,405 | 700 | 100.0 |
| Вс | 48,180 | 0 | 28,322 | 17,161 | 0 | 0 | 2,697 | 472 | 100.0 |
| **Итог** | **365,658** | 33,047 | 173,507 | 122,218 | 4,446 | -3,902 | **37,430** | **700** | |

============================================================================================
СВОДНАЯ ТАБЛИЦА
============================================================================================
| Показатель | A. Без батареи | B. Арбитраж | C. Срез пика |
|---|---:|---:|---:|
| **ТИПИЧНЫЕ СУТКИ** | | | |
| Нагрузка завода, кВт·ч | 56,212 | 56,212 | 56,212 |
| Выработка КГУ, кВт·ч | 47,912 | 57,168 | 47,912 |
| Заряд BESS, кВт·ч | 0 | 5,151 | 0 |
| Разряд BESS, кВт·ч | 0 | 3,888 | 136 |
| Закуп из сети, кВт·ч | 8,300 | 307 | 8,164 |
| Пик закупа, кВт | 836 | 307 | 700 |
| Затраты, ₸ | 1,552,066 | 1,276,094 | 1,543,882 |
| Экономия к варианту A, ₸ | 0 | 275,973 | 8,184 |
| **НЕДЕЛЯ** | | | |
| Нагрузка завода, кВт·ч | 365,658 | 365,658 | 365,658 |
| Выработка КГУ, кВт·ч | 325,710 | 361,715 | 328,772 |
| Заряд BESS, кВт·ч | 0 | 29,822 | 4,446 |
| Разряд BESS, кВт·ч | 0 | 25,562 | 3,902 |
| Закуп из сети, кВт·ч | 39,948 | 8,204 | 37,430 |
| Пик закупа, кВт | 914 | 1,395 | 700 |
| Затраты, ₸ | 9,562,503 | 8,449,963 | 9,478,771 |
| Экономия к варианту A, ₸ | 0 | 1,112,540 | 83,732 |
| **ГОД** | | | |
| Выработка КГУ, МВт·ч | 18,201 | 21,099 | 18,525 |
| Закуп из сети, МВт·ч | 2,823 | 133 | 2,518 |
| Доля сети в питании, % | 13.4 | 0.6 | 12.0 |
| Годовой максимум закупа, кВт | 3,200 | 3,517 | 1,294 |
| Сумма 12 месячных максимумов, кВт | 30,306 | 18,709 | 9,441 |
| Пусков КГУ за год | 1,477 | 1,913 | 1,486 |
| Затраты, ₸/год | 591,937,472 | 500,844,166 | 580,897,459 |
| Экономия к варианту A, ₸/год | 0 | 91,093,306 | 11,040,013 |
| Экономия, % | 0.0 | 15.4 | 1.9 |
| Простая окупаемость, лет | nan | 4.5 | 37.0 |

CAPEX BESS: $850,000 = 408,000,000 ₸
Полный отчёт с почасовой неделей: report_final.md
```


## G.6 Суточная модель на двух КГУ


```text
==============================================================================
ПАРАМЕТРЫ СИСТЕМЫ
==============================================================================
  Jenbacher    номинал  1067.0 кВт   рабочая полоса   960.3 -  1067.0 кВт
  TEDOM        номинал  1200.0 кВт   рабочая полоса  1080.0 -  1200.0 кВт
  Суммарно КГУ                 2267.0 кВт
  BESS  ёмкость 5500 кВт*ч, полезная 3850 кВт*ч, инвертор 2500 кВт
  Тарифы: КГУ 22 ₸   BESS 25.00 ₸   сеть 60 ₸ за кВт*ч
  Профиль: средняя 2376 кВт, пик 5500 кВт, сутки 57020 кВт*ч

ДОПУСТИМЫЕ СОСТАВЫ КГУ (полосы суммарной мощности)
  Jenbacher + TEDOM            2040.3 -   2267.0 кВт
  TEDOM                        1080.0 -   1200.0 кВт
  Jenbacher                     960.3 -   1067.0 кВт
  все выключены                   0.0 -      0.0 кВт

ТАБЛИЦА 1. БЕЗ БАТАРЕИ

 Час  Нагрузка  Jenbacher    TEDOM  Заряд +  Разряд -     Сеть   SoC,%
----------------------------------------------------------------------
   1      1450        0.0   1200.0        0         0      250   100.0
   2      1400        0.0   1200.0        0         0      200   100.0
   3      1380        0.0   1200.0        0         0      180   100.0
   4      1380        0.0   1200.0        0         0      180   100.0
   5      1420        0.0   1200.0        0         0      220   100.0
   6      1480        0.0   1200.0        0         0      280   100.0
   7      1800        0.0   1200.0        0         0      600   100.0
   8      2600     1067.0   1200.0        0         0      333   100.0
   9      3600     1067.0   1200.0        0         0     1333   100.0
  10      5500     1067.0   1200.0        0         0     3233   100.0
  11      4200     1067.0   1200.0        0         0     1933   100.0
  12      3400     1067.0   1200.0        0         0     1133   100.0
  13      2000        0.0   1200.0        0         0      800   100.0
  14      1900        0.0   1200.0        0         0      700   100.0
  15      3000        0.0   1200.0        0         0     1800   100.0
  16      4200     1067.0   1200.0        0         0     1933   100.0
  17      3600     1067.0   1200.0        0         0     1333   100.0
  18      2700     1067.0   1200.0        0         0      433   100.0
  19      2200     1035.5   1164.5        0         0        0   100.0
  20      1800        0.0   1200.0        0         0      600   100.0
  21      1650        0.0   1200.0        0         0      450   100.0
  22      1520        0.0   1200.0        0         0      320   100.0
  23      1440        0.0   1200.0        0         0      240   100.0
  24      1400        0.0   1200.0        0         0      200   100.0
----------------------------------------------------------------------
ИТОГО     57020       9571    28765        0        -0    18684

ТАБЛИЦА 2. С БАТАРЕЕЙ, жадный арбитраж (разряд при первой нужде)

==============================================================================
Вариант                                  КГУ, ₸      Сеть, ₸     Всего, ₸   Пик сети
------------------------------------------------------------------------------------
Без батареи                             843,392    1,121,040    2,009,432      3,233
С батареей, арбитраж                  1,056,132      574,630    1,645,762      1,933
С батареей, peak shaving 1200         1,032,658      632,520    1,680,178      1,200
------------------------------------------------------------------------------------
Экономия (арбитраж):      363,670 ₸/сут      132,739,696 ₸/год
Экономия (peak shaving 1200):      329,254 ₸/сут      120,177,710 ₸/год

Максимальный пиковый закуп из сети за сутки:
  без батареи                     3,233 кВт
  с батареей, арбитраж            1,933 кВт
  с батареей, peak shaving        1,200 кВт  (срез 62.9%)

CAPEX BESS: $850,000 = 408,000,000 ₸
Простая окупаемость на арбитраже: 3.1 лет

ПУСКИ КГУ ЗА СУТКИ (защита от short-cycling)
  без батареи     3 пусков
  арбитраж        1 пусков
  peak shaving    1 пусков

ПЛАТА ЗА ЗАЯВЛЕННУЮ МОЩНОСТЬ
  peak shaving срезает ещё 733 кВт пика,
  но обходится дороже арбитража на 12,561,986 ₸/год.
  Порог окупаемости: тариф на мощность выше 1,428 ₸ за кВт в месяц.
  Сейчас DEMAND_CHARGE = 0 ₸/кВт/мес, то есть пик в модели ничего не стоит.
```

---

# Приложение H. Файлы проекта

| Файл | Что это |
|---|---|
| `chp_bess_dispatch.py` | MILP на трёх КГУ, итерация 1 |
| `ems_lab.py` | Два слоя моделей, optimality gap, итерация 2 |
| `microgrid_ems.py` | Три сценария, Monte Carlo, FSM, итерация 3 |
| `microgrid_ems_v2.py` | Исправленный EMS, шесть правок, итерация 4 |
| `factory_2chp.py` | Два КГУ, правило 90%, суточная модель, итерация 5 |
| `factory_year.py` | Годовая модель на двух КГУ, 10 сценариев, итерация 6-7 |
| `factory_year3.py` | Годовая модель на трёх КГУ, итерация 8 |
| `report_final.py`, `report_final3.py` | Генераторы финальных отчётов |
| `sens.py`, `sens2.py`, `sens3.py` | Скрипты чувствительности |
| `sweep_cap.py`, `sweep3.py` | Подбор параметров контроллера |
| `export_js.py` | Экспорт данных в артефакт |
| `validate.py`, `validate2.py`, `verify_report.py`, `final_check.py` | Кросс-валидация |
| `render_test2.js` | Рендер-тест интерфейса |
| `bess_profile.jsx` | Интерактивная визуализация |
| `load_year_01..10.csv` | Десять годовых профилей, 8760 строк каждый |
| `load_template.csv` | Шаблон для подстановки реальных данных |
| `results.md`, `ems_tables.md` | Отчёты ранних итераций |
| `report_final.md`, `report_final_3chp.md` | Финальные отчёты |
| `week_tables.md` | Недельные почасовые таблицы |

## Формат входных данных

```
timestamp,load_kw
2026-01-01 00:00:00,1450.3
2026-01-01 01:00:00,1400.1
...
```

8760 строк. Загрузка:

```python
def load_from_csv(path, column="load_kw"):
    df = pd.read_csv(path)
    v = df[column].to_numpy(dtype=float)
    return [v[i * 24:(i + 1) * 24] for i in range(len(v) // 24)]
```

## Зависимости

```
python3 -m pip install pulp numpy pandas --break-system-packages
npm install react react-dom recharts        # для рендер-теста артефакта
```

---

*Конец документа.*
